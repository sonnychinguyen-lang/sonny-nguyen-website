import {
  Canvas,
  applyProps,
  createPortal,
  extend,
  useFrame,
  useLoader,
  useThree
} from "./chunk-HLS3KN3J.js";
import "./chunk-PX4OR76I.js";
import "./chunk-UFORGWNI.js";
import {
  Box3,
  BufferGeometry,
  Camera,
  CanvasTexture,
  Clock,
  Color,
  DataTextureLoader,
  DataUtils,
  DoubleSide,
  EquirectangularReflectionMapping,
  EventDispatcher,
  Float32BufferAttribute,
  FloatType,
  HalfFloatType,
  Line3,
  LinearFilter,
  Matrix4,
  Mesh,
  MeshPhysicalMaterial,
  Object3D,
  OrthographicCamera,
  Quaternion,
  REVISION,
  RGBAFormat,
  Raycaster,
  Scene,
  ShaderChunk,
  ShaderMaterial,
  Sphere,
  Uniform,
  UniformsUtils,
  Vector2,
  Vector3,
  Vector4,
  WebGLRenderTarget,
  three_module_exports
} from "./chunk-QZYMFMLJ.js";
import {
  require_jsx_runtime
} from "./chunk-YIEQNEKG.js";
import {
  require_react
} from "./chunk-JHYWF6JX.js";
import {
  __toESM
} from "./chunk-DC5AMYBS.js";

// node_modules/@shadergradient/react/dist/chunk-23DH3CGE.mjs
var i = (t15, a11 = "") => ({ control: { type: t15.Enum, options: ["props", "query"], optionTitles: ["Props", "Query"], displaySegmentedControl: true, defaultValue: a11 === "stateless" ? "query" : "props" }, activeTab: { title: "Settings Group", type: t15.Enum, options: ["Shape", "Colors", "Effects", "View"], hidden: (e19) => e19.control === "query" }, type: { type: t15.Enum, options: ["plane", "sphere", "waterPlane"], optionTitles: ["Plane", "Sphere", "Water"], displaySegmentedControl: true, hidden: (e19) => e19.control === "query" || e19.activeTab !== "Shape" }, animate: { type: t15.Enum, options: ["on", "off"], optionTitles: ["On", "Off"], displaySegmentedControl: true, hidden: (e19) => e19.control === "query" || e19.activeTab !== "Shape" && e19.activeTab !== "URL" }, uTime: { title: "Movements", type: t15.Number, step: 0.1, displayStepper: true, hidden: (e19) => e19.animate === "on" || e19.control === "query" || e19.activeTab !== "Shape" }, uSpeed: { title: "Speed", type: t15.Number, step: 0.1, min: 0.1, displayStepper: true, defaultValue: 0.4, hidden: (e19) => e19.animate === "off" || e19.control === "query" || e19.activeTab !== "Shape" }, noise: { type: t15.Object, controls: { uStrength: { type: t15.Number, title: "Strength", step: 0.1, min: 0.1, displayStepper: true, defaultValue: 4 }, uDensity: { type: t15.Number, title: "Density", step: 0.1, min: 0.1, displayStepper: true, defaultValue: 1.3 } }, hidden: (e19) => e19.control === "query" || e19.activeTab !== "Shape" }, uAmplitude: { type: t15.Number, title: "Spiral", step: 0.1, min: 0.1, displayStepper: true, defaultValue: 1, hidden: (e19) => e19.type !== "sphere" || e19.control === "query" || e19.activeTab !== "Shape" }, grain: { type: t15.Enum, options: ["on", "off"], optionTitles: ["On", "Off"], displaySegmentedControl: true, hidden: (e19) => e19.control === "query" || e19.activeTab !== "Effects" }, lightType: { type: t15.Enum, options: ["env", "3d"], optionTitles: ["Env", "Light"], displaySegmentedControl: true, defaultValue: "3d", hidden: (e19) => e19.control === "query" || e19.activeTab !== "Effects" }, envPreset: { type: t15.Enum, options: ["city", "dawn", "lobby"], optionTitles: ["City", "Dawn", "Lobby"], displaySegmentedControl: true, hidden: (e19) => e19.lightType === "3d" || e19.control === "query" || e19.activeTab !== "Effects" }, brightness: { type: t15.Number, step: 0.1, min: 0.1, max: 3, defaultValue: 1.2, hidden: (e19) => e19.lightType === "env" || e19.control === "query" || e19.activeTab !== "Effects" || e19.url }, reflection: { type: t15.Number, step: 0.1, min: 0, max: 1, defaultValue: 0.1, hidden: (e19) => e19.lightType === "3d" || e19.control === "query" || e19.activeTab !== "Effects" || e19.url }, color1: { type: t15.Color, defaultValue: "#ff5005", hidden: (e19) => e19.control === "query" || e19.activeTab !== "Colors" }, color2: { type: t15.Color, defaultValue: "#dbba95", hidden: (e19) => e19.control === "query" || e19.activeTab !== "Colors" }, color3: { type: t15.Color, defaultValue: "#d0bce1", hidden: (e19) => e19.control === "query" || e19.activeTab !== "Colors" }, cDistance: { title: "Distance", type: t15.Number, displayStepper: true, min: 0, max: 20, defaultValue: 3.6, hidden: (e19) => e19.type === "sphere" || e19.control === "query" || e19.activeTab !== "View" }, cameraZoom: { type: t15.Number, displayStepper: true, step: 0.1, min: 0.1, max: 30, defaultValue: 1, hidden: (e19) => e19.type !== "sphere" || e19.control === "query" || e19.activeTab !== "View" }, cameraAngle: { type: t15.Object, controls: { cAzimuthAngle: { title: "Azimuth", type: t15.Number, displayStepper: true, step: 10, min: 0, max: 360, defaultValue: 180 }, cPolarAngle: { title: "Polar", type: t15.Number, displayStepper: true, step: 10, min: 0, max: 180, defaultValue: 90 } }, hidden: (e19) => e19.control === "query" || e19.activeTab !== "View" }, position: { type: t15.Object, controls: { positionX: { type: t15.Number, step: 0.1, displayStepper: true, defaultValue: -1.4 }, positionY: { type: t15.Number, step: 0.1, displayStepper: true, defaultValue: 0 }, positionZ: { type: t15.Number, step: 0.1, displayStepper: true, defaultValue: 0 } }, hidden: (e19) => e19.control === "query" || e19.activeTab !== "View" }, rotation: { type: t15.Object, controls: { rotationX: { type: t15.Number, step: 10, min: -360, max: 360, displayStepper: true, defaultValue: 0 }, rotationY: { type: t15.Number, step: 10, min: -360, max: 360, displayStepper: true, defaultValue: 10 }, rotationZ: { type: t15.Number, step: 10, min: -360, max: 360, displayStepper: true, defaultValue: 50 } }, hidden: (e19) => e19.control === "query" || e19.activeTab !== "View" }, urlString: { type: t15.String, placeholder: "URL from shadergradient.co", defaultValue: a11 === "stateless" ? "" : "https://www.shadergradient.co/customize?animate=on&axesHelper=off&bgColor1=%23000000&bgColor2=%23000000&brightness=1.2&cAzimuthAngle=180&cDistance=3.6&cPolarAngle=90&cameraZoom=1&color1=%23ff5005&color2=%23dbba95&color3=%23d0bce1&destination=onCanvas&embedMode=off&envPreset=city&format=gif&fov=45&frameRate=10&gizmoHelper=hide&grain=on&lightType=3d&pixelDensity=1&positionX=-1.4&positionY=0&positionZ=0&range=disabled&rangeEnd=40&rangeStart=0&reflection=0.1&rotationX=0&rotationY=10&rotationZ=50&shader=defaults&type=plane&uDensity=1.3&uFrequency=5.5&uSpeed=0.4&uStrength=4&uTime=0&wireframe=false", hidden: (e19) => e19.control === "props" }, zoomOut: { type: t15.Boolean, title: "View", enabledTitle: "Wide", disabledTitle: "-", defaultValue: false, hidden: (e19) => e19.control === "query" }, toggleAxis: { type: t15.Boolean, title: "Axis", enabledTitle: "Guide", disabledTitle: "-", defaultValue: false, hidden: (e19) => e19.control === "query" }, enableTransition: { type: t15.Boolean, title: "Camera Transition", enabledTitle: "Smooth", disabledTitle: "Instant", defaultValue: true, hidden: (e19) => e19.control === "query" }, enableCameraUpdate: { type: t15.Boolean, title: "Camera Update", enabledTitle: "On", disabledTitle: "Off", defaultValue: false, hidden: (e19) => a11 !== "stateless" }, pointerEvents: { type: t15.Enum, title: "Touch Control", options: ["none", "auto"], optionTitles: ["Disable", "Enable"], displaySegmentedControl: true, hidden: (e19) => e19.control === "query" }, lazyLoad: { type: t15.Boolean, defaultValue: true }, threshold: { type: t15.Number, min: 0, max: 1, step: 0.1, defaultValue: 0.1, displayStepper: true, title: "↳ Threshold", hidden: (e19) => !e19.lazyLoad }, rootMargin: { type: t15.String, placeholder: "0px", defaultValue: "0px", title: "↳ Root Margin", description: 'Offset before triggering (e.g., "100px", "-50px")', hidden: (e19) => !e19.lazyLoad }, canvas: { type: t15.Object, title: "Canvas", controls: { pixelDensity: { type: t15.Number, step: 0.1, min: 0, max: 3, displayStepper: true, defaultValue: 1 }, fov: { type: t15.Number, step: 1, min: 10, max: 180, displayStepper: true, defaultValue: 45 }, preserveDrawingBuffer: { type: t15.Boolean, title: "Keep Buffer", defaultValue: false }, powerPreference: { type: t15.Enum, title: "Power Pref", options: ["default", "high-performance", "low-power"], optionTitles: ["Auto", "High", "Low"], displaySegmentedControl: true, defaultValue: "low-power" } } } });

// node_modules/@shadergradient/react/dist/chunk-27WNIDXB.mjs
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var e = 192;
function n({ type: r10 }) {
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [r10 === "plane" && (0, import_jsx_runtime.jsx)("planeGeometry", { args: [10, 10, 1, e] }), r10 === "sphere" && (0, import_jsx_runtime.jsx)("icosahedronGeometry", { args: [1, e / 3] }), r10 === "waterPlane" && (0, import_jsx_runtime.jsx)("planeGeometry", { args: [10, 10, e, e] })] });
}

// node_modules/@shadergradient/react/dist/chunk-F3ULA5MU.mjs
function n2(r10) {
  let t15 = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(r10);
  return t15 ? { r: parseInt(t15[1], 16), g: parseInt(t15[2], 16), b: parseInt(t15[3], 16) } : null;
}
function e2(r10) {
  let t15 = r10.match(/^rgb\((\d+),\s*(\d+),\s*(\d+)\)$/);
  return t15 ? { r: parseInt(t15[1]), g: parseInt(t15[2]), b: parseInt(t15[3]) } : null;
}
function s(r10) {
  if (r10.startsWith("#")) return n2(r10);
  if (r10.startsWith("rgb")) return e2(r10);
  throw new Error("Invalid color format");
}
function a2(r10 = 0) {
  return r10 / 255;
}

// node_modules/@shadergradient/react/dist/chunk-7RJG5BRD.mjs
var p = Object.create;
var h = Object.defineProperty;
var q = Object.defineProperties;
var r = Object.getOwnPropertyDescriptor;
var s2 = Object.getOwnPropertyDescriptors;
var t2 = Object.getOwnPropertyNames;
var g = Object.getOwnPropertySymbols;
var u = Object.getPrototypeOf;
var k = Object.prototype.hasOwnProperty;
var m = Object.prototype.propertyIsEnumerable;
var l = (a11, b8, c3) => b8 in a11 ? h(a11, b8, { enumerable: true, configurable: true, writable: true, value: c3 }) : a11[b8] = c3;
var w = (a11, b8) => {
  for (var c3 in b8 || (b8 = {})) k.call(b8, c3) && l(a11, c3, b8[c3]);
  if (g) for (var c3 of g(b8)) m.call(b8, c3) && l(a11, c3, b8[c3]);
  return a11;
};
var x = (a11, b8) => q(a11, s2(b8));
var y = (a11, b8) => {
  var c3 = {};
  for (var d5 in a11) k.call(a11, d5) && b8.indexOf(d5) < 0 && (c3[d5] = a11[d5]);
  if (a11 != null && g) for (var d5 of g(a11)) b8.indexOf(d5) < 0 && m.call(a11, d5) && (c3[d5] = a11[d5]);
  return c3;
};
var z = (a11, b8) => () => (b8 || a11((b8 = { exports: {} }).exports, b8), b8.exports);
var A = (a11, b8) => {
  for (var c3 in b8) h(a11, c3, { get: b8[c3], enumerable: true });
};
var v = (a11, b8, c3, d5) => {
  if (b8 && typeof b8 == "object" || typeof b8 == "function") for (let e19 of t2(b8)) !k.call(a11, e19) && e19 !== c3 && h(a11, e19, { get: () => b8[e19], enumerable: !(d5 = r(b8, e19)) || d5.enumerable });
  return a11;
};
var B = (a11, b8, c3) => (c3 = a11 != null ? p(u(a11)) : {}, v(b8 || !a11 || !a11.__esModule ? h(c3, "default", { value: a11, enumerable: true }) : c3, a11));

// node_modules/@shadergradient/react/dist/chunk-HXMZSSU4.mjs
var import_react = __toESM(require_react(), 1);
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var A2 = ({ animate: g6, range: N5, rangeStart: m4, rangeEnd: E4, loop: j, loopDuration: f5, reflection: R4, uniforms: D3, vertexShader: H3, fragmentShader: M5, onInit: T4, shader: b8 }) => {
  let p10 = (0, import_react.useRef)(new Clock()), e19 = (0, import_react.useMemo)(() => {
    let s10 = Object.entries(D3), L = D3.colors, t15 = s(L[0]), a11 = s(L[1]), u5 = s(L[2]), w4 = { uC1r: { value: a2(t15 == null ? void 0 : t15.r) }, uC1g: { value: a2(t15 == null ? void 0 : t15.g) }, uC1b: { value: a2(t15 == null ? void 0 : t15.b) }, uC2r: { value: a2(a11 == null ? void 0 : a11.r) }, uC2g: { value: a2(a11 == null ? void 0 : a11.g) }, uC2b: { value: a2(a11 == null ? void 0 : a11.b) }, uC3r: { value: a2(u5 == null ? void 0 : u5.r) }, uC3g: { value: a2(u5 == null ? void 0 : u5.g) }, uC3b: { value: a2(u5 == null ? void 0 : u5.b) } }, F4 = s10.reduce((o11, [y8, O3]) => {
      let P5 = UniformsUtils.clone({ [y8]: { value: O3 } });
      return w(w({}, o11), P5);
    }, {}), i9 = { userData: F4, metalness: b8 === "glass" ? 0 : 0.2, roughness: b8 === "glass" ? 0.1 : 1 - (typeof R4 == "number" ? R4 : 0.1), side: DoubleSide, onBeforeCompile: (o11) => {
      o11.uniforms = w(w(w({}, o11.uniforms), F4), w4), o11.vertexShader = H3, o11.fragmentShader = M5;
    } };
    b8 === "glass" && (i9.transparent = true, i9.opacity = 0.3, i9.transmission = 0.9, i9.thickness = 0.5, i9.clearcoat = 1, i9.clearcoatRoughness = 0, i9.ior = 1.5, i9.envMapIntensity = 1);
    let c3 = new MeshPhysicalMaterial(i9);
    return s10.forEach(([o11]) => Object.defineProperty(c3, o11, { get: () => c3.uniforms[o11].value, set: (y8) => c3.uniforms[o11].value = y8 })), T4 && T4(c3), c3;
  }, [D3, H3, M5, T4, R4, b8]);
  return (0, import_react.useEffect)(() => () => {
    e19.dispose();
  }, [e19]), (0, import_react.useEffect)(() => {
    g6 === "on" ? p10.current.start() : p10.current.stop();
  }, [g6]), useFrame(() => {
    if (g6 === "on" && e19.userData.uTime) {
      let s10 = p10.current.getElapsedTime();
      j === "on" && Number.isFinite(f5) && f5 > 0 ? (s10 = s10 % f5, e19.userData.uLoop && (e19.userData.uLoop.value = 1), e19.userData.uLoopDuration && (e19.userData.uLoopDuration.value = f5)) : (e19.userData.uLoop && (e19.userData.uLoop.value = 0), N5 === "enabled" && Number.isFinite(m4) && Number.isFinite(E4) && E4 > m4 && (s10 = m4 + s10, s10 >= E4 && (s10 = m4, p10.current.start()))), e19.userData.uTime.value = s10;
    }
  }), (0, import_jsx_runtime2.jsx)("primitive", { attach: "material", object: e19 });
};

// node_modules/@shadergradient/react/dist/chunk-OZWEDB42.mjs
var o2 = {};
A(o2, { fragment: () => n3, vertex: () => e3 });
var n3 = `
#define STANDARD
#ifdef PHYSICAL
#define REFLECTIVITY
#define CLEARCOAT
#define TRANSMISSION
#endif

uniform vec3 diffuse;
uniform vec3 emissive;
uniform float roughness;
uniform float metalness;
uniform float opacity;

#ifdef TRANSMISSION
uniform float transmission;
#endif
#ifdef REFLECTIVITY
uniform float reflectivity;
#endif
#ifdef CLEARCOAT
uniform float clearcoat;
uniform float clearcoatRoughness;
#endif
#ifdef USE_SHEEN
uniform vec3 sheen;
#endif
varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif
#include <alphamap_pars_fragment>
#include <aomap_pars_fragment>
#include <color_pars_fragment>
#include <common>
#include <dithering_pars_fragment>
#include <emissivemap_pars_fragment>
#include <lightmap_pars_fragment>
#include <map_pars_fragment>
#include <packing>
#include <uv2_pars_fragment>
#include <uv_pars_fragment>
// #include <transmissionmap_pars_fragment>
#include <bsdfs>
#include <bumpmap_pars_fragment>
#include <clearcoat_pars_fragment>
#include <clipping_planes_pars_fragment>
#include <cube_uv_reflection_fragment>
#include <envmap_common_pars_fragment>
#include <envmap_physical_pars_fragment>
#include <fog_pars_fragment>
#include <lights_pars_begin>
#include <lights_physical_pars_fragment>
#include <logdepthbuf_pars_fragment>
#include <metalnessmap_pars_fragment>
#include <normalmap_pars_fragment>
#include <roughnessmap_pars_fragment>
#include <shadowmap_pars_fragment>
// include를 통해 가져온 값은 대부분 환경, 빛 등을 계산하기 위해서 기본 fragment
// shader의 값들을 받아왔습니다. 일단은 무시하셔도 됩니다.

varying vec3 vNormal;
varying float displacement;
varying vec3 vPos;
varying float vDistort;

uniform float uC1r;
uniform float uC1g;
uniform float uC1b;
uniform float uC2r;
uniform float uC2g;
uniform float uC2b;
uniform float uC3r;
uniform float uC3g;
uniform float uC3b;

varying vec3 color1;
varying vec3 color2;
varying vec3 color3;

// for npm package, need to add this manually
float linearToRelativeLuminance2( const in vec3 color ) {
    vec3 weights = vec3( 0.2126, 0.7152, 0.0722 );
    return dot( weights, color.rgb );
}

void main() {

  //-------- basic gradient ------------
  vec3 color1 = vec3(uC1r, uC1g, uC1b);
  vec3 color2 = vec3(uC2r, uC2g, uC2b);
  vec3 color3 = vec3(uC3r, uC3g, uC3b);
  float clearcoat = 1.0;
  float clearcoatRoughness = 0.5;

  #include <clipping_planes_fragment>

  vec4 diffuseColor = vec4(
      mix(mix(color1, color2, smoothstep(-3.0, 3.0, vPos.x)), color3, vPos.z),
      1);
  // diffuseColor는 오브젝트의 베이스 색상 (환경이나 빛이 고려되지 않은 본연의
  // 색)

  // mix(x, y, a): a를 축으로 했을 때 가장 낮은 값에서 x값의 영향력을 100%, 가장
  // 높은 값에서 y값의 영향력을 100%로 만든다. smoothstep(x, y, a): a축을
  // 기준으로 x를 최소값, y를 최대값으로 그 사이의 값을 쪼갠다. x와 y 사이를
  // 0-100 사이의 그라디언트처럼 단계별로 표현하고, x 미만의 값은 0, y 이상의
  // 값은 100으로 처리

  // 1. smoothstep(-3.0, 3.0,vPos.x)로 x축의 그라디언트가 표현 될 범위를 -3,
  // 3으로 정한다.
  // 2. mix(color1, color3, smoothstep(-3.0, 3.0,vPos.x))로 color1과 color3을
  // 위의 범위 안에서 그라디언트로 표현한다.
  // 예를 들어 color1이 노랑, color3이 파랑이라고 치면, x축 기준 -3부터 3까지
  // 노랑과 파랑 사이의 그라디언트가 나타나고, -3보다 작은 값에서는 계속 노랑,
  // 3보다 큰 값에서는 계속 파랑이 나타난다.
  // 3. mix()를 한 번 더 사용해서 위의 그라디언트와 color2를 z축 기준으로
  // 분배한다.

  //-------- materiality ------------
  ReflectedLight reflectedLight =
      ReflectedLight(vec3(0.0), vec3(0.0), vec3(0.0), vec3(0.0));
  vec3 totalEmissiveRadiance = emissive;

  #ifdef TRANSMISSION
    float totalTransmission = transmission;
  #endif
  #include <logdepthbuf_fragment>
  #include <map_fragment>
  #include <color_fragment>
  #include <alphamap_fragment>
  #include <alphatest_fragment>
  #include <roughnessmap_fragment>
  #include <metalnessmap_fragment>
  #include <normal_fragment_begin>
  #include <normal_fragment_maps>
  #include <clearcoat_normal_fragment_begin>
  #include <clearcoat_normal_fragment_maps>
  #include <emissivemap_fragment>
  // #include <transmissionmap_fragment>
  #include <lights_physical_fragment>
  #include <lights_fragment_begin>
  #include <lights_fragment_maps>
  #include <lights_fragment_end>
  #include <aomap_fragment>
    vec3 outgoingLight =
        reflectedLight.directDiffuse + reflectedLight.indirectDiffuse +
        reflectedLight.directSpecular + reflectedLight.indirectSpecular;
    //위에서 정의한 diffuseColor에 환경이나 반사값들을 반영한 값.
  #ifdef TRANSMISSION
    diffuseColor.a *=
        mix(saturate(1. - totalTransmission +
                    linearToRelativeLuminance2(reflectedLight.directSpecular +
                                              reflectedLight.indirectSpecular)),
            1.0, metalness);
  #endif


  #include <tonemapping_fragment>
  #include <encodings_fragment>
  #include <fog_fragment>
  #include <premultiplied_alpha_fragment>
  #include <dithering_fragment>


  gl_FragColor = vec4(outgoingLight, diffuseColor.a);
  // gl_FragColor가 fragment shader를 통해 나타나는 최종값으로, diffuseColor에서
  // 정의한 그라디언트 색상 위에 반사나 빛을 계산한 값을 최종값으로 정의.
  // gl_FragColor = vec4(mix(mix(color1, color3, smoothstep(-3.0, 3.0,vPos.x)),
  // color2, vNormal.z), 1.0); 위처럼 최종값을 그라디언트 값 자체를 넣으면 환경
  // 영향없는 그라디언트만 표현됨.
}
`;
var e3 = `// #pragma glslify: cnoise3 = require(glsl-noise/classic/3d) 

// noise source from https://github.com/hughsk/glsl-noise/blob/master/periodic/3d.glsl

vec3 mod289(vec3 x)
{
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 mod289(vec4 x)
{
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 permute(vec4 x)
{
  return mod289(((x*34.0)+1.0)*x);
}

vec4 taylorInvSqrt(vec4 r)
{
  return 1.79284291400159 - 0.85373472095314 * r;
}

vec3 fade(vec3 t) {
  return t*t*t*(t*(t*6.0-15.0)+10.0);
}

float cnoise(vec3 P)
{
  vec3 Pi0 = floor(P); // Integer part for indexing
  vec3 Pi1 = Pi0 + vec3(1.0); // Integer part + 1
  Pi0 = mod289(Pi0);
  Pi1 = mod289(Pi1);
  vec3 Pf0 = fract(P); // Fractional part for interpolation
  vec3 Pf1 = Pf0 - vec3(1.0); // Fractional part - 1.0
  vec4 ix = vec4(Pi0.x, Pi1.x, Pi0.x, Pi1.x);
  vec4 iy = vec4(Pi0.yy, Pi1.yy);
  vec4 iz0 = Pi0.zzzz;
  vec4 iz1 = Pi1.zzzz;

  vec4 ixy = permute(permute(ix) + iy);
  vec4 ixy0 = permute(ixy + iz0);
  vec4 ixy1 = permute(ixy + iz1);

  vec4 gx0 = ixy0 * (1.0 / 7.0);
  vec4 gy0 = fract(floor(gx0) * (1.0 / 7.0)) - 0.5;
  gx0 = fract(gx0);
  vec4 gz0 = vec4(0.5) - abs(gx0) - abs(gy0);
  vec4 sz0 = step(gz0, vec4(0.0));
  gx0 -= sz0 * (step(0.0, gx0) - 0.5);
  gy0 -= sz0 * (step(0.0, gy0) - 0.5);

  vec4 gx1 = ixy1 * (1.0 / 7.0);
  vec4 gy1 = fract(floor(gx1) * (1.0 / 7.0)) - 0.5;
  gx1 = fract(gx1);
  vec4 gz1 = vec4(0.5) - abs(gx1) - abs(gy1);
  vec4 sz1 = step(gz1, vec4(0.0));
  gx1 -= sz1 * (step(0.0, gx1) - 0.5);
  gy1 -= sz1 * (step(0.0, gy1) - 0.5);

  vec3 g000 = vec3(gx0.x,gy0.x,gz0.x);
  vec3 g100 = vec3(gx0.y,gy0.y,gz0.y);
  vec3 g010 = vec3(gx0.z,gy0.z,gz0.z);
  vec3 g110 = vec3(gx0.w,gy0.w,gz0.w);
  vec3 g001 = vec3(gx1.x,gy1.x,gz1.x);
  vec3 g101 = vec3(gx1.y,gy1.y,gz1.y);
  vec3 g011 = vec3(gx1.z,gy1.z,gz1.z);
  vec3 g111 = vec3(gx1.w,gy1.w,gz1.w);

  vec4 norm0 = taylorInvSqrt(vec4(dot(g000, g000), dot(g010, g010), dot(g100, g100), dot(g110, g110)));
  g000 *= norm0.x;
  g010 *= norm0.y;
  g100 *= norm0.z;
  g110 *= norm0.w;
  vec4 norm1 = taylorInvSqrt(vec4(dot(g001, g001), dot(g011, g011), dot(g101, g101), dot(g111, g111)));
  g001 *= norm1.x;
  g011 *= norm1.y;
  g101 *= norm1.z;
  g111 *= norm1.w;

  float n000 = dot(g000, Pf0);
  float n100 = dot(g100, vec3(Pf1.x, Pf0.yz));
  float n010 = dot(g010, vec3(Pf0.x, Pf1.y, Pf0.z));
  float n110 = dot(g110, vec3(Pf1.xy, Pf0.z));
  float n001 = dot(g001, vec3(Pf0.xy, Pf1.z));
  float n101 = dot(g101, vec3(Pf1.x, Pf0.y, Pf1.z));
  float n011 = dot(g011, vec3(Pf0.x, Pf1.yz));
  float n111 = dot(g111, Pf1);

  vec3 fade_xyz = fade(Pf0);
  vec4 n_z = mix(vec4(n000, n100, n010, n110), vec4(n001, n101, n011, n111), fade_xyz.z);
  vec2 n_yz = mix(n_z.xy, n_z.zw, fade_xyz.y);
  float n_xyz = mix(n_yz.x, n_yz.y, fade_xyz.x); 
  return 2.2 * n_xyz;
}

//-------- start here ------------

mat3 rotation3dY(float angle) {
  float s = sin(angle);
  float c = cos(angle);

  return mat3(c, 0.0, -s, 0.0, 1.0, 0.0, s, 0.0, c);
}

vec3 rotateY(vec3 v, float angle) { return rotation3dY(angle) * v; }

varying vec3 vNormal;
varying float displacement;
varying vec3 vPos;
varying float vDistort;

varying vec2 vUv;

uniform float uTime;
uniform float uSpeed;
uniform float uLoop;
uniform float uLoopDuration;

uniform float uLoadingTime;

uniform float uNoiseDensity;
uniform float uNoiseStrength;

#define STANDARD
varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif
#include <clipping_planes_pars_vertex>
#include <color_pars_vertex>
#include <common>
#include <displacementmap_pars_vertex>
#include <fog_pars_vertex>
#include <logdepthbuf_pars_vertex>
#include <morphtarget_pars_vertex>
#include <shadowmap_pars_vertex>
#include <skinning_pars_vertex>
#include <uv2_pars_vertex>
#include <uv_pars_vertex>

void main() {

  #include <beginnormal_vertex>
  #include <color_vertex>
  #include <defaultnormal_vertex>
  #include <morphnormal_vertex>
  #include <skinbase_vertex>
  #include <skinnormal_vertex>
  #include <uv2_vertex>
  #include <uv_vertex>
  #ifndef FLAT_SHADED
    vNormal = normalize(transformedNormal);
  #ifdef USE_TANGENT
    vTangent = normalize(transformedTangent);
    vBitangent = normalize(cross(vNormal, vTangent) * tangent.w);
  #endif
  #endif
  #include <begin_vertex>

  #include <clipping_planes_vertex>
  #include <displacementmap_vertex>
  #include <logdepthbuf_vertex>
  #include <morphtarget_vertex>
  #include <project_vertex>
  #include <skinning_vertex>
    vViewPosition = -mvPosition.xyz;
  #include <fog_vertex>
  #include <shadowmap_vertex>
  #include <worldpos_vertex>

  //-------- start vertex ------------
  vUv = uv;

  float t = uTime * uSpeed;
  
  // For seamless loops, sample noise using 4D-like circular interpolation
  vec3 noisePos = 0.43 * position * uNoiseDensity;
  float distortion;
  
  if (uLoop > 0.5) {
    // Create truly dynamic seamless loop using 4D noise simulation
    // Loop progress only depends on time and duration, not speed
    float loopProgress = uTime / uLoopDuration;
    float angle = loopProgress * 6.28318530718; // 2*PI
    
    // Radius scales with speed to maintain consistent visual speed
    // Larger radius = more distance traveled = faster perceived motion
    float radius = 5.0 * uSpeed;
    
    // Sample 4 noise values at cardinal points around the circle
    vec3 offset0 = vec3(cos(angle) * radius, sin(angle) * radius, 0.0);
    vec3 offset1 = vec3(cos(angle + 1.57079632679) * radius, sin(angle + 1.57079632679) * radius, 0.0);
    vec3 offset2 = vec3(cos(angle + 3.14159265359) * radius, sin(angle + 3.14159265359) * radius, 0.0);
    vec3 offset3 = vec3(cos(angle + 4.71238898038) * radius, sin(angle + 4.71238898038) * radius, 0.0);
    
    // Get noise at all 4 points
    float n0 = cnoise(noisePos + offset0);
    float n1 = cnoise(noisePos + offset1);
    float n2 = cnoise(noisePos + offset2);
    float n3 = cnoise(noisePos + offset3);
    
    // Smooth interpolation weights using cosine
    float w0 = (cos(angle) + 1.0) * 0.5;
    float w1 = (cos(angle + 1.57079632679) + 1.0) * 0.5;
    float w2 = (cos(angle + 3.14159265359) + 1.0) * 0.5;
    float w3 = (cos(angle + 4.71238898038) + 1.0) * 0.5;
    
    // Normalize weights
    float totalWeight = w0 + w1 + w2 + w3;
    w0 /= totalWeight;
    w1 /= totalWeight;
    w2 /= totalWeight;
    w3 /= totalWeight;
    
    // Blend all samples with amplitude boost to match single-sample strength
    // Blending reduces amplitude by ~30%, so we compensate
    float blendedNoise = n0 * w0 + n1 * w1 + n2 * w2 + n3 * w3;
    distortion = 0.75 * blendedNoise * 1.5;
  } else {
    // Normal linear time progression
    distortion = 0.75 * cnoise(noisePos + t);
  }

  vec3 pos = position + normal * distortion * uNoiseStrength * uLoadingTime;
  vPos = pos;

  gl_Position = projectionMatrix * modelViewMatrix * vec4(pos, 1.);
}
`;

// node_modules/@shadergradient/react/dist/chunk-VE5U2NIR.mjs
var i2 = {};
A(i2, { fragment: () => n4, vertex: () => e4 });
var n4 = `
#define STANDARD
#ifdef PHYSICAL
#define REFLECTIVITY
#define CLEARCOAT
#define TRANSMISSION
#endif
uniform vec3 diffuse;
uniform vec3 emissive;
uniform float roughness;
uniform float metalness;
uniform float opacity;
#ifdef TRANSMISSION
uniform float transmission;
#endif
#ifdef REFLECTIVITY
uniform float reflectivity;
#endif
#ifdef CLEARCOAT
uniform float clearcoat;
uniform float clearcoatRoughness;
#endif
#ifdef USE_SHEEN
uniform vec3 sheen;
#endif
varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif
#include <alphamap_pars_fragment>
#include <aomap_pars_fragment>
#include <color_pars_fragment>
#include <common>
#include <dithering_pars_fragment>
#include <emissivemap_pars_fragment>
#include <lightmap_pars_fragment>
#include <map_pars_fragment>
#include <packing>
#include <uv2_pars_fragment>
#include <uv_pars_fragment>
// #include <transmissionmap_pars_fragment>
#include <bsdfs>
#include <bumpmap_pars_fragment>
#include <clearcoat_pars_fragment>
#include <clipping_planes_pars_fragment>
#include <cube_uv_reflection_fragment>
#include <envmap_common_pars_fragment>
#include <envmap_physical_pars_fragment>
#include <fog_pars_fragment>
#include <lights_pars_begin>
#include <lights_physical_pars_fragment>
#include <logdepthbuf_pars_fragment>
#include <metalnessmap_pars_fragment>
#include <normalmap_pars_fragment>
#include <roughnessmap_pars_fragment>
#include <shadowmap_pars_fragment>
// include를 통해 가져온 값은 대부분 환경, 빛 등을 계산하기 위해서 기본 fragment
// shader의 값들을 받아왔습니다. 일단은 무시하셔도 됩니다.
varying vec3 vNormal;
varying float displacement;
varying vec3 vPos;
varying float vDistort;
uniform float uC1r;
uniform float uC1g;
uniform float uC1b;
uniform float uC2r;
uniform float uC2g;
uniform float uC2b;
uniform float uC3r;
uniform float uC3g;
uniform float uC3b;
varying vec3 color1;
varying vec3 color2;
varying vec3 color3;
varying float distanceToCenter;


// for npm package, need to add this manually
// 'linearToRelativeLuminance' : function already has a body
float linearToRelativeLuminance2( const in vec3 color ) {
    vec3 weights = vec3( 0.2126, 0.7152, 0.0722 );
    return dot( weights, color.rgb );
}

void main() {
  //-------- basic gradient ------------
  vec3 color1 = vec3(uC1r, uC1g, uC1b);
  vec3 color2 = vec3(uC2r, uC2g, uC2b);
  vec3 color3 = vec3(uC3r, uC3g, uC3b);
  float clearcoat = 1.0;
  float clearcoatRoughness = 0.5;
#include <clipping_planes_fragment>

  float distanceToCenter = distance(vPos, vec3(0, 0, 0));
  // distanceToCenter로 중심점과의 거리를 구함.

  vec4 diffuseColor =
      vec4(mix(color3, mix(color2, color1, smoothstep(-1.0, 1.0, vPos.y)),
               distanceToCenter),
           1);

  //-------- materiality ------------
  ReflectedLight reflectedLight =
      ReflectedLight(vec3(0.0), vec3(0.0), vec3(0.0), vec3(0.0));
  vec3 totalEmissiveRadiance = emissive;
#ifdef TRANSMISSION
  float totalTransmission = transmission;
#endif
#include <logdepthbuf_fragment>
#include <map_fragment>
#include <color_fragment>
#include <alphamap_fragment>
#include <alphatest_fragment>
#include <roughnessmap_fragment>
#include <metalnessmap_fragment>
#include <normal_fragment_begin>
#include <normal_fragment_maps>
#include <clearcoat_normal_fragment_begin>
#include <clearcoat_normal_fragment_maps>
#include <emissivemap_fragment>
// #include <transmissionmap_fragment>
#include <lights_physical_fragment>
#include <lights_fragment_begin>
#include <lights_fragment_maps>
#include <lights_fragment_end>
#include <aomap_fragment>
  vec3 outgoingLight =
      reflectedLight.directDiffuse + reflectedLight.indirectDiffuse +
      reflectedLight.directSpecular + reflectedLight.indirectSpecular;
//위에서 정의한 diffuseColor에 환경이나 반사값들을 반영한 값.
#ifdef TRANSMISSION
  diffuseColor.a *=
      mix(saturate(1. - totalTransmission +
                   linearToRelativeLuminance2(reflectedLight.directSpecular +
                                             reflectedLight.indirectSpecular)),
          1.0, metalness);
#endif
  gl_FragColor = vec4(outgoingLight, diffuseColor.a);
  // gl_FragColor가 fragment shader를 통해 나타나는 최종값으로, diffuseColor에서
  // 정의한 그라디언트 색상 위에 반사나 빛을 계산한 값을 최종값으로 정의.
  // gl_FragColor = vec4(mix(mix(color1, color3, smoothstep(-3.0, 3.0,vPos.x)),
  // color2, vNormal.z), 1.0); 위처럼 최종값을 그라디언트 값 자체를 넣으면 환경
  // 영향없는 그라디언트만 표현됨.

#include <tonemapping_fragment>
#include <encodings_fragment>
#include <fog_fragment>
#include <premultiplied_alpha_fragment>
#include <dithering_fragment>
}
`;
var e4 = `// #pragma glslify: pnoise = require(glsl-noise/periodic/3d)

vec3 mod289(vec3 x)
{
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 mod289(vec4 x)
{
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 permute(vec4 x)
{
  return mod289(((x*34.0)+1.0)*x);
}

vec4 taylorInvSqrt(vec4 r)
{
  return 1.79284291400159 - 0.85373472095314 * r;
}

vec3 fade(vec3 t) {
  return t*t*t*(t*(t*6.0-15.0)+10.0);
}

// Classic Perlin noise, periodic variant
float pnoise(vec3 P, vec3 rep)
{
  vec3 Pi0 = mod(floor(P), rep); // Integer part, modulo period
  vec3 Pi1 = mod(Pi0 + vec3(1.0), rep); // Integer part + 1, mod period
  Pi0 = mod289(Pi0);
  Pi1 = mod289(Pi1);
  vec3 Pf0 = fract(P); // Fractional part for interpolation
  vec3 Pf1 = Pf0 - vec3(1.0); // Fractional part - 1.0
  vec4 ix = vec4(Pi0.x, Pi1.x, Pi0.x, Pi1.x);
  vec4 iy = vec4(Pi0.yy, Pi1.yy);
  vec4 iz0 = Pi0.zzzz;
  vec4 iz1 = Pi1.zzzz;

  vec4 ixy = permute(permute(ix) + iy);
  vec4 ixy0 = permute(ixy + iz0);
  vec4 ixy1 = permute(ixy + iz1);

  vec4 gx0 = ixy0 * (1.0 / 7.0);
  vec4 gy0 = fract(floor(gx0) * (1.0 / 7.0)) - 0.5;
  gx0 = fract(gx0);
  vec4 gz0 = vec4(0.5) - abs(gx0) - abs(gy0);
  vec4 sz0 = step(gz0, vec4(0.0));
  gx0 -= sz0 * (step(0.0, gx0) - 0.5);
  gy0 -= sz0 * (step(0.0, gy0) - 0.5);

  vec4 gx1 = ixy1 * (1.0 / 7.0);
  vec4 gy1 = fract(floor(gx1) * (1.0 / 7.0)) - 0.5;
  gx1 = fract(gx1);
  vec4 gz1 = vec4(0.5) - abs(gx1) - abs(gy1);
  vec4 sz1 = step(gz1, vec4(0.0));
  gx1 -= sz1 * (step(0.0, gx1) - 0.5);
  gy1 -= sz1 * (step(0.0, gy1) - 0.5);

  vec3 g000 = vec3(gx0.x,gy0.x,gz0.x);
  vec3 g100 = vec3(gx0.y,gy0.y,gz0.y);
  vec3 g010 = vec3(gx0.z,gy0.z,gz0.z);
  vec3 g110 = vec3(gx0.w,gy0.w,gz0.w);
  vec3 g001 = vec3(gx1.x,gy1.x,gz1.x);
  vec3 g101 = vec3(gx1.y,gy1.y,gz1.y);
  vec3 g011 = vec3(gx1.z,gy1.z,gz1.z);
  vec3 g111 = vec3(gx1.w,gy1.w,gz1.w);

  vec4 norm0 = taylorInvSqrt(vec4(dot(g000, g000), dot(g010, g010), dot(g100, g100), dot(g110, g110)));
  g000 *= norm0.x;
  g010 *= norm0.y;
  g100 *= norm0.z;
  g110 *= norm0.w;
  vec4 norm1 = taylorInvSqrt(vec4(dot(g001, g001), dot(g011, g011), dot(g101, g101), dot(g111, g111)));
  g001 *= norm1.x;
  g011 *= norm1.y;
  g101 *= norm1.z;
  g111 *= norm1.w;

  float n000 = dot(g000, Pf0);
  float n100 = dot(g100, vec3(Pf1.x, Pf0.yz));
  float n010 = dot(g010, vec3(Pf0.x, Pf1.y, Pf0.z));
  float n110 = dot(g110, vec3(Pf1.xy, Pf0.z));
  float n001 = dot(g001, vec3(Pf0.xy, Pf1.z));
  float n101 = dot(g101, vec3(Pf1.x, Pf0.y, Pf1.z));
  float n011 = dot(g011, vec3(Pf0.x, Pf1.yz));
  float n111 = dot(g111, Pf1);

  vec3 fade_xyz = fade(Pf0);
  vec4 n_z = mix(vec4(n000, n100, n010, n110), vec4(n001, n101, n011, n111), fade_xyz.z);
  vec2 n_yz = mix(n_z.xy, n_z.zw, fade_xyz.y);
  float n_xyz = mix(n_yz.x, n_yz.y, fade_xyz.x);
  return 2.2 * n_xyz;
}


//-------- start here ------------

varying vec3 vNormal;
uniform float uTime;
uniform float uSpeed;
uniform float uLoop;
uniform float uLoopDuration;
uniform float uNoiseDensity;
uniform float uNoiseStrength;
uniform float uFrequency;
uniform float uAmplitude;
varying vec3 vPos;
varying float vDistort;
varying vec2 vUv;
varying vec3 vViewPosition;

#define STANDARD
#ifndef FLAT_SHADED
  #ifdef USE_TANGENT
    varying vec3 vTangent;
    varying vec3 vBitangent;
  #endif
#endif

#include <clipping_planes_pars_vertex>
#include <color_pars_vertex>
#include <common>
#include <displacementmap_pars_vertex>
#include <fog_pars_vertex>
#include <logdepthbuf_pars_vertex>
#include <morphtarget_pars_vertex>
#include <shadowmap_pars_vertex>
#include <skinning_pars_vertex>
#include <uv2_pars_vertex>
#include <uv_pars_vertex>


// rotation
mat3 rotation3dY(float angle) {
  float s = sin(angle);
  float c = cos(angle);
  return mat3(c, 0.0, -s, 0.0, 1.0, 0.0, s, 0.0, c);
}

vec3 rotateY(vec3 v, float angle) { return rotation3dY(angle) * v; }

void main() {
  #include <beginnormal_vertex>
  #include <color_vertex>
  #include <defaultnormal_vertex>
  #include <morphnormal_vertex>
  #include <skinbase_vertex>
  #include <skinnormal_vertex>
  #include <uv2_vertex>
  #include <uv_vertex>
  #ifndef FLAT_SHADED
    vNormal = normalize(transformedNormal);
  #ifdef USE_TANGENT
    vTangent = normalize(transformedTangent);
    vBitangent = normalize(cross(vNormal, vTangent) * tangent.w);
  #endif
  #endif
  #include <begin_vertex>

  #include <clipping_planes_vertex>
  #include <displacementmap_vertex>
  #include <logdepthbuf_vertex>
  #include <morphtarget_vertex>
  #include <project_vertex>
  #include <skinning_vertex>
    vViewPosition = -mvPosition.xyz;
  #include <fog_vertex>
  #include <shadowmap_vertex>
  #include <worldpos_vertex>

  //-------- start vertex ------------
  float t = uTime * uSpeed;
  
  // For seamless loops, sample noise using 4D-like circular interpolation
  float distortion;
  float angle;
  
  if (uLoop > 0.5) {
    // Create truly dynamic seamless loop using 4D noise simulation
    float loopProgress = uTime / uLoopDuration;
    float loopAngle = loopProgress * 6.28318530718; // 2*PI
    
    // Radius scales with speed to maintain consistent visual speed
    float radius = 5.0 * uSpeed;
    
    // Sample 4 noise values at cardinal points
    vec3 offset0 = vec3(cos(loopAngle) * radius, sin(loopAngle) * radius, 0.0);
    vec3 offset1 = vec3(cos(loopAngle + 1.57079632679) * radius, sin(loopAngle + 1.57079632679) * radius, 0.0);
    vec3 offset2 = vec3(cos(loopAngle + 3.14159265359) * radius, sin(loopAngle + 3.14159265359) * radius, 0.0);
    vec3 offset3 = vec3(cos(loopAngle + 4.71238898038) * radius, sin(loopAngle + 4.71238898038) * radius, 0.0);
    
    // Get noise at all 4 points
    float n0 = pnoise((normal + offset0) * uNoiseDensity, vec3(10.0));
    float n1 = pnoise((normal + offset1) * uNoiseDensity, vec3(10.0));
    float n2 = pnoise((normal + offset2) * uNoiseDensity, vec3(10.0));
    float n3 = pnoise((normal + offset3) * uNoiseDensity, vec3(10.0));
    
    // Smooth interpolation weights
    float w0 = (cos(loopAngle) + 1.0) * 0.5;
    float w1 = (cos(loopAngle + 1.57079632679) + 1.0) * 0.5;
    float w2 = (cos(loopAngle + 3.14159265359) + 1.0) * 0.5;
    float w3 = (cos(loopAngle + 4.71238898038) + 1.0) * 0.5;
    
    float totalWeight = w0 + w1 + w2 + w3;
    w0 /= totalWeight;
    w1 /= totalWeight;
    w2 /= totalWeight;
    w3 /= totalWeight;
    
    // Blend samples with amplitude boost to match single-sample strength
    float blendedNoise = n0 * w0 + n1 * w1 + n2 * w2 + n3 * w3;
    distortion = blendedNoise * 1.5 * uNoiseStrength;
    
    // Apply loop to spiral effect with blended offset
    float angleOffset = offset0.x * w0 + offset1.x * w1 + offset2.x * w2 + offset3.x * w3;
    angle = sin(uv.y * uFrequency + angleOffset) * uAmplitude;
  } else {
    // Normal linear time progression
    distortion = pnoise((normal + t) * uNoiseDensity, vec3(10.0)) * uNoiseStrength;
    angle = sin(uv.y * uFrequency + t) * uAmplitude;
  }
  
  vec3 pos = position + (normal * distortion);
  pos = rotateY(pos, angle);

  vPos = pos;
  vDistort = distortion;
  vNormal = normal;
  vUv = uv;

  gl_Position = projectionMatrix * modelViewMatrix * vec4(pos, 1.);
}
`;

// node_modules/@shadergradient/react/dist/chunk-S63T3L7T.mjs
var o3 = {};
A(o3, { fragment: () => n5, vertex: () => e5 });
var n5 = `
#define STANDARD
#ifdef PHYSICAL
#define REFLECTIVITY
#define CLEARCOAT
#define TRANSMISSION
#endif

uniform vec3 diffuse;
uniform vec3 emissive;
uniform float roughness;
uniform float metalness;
uniform float opacity;

#ifdef TRANSMISSION
uniform float transmission;
#endif
#ifdef REFLECTIVITY
uniform float reflectivity;
#endif
#ifdef CLEARCOAT
uniform float clearcoat;
uniform float clearcoatRoughness;
#endif
#ifdef USE_SHEEN
uniform vec3 sheen;
#endif
varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif
#include <alphamap_pars_fragment>
#include <aomap_pars_fragment>
#include <color_pars_fragment>
#include <common>
#include <dithering_pars_fragment>
#include <emissivemap_pars_fragment>
#include <lightmap_pars_fragment>
#include <map_pars_fragment>
#include <packing>
#include <uv2_pars_fragment>
#include <uv_pars_fragment>
// #include <transmissionmap_pars_fragment>
#include <bsdfs>
#include <bumpmap_pars_fragment>
#include <clearcoat_pars_fragment>
#include <clipping_planes_pars_fragment>
#include <cube_uv_reflection_fragment>
#include <envmap_common_pars_fragment>
#include <envmap_physical_pars_fragment>
#include <fog_pars_fragment>
#include <lights_pars_begin>
#include <lights_physical_pars_fragment>
#include <logdepthbuf_pars_fragment>
#include <metalnessmap_pars_fragment>
#include <normalmap_pars_fragment>
#include <roughnessmap_pars_fragment>
#include <shadowmap_pars_fragment>
// include를 통해 가져온 값은 대부분 환경, 빛 등을 계산하기 위해서 기본 fragment
// shader의 값들을 받아왔습니다. 일단은 무시하셔도 됩니다.

varying vec3 vNormal;
varying float displacement;
varying vec3 vPos;
varying float vDistort;

uniform float uC1r;
uniform float uC1g;
uniform float uC1b;
uniform float uC2r;
uniform float uC2g;
uniform float uC2b;
uniform float uC3r;
uniform float uC3g;
uniform float uC3b;

varying vec3 color1;
varying vec3 color2;
varying vec3 color3;

// for npm package, need to add this manually
// 'linearToRelativeLuminance' : function already has a body
float linearToRelativeLuminance2( const in vec3 color ) {
    vec3 weights = vec3( 0.2126, 0.7152, 0.0722 );
    return dot( weights, color.rgb );
}

void main() {

  //-------- basic gradient ------------
  vec3 color1 = vec3(uC1r, uC1g, uC1b);
  vec3 color2 = vec3(uC2r, uC2g, uC2b);
  vec3 color3 = vec3(uC3r, uC3g, uC3b);
  float clearcoat = 1.0;
  float clearcoatRoughness = 0.5;

  #include <clipping_planes_fragment>

  vec4 diffuseColor = vec4(
      mix(mix(color1, color2, smoothstep(-3.0, 3.0, vPos.x)), color3, vPos.z),
      1);
  // diffuseColor는 오브젝트의 베이스 색상 (환경이나 빛이 고려되지 않은 본연의
  // 색)

  // mix(x, y, a): a를 축으로 했을 때 가장 낮은 값에서 x값의 영향력을 100%, 가장
  // 높은 값에서 y값의 영향력을 100%로 만든다. smoothstep(x, y, a): a축을
  // 기준으로 x를 최소값, y를 최대값으로 그 사이의 값을 쪼갠다. x와 y 사이를
  // 0-100 사이의 그라디언트처럼 단계별로 표현하고, x 미만의 값은 0, y 이상의
  // 값은 100으로 처리

  // 1. smoothstep(-3.0, 3.0,vPos.x)로 x축의 그라디언트가 표현 될 범위를 -3,
  // 3으로 정한다.
  // 2. mix(color1, color3, smoothstep(-3.0, 3.0,vPos.x))로 color1과 color3을
  // 위의 범위 안에서 그라디언트로 표현한다.
  // 예를 들어 color1이 노랑, color3이 파랑이라고 치면, x축 기준 -3부터 3까지
  // 노랑과 파랑 사이의 그라디언트가 나타나고, -3보다 작은 값에서는 계속 노랑,
  // 3보다 큰 값에서는 계속 파랑이 나타난다.
  // 3. mix()를 한 번 더 사용해서 위의 그라디언트와 color2를 z축 기준으로
  // 분배한다.

  //-------- materiality ------------
  ReflectedLight reflectedLight =
      ReflectedLight(vec3(0.0), vec3(0.0), vec3(0.0), vec3(0.0));
  vec3 totalEmissiveRadiance = emissive;

  #ifdef TRANSMISSION
    float totalTransmission = transmission;
  #endif
  #include <logdepthbuf_fragment>
  #include <map_fragment>
  #include <color_fragment>
  #include <alphamap_fragment>
  #include <alphatest_fragment>
  #include <roughnessmap_fragment>
  #include <metalnessmap_fragment>
  #include <normal_fragment_begin>
  #include <normal_fragment_maps>
  #include <clearcoat_normal_fragment_begin>
  #include <clearcoat_normal_fragment_maps>
  #include <emissivemap_fragment>
  // #include <transmissionmap_fragment>
  #include <lights_physical_fragment>
  #include <lights_fragment_begin>
  #include <lights_fragment_maps>
  #include <lights_fragment_end>
  #include <aomap_fragment>
    vec3 outgoingLight =
        reflectedLight.directDiffuse + reflectedLight.indirectDiffuse +
        reflectedLight.directSpecular + reflectedLight.indirectSpecular;
    //위에서 정의한 diffuseColor에 환경이나 반사값들을 반영한 값.
  #ifdef TRANSMISSION
    diffuseColor.a *=
        mix(saturate(1. - totalTransmission +
                    linearToRelativeLuminance2(reflectedLight.directSpecular +
                                              reflectedLight.indirectSpecular)),
            1.0, metalness);
  #endif


  #include <tonemapping_fragment>
  #include <encodings_fragment>
  #include <fog_fragment>
  #include <premultiplied_alpha_fragment>
  #include <dithering_fragment>


  gl_FragColor = vec4(outgoingLight, diffuseColor.a);
  // gl_FragColor가 fragment shader를 통해 나타나는 최종값으로, diffuseColor에서
  // 정의한 그라디언트 색상 위에 반사나 빛을 계산한 값을 최종값으로 정의.
  // gl_FragColor = vec4(mix(mix(color1, color3, smoothstep(-3.0, 3.0,vPos.x)),
  // color2, vNormal.z), 1.0); 위처럼 최종값을 그라디언트 값 자체를 넣으면 환경
  // 영향없는 그라디언트만 표현됨.
}
`;
var e5 = `// #pragma glslify: cnoise3 = require(glsl-noise/classic/3d) 
vec3 mod289(vec3 x)
{
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 mod289(vec4 x)
{
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 permute(vec4 x)
{
  return mod289(((x*34.0)+1.0)*x);
}

vec4 taylorInvSqrt(vec4 r)
{
  return 1.79284291400159 - 0.85373472095314 * r;
}

vec3 fade(vec3 t) {
  return t*t*t*(t*(t*6.0-15.0)+10.0);
}

float cnoise(vec3 P)
{
  vec3 Pi0 = floor(P); // Integer part for indexing
  vec3 Pi1 = Pi0 + vec3(1.0); // Integer part + 1
  Pi0 = mod289(Pi0);
  Pi1 = mod289(Pi1);
  vec3 Pf0 = fract(P); // Fractional part for interpolation
  vec3 Pf1 = Pf0 - vec3(1.0); // Fractional part - 1.0
  vec4 ix = vec4(Pi0.x, Pi1.x, Pi0.x, Pi1.x);
  vec4 iy = vec4(Pi0.yy, Pi1.yy);
  vec4 iz0 = Pi0.zzzz;
  vec4 iz1 = Pi1.zzzz;

  vec4 ixy = permute(permute(ix) + iy);
  vec4 ixy0 = permute(ixy + iz0);
  vec4 ixy1 = permute(ixy + iz1);

  vec4 gx0 = ixy0 * (1.0 / 7.0);
  vec4 gy0 = fract(floor(gx0) * (1.0 / 7.0)) - 0.5;
  gx0 = fract(gx0);
  vec4 gz0 = vec4(0.5) - abs(gx0) - abs(gy0);
  vec4 sz0 = step(gz0, vec4(0.0));
  gx0 -= sz0 * (step(0.0, gx0) - 0.5);
  gy0 -= sz0 * (step(0.0, gy0) - 0.5);

  vec4 gx1 = ixy1 * (1.0 / 7.0);
  vec4 gy1 = fract(floor(gx1) * (1.0 / 7.0)) - 0.5;
  gx1 = fract(gx1);
  vec4 gz1 = vec4(0.5) - abs(gx1) - abs(gy1);
  vec4 sz1 = step(gz1, vec4(0.0));
  gx1 -= sz1 * (step(0.0, gx1) - 0.5);
  gy1 -= sz1 * (step(0.0, gy1) - 0.5);

  vec3 g000 = vec3(gx0.x,gy0.x,gz0.x);
  vec3 g100 = vec3(gx0.y,gy0.y,gz0.y);
  vec3 g010 = vec3(gx0.z,gy0.z,gz0.z);
  vec3 g110 = vec3(gx0.w,gy0.w,gz0.w);
  vec3 g001 = vec3(gx1.x,gy1.x,gz1.x);
  vec3 g101 = vec3(gx1.y,gy1.y,gz1.y);
  vec3 g011 = vec3(gx1.z,gy1.z,gz1.z);
  vec3 g111 = vec3(gx1.w,gy1.w,gz1.w);

  vec4 norm0 = taylorInvSqrt(vec4(dot(g000, g000), dot(g010, g010), dot(g100, g100), dot(g110, g110)));
  g000 *= norm0.x;
  g010 *= norm0.y;
  g100 *= norm0.z;
  g110 *= norm0.w;
  vec4 norm1 = taylorInvSqrt(vec4(dot(g001, g001), dot(g011, g011), dot(g101, g101), dot(g111, g111)));
  g001 *= norm1.x;
  g011 *= norm1.y;
  g101 *= norm1.z;
  g111 *= norm1.w;

  float n000 = dot(g000, Pf0);
  float n100 = dot(g100, vec3(Pf1.x, Pf0.yz));
  float n010 = dot(g010, vec3(Pf0.x, Pf1.y, Pf0.z));
  float n110 = dot(g110, vec3(Pf1.xy, Pf0.z));
  float n001 = dot(g001, vec3(Pf0.xy, Pf1.z));
  float n101 = dot(g101, vec3(Pf1.x, Pf0.y, Pf1.z));
  float n011 = dot(g011, vec3(Pf0.x, Pf1.yz));
  float n111 = dot(g111, Pf1);

  vec3 fade_xyz = fade(Pf0);
  vec4 n_z = mix(vec4(n000, n100, n010, n110), vec4(n001, n101, n011, n111), fade_xyz.z);
  vec2 n_yz = mix(n_z.xy, n_z.zw, fade_xyz.y);
  float n_xyz = mix(n_yz.x, n_yz.y, fade_xyz.x); 
  return 2.2 * n_xyz;
}

//-------- start here ------------

mat3 rotation3dY(float angle) {
  float s = sin(angle);
  float c = cos(angle);

  return mat3(c, 0.0, -s, 0.0, 1.0, 0.0, s, 0.0, c);
}

vec3 rotateY(vec3 v, float angle) { return rotation3dY(angle) * v; }

varying vec3 vNormal;
varying float displacement;
varying vec3 vPos;
varying float vDistort;

uniform float uTime;
uniform float uSpeed;
uniform float uLoop;
uniform float uLoopDuration;
uniform float uNoiseDensity;
uniform float uNoiseStrength;

#define STANDARD
varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif
#include <clipping_planes_pars_vertex>
#include <color_pars_vertex>
#include <common>
#include <displacementmap_pars_vertex>
#include <fog_pars_vertex>
#include <logdepthbuf_pars_vertex>
#include <morphtarget_pars_vertex>
#include <shadowmap_pars_vertex>
#include <skinning_pars_vertex>
#include <uv2_pars_vertex>
#include <uv_pars_vertex>

void main() {

  #include <beginnormal_vertex>
  #include <color_vertex>
  #include <defaultnormal_vertex>
  #include <morphnormal_vertex>
  #include <skinbase_vertex>
  #include <skinnormal_vertex>
  #include <uv2_vertex>
  #include <uv_vertex>
  #ifndef FLAT_SHADED
    vNormal = normalize(transformedNormal);
  #ifdef USE_TANGENT
    vTangent = normalize(transformedTangent);
    vBitangent = normalize(cross(vNormal, vTangent) * tangent.w);
  #endif
  #endif
  #include <begin_vertex>

  #include <clipping_planes_vertex>
  #include <displacementmap_vertex>
  #include <logdepthbuf_vertex>
  #include <morphtarget_vertex>
  #include <project_vertex>
  #include <skinning_vertex>
    vViewPosition = -mvPosition.xyz;
  #include <fog_vertex>
  #include <shadowmap_vertex>
  #include <worldpos_vertex>

  //-------- start vertex ------------
  float t = uTime * uSpeed;
  
  // For seamless loops, sample noise using 4D-like circular interpolation
  vec3 noisePos = 0.43 * position * uNoiseDensity;
  float distortion;
  
  if (uLoop > 0.5) {
    // Create truly dynamic seamless loop using 4D noise simulation
    float loopProgress = uTime / uLoopDuration;
    float angle = loopProgress * 6.28318530718; // 2*PI
    
    // Radius scales with speed to maintain consistent visual speed
    float radius = 5.0 * uSpeed;
    
    // Sample 4 noise values at cardinal points
    vec3 offset0 = vec3(cos(angle) * radius, sin(angle) * radius, 0.0);
    vec3 offset1 = vec3(cos(angle + 1.57079632679) * radius, sin(angle + 1.57079632679) * radius, 0.0);
    vec3 offset2 = vec3(cos(angle + 3.14159265359) * radius, sin(angle + 3.14159265359) * radius, 0.0);
    vec3 offset3 = vec3(cos(angle + 4.71238898038) * radius, sin(angle + 4.71238898038) * radius, 0.0);
    
    // Get noise at all 4 points
    float n0 = cnoise(noisePos + offset0);
    float n1 = cnoise(noisePos + offset1);
    float n2 = cnoise(noisePos + offset2);
    float n3 = cnoise(noisePos + offset3);
    
    // Smooth interpolation weights
    float w0 = (cos(angle) + 1.0) * 0.5;
    float w1 = (cos(angle + 1.57079632679) + 1.0) * 0.5;
    float w2 = (cos(angle + 3.14159265359) + 1.0) * 0.5;
    float w3 = (cos(angle + 4.71238898038) + 1.0) * 0.5;
    
    float totalWeight = w0 + w1 + w2 + w3;
    w0 /= totalWeight;
    w1 /= totalWeight;
    w2 /= totalWeight;
    w3 /= totalWeight;
    
    // Blend samples with amplitude boost to match single-sample strength
    float blendedNoise = n0 * w0 + n1 * w1 + n2 * w2 + n3 * w3;
    distortion = 0.75 * blendedNoise * 1.5;
  } else {
    // Normal linear time progression
    distortion = 0.75 * cnoise(noisePos + t);
  }

  vec3 pos = position + normal * distortion * uNoiseStrength;
  vPos = pos;

  gl_Position = projectionMatrix * modelViewMatrix * vec4(pos, 1.);
}
`;

// node_modules/@shadergradient/react/dist/chunk-5UBL6RAK.mjs
var p2 = {};
A(p2, { plane: () => o2, sphere: () => i2, waterPlane: () => o3 });

// node_modules/@shadergradient/react/dist/chunk-GEYS2LRX.mjs
var i3 = {};
A(i3, { fragment: () => n6, vertex: () => e6 });
var n6 = `// Glass Plane Fragment Shader - Transparency & Refraction

#define STANDARD
#ifdef PHYSICAL
#define REFLECTIVITY
#define CLEARCOAT
#define TRANSMISSION
#endif

uniform vec3 diffuse;
uniform vec3 emissive;
uniform float roughness;
uniform float metalness;
uniform float opacity;

// transmission is already defined by Three.js when TRANSMISSION is enabled
#ifdef REFLECTIVITY
uniform float reflectivity;
#endif
#ifdef CLEARCOAT
uniform float clearcoat;
uniform float clearcoatRoughness;
#endif
#ifdef USE_SHEEN
uniform vec3 sheen;
#endif

varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif

#include <alphamap_pars_fragment>
#include <aomap_pars_fragment>
#include <color_pars_fragment>
#include <common>
#include <dithering_pars_fragment>
#include <emissivemap_pars_fragment>
#include <lightmap_pars_fragment>
#include <map_pars_fragment>
#include <packing>
#include <uv2_pars_fragment>
#include <uv_pars_fragment>
#include <bsdfs>
#include <bumpmap_pars_fragment>
#include <clearcoat_pars_fragment>
#include <clipping_planes_pars_fragment>
#include <envmap_common_pars_fragment>
#include <envmap_pars_fragment>
#include <envmap_physical_pars_fragment>
#include <fog_pars_fragment>
#include <lights_pars_begin>
#include <lights_physical_pars_fragment>
#include <logdepthbuf_pars_fragment>
#include <metalnessmap_pars_fragment>
#include <normalmap_pars_fragment>
#include <roughnessmap_pars_fragment>
#include <shadowmap_pars_fragment>
#include <transmission_pars_fragment>

// Custom uniforms for glass effect
uniform float uTime;
uniform vec3 uColor1;
uniform vec3 uColor2;
uniform vec3 uColor3;
uniform float uTransparency;
uniform float uRefraction;
uniform float uChromaticAberration;
uniform float uFresnelPower;
uniform float uReflectivity;
// envMap and envMapIntensity are provided by Three.js

varying vec2 vUv;
varying vec3 vPosition;
varying vec3 vNormal;
varying vec3 vGlassWorldPos;
varying vec3 vReflect;
varying vec3 vRefract;

// Fresnel calculation
float fresnel(vec3 viewDirection, vec3 normal, float power) {
  return pow(1.0 - dot(viewDirection, normal), power);
}

// Chromatic aberration for refraction
vec3 chromaticRefraction(vec3 viewDirection, vec3 normal, float ior, float chromaticStrength) {
  vec3 refractedR = refract(viewDirection, normal, 1.0 / (ior - chromaticStrength));
  vec3 refractedG = refract(viewDirection, normal, 1.0 / ior);
  vec3 refractedB = refract(viewDirection, normal, 1.0 / (ior + chromaticStrength));
  
  #ifdef ENVMAP_TYPE_CUBE
  return vec3(
    textureCube(envMap, refractedR).r,
    textureCube(envMap, refractedG).g,
    textureCube(envMap, refractedB).b
  );
  #else
  return vec3(0.5);
  #endif
}

void main() {
  #include <clipping_planes_fragment>
  
  vec4 diffuseColor = vec4(diffuse, opacity);
  ReflectedLight reflectedLight = ReflectedLight(vec3(0.0), vec3(0.0), vec3(0.0), vec3(0.0));
  vec3 totalEmissiveRadiance = emissive;
  
  #include <logdepthbuf_fragment>
  #include <map_fragment>
  #include <color_fragment>
  #include <alphamap_fragment>
  #include <alphatest_fragment>
  #include <specularmap_fragment>
  #include <roughnessmap_fragment>
  #include <metalnessmap_fragment>
  #include <normal_fragment_begin>
  #include <normal_fragment_maps>
  #include <clearcoat_normal_fragment_begin>
  #include <clearcoat_normal_fragment_maps>
  #include <emissivemap_fragment>
  
  // Glass-specific calculations
  vec3 viewDirection = normalize(vViewPosition);
  vec3 worldNormal = normalize(vNormal);
  
  // Calculate Fresnel effect
  float fresnelFactor = fresnel(viewDirection, worldNormal, uFresnelPower);
  
  // Base glass color gradient
  vec3 gradientColor = mix(uColor1, uColor2, vUv.y);
  gradientColor = mix(gradientColor, uColor3, fresnelFactor);
  
  // Reflection
  #ifdef ENVMAP_TYPE_CUBE
  vec3 reflectionColor = textureCube(envMap, vReflect).rgb * envMapIntensity;
  #else
  vec3 reflectionColor = vec3(0.5);
  #endif
  
  // Refraction with chromatic aberration
  vec3 refractionColor;
  #ifdef ENVMAP_TYPE_CUBE
  if (uChromaticAberration > 0.0) {
    refractionColor = chromaticRefraction(-viewDirection, worldNormal, uRefraction, uChromaticAberration);
  } else {
    refractionColor = textureCube(envMap, vRefract).rgb;
  }
  refractionColor *= envMapIntensity;
  #else
  refractionColor = vec3(0.3);
  #endif
  
  // Mix reflection and refraction based on Fresnel
  vec3 envColor = mix(refractionColor, reflectionColor, fresnelFactor * uReflectivity);
  
  // Combine with gradient color
  vec3 finalColor = mix(gradientColor, envColor, 0.7);
  
  // Apply transparency
  float finalAlpha = mix(uTransparency, 1.0, fresnelFactor * 0.5);
  
  // Set diffuse color for standard lighting
  diffuseColor.rgb = finalColor;
  diffuseColor.a = finalAlpha;
  
  // Skip transmission_fragment to avoid conflicts
  
  vec3 outgoingLight = reflectedLight.directDiffuse + reflectedLight.indirectDiffuse + 
                       reflectedLight.directSpecular + reflectedLight.indirectSpecular + 
                       totalEmissiveRadiance;
  
  // Add our glass color contribution
  outgoingLight += finalColor * 0.8;
  
  gl_FragColor = vec4(outgoingLight, diffuseColor.a);
  
  #include <tonemapping_fragment>
  #include <colorspace_fragment>
  #include <fog_fragment>
  #include <premultiplied_alpha_fragment>
  #include <dithering_fragment>
}
`;
var e6 = `// Glass Plane Vertex Shader - Refraction & Transparency Effects

#define STANDARD
varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif
#include <clipping_planes_pars_vertex>
#include <color_pars_vertex>
#include <common>
#include <displacementmap_pars_vertex>
#include <fog_pars_vertex>
#include <logdepthbuf_pars_vertex>
#include <morphtarget_pars_vertex>
#include <shadowmap_pars_vertex>
#include <skinning_pars_vertex>
#include <uv2_pars_vertex>
#include <uv_pars_vertex>

varying vec2 vUv;
varying vec3 vPosition;
varying vec3 vNormal;
varying vec3 vGlassWorldPos;
varying vec3 vReflect;
varying vec3 vRefract;

uniform float uTime;
uniform float uSpeed;
uniform float uWaveAmplitude;
uniform float uWaveFrequency;
uniform float uNoiseStrength;
uniform float uDistortion;

// Noise functions for glass distortion
vec3 mod289(vec3 x) {
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 mod289(vec4 x) {
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 permute(vec4 x) {
  return mod289(((x * 34.0) + 1.0) * x);
}

vec4 taylorInvSqrt(vec4 r) {
  return 1.79284291400159 - 0.85373472095314 * r;
}

float snoise(vec3 v) {
  const vec2 C = vec2(1.0 / 6.0, 1.0 / 3.0);
  const vec4 D = vec4(0.0, 0.5, 1.0, 2.0);

  vec3 i = floor(v + dot(v, C.yyy));
  vec3 x0 = v - i + dot(i, C.xxx);

  vec3 g = step(x0.yzx, x0.xyz);
  vec3 l = 1.0 - g;
  vec3 i1 = min(g.xyz, l.zxy);
  vec3 i2 = max(g.xyz, l.zxy);

  vec3 x1 = x0 - i1 + C.xxx;
  vec3 x2 = x0 - i2 + C.yyy;
  vec3 x3 = x0 - D.yyy;

  i = mod289(i);
  vec4 p = permute(permute(permute(
    i.z + vec4(0.0, i1.z, i2.z, 1.0))
    + i.y + vec4(0.0, i1.y, i2.y, 1.0))
    + i.x + vec4(0.0, i1.x, i2.x, 1.0));

  float n_ = 0.142857142857;
  vec3 ns = n_ * D.wyz - D.xzx;

  vec4 j = p - 49.0 * floor(p * ns.z * ns.z);

  vec4 x_ = floor(j * ns.z);
  vec4 y_ = floor(j - 7.0 * x_);

  vec4 x = x_ * ns.x + ns.yyyy;
  vec4 y = y_ * ns.x + ns.yyyy;
  vec4 h = 1.0 - abs(x) - abs(y);

  vec4 b0 = vec4(x.xy, y.xy);
  vec4 b1 = vec4(x.zw, y.zw);

  vec4 s0 = floor(b0) * 2.0 + 1.0;
  vec4 s1 = floor(b1) * 2.0 + 1.0;
  vec4 sh = -step(h, vec4(0.0));

  vec4 a0 = b0.xzyw + s0.xzyw * sh.xxyy;
  vec4 a1 = b1.xzyw + s1.xzyw * sh.zzww;

  vec3 p0 = vec3(a0.xy, h.x);
  vec3 p1 = vec3(a0.zw, h.y);
  vec3 p2 = vec3(a1.xy, h.z);
  vec3 p3 = vec3(a1.zw, h.w);

  vec4 norm = taylorInvSqrt(vec4(dot(p0, p0), dot(p1, p1), dot(p2, p2), dot(p3, p3)));
  p0 *= norm.x;
  p1 *= norm.y;
  p2 *= norm.z;
  p3 *= norm.w;

  vec4 m = max(0.6 - vec4(dot(x0, x0), dot(x1, x1), dot(x2, x2), dot(x3, x3)), 0.0);
  m = m * m;
  return 42.0 * dot(m * m, vec4(dot(p0, x0), dot(p1, x1),
    dot(p2, x2), dot(p3, x3)));
}

void main() {
  #include <uv_pars_vertex>
  #include <uv_vertex>
  #include <uv2_pars_vertex>
  #include <uv2_vertex>
  #include <color_pars_vertex>
  #include <color_vertex>
  #include <morphcolor_vertex>
  #include <beginnormal_vertex>
  #include <morphnormal_vertex>
  #include <skinbase_vertex>
  #include <skinnormal_vertex>
  #include <defaultnormal_vertex>
  #include <normal_vertex>
  
  #ifndef FLAT_SHADED
  vNormal = normalize(transformedNormal);
  #ifdef USE_TANGENT
  vTangent = normalize(transformedTangent);
  vBitangent = normalize(cross(vNormal, vTangent) * tangent.w);
  #endif
  #endif
  
  #include <begin_vertex>
  #include <morphtarget_vertex>
  #include <skinning_vertex>
  #include <displacementmap_vertex>
  
  // Pass UV coordinates
  vUv = uv;

  // Calculate time-based animation
  float time = uTime * uSpeed;
  
  // Create subtle wave distortion for glass effect
  float waveX = sin(position.x * uWaveFrequency + time) * uWaveAmplitude;
  float waveY = cos(position.y * uWaveFrequency + time) * uWaveAmplitude;
  float waveZ = sin(position.z * uWaveFrequency + time * 0.5) * uWaveAmplitude * 0.5;
  
  // Add noise for organic glass distortion
  vec3 noisePos = position + vec3(time * 0.1);
  float noise = snoise(noisePos * 0.5) * uNoiseStrength;
  
  // Apply distortion to transformed position
  transformed += vec3(waveX, waveY, waveZ) * uDistortion + normal * noise;
  
  #include <project_vertex>
  #include <logdepthbuf_vertex>
  #include <clipping_planes_vertex>
  
  vViewPosition = -mvPosition.xyz;
  vPosition = transformed;
  
  // Calculate world position for refraction
  vec4 worldPosition = modelMatrix * vec4(transformed, 1.0);
  vGlassWorldPos = worldPosition.xyz;
  
  // Calculate reflection and refraction vectors
  vec3 worldNormal = normalize(mat3(modelMatrix) * normal);
  vec3 viewVector = normalize(cameraPosition - worldPosition.xyz);
  
  // Reflection vector
  vReflect = reflect(-viewVector, worldNormal);
  
  // Refraction vector with index of refraction for glass (1.5)
  float ior = 1.5;
  vRefract = refract(-viewVector, worldNormal, 1.0 / ior);
  
  #include <fog_vertex>
  #include <shadowmap_vertex>
}
`;

// node_modules/@shadergradient/react/dist/chunk-LNX2N5NW.mjs
var i4 = {};
A(i4, { fragment: () => n7, vertex: () => e7 });
var n7 = `// Glass Sphere Fragment Shader - Transparency & Refraction

#define STANDARD
#ifdef PHYSICAL
#define REFLECTIVITY
#define CLEARCOAT
#define TRANSMISSION
#endif

uniform vec3 diffuse;
uniform vec3 emissive;
uniform float roughness;
uniform float metalness;
uniform float opacity;

// transmission is already defined by Three.js when TRANSMISSION is enabled
#ifdef REFLECTIVITY
uniform float reflectivity;
#endif
#ifdef CLEARCOAT
uniform float clearcoat;
uniform float clearcoatRoughness;
#endif
#ifdef USE_SHEEN
uniform vec3 sheen;
#endif

varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif

#include <alphamap_pars_fragment>
#include <aomap_pars_fragment>
#include <color_pars_fragment>
#include <common>
#include <dithering_pars_fragment>
#include <emissivemap_pars_fragment>
#include <lightmap_pars_fragment>
#include <map_pars_fragment>
#include <packing>
#include <uv2_pars_fragment>
#include <uv_pars_fragment>
#include <bsdfs>
#include <bumpmap_pars_fragment>
#include <clearcoat_pars_fragment>
#include <clipping_planes_pars_fragment>
#include <envmap_common_pars_fragment>
#include <envmap_pars_fragment>
#include <envmap_physical_pars_fragment>
#include <fog_pars_fragment>
#include <lights_pars_begin>
#include <lights_physical_pars_fragment>
#include <logdepthbuf_pars_fragment>
#include <metalnessmap_pars_fragment>
#include <normalmap_pars_fragment>
#include <roughnessmap_pars_fragment>
#include <shadowmap_pars_fragment>
#include <transmission_pars_fragment>

// Custom uniforms for glass effect
uniform float uTime;
uniform vec3 uColor1;
uniform vec3 uColor2;
uniform vec3 uColor3;
uniform float uTransparency;
uniform float uRefraction;
uniform float uChromaticAberration;
uniform float uFresnelPower;
uniform float uReflectivity;
// envMap and envMapIntensity are provided by Three.js

varying vec2 vUv;
varying vec3 vPosition;
varying vec3 vNormal;
varying vec3 vGlassWorldPos;
varying vec3 vReflect;
varying vec3 vRefract;
varying float vDistortion;

// Fresnel calculation
float fresnel(vec3 viewDirection, vec3 normal, float power) {
  return pow(1.0 - abs(dot(viewDirection, normal)), power);
}

// Chromatic aberration for refraction
vec3 chromaticRefraction(vec3 viewDirection, vec3 normal, float ior, float chromaticStrength) {
  vec3 refractedR = refract(viewDirection, normal, 1.0 / (ior - chromaticStrength));
  vec3 refractedG = refract(viewDirection, normal, 1.0 / ior);
  vec3 refractedB = refract(viewDirection, normal, 1.0 / (ior + chromaticStrength));
  
  #ifdef ENVMAP_TYPE_CUBE
  return vec3(
    textureCube(envMap, refractedR).r,
    textureCube(envMap, refractedG).g,
    textureCube(envMap, refractedB).b
  );
  #else
  return vec3(0.5);
  #endif
}

// Caustics simulation for sphere
float caustics(vec3 position, float time) {
  float c1 = sin(position.x * 4.0 + time) * sin(position.y * 4.0 + time * 0.8);
  float c2 = sin(position.z * 3.0 - time * 1.2) * sin(position.x * 3.0 + time);
  return (c1 + c2) * 0.5 + 0.5;
}

void main() {
  #include <clipping_planes_fragment>
  
  vec4 diffuseColor = vec4(diffuse, opacity);
  ReflectedLight reflectedLight = ReflectedLight(vec3(0.0), vec3(0.0), vec3(0.0), vec3(0.0));
  vec3 totalEmissiveRadiance = emissive;
  
  #include <logdepthbuf_fragment>
  #include <map_fragment>
  #include <color_fragment>
  #include <alphamap_fragment>
  #include <alphatest_fragment>
  #include <specularmap_fragment>
  #include <roughnessmap_fragment>
  #include <metalnessmap_fragment>
  #include <normal_fragment_begin>
  #include <normal_fragment_maps>
  #include <clearcoat_normal_fragment_begin>
  #include <clearcoat_normal_fragment_maps>
  #include <emissivemap_fragment>
  
  // Glass-specific calculations
  vec3 viewDirection = normalize(vViewPosition);
  vec3 worldNormal = normalize(vNormal);
  
  // Calculate Fresnel effect
  float fresnelFactor = fresnel(viewDirection, worldNormal, uFresnelPower);
  
  // For sphere, use spherical UV mapping for gradient
  float sphericalU = atan(vPosition.z, vPosition.x) / (2.0 * PI) + 0.5;
  float sphericalV = acos(vPosition.y / length(vPosition)) / PI;
  vec2 sphericalUV = vec2(sphericalU, sphericalV);
  
  // Create color gradient based on spherical coordinates
  vec3 gradientColor = mix(uColor1, uColor2, sphericalUV.y);
  gradientColor = mix(gradientColor, uColor3, pow(fresnelFactor, 1.5));
  
  // Add caustics effect for sphere
  float causticsValue = caustics(vGlassWorldPos, uTime);
  gradientColor += vec3(causticsValue * 0.1);
  
  // Reflection
  #ifdef ENVMAP_TYPE_CUBE
  vec3 reflectionColor = textureCube(envMap, vReflect).rgb * envMapIntensity;
  #else
  vec3 reflectionColor = vec3(0.5);
  #endif
  
  // Refraction with chromatic aberration (enhanced for sphere)
  vec3 refractionColor;
  #ifdef ENVMAP_TYPE_CUBE
  if (uChromaticAberration > 0.0) {
    float chromaticIntensity = uChromaticAberration * (1.0 + vDistortion * 0.5);
    refractionColor = chromaticRefraction(-viewDirection, worldNormal, uRefraction, chromaticIntensity);
  } else {
    refractionColor = textureCube(envMap, vRefract).rgb;
  }
  refractionColor *= envMapIntensity;
  #else
  refractionColor = vec3(0.3);
  #endif
  
  // Mix reflection and refraction based on Fresnel (stronger effect for sphere)
  vec3 envColor = mix(refractionColor, reflectionColor, fresnelFactor * uReflectivity);
  
  // Add inner glow effect for sphere
  float innerGlow = pow(1.0 - abs(dot(viewDirection, worldNormal)), 3.0);
  vec3 glowColor = mix(uColor2, uColor3, innerGlow) * innerGlow * 0.5;
  
  // Combine all effects
  vec3 finalColor = mix(gradientColor, envColor, 0.8) + glowColor;
  
  // Apply transparency with sphere thickness consideration
  float thickness = 1.0 - pow(abs(dot(viewDirection, worldNormal)), 0.5);
  float finalAlpha = mix(uTransparency * thickness, 1.0, fresnelFactor * 0.7);
  
  // Set diffuse color for standard lighting
  diffuseColor.rgb = finalColor;
  diffuseColor.a = finalAlpha;
  
  // Skip transmission_fragment to avoid conflicts
  
  vec3 outgoingLight = reflectedLight.directDiffuse + reflectedLight.indirectDiffuse + 
                       reflectedLight.directSpecular + reflectedLight.indirectSpecular + 
                       totalEmissiveRadiance;
  
  // Add our glass color contribution
  outgoingLight += finalColor * 0.9;
  
  gl_FragColor = vec4(outgoingLight, diffuseColor.a);
  
  #include <tonemapping_fragment>
  #include <colorspace_fragment>
  #include <fog_fragment>
  #include <premultiplied_alpha_fragment>
  #include <dithering_fragment>
}
`;
var e7 = `// Glass Sphere Vertex Shader - Refraction & Transparency Effects

#define STANDARD
varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif
#include <clipping_planes_pars_vertex>
#include <color_pars_vertex>
#include <common>
#include <displacementmap_pars_vertex>
#include <fog_pars_vertex>
#include <logdepthbuf_pars_vertex>
#include <morphtarget_pars_vertex>
#include <shadowmap_pars_vertex>
#include <skinning_pars_vertex>
#include <uv2_pars_vertex>
#include <uv_pars_vertex>

varying vec2 vUv;
varying vec3 vPosition;
varying vec3 vNormal;
varying vec3 vGlassWorldPos;
varying vec3 vReflect;
varying vec3 vRefract;
varying float vDistortion;

uniform float uTime;
uniform float uSpeed;
uniform float uWaveAmplitude;
uniform float uWaveFrequency;
uniform float uNoiseStrength;
uniform float uDistortion;

// Noise functions for glass distortion
vec3 mod289(vec3 x) {
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 mod289(vec4 x) {
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 permute(vec4 x) {
  return mod289(((x * 34.0) + 1.0) * x);
}

vec4 taylorInvSqrt(vec4 r) {
  return 1.79284291400159 - 0.85373472095314 * r;
}

float snoise(vec3 v) {
  const vec2 C = vec2(1.0 / 6.0, 1.0 / 3.0);
  const vec4 D = vec4(0.0, 0.5, 1.0, 2.0);

  vec3 i = floor(v + dot(v, C.yyy));
  vec3 x0 = v - i + dot(i, C.xxx);

  vec3 g = step(x0.yzx, x0.xyz);
  vec3 l = 1.0 - g;
  vec3 i1 = min(g.xyz, l.zxy);
  vec3 i2 = max(g.xyz, l.zxy);

  vec3 x1 = x0 - i1 + C.xxx;
  vec3 x2 = x0 - i2 + C.yyy;
  vec3 x3 = x0 - D.yyy;

  i = mod289(i);
  vec4 p = permute(permute(permute(
    i.z + vec4(0.0, i1.z, i2.z, 1.0))
    + i.y + vec4(0.0, i1.y, i2.y, 1.0))
    + i.x + vec4(0.0, i1.x, i2.x, 1.0));

  float n_ = 0.142857142857;
  vec3 ns = n_ * D.wyz - D.xzx;

  vec4 j = p - 49.0 * floor(p * ns.z * ns.z);

  vec4 x_ = floor(j * ns.z);
  vec4 y_ = floor(j - 7.0 * x_);

  vec4 x = x_ * ns.x + ns.yyyy;
  vec4 y = y_ * ns.x + ns.yyyy;
  vec4 h = 1.0 - abs(x) - abs(y);

  vec4 b0 = vec4(x.xy, y.xy);
  vec4 b1 = vec4(x.zw, y.zw);

  vec4 s0 = floor(b0) * 2.0 + 1.0;
  vec4 s1 = floor(b1) * 2.0 + 1.0;
  vec4 sh = -step(h, vec4(0.0));

  vec4 a0 = b0.xzyw + s0.xzyw * sh.xxyy;
  vec4 a1 = b1.xzyw + s1.xzyw * sh.zzww;

  vec3 p0 = vec3(a0.xy, h.x);
  vec3 p1 = vec3(a0.zw, h.y);
  vec3 p2 = vec3(a1.xy, h.z);
  vec3 p3 = vec3(a1.zw, h.w);

  vec4 norm = taylorInvSqrt(vec4(dot(p0, p0), dot(p1, p1), dot(p2, p2), dot(p3, p3)));
  p0 *= norm.x;
  p1 *= norm.y;
  p2 *= norm.z;
  p3 *= norm.w;

  vec4 m = max(0.6 - vec4(dot(x0, x0), dot(x1, x1), dot(x2, x2), dot(x3, x3)), 0.0);
  m = m * m;
  return 42.0 * dot(m * m, vec4(dot(p0, x0), dot(p1, x1),
    dot(p2, x2), dot(p3, x3)));
}

void main() {
  #include <uv_pars_vertex>
  #include <uv_vertex>
  #include <uv2_pars_vertex>
  #include <uv2_vertex>
  #include <color_pars_vertex>
  #include <color_vertex>
  #include <morphcolor_vertex>
  #include <beginnormal_vertex>
  #include <morphnormal_vertex>
  #include <skinbase_vertex>
  #include <skinnormal_vertex>
  #include <defaultnormal_vertex>
  #include <normal_vertex>
  
  #ifndef FLAT_SHADED
  vNormal = normalize(transformedNormal);
  #ifdef USE_TANGENT
  vTangent = normalize(transformedTangent);
  vBitangent = normalize(cross(vNormal, vTangent) * tangent.w);
  #endif
  #endif
  
  #include <begin_vertex>
  #include <morphtarget_vertex>
  #include <skinning_vertex>
  #include <displacementmap_vertex>
  
  // Pass UV coordinates
  vUv = uv;

  // Calculate time-based animation
  float time = uTime * uSpeed;
  
  // For sphere, use spherical coordinates for better distortion
  float theta = atan(position.z, position.x);
  float phi = acos(position.y / length(position));
  
  // Create waves based on spherical coordinates
  float waveTheta = sin(theta * uWaveFrequency * 2.0 + time) * uWaveAmplitude;
  float wavePhi = cos(phi * uWaveFrequency + time * 1.5) * uWaveAmplitude;
  
  // Add noise for organic glass distortion
  vec3 noisePos = position + vec3(time * 0.1);
  float noise = snoise(noisePos * 0.8) * uNoiseStrength;
  
  // Calculate distortion based on position on sphere
  float distortionAmount = (waveTheta + wavePhi) * uDistortion + noise;
  vDistortion = distortionAmount;
  
  // Apply distortion along normal for sphere
  transformed += normal * distortionAmount;
  
  #include <project_vertex>
  #include <logdepthbuf_vertex>
  #include <clipping_planes_vertex>
  
  vViewPosition = -mvPosition.xyz;
  vPosition = transformed;
  
  // Calculate world position for refraction
  vec4 worldPosition = modelMatrix * vec4(transformed, 1.0);
  vGlassWorldPos = worldPosition.xyz;
  
  // Calculate reflection and refraction vectors
  vec3 worldNormal = normalize(mat3(modelMatrix) * normal);
  vec3 viewVector = normalize(cameraPosition - worldPosition.xyz);
  
  // Reflection vector
  vReflect = reflect(-viewVector, worldNormal);
  
  // Refraction vector with index of refraction for glass (1.5)
  // For sphere, adjust IOR based on curvature
  float ior = 1.5 + sin(theta * 2.0 + time) * 0.1;
  vRefract = refract(-viewVector, worldNormal, 1.0 / ior);
  
  #include <fog_vertex>
  #include <shadowmap_vertex>
}
`;

// node_modules/@shadergradient/react/dist/chunk-4ZGMESHB.mjs
var o4 = {};
A(o4, { fragment: () => e8, vertex: () => n8 });
var e8 = `// Glass WaterPlane Fragment Shader - Liquid Glass Effect

#define STANDARD
#ifdef PHYSICAL
#define REFLECTIVITY
#define CLEARCOAT
#define TRANSMISSION
#endif

uniform vec3 diffuse;
uniform vec3 emissive;
uniform float roughness;
uniform float metalness;
uniform float opacity;

// transmission is already defined by Three.js when TRANSMISSION is enabled
#ifdef REFLECTIVITY
uniform float reflectivity;
#endif
#ifdef CLEARCOAT
uniform float clearcoat;
uniform float clearcoatRoughness;
#endif
#ifdef USE_SHEEN
uniform vec3 sheen;
#endif

varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif

#include <alphamap_pars_fragment>
#include <aomap_pars_fragment>
#include <color_pars_fragment>
#include <common>
#include <dithering_pars_fragment>
#include <emissivemap_pars_fragment>
#include <lightmap_pars_fragment>
#include <map_pars_fragment>
#include <packing>
#include <uv2_pars_fragment>
#include <uv_pars_fragment>
#include <bsdfs>
#include <bumpmap_pars_fragment>
#include <clearcoat_pars_fragment>
#include <clipping_planes_pars_fragment>
#include <envmap_common_pars_fragment>
#include <envmap_pars_fragment>
#include <envmap_physical_pars_fragment>
#include <fog_pars_fragment>
#include <lights_pars_begin>
#include <lights_physical_pars_fragment>
#include <logdepthbuf_pars_fragment>
#include <metalnessmap_pars_fragment>
#include <normalmap_pars_fragment>
#include <roughnessmap_pars_fragment>
#include <shadowmap_pars_fragment>
#include <transmission_pars_fragment>

// Custom uniforms for liquid glass effect
uniform float uTime;
uniform vec3 uColor1;
uniform vec3 uColor2;
uniform vec3 uColor3;
uniform float uTransparency;
uniform float uRefraction;
uniform float uChromaticAberration;
uniform float uFresnelPower;
uniform float uReflectivity;
// envMap and envMapIntensity are provided by Three.js
uniform float uLiquidEffect;
uniform float uFoamIntensity;

varying vec2 vUv;
varying vec3 vPosition;
varying vec3 vNormal;
varying vec3 vGlassWorldPos;
varying vec3 vReflect;
varying vec3 vRefract;
varying float vWaveHeight;
varying vec3 vWaveNormal;

// Fresnel calculation
float fresnel(vec3 viewDirection, vec3 normal, float power) {
  return pow(1.0 - abs(dot(viewDirection, normal)), power);
}

// Chromatic aberration for refraction
vec3 chromaticRefraction(vec3 viewDirection, vec3 normal, float ior, float chromaticStrength) {
  vec3 refractedR = refract(viewDirection, normal, 1.0 / (ior - chromaticStrength));
  vec3 refractedG = refract(viewDirection, normal, 1.0 / ior);
  vec3 refractedB = refract(viewDirection, normal, 1.0 / (ior + chromaticStrength));
  
  #ifdef ENVMAP_TYPE_CUBE
  vec3 result = vec3(
    textureCube(envMap, refractedR).r,
    textureCube(envMap, refractedG).g,
    textureCube(envMap, refractedB).b
  );
  
  // Add distortion based on wave height
  float distortion = vWaveHeight * 0.1;
  result = mix(result, textureCube(envMap, refractedG + vec3(distortion)).rgb, 0.3);
  #else
  vec3 result = vec3(0.5);
  #endif
  
  return result;
}

// Foam effect for water surface
float foam(vec2 uv, float waveHeight, float time) {
  float foamThreshold = 0.3;
  float foamAmount = smoothstep(foamThreshold - 0.1, foamThreshold + 0.1, abs(waveHeight));
  
  // Add foam texture pattern
  float foamPattern = sin(uv.x * 40.0 + time) * cos(uv.y * 30.0 - time * 0.5);
  foamPattern += sin(uv.x * 25.0 - time * 0.8) * sin(uv.y * 35.0 + time);
  foamPattern = clamp(foamPattern * 0.5 + 0.5, 0.0, 1.0);
  
  return foamAmount * foamPattern;
}

// Caustics for underwater effect
vec3 caustics(vec3 position, float time) {
  float c1 = sin(position.x * 6.0 + time * 1.5) * sin(position.z * 6.0 + time);
  float c2 = cos(position.x * 4.0 - time) * cos(position.z * 5.0 + time * 1.2);
  float c3 = sin((position.x + position.z) * 3.0 + time * 0.8);
  
  float causticPattern = (c1 + c2 + c3) / 3.0;
  causticPattern = pow(max(0.0, causticPattern), 2.0);
  
  return vec3(causticPattern) * vec3(0.3, 0.6, 1.0);
}

void main() {
  #include <clipping_planes_fragment>
  
  vec4 diffuseColor = vec4(diffuse, opacity);
  ReflectedLight reflectedLight = ReflectedLight(vec3(0.0), vec3(0.0), vec3(0.0), vec3(0.0));
  vec3 totalEmissiveRadiance = emissive;
  
  #include <logdepthbuf_fragment>
  #include <map_fragment>
  #include <color_fragment>
  #include <alphamap_fragment>
  #include <alphatest_fragment>
  #include <specularmap_fragment>
  #include <roughnessmap_fragment>
  #include <metalnessmap_fragment>
  #include <normal_fragment_begin>
  #include <normal_fragment_maps>
  #include <clearcoat_normal_fragment_begin>
  #include <clearcoat_normal_fragment_maps>
  #include <emissivemap_fragment>
  
  // Use wave normal for more accurate water surface
  vec3 viewDirection = normalize(vViewPosition);
  vec3 worldNormal = normalize(vWaveNormal);
  
  // Calculate Fresnel effect
  float fresnelFactor = fresnel(viewDirection, worldNormal, uFresnelPower);
  
  // Water color gradient with depth effect
  float depth = 1.0 - abs(vWaveHeight) * 2.0;
  vec3 shallowColor = mix(uColor1, uColor2, vUv.y);
  vec3 deepColor = mix(uColor2, uColor3, depth);
  vec3 gradientColor = mix(shallowColor, deepColor, fresnelFactor);
  
  // Add foam effect
  float foamAmount = foam(vUv, vWaveHeight, uTime) * uFoamIntensity;
  vec3 foamColor = vec3(1.0, 1.0, 1.0);
  gradientColor = mix(gradientColor, foamColor, foamAmount);
  
  // Reflection
  #ifdef ENVMAP_TYPE_CUBE
  vec3 reflectionColor = textureCube(envMap, vReflect).rgb * envMapIntensity;
  
  // Add slight blur to reflection for water effect
  vec3 blurredReflection = reflectionColor;
  for (int i = 0; i < 4; i++) {
    vec3 offset = vec3(
      sin(float(i) * 2.0) * 0.01,
      0.0,
      cos(float(i) * 2.0) * 0.01
    );
    blurredReflection += textureCube(envMap, vReflect + offset).rgb * envMapIntensity;
  }
  blurredReflection /= 5.0;
  reflectionColor = mix(reflectionColor, blurredReflection, uLiquidEffect);
  #else
  vec3 reflectionColor = vec3(0.5);
  #endif
  
  // Refraction with chromatic aberration (stronger for water)
  vec3 refractionColor;
  #ifdef ENVMAP_TYPE_CUBE
  if (uChromaticAberration > 0.0) {
    float waterIOR = 1.33 + vWaveHeight * 0.1;
    refractionColor = chromaticRefraction(-viewDirection, worldNormal, waterIOR, uChromaticAberration * 1.5);
  } else {
    refractionColor = textureCube(envMap, vRefract).rgb;
  }
  refractionColor *= envMapIntensity;
  #else
  refractionColor = vec3(0.3);
  #endif
  
  // Add caustics to refraction
  vec3 causticsColor = caustics(vGlassWorldPos, uTime);
  refractionColor += causticsColor * 0.3 * uLiquidEffect;
  
  // Mix reflection and refraction based on Fresnel and wave
  float reflectionMix = fresnelFactor * uReflectivity * (1.0 + abs(vWaveHeight));
  vec3 envColor = mix(refractionColor, reflectionColor, clamp(reflectionMix, 0.0, 1.0));
  
  // Combine all effects
  vec3 finalColor = mix(gradientColor, envColor, 0.85);
  
  // Apply transparency with wave variation
  float waveAlpha = 1.0 - abs(vWaveHeight) * 0.3;
  float finalAlpha = mix(uTransparency * waveAlpha, 1.0, fresnelFactor * 0.6 + foamAmount * 0.4);
  
  // Set diffuse color for standard lighting
  diffuseColor.rgb = finalColor;
  diffuseColor.a = finalAlpha;
  
  // Skip transmission_fragment to avoid conflicts
  
  vec3 outgoingLight = reflectedLight.directDiffuse + reflectedLight.indirectDiffuse + 
                       reflectedLight.directSpecular + reflectedLight.indirectSpecular + 
                       totalEmissiveRadiance;
  
  // Add our liquid glass color contribution
  outgoingLight += finalColor * 0.95;
  
  gl_FragColor = vec4(outgoingLight, diffuseColor.a);
  
  #include <tonemapping_fragment>
  #include <colorspace_fragment>
  #include <fog_fragment>
  #include <premultiplied_alpha_fragment>
  #include <dithering_fragment>
}
`;
var n8 = `// Glass WaterPlane Vertex Shader - Liquid Glass Effect

#define STANDARD
varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif
#include <clipping_planes_pars_vertex>
#include <color_pars_vertex>
#include <common>
#include <displacementmap_pars_vertex>
#include <fog_pars_vertex>
#include <logdepthbuf_pars_vertex>
#include <morphtarget_pars_vertex>
#include <shadowmap_pars_vertex>
#include <skinning_pars_vertex>
#include <uv2_pars_vertex>
#include <uv_pars_vertex>

varying vec2 vUv;
varying vec3 vPosition;
varying vec3 vNormal;
varying vec3 vGlassWorldPos;
varying vec3 vReflect;
varying vec3 vRefract;
varying float vWaveHeight;
varying vec3 vWaveNormal;

uniform float uTime;
uniform float uSpeed;
uniform float uWaveAmplitude;
uniform float uWaveFrequency;
uniform float uNoiseStrength;
uniform float uDistortion;
uniform float uFlowSpeed;
uniform vec2 uFlowDirection;

// Noise functions for water-like glass distortion
vec3 mod289(vec3 x) {
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 mod289(vec4 x) {
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 permute(vec4 x) {
  return mod289(((x * 34.0) + 1.0) * x);
}

vec4 taylorInvSqrt(vec4 r) {
  return 1.79284291400159 - 0.85373472095314 * r;
}

float snoise(vec3 v) {
  const vec2 C = vec2(1.0 / 6.0, 1.0 / 3.0);
  const vec4 D = vec4(0.0, 0.5, 1.0, 2.0);

  vec3 i = floor(v + dot(v, C.yyy));
  vec3 x0 = v - i + dot(i, C.xxx);

  vec3 g = step(x0.yzx, x0.xyz);
  vec3 l = 1.0 - g;
  vec3 i1 = min(g.xyz, l.zxy);
  vec3 i2 = max(g.xyz, l.zxy);

  vec3 x1 = x0 - i1 + C.xxx;
  vec3 x2 = x0 - i2 + C.yyy;
  vec3 x3 = x0 - D.yyy;

  i = mod289(i);
  vec4 p = permute(permute(permute(
    i.z + vec4(0.0, i1.z, i2.z, 1.0))
    + i.y + vec4(0.0, i1.y, i2.y, 1.0))
    + i.x + vec4(0.0, i1.x, i2.x, 1.0));

  float n_ = 0.142857142857;
  vec3 ns = n_ * D.wyz - D.xzx;

  vec4 j = p - 49.0 * floor(p * ns.z * ns.z);

  vec4 x_ = floor(j * ns.z);
  vec4 y_ = floor(j - 7.0 * x_);

  vec4 x = x_ * ns.x + ns.yyyy;
  vec4 y = y_ * ns.x + ns.yyyy;
  vec4 h = 1.0 - abs(x) - abs(y);

  vec4 b0 = vec4(x.xy, y.xy);
  vec4 b1 = vec4(x.zw, y.zw);

  vec4 s0 = floor(b0) * 2.0 + 1.0;
  vec4 s1 = floor(b1) * 2.0 + 1.0;
  vec4 sh = -step(h, vec4(0.0));

  vec4 a0 = b0.xzyw + s0.xzyw * sh.xxyy;
  vec4 a1 = b1.xzyw + s1.xzyw * sh.zzww;

  vec3 p0 = vec3(a0.xy, h.x);
  vec3 p1 = vec3(a0.zw, h.y);
  vec3 p2 = vec3(a1.xy, h.z);
  vec3 p3 = vec3(a1.zw, h.w);

  vec4 norm = taylorInvSqrt(vec4(dot(p0, p0), dot(p1, p1), dot(p2, p2), dot(p3, p3)));
  p0 *= norm.x;
  p1 *= norm.y;
  p2 *= norm.z;
  p3 *= norm.w;

  vec4 m = max(0.6 - vec4(dot(x0, x0), dot(x1, x1), dot(x2, x2), dot(x3, x3)), 0.0);
  m = m * m;
  return 42.0 * dot(m * m, vec4(dot(p0, x0), dot(p1, x1),
    dot(p2, x2), dot(p3, x3)));
}

// Water wave function
vec3 waterWave(vec2 pos, float time) {
  // Flow effect
  vec2 flowPos = pos + uFlowDirection * time * uFlowSpeed;
  
  // Multiple wave layers for realistic water
  float wave1 = sin(flowPos.x * uWaveFrequency + time) * cos(flowPos.y * uWaveFrequency * 0.8 + time * 0.7);
  float wave2 = sin(flowPos.x * uWaveFrequency * 1.7 - time * 1.3) * sin(flowPos.y * uWaveFrequency * 1.3 + time);
  float wave3 = cos(flowPos.x * uWaveFrequency * 0.5 + time * 0.5) * sin(flowPos.y * uWaveFrequency * 0.6 - time * 0.8);
  
  // Combine waves
  float height = (wave1 * 0.5 + wave2 * 0.3 + wave3 * 0.2) * uWaveAmplitude;
  
  // Calculate wave normals
  float dx = cos(flowPos.x * uWaveFrequency + time) * uWaveFrequency * 0.5 * uWaveAmplitude;
  float dz = -sin(flowPos.y * uWaveFrequency * 0.8 + time * 0.7) * uWaveFrequency * 0.8 * 0.5 * uWaveAmplitude;
  
  return vec3(dx, height, dz);
}

void main() {
  #include <uv_pars_vertex>
  #include <uv_vertex>
  #include <uv2_pars_vertex>
  #include <uv2_vertex>
  #include <color_pars_vertex>
  #include <color_vertex>
  #include <morphcolor_vertex>
  #include <beginnormal_vertex>
  #include <morphnormal_vertex>
  #include <skinbase_vertex>
  #include <skinnormal_vertex>
  #include <defaultnormal_vertex>
  #include <normal_vertex>
  
  // Pass UV coordinates
  vUv = uv;

  // Calculate time-based animation
  float time = uTime * uSpeed;
  
  // Calculate water waves
  vec3 waveData = waterWave(position.xz, time);
  float waveHeight = waveData.y;
  vec2 waveGradient = waveData.xz;
  
  // Add noise for organic water movement
  vec3 noisePos = vec3(position.x, position.y, position.z) + vec3(time * 0.05);
  float noise = snoise(noisePos * 1.2) * uNoiseStrength * 0.5;
  
  // Store wave height for fragment shader
  vWaveHeight = waveHeight + noise;
  
  // Calculate perturbed normal for water surface
  vec3 waveNormal = normalize(vec3(-waveGradient.x, 1.0, -waveGradient.y));
  vWaveNormal = waveNormal;
  
  // Blend original normal with wave normal
  vec3 blendedNormal = normalize(mix(normal, waveNormal, 0.7));
  
  #ifndef FLAT_SHADED
  vNormal = normalize(mat3(modelViewMatrix) * blendedNormal);
  #ifdef USE_TANGENT
  vTangent = normalize(transformedTangent);
  vBitangent = normalize(cross(vNormal, vTangent) * tangent.w);
  #endif
  #endif
  
  #include <begin_vertex>
  #include <morphtarget_vertex>
  #include <skinning_vertex>
  #include <displacementmap_vertex>
  
  // Apply wave displacement and additional distortion
  transformed.y += waveHeight + noise;
  transformed += blendedNormal * uDistortion * noise;
  
  #include <project_vertex>
  #include <logdepthbuf_vertex>
  #include <clipping_planes_vertex>
  
  vViewPosition = -mvPosition.xyz;
  vPosition = transformed;
  
  // Calculate world position for refraction
  vec4 worldPosition = modelMatrix * vec4(transformed, 1.0);
  vGlassWorldPos = worldPosition.xyz;
  
  // Calculate reflection and refraction vectors with wave normal
  vec3 worldNormal = normalize(mat3(modelMatrix) * blendedNormal);
  vec3 viewVector = normalize(cameraPosition - worldPosition.xyz);
  
  // Reflection vector
  vReflect = reflect(-viewVector, worldNormal);
  
  // Refraction vector with varying IOR for water effect
  float ior = 1.33 + sin(time + position.x * 2.0) * 0.1; // Water IOR ~1.33
  vRefract = refract(-viewVector, worldNormal, 1.0 / ior);
  
  #include <fog_vertex>
  #include <shadowmap_vertex>
}
`;

// node_modules/@shadergradient/react/dist/chunk-SNM3FAAB.mjs
var p3 = {};
A(p3, { plane: () => i3, sphere: () => i4, waterPlane: () => o4 });

// node_modules/@shadergradient/react/dist/chunk-QDM5QQJ2.mjs
var t3 = {};
A(t3, { fragment: () => n9, vertex: () => e9 });
var n9 = `uniform float uC1r;
uniform float uC1g;
uniform float uC1b;
uniform float uC2r;
uniform float uC2g;
uniform float uC2b;
uniform float uC3r;
uniform float uC3g;
uniform float uC3b;


varying vec3 vNormal;
varying vec3 vPos;

void main() {
  vec3 color1 = vec3(uC1r, uC1g, uC1b);
  vec3 color2 = vec3(uC2r, uC2g, uC2b);
  vec3 color3 = vec3(uC3r, uC3g, uC3b);

  gl_FragColor = vec4(color1 * vPos.x + color2 * vPos.y + color3 * vPos.z, 1.);

}
`;
var e9 = `// #pragma glslify: cnoise3 = require(glsl-noise/classic/3d) 

// noise source from https://github.com/hughsk/glsl-noise/blob/master/periodic/3d.glsl

vec3 mod289(vec3 x)
{
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 mod289(vec4 x)
{
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 permute(vec4 x)
{
  return mod289(((x*34.0)+1.0)*x);
}

vec4 taylorInvSqrt(vec4 r)
{
  return 1.79284291400159 - 0.85373472095314 * r;
}

vec3 fade(vec3 t) {
  return t*t*t*(t*(t*6.0-15.0)+10.0);
}

float cnoise(vec3 P)
{
  vec3 Pi0 = floor(P); // Integer part for indexing
  vec3 Pi1 = Pi0 + vec3(1.0); // Integer part + 1
  Pi0 = mod289(Pi0);
  Pi1 = mod289(Pi1);
  vec3 Pf0 = fract(P); // Fractional part for interpolation
  vec3 Pf1 = Pf0 - vec3(1.0); // Fractional part - 1.0
  vec4 ix = vec4(Pi0.x, Pi1.x, Pi0.x, Pi1.x);
  vec4 iy = vec4(Pi0.yy, Pi1.yy);
  vec4 iz0 = Pi0.zzzz;
  vec4 iz1 = Pi1.zzzz;

  vec4 ixy = permute(permute(ix) + iy);
  vec4 ixy0 = permute(ixy + iz0);
  vec4 ixy1 = permute(ixy + iz1);

  vec4 gx0 = ixy0 * (1.0 / 7.0);
  vec4 gy0 = fract(floor(gx0) * (1.0 / 7.0)) - 0.5;
  gx0 = fract(gx0);
  vec4 gz0 = vec4(0.5) - abs(gx0) - abs(gy0);
  vec4 sz0 = step(gz0, vec4(0.0));
  gx0 -= sz0 * (step(0.0, gx0) - 0.5);
  gy0 -= sz0 * (step(0.0, gy0) - 0.5);

  vec4 gx1 = ixy1 * (1.0 / 7.0);
  vec4 gy1 = fract(floor(gx1) * (1.0 / 7.0)) - 0.5;
  gx1 = fract(gx1);
  vec4 gz1 = vec4(0.5) - abs(gx1) - abs(gy1);
  vec4 sz1 = step(gz1, vec4(0.0));
  gx1 -= sz1 * (step(0.0, gx1) - 0.5);
  gy1 -= sz1 * (step(0.0, gy1) - 0.5);

  vec3 g000 = vec3(gx0.x,gy0.x,gz0.x);
  vec3 g100 = vec3(gx0.y,gy0.y,gz0.y);
  vec3 g010 = vec3(gx0.z,gy0.z,gz0.z);
  vec3 g110 = vec3(gx0.w,gy0.w,gz0.w);
  vec3 g001 = vec3(gx1.x,gy1.x,gz1.x);
  vec3 g101 = vec3(gx1.y,gy1.y,gz1.y);
  vec3 g011 = vec3(gx1.z,gy1.z,gz1.z);
  vec3 g111 = vec3(gx1.w,gy1.w,gz1.w);

  vec4 norm0 = taylorInvSqrt(vec4(dot(g000, g000), dot(g010, g010), dot(g100, g100), dot(g110, g110)));
  g000 *= norm0.x;
  g010 *= norm0.y;
  g100 *= norm0.z;
  g110 *= norm0.w;
  vec4 norm1 = taylorInvSqrt(vec4(dot(g001, g001), dot(g011, g011), dot(g101, g101), dot(g111, g111)));
  g001 *= norm1.x;
  g011 *= norm1.y;
  g101 *= norm1.z;
  g111 *= norm1.w;

  float n000 = dot(g000, Pf0);
  float n100 = dot(g100, vec3(Pf1.x, Pf0.yz));
  float n010 = dot(g010, vec3(Pf0.x, Pf1.y, Pf0.z));
  float n110 = dot(g110, vec3(Pf1.xy, Pf0.z));
  float n001 = dot(g001, vec3(Pf0.xy, Pf1.z));
  float n101 = dot(g101, vec3(Pf1.x, Pf0.y, Pf1.z));
  float n011 = dot(g011, vec3(Pf0.x, Pf1.yz));
  float n111 = dot(g111, Pf1);

  vec3 fade_xyz = fade(Pf0);
  vec4 n_z = mix(vec4(n000, n100, n010, n110), vec4(n001, n101, n011, n111), fade_xyz.z);
  vec2 n_yz = mix(n_z.xy, n_z.zw, fade_xyz.y);
  float n_xyz = mix(n_yz.x, n_yz.y, fade_xyz.x); 
  return 2.2 * n_xyz;
}

//-------- start here ------------

mat3 rotation3dY(float angle) {
  float s = sin(angle);
  float c = cos(angle);

  return mat3(c, 0.0, -s, 0.0, 1.0, 0.0, s, 0.0, c);
}

vec3 rotateY(vec3 v, float angle) { return rotation3dY(angle) * v; }

varying vec3 vNormal;
varying float displacement;
varying vec3 vPos;
varying float vDistort;

varying vec2 vUv;

uniform float uTime;
uniform float uSpeed;

uniform float uLoadingTime;

uniform float uNoiseDensity;
uniform float uNoiseStrength;

#define STANDARD
varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif
#include <clipping_planes_pars_vertex>
#include <color_pars_vertex>
#include <common>
#include <displacementmap_pars_vertex>
#include <fog_pars_vertex>
#include <logdepthbuf_pars_vertex>
#include <morphtarget_pars_vertex>
#include <shadowmap_pars_vertex>
#include <skinning_pars_vertex>
#include <uv2_pars_vertex>
#include <uv_pars_vertex>

void main() {

  #include <beginnormal_vertex>
  #include <color_vertex>
  #include <defaultnormal_vertex>
  #include <morphnormal_vertex>
  #include <skinbase_vertex>
  #include <skinnormal_vertex>
  #include <uv2_vertex>
  #include <uv_vertex>
  #ifndef FLAT_SHADED
    vNormal = normalize(transformedNormal);
  #ifdef USE_TANGENT
    vTangent = normalize(transformedTangent);
    vBitangent = normalize(cross(vNormal, vTangent) * tangent.w);
  #endif
  #endif
  #include <begin_vertex>

  #include <clipping_planes_vertex>
  #include <displacementmap_vertex>
  #include <logdepthbuf_vertex>
  #include <morphtarget_vertex>
  #include <project_vertex>
  #include <skinning_vertex>
    vViewPosition = -mvPosition.xyz;
  #include <fog_vertex>
  #include <shadowmap_vertex>
  #include <worldpos_vertex>

  //-------- start vertex ------------
  vUv = uv;

  // vNormal = normal;

  float t = uTime * uSpeed;
  // Create a sine wave from top to bottom of the sphere
  float distortion = 0.75 * cnoise(0.43 * position * uNoiseDensity + t);

  vec3 pos = position + normal * distortion * uNoiseStrength * uLoadingTime;
  vPos = pos;

  gl_Position = projectionMatrix * modelViewMatrix * vec4(pos, 1.);
}
`;

// node_modules/@shadergradient/react/dist/chunk-7UVO7YQS.mjs
var r2 = {};
A(r2, { fragment: () => n10, vertex: () => e10 });
var n10 = `
#define STANDARD
#ifdef PHYSICAL
#define REFLECTIVITY
#define CLEARCOAT
#define TRANSMISSION
#endif
uniform vec3 diffuse;
uniform vec3 emissive;
uniform float roughness;
uniform float metalness;
uniform float opacity;
#ifdef TRANSMISSION
uniform float transmission;
#endif
#ifdef REFLECTIVITY
uniform float reflectivity;
#endif
#ifdef CLEARCOAT
uniform float clearcoat;
uniform float clearcoatRoughness;
#endif
#ifdef USE_SHEEN
uniform vec3 sheen;
#endif
varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif
#include <alphamap_pars_fragment>
#include <aomap_pars_fragment>
#include <color_pars_fragment>
#include <common>
#include <dithering_pars_fragment>
#include <emissivemap_pars_fragment>
#include <lightmap_pars_fragment>
#include <map_pars_fragment>
#include <packing>
#include <uv2_pars_fragment>
#include <uv_pars_fragment>
// #include <transmissionmap_pars_fragment>
#include <bsdfs>
#include <bumpmap_pars_fragment>
#include <clearcoat_pars_fragment>
#include <clipping_planes_pars_fragment>
#include <cube_uv_reflection_fragment>
#include <envmap_common_pars_fragment>
#include <envmap_physical_pars_fragment>
#include <fog_pars_fragment>
#include <lights_pars_begin>
#include <lights_physical_pars_fragment>
#include <logdepthbuf_pars_fragment>
#include <metalnessmap_pars_fragment>
#include <normalmap_pars_fragment>
#include <roughnessmap_pars_fragment>
#include <shadowmap_pars_fragment>
// include를 통해 가져온 값은 대부분 환경, 빛 등을 계산하기 위해서 기본 fragment
// shader의 값들을 받아왔습니다. 일단은 무시하셔도 됩니다.
varying vec3 vNormal;
varying float displacement;
varying vec3 vPos;
varying float vDistort;
uniform float uC1r;
uniform float uC1g;
uniform float uC1b;
uniform float uC2r;
uniform float uC2g;
uniform float uC2b;
uniform float uC3r;
uniform float uC3g;
uniform float uC3b;
varying vec3 color1;
varying vec3 color2;
varying vec3 color3;
varying float distanceToCenter;
void main() {
  //-------- basic gradient ------------
  vec3 color1 = vec3(uC1r, uC1g, uC1b);
  vec3 color2 = vec3(uC2r, uC2g, uC2b);
  vec3 color3 = vec3(uC3r, uC3g, uC3b);
  float clearcoat = 1.0;
  float clearcoatRoughness = 0.5;
#include <clipping_planes_fragment>

  float distanceToCenter = distance(vPos, vec3(0, 0, 0));
  // distanceToCenter로 중심점과의 거리를 구함.

  vec4 diffuseColor =
      vec4(mix(color3, mix(color2, color1, smoothstep(-1.0, 1.0, vPos.y)),
               distanceToCenter),
           1);

  //-------- materiality ------------
  ReflectedLight reflectedLight =
      ReflectedLight(vec3(0.0), vec3(0.0), vec3(0.0), vec3(0.0));
  vec3 totalEmissiveRadiance = emissive;
#ifdef TRANSMISSION
  float totalTransmission = transmission;
#endif
#include <logdepthbuf_fragment>
#include <map_fragment>
#include <color_fragment>
#include <alphamap_fragment>
#include <alphatest_fragment>
#include <roughnessmap_fragment>
#include <metalnessmap_fragment>
#include <normal_fragment_begin>
#include <normal_fragment_maps>
#include <clearcoat_normal_fragment_begin>
#include <clearcoat_normal_fragment_maps>
#include <emissivemap_fragment>
// #include <transmissionmap_fragment>
#include <lights_physical_fragment>
#include <lights_fragment_begin>
#include <lights_fragment_maps>
#include <lights_fragment_end>
#include <aomap_fragment>
  vec3 outgoingLight =
      reflectedLight.directDiffuse + reflectedLight.indirectDiffuse +
      reflectedLight.directSpecular + reflectedLight.indirectSpecular;
//위에서 정의한 diffuseColor에 환경이나 반사값들을 반영한 값.
#ifdef TRANSMISSION
  diffuseColor.a *=
      mix(saturate(1. - totalTransmission +
                   linearToRelativeLuminance(reflectedLight.directSpecular +
                                             reflectedLight.indirectSpecular)),
          1.0, metalness);
#endif
  gl_FragColor = vec4(outgoingLight, diffuseColor.a);
  // gl_FragColor가 fragment shader를 통해 나타나는 최종값으로, diffuseColor에서
  // 정의한 그라디언트 색상 위에 반사나 빛을 계산한 값을 최종값으로 정의.
  // gl_FragColor = vec4(mix(mix(color1, color3, smoothstep(-3.0, 3.0,vPos.x)),
  // color2, vNormal.z), 1.0); 위처럼 최종값을 그라디언트 값 자체를 넣으면 환경
  // 영향없는 그라디언트만 표현됨.

#include <tonemapping_fragment>
#include <encodings_fragment>
#include <fog_fragment>
#include <premultiplied_alpha_fragment>
#include <dithering_fragment>
}
`;
var e10 = `// #pragma glslify: pnoise = require(glsl-noise/periodic/3d)

vec3 mod289(vec3 x)
{
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 mod289(vec4 x)
{
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 permute(vec4 x)
{
  return mod289(((x*34.0)+1.0)*x);
}

vec4 taylorInvSqrt(vec4 r)
{
  return 1.79284291400159 - 0.85373472095314 * r;
}

vec3 fade(vec3 t) {
  return t*t*t*(t*(t*6.0-15.0)+10.0);
}

// Classic Perlin noise, periodic variant
float pnoise(vec3 P, vec3 rep)
{
  vec3 Pi0 = mod(floor(P), rep); // Integer part, modulo period
  vec3 Pi1 = mod(Pi0 + vec3(1.0), rep); // Integer part + 1, mod period
  Pi0 = mod289(Pi0);
  Pi1 = mod289(Pi1);
  vec3 Pf0 = fract(P); // Fractional part for interpolation
  vec3 Pf1 = Pf0 - vec3(1.0); // Fractional part - 1.0
  vec4 ix = vec4(Pi0.x, Pi1.x, Pi0.x, Pi1.x);
  vec4 iy = vec4(Pi0.yy, Pi1.yy);
  vec4 iz0 = Pi0.zzzz;
  vec4 iz1 = Pi1.zzzz;

  vec4 ixy = permute(permute(ix) + iy);
  vec4 ixy0 = permute(ixy + iz0);
  vec4 ixy1 = permute(ixy + iz1);

  vec4 gx0 = ixy0 * (1.0 / 7.0);
  vec4 gy0 = fract(floor(gx0) * (1.0 / 7.0)) - 0.5;
  gx0 = fract(gx0);
  vec4 gz0 = vec4(0.5) - abs(gx0) - abs(gy0);
  vec4 sz0 = step(gz0, vec4(0.0));
  gx0 -= sz0 * (step(0.0, gx0) - 0.5);
  gy0 -= sz0 * (step(0.0, gy0) - 0.5);

  vec4 gx1 = ixy1 * (1.0 / 7.0);
  vec4 gy1 = fract(floor(gx1) * (1.0 / 7.0)) - 0.5;
  gx1 = fract(gx1);
  vec4 gz1 = vec4(0.5) - abs(gx1) - abs(gy1);
  vec4 sz1 = step(gz1, vec4(0.0));
  gx1 -= sz1 * (step(0.0, gx1) - 0.5);
  gy1 -= sz1 * (step(0.0, gy1) - 0.5);

  vec3 g000 = vec3(gx0.x,gy0.x,gz0.x);
  vec3 g100 = vec3(gx0.y,gy0.y,gz0.y);
  vec3 g010 = vec3(gx0.z,gy0.z,gz0.z);
  vec3 g110 = vec3(gx0.w,gy0.w,gz0.w);
  vec3 g001 = vec3(gx1.x,gy1.x,gz1.x);
  vec3 g101 = vec3(gx1.y,gy1.y,gz1.y);
  vec3 g011 = vec3(gx1.z,gy1.z,gz1.z);
  vec3 g111 = vec3(gx1.w,gy1.w,gz1.w);

  vec4 norm0 = taylorInvSqrt(vec4(dot(g000, g000), dot(g010, g010), dot(g100, g100), dot(g110, g110)));
  g000 *= norm0.x;
  g010 *= norm0.y;
  g100 *= norm0.z;
  g110 *= norm0.w;
  vec4 norm1 = taylorInvSqrt(vec4(dot(g001, g001), dot(g011, g011), dot(g101, g101), dot(g111, g111)));
  g001 *= norm1.x;
  g011 *= norm1.y;
  g101 *= norm1.z;
  g111 *= norm1.w;

  float n000 = dot(g000, Pf0);
  float n100 = dot(g100, vec3(Pf1.x, Pf0.yz));
  float n010 = dot(g010, vec3(Pf0.x, Pf1.y, Pf0.z));
  float n110 = dot(g110, vec3(Pf1.xy, Pf0.z));
  float n001 = dot(g001, vec3(Pf0.xy, Pf1.z));
  float n101 = dot(g101, vec3(Pf1.x, Pf0.y, Pf1.z));
  float n011 = dot(g011, vec3(Pf0.x, Pf1.yz));
  float n111 = dot(g111, Pf1);

  vec3 fade_xyz = fade(Pf0);
  vec4 n_z = mix(vec4(n000, n100, n010, n110), vec4(n001, n101, n011, n111), fade_xyz.z);
  vec2 n_yz = mix(n_z.xy, n_z.zw, fade_xyz.y);
  float n_xyz = mix(n_yz.x, n_yz.y, fade_xyz.x);
  return 2.2 * n_xyz;
}


//-------- start here ------------

varying vec3 vNormal;
uniform float uTime;
uniform float uSpeed;
uniform float uNoiseDensity;
uniform float uNoiseStrength;
uniform float uFrequency;
uniform float uAmplitude;
varying vec3 vPos;
varying float vDistort;
varying vec2 vUv;
varying vec3 vViewPosition;

#define STANDARD
#ifndef FLAT_SHADED
  #ifdef USE_TANGENT
    varying vec3 vTangent;
    varying vec3 vBitangent;
  #endif
#endif

#include <clipping_planes_pars_vertex>
#include <color_pars_vertex>
#include <common>
#include <displacementmap_pars_vertex>
#include <fog_pars_vertex>
#include <logdepthbuf_pars_vertex>
#include <morphtarget_pars_vertex>
#include <shadowmap_pars_vertex>
#include <skinning_pars_vertex>
#include <uv2_pars_vertex>
#include <uv_pars_vertex>


// rotation
mat3 rotation3dY(float angle) {
  float s = sin(angle);
  float c = cos(angle);
  return mat3(c, 0.0, -s, 0.0, 1.0, 0.0, s, 0.0, c);
}

vec3 rotateY(vec3 v, float angle) { return rotation3dY(angle) * v; }

void main() {
  #include <beginnormal_vertex>
  #include <color_vertex>
  #include <defaultnormal_vertex>
  #include <morphnormal_vertex>
  #include <skinbase_vertex>
  #include <skinnormal_vertex>
  #include <uv2_vertex>
  #include <uv_vertex>
  #ifndef FLAT_SHADED
    vNormal = normalize(transformedNormal);
  #ifdef USE_TANGENT
    vTangent = normalize(transformedTangent);
    vBitangent = normalize(cross(vNormal, vTangent) * tangent.w);
  #endif
  #endif
  #include <begin_vertex>

  #include <clipping_planes_vertex>
  #include <displacementmap_vertex>
  #include <logdepthbuf_vertex>
  #include <morphtarget_vertex>
  #include <project_vertex>
  #include <skinning_vertex>
    vViewPosition = -mvPosition.xyz;
  #include <fog_vertex>
  #include <shadowmap_vertex>
  #include <worldpos_vertex>

  //-------- start vertex ------------
  float t = uTime * uSpeed;
  float distortion =
      pnoise((normal + t) * uNoiseDensity, vec3(10.0)) * uNoiseStrength;
  vec3 pos = position + (normal * distortion);
  float angle = sin(uv.y * uFrequency + t) * uAmplitude;
  pos = rotateY(pos, angle);

  vPos = pos;
  vDistort = distortion;
  vNormal = normal;
  vUv = uv;

  gl_Position = projectionMatrix * modelViewMatrix * vec4(pos, 1.);
}
`;

// node_modules/@shadergradient/react/dist/chunk-KSOJKU6C.mjs
var t4 = {};
A(t4, { fragment: () => n11, vertex: () => e11 });
var n11 = `uniform float uC1r;
uniform float uC1g;
uniform float uC1b;
uniform float uC2r;
uniform float uC2g;
uniform float uC2b;
uniform float uC3r;
uniform float uC3g;
uniform float uC3b;


varying vec3 vNormal;
varying vec3 vPos;

void main() {
  vec3 color1 = vec3(uC1r, uC1g, uC1b);
  vec3 color2 = vec3(uC2r, uC2g, uC2b);
  vec3 color3 = vec3(uC3r, uC3g, uC3b);

  gl_FragColor = vec4(color1 * vPos.x + color2 * vPos.y + color3 * vPos.z, 1.);

}
`;
var e11 = `// #pragma glslify: cnoise3 = require(glsl-noise/classic/3d) 

// noise source from https://github.com/hughsk/glsl-noise/blob/master/periodic/3d.glsl

vec3 mod289(vec3 x)
{
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 mod289(vec4 x)
{
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 permute(vec4 x)
{
  return mod289(((x*34.0)+1.0)*x);
}

vec4 taylorInvSqrt(vec4 r)
{
  return 1.79284291400159 - 0.85373472095314 * r;
}

vec3 fade(vec3 t) {
  return t*t*t*(t*(t*6.0-15.0)+10.0);
}

float cnoise(vec3 P)
{
  vec3 Pi0 = floor(P); // Integer part for indexing
  vec3 Pi1 = Pi0 + vec3(1.0); // Integer part + 1
  Pi0 = mod289(Pi0);
  Pi1 = mod289(Pi1);
  vec3 Pf0 = fract(P); // Fractional part for interpolation
  vec3 Pf1 = Pf0 - vec3(1.0); // Fractional part - 1.0
  vec4 ix = vec4(Pi0.x, Pi1.x, Pi0.x, Pi1.x);
  vec4 iy = vec4(Pi0.yy, Pi1.yy);
  vec4 iz0 = Pi0.zzzz;
  vec4 iz1 = Pi1.zzzz;

  vec4 ixy = permute(permute(ix) + iy);
  vec4 ixy0 = permute(ixy + iz0);
  vec4 ixy1 = permute(ixy + iz1);

  vec4 gx0 = ixy0 * (1.0 / 7.0);
  vec4 gy0 = fract(floor(gx0) * (1.0 / 7.0)) - 0.5;
  gx0 = fract(gx0);
  vec4 gz0 = vec4(0.5) - abs(gx0) - abs(gy0);
  vec4 sz0 = step(gz0, vec4(0.0));
  gx0 -= sz0 * (step(0.0, gx0) - 0.5);
  gy0 -= sz0 * (step(0.0, gy0) - 0.5);

  vec4 gx1 = ixy1 * (1.0 / 7.0);
  vec4 gy1 = fract(floor(gx1) * (1.0 / 7.0)) - 0.5;
  gx1 = fract(gx1);
  vec4 gz1 = vec4(0.5) - abs(gx1) - abs(gy1);
  vec4 sz1 = step(gz1, vec4(0.0));
  gx1 -= sz1 * (step(0.0, gx1) - 0.5);
  gy1 -= sz1 * (step(0.0, gy1) - 0.5);

  vec3 g000 = vec3(gx0.x,gy0.x,gz0.x);
  vec3 g100 = vec3(gx0.y,gy0.y,gz0.y);
  vec3 g010 = vec3(gx0.z,gy0.z,gz0.z);
  vec3 g110 = vec3(gx0.w,gy0.w,gz0.w);
  vec3 g001 = vec3(gx1.x,gy1.x,gz1.x);
  vec3 g101 = vec3(gx1.y,gy1.y,gz1.y);
  vec3 g011 = vec3(gx1.z,gy1.z,gz1.z);
  vec3 g111 = vec3(gx1.w,gy1.w,gz1.w);

  vec4 norm0 = taylorInvSqrt(vec4(dot(g000, g000), dot(g010, g010), dot(g100, g100), dot(g110, g110)));
  g000 *= norm0.x;
  g010 *= norm0.y;
  g100 *= norm0.z;
  g110 *= norm0.w;
  vec4 norm1 = taylorInvSqrt(vec4(dot(g001, g001), dot(g011, g011), dot(g101, g101), dot(g111, g111)));
  g001 *= norm1.x;
  g011 *= norm1.y;
  g101 *= norm1.z;
  g111 *= norm1.w;

  float n000 = dot(g000, Pf0);
  float n100 = dot(g100, vec3(Pf1.x, Pf0.yz));
  float n010 = dot(g010, vec3(Pf0.x, Pf1.y, Pf0.z));
  float n110 = dot(g110, vec3(Pf1.xy, Pf0.z));
  float n001 = dot(g001, vec3(Pf0.xy, Pf1.z));
  float n101 = dot(g101, vec3(Pf1.x, Pf0.y, Pf1.z));
  float n011 = dot(g011, vec3(Pf0.x, Pf1.yz));
  float n111 = dot(g111, Pf1);

  vec3 fade_xyz = fade(Pf0);
  vec4 n_z = mix(vec4(n000, n100, n010, n110), vec4(n001, n101, n011, n111), fade_xyz.z);
  vec2 n_yz = mix(n_z.xy, n_z.zw, fade_xyz.y);
  float n_xyz = mix(n_yz.x, n_yz.y, fade_xyz.x); 
  return 2.2 * n_xyz;
}

//-------- start here ------------

mat3 rotation3dY(float angle) {
  float s = sin(angle);
  float c = cos(angle);

  return mat3(c, 0.0, -s, 0.0, 1.0, 0.0, s, 0.0, c);
}

vec3 rotateY(vec3 v, float angle) { return rotation3dY(angle) * v; }

varying vec3 vNormal;
varying float displacement;
varying vec3 vPos;
varying float vDistort;

varying vec2 vUv;

uniform float uTime;
uniform float uSpeed;

uniform float uLoadingTime;

uniform float uNoiseDensity;
uniform float uNoiseStrength;

#define STANDARD
varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif
#include <clipping_planes_pars_vertex>
#include <color_pars_vertex>
#include <common>
#include <displacementmap_pars_vertex>
#include <fog_pars_vertex>
#include <logdepthbuf_pars_vertex>
#include <morphtarget_pars_vertex>
#include <shadowmap_pars_vertex>
#include <skinning_pars_vertex>
#include <uv2_pars_vertex>
#include <uv_pars_vertex>

void main() {

  #include <beginnormal_vertex>
  #include <color_vertex>
  #include <defaultnormal_vertex>
  #include <morphnormal_vertex>
  #include <skinbase_vertex>
  #include <skinnormal_vertex>
  #include <uv2_vertex>
  #include <uv_vertex>
  #ifndef FLAT_SHADED
    vNormal = normalize(transformedNormal);
  #ifdef USE_TANGENT
    vTangent = normalize(transformedTangent);
    vBitangent = normalize(cross(vNormal, vTangent) * tangent.w);
  #endif
  #endif
  #include <begin_vertex>

  #include <clipping_planes_vertex>
  #include <displacementmap_vertex>
  #include <logdepthbuf_vertex>
  #include <morphtarget_vertex>
  #include <project_vertex>
  #include <skinning_vertex>
    vViewPosition = -mvPosition.xyz;
  #include <fog_vertex>
  #include <shadowmap_vertex>
  #include <worldpos_vertex>

  //-------- start vertex ------------
  vUv = uv;

  // vNormal = normal;

  float t = uTime * uSpeed;
  // Create a sine wave from top to bottom of the sphere
  float distortion = 0.75 * cnoise(0.43 * position * uNoiseDensity + t);

  vec3 pos = position + normal * distortion * uNoiseStrength * uLoadingTime;
  vPos = pos;

  gl_Position = projectionMatrix * modelViewMatrix * vec4(pos, 1.);
}
`;

// node_modules/@shadergradient/react/dist/chunk-65KZC4AC.mjs
var p4 = {};
A(p4, { plane: () => t3, sphere: () => r2, waterPlane: () => t4 });

// node_modules/@shadergradient/react/dist/chunk-6I4MTSXI.mjs
var o5 = {};
A(o5, { fragment: () => n12, vertex: () => e12 });
var n12 = `// Cosmic Plane Fragment Shader - Holographic Gradient

#define STANDARD
#ifdef PHYSICAL
#define REFLECTIVITY
#define CLEARCOAT
#define TRANSMISSION
#endif

uniform vec3 diffuse;
uniform vec3 emissive;
uniform float roughness;
uniform float metalness;
uniform float opacity;

#ifdef TRANSMISSION
uniform float transmission;
#endif
#ifdef REFLECTIVITY
uniform float reflectivity;
#endif
#ifdef CLEARCOAT
uniform float clearcoat;
uniform float clearcoatRoughness;
#endif
#ifdef USE_SHEEN
uniform vec3 sheen;
#endif
varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif
#include <alphamap_pars_fragment>
#include <aomap_pars_fragment>
#include <color_pars_fragment>
#include <common>
#include <dithering_pars_fragment>
#include <emissivemap_pars_fragment>
#include <lightmap_pars_fragment>
#include <map_pars_fragment>
#include <packing>
#include <uv2_pars_fragment>
#include <uv_pars_fragment>
#include <bsdfs>
#include <bumpmap_pars_fragment>
#include <clearcoat_pars_fragment>
#include <clipping_planes_pars_fragment>
#include <cube_uv_reflection_fragment>
#include <envmap_common_pars_fragment>
#include <envmap_physical_pars_fragment>
#include <fog_pars_fragment>
#include <lights_pars_begin>
#include <lights_physical_pars_fragment>
#include <logdepthbuf_pars_fragment>
#include <metalnessmap_pars_fragment>
#include <normalmap_pars_fragment>
#include <roughnessmap_pars_fragment>
#include <shadowmap_pars_fragment>

varying vec3 vNormal;
varying float displacement;
varying vec3 vPos;
varying float vDistort;
varying vec2 vUv;
varying float vHolographicIntensity;
varying float vCosmicWave;

uniform float uTime;
uniform float uSpeed;

uniform float uC1r;
uniform float uC1g;
uniform float uC1b;
uniform float uC2r;
uniform float uC2g;
uniform float uC2b;
uniform float uC3r;
uniform float uC3g;
uniform float uC3b;

// Holographic helper functions
float hash(vec2 p) {
    return fract(sin(dot(p, vec2(127.1, 311.7))) * 43758.5453123);
}

float noise2D(vec2 p) {
    vec2 i = floor(p);
    vec2 f = fract(p);
    vec2 u = f * f * (3.0 - 2.0 * f);
    
    return mix(mix(hash(i + vec2(0.0, 0.0)), 
                   hash(i + vec2(1.0, 0.0)), u.x),
               mix(hash(i + vec2(0.0, 1.0)), 
                   hash(i + vec2(1.0, 1.0)), u.x), u.y);
}

// for npm package, need to add this manually
float linearToRelativeLuminance2( const in vec3 color ) {
    vec3 weights = vec3( 0.2126, 0.7152, 0.0722 );
    return dot( weights, color.rgb );
}

void main() {

  //-------- Cosmic Holographic Gradient ------------
  vec3 color1 = vec3(uC1r, uC1g, uC1b);
  vec3 color2 = vec3(uC2r, uC2g, uC2b);
  vec3 color3 = vec3(uC3r, uC3g, uC3b);
  
  float clearcoat = 1.0;
  float clearcoatRoughness = 0.2; // More reflective for holographic effect

  #include <clipping_planes_fragment>

  float t = uTime * uSpeed;
  
  // Create holographic interference patterns
  float interference1 = sin(vPos.x * 20.0 + t * 3.0) * cos(vPos.y * 15.0 + t * 2.0);
  float interference2 = sin(vPos.x * 35.0 + t * 4.0) * sin(vPos.y * 30.0 + t * 3.5);
  float interference3 = cos(vPos.x * 50.0 + t * 5.0) * cos(vPos.y * 45.0 + t * 4.5);
  
  // Combine interference patterns
  float holographicPattern = (interference1 + interference2 * 0.5 + interference3 * 0.25) / 1.75;
  
  // Create cosmic shimmer effect
  float shimmer = noise2D(vPos.xy * 40.0 + t * 2.0) * 0.3;
  float cosmicGlow = noise2D(vPos.xy * 8.0 + t * 0.5) * 0.5;
  
  // Holographic color shifting
  vec3 holographicShift = vec3(
    sin(vPos.x * 10.0 + t * 2.0 + 0.0) * 0.1,
    sin(vPos.x * 10.0 + t * 2.0 + 2.094) * 0.1,  // 120 degrees
    sin(vPos.x * 10.0 + t * 2.0 + 4.188) * 0.1   // 240 degrees
  );
  
  // Enhanced gradient mixing with cosmic effects
  float gradientX = smoothstep(-4.0, 4.0, vPos.x + holographicPattern * 2.0);
  float gradientY = smoothstep(-4.0, 4.0, vPos.y + vCosmicWave * 1.5);
  float gradientZ = smoothstep(-2.0, 2.0, vPos.z + shimmer);
  
  // Multi-layer color mixing for depth
  vec3 baseGradient = mix(
    mix(color1, color2, gradientX), 
    color3, 
    gradientY * 0.7 + gradientZ * 0.3
  );
  
  // Apply holographic color shifts
  vec3 holographicColor = baseGradient + holographicShift;
  
  // Add cosmic glow and shimmer
  vec3 cosmicEnhancement = vec3(
    cosmicGlow * 0.2,
    shimmer * 0.15,
    (cosmicGlow + shimmer) * 0.1
  );
  
  // Holographic intensity modulation
  float intensityMod = 1.0 + vHolographicIntensity * 0.5 + abs(holographicPattern) * 0.3;
  
  // Final color with cosmic and holographic effects
  vec3 finalColor = (holographicColor + cosmicEnhancement) * intensityMod;
  
  // Add subtle iridescence
  float iridescence = sin(vPos.x * 25.0 + t * 3.0) * cos(vPos.y * 20.0 + t * 2.5) * 0.1;
  finalColor += vec3(iridescence * 0.2, iridescence * 0.3, iridescence * 0.4);

  vec4 diffuseColor = vec4(finalColor, 1.0);

  //-------- Enhanced Materiality for Holographic Effect ------------
  ReflectedLight reflectedLight = ReflectedLight(vec3(0.0), vec3(0.0), vec3(0.0), vec3(0.0));
  vec3 totalEmissiveRadiance = emissive + finalColor * 0.1; // Add some emission for glow

  #ifdef TRANSMISSION
    float totalTransmission = transmission;
  #endif
  #include <logdepthbuf_fragment>
  #include <map_fragment>
  #include <color_fragment>
  #include <alphamap_fragment>
  #include <alphatest_fragment>
  #include <roughnessmap_fragment>
  #include <metalnessmap_fragment>
  #include <normal_fragment_begin>
  #include <normal_fragment_maps>
  #include <clearcoat_normal_fragment_begin>
  #include <clearcoat_normal_fragment_maps>
  #include <emissivemap_fragment>
  #include <lights_physical_fragment>
  #include <lights_fragment_begin>
  #include <lights_fragment_maps>
  #include <lights_fragment_end>
  #include <aomap_fragment>
  
  vec3 outgoingLight = reflectedLight.directDiffuse + reflectedLight.indirectDiffuse +
                      reflectedLight.directSpecular + reflectedLight.indirectSpecular +
                      totalEmissiveRadiance;

  #ifdef TRANSMISSION
    diffuseColor.a *= mix(saturate(1. - totalTransmission +
                        linearToRelativeLuminance2(reflectedLight.directSpecular +
                                                  reflectedLight.indirectSpecular)),
                1.0, metalness);
  #endif

  #include <tonemapping_fragment>
  #include <encodings_fragment>
  #include <fog_fragment>
  #include <premultiplied_alpha_fragment>
  #include <dithering_fragment>

  gl_FragColor = vec4(outgoingLight, diffuseColor.a);
}
`;
var e12 = `// Cosmic Plane Vertex Shader - Holographic Effect
// #pragma glslify: cnoise3 = require(glsl-noise/classic/3d) 

// noise source from https://github.com/hughsk/glsl-noise/blob/master/periodic/3d.glsl

vec3 mod289(vec3 x)
{
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 mod289(vec4 x)
{
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 permute(vec4 x)
{
  return mod289(((x*34.0)+1.0)*x);
}

vec4 taylorInvSqrt(vec4 r)
{
  return 1.79284291400159 - 0.85373472095314 * r;
}

vec3 fade(vec3 t) {
  return t*t*t*(t*(t*6.0-15.0)+10.0);
}

float cnoise(vec3 P)
{
  vec3 Pi0 = floor(P); // Integer part for indexing
  vec3 Pi1 = Pi0 + vec3(1.0); // Integer part + 1
  Pi0 = mod289(Pi0);
  Pi1 = mod289(Pi1);
  vec3 Pf0 = fract(P); // Fractional part for interpolation
  vec3 Pf1 = Pf0 - vec3(1.0); // Fractional part - 1.0
  vec4 ix = vec4(Pi0.x, Pi1.x, Pi0.x, Pi1.x);
  vec4 iy = vec4(Pi0.yy, Pi1.yy);
  vec4 iz0 = Pi0.zzzz;
  vec4 iz1 = Pi1.zzzz;

  vec4 ixy = permute(permute(ix) + iy);
  vec4 ixy0 = permute(ixy + iz0);
  vec4 ixy1 = permute(ixy + iz1);

  vec4 gx0 = ixy0 * (1.0 / 7.0);
  vec4 gy0 = fract(floor(gx0) * (1.0 / 7.0)) - 0.5;
  gx0 = fract(gx0);
  vec4 gz0 = vec4(0.5) - abs(gx0) - abs(gy0);
  vec4 sz0 = step(gz0, vec4(0.0));
  gx0 -= sz0 * (step(0.0, gx0) - 0.5);
  gy0 -= sz0 * (step(0.0, gy0) - 0.5);

  vec4 gx1 = ixy1 * (1.0 / 7.0);
  vec4 gy1 = fract(floor(gx1) * (1.0 / 7.0)) - 0.5;
  gx1 = fract(gx1);
  vec4 gz1 = vec4(0.5) - abs(gx1) - abs(gy1);
  vec4 sz1 = step(gz1, vec4(0.0));
  gx1 -= sz1 * (step(0.0, gx1) - 0.5);
  gy1 -= sz1 * (step(0.0, gy1) - 0.5);

  vec3 g000 = vec3(gx0.x,gy0.x,gz0.x);
  vec3 g100 = vec3(gx0.y,gy0.y,gz0.y);
  vec3 g010 = vec3(gx0.z,gy0.z,gz0.z);
  vec3 g110 = vec3(gx0.w,gy0.w,gz0.w);
  vec3 g001 = vec3(gx1.x,gy1.x,gz1.x);
  vec3 g101 = vec3(gx1.y,gy1.y,gz1.y);
  vec3 g011 = vec3(gx1.z,gy1.z,gz1.z);
  vec3 g111 = vec3(gx1.w,gy1.w,gz1.w);

  vec4 norm0 = taylorInvSqrt(vec4(dot(g000, g000), dot(g010, g010), dot(g100, g100), dot(g110, g110)));
  g000 *= norm0.x;
  g010 *= norm0.y;
  g100 *= norm0.z;
  g110 *= norm0.w;
  vec4 norm1 = taylorInvSqrt(vec4(dot(g001, g001), dot(g011, g011), dot(g101, g101), dot(g111, g111)));
  g001 *= norm1.x;
  g011 *= norm1.y;
  g101 *= norm1.z;
  g111 *= norm1.w;

  float n000 = dot(g000, Pf0);
  float n100 = dot(g100, vec3(Pf1.x, Pf0.yz));
  float n010 = dot(g010, vec3(Pf0.x, Pf1.y, Pf0.z));
  float n110 = dot(g110, vec3(Pf1.xy, Pf0.z));
  float n001 = dot(g001, vec3(Pf0.xy, Pf1.z));
  float n101 = dot(g101, vec3(Pf1.x, Pf0.y, Pf1.z));
  float n011 = dot(g011, vec3(Pf0.x, Pf1.yz));
  float n111 = dot(g111, Pf1);

  vec3 fade_xyz = fade(Pf0);
  vec4 n_z = mix(vec4(n000, n100, n010, n110), vec4(n001, n101, n011, n111), fade_xyz.z);
  vec2 n_yz = mix(n_z.xy, n_z.zw, fade_xyz.y);
  float n_xyz = mix(n_yz.x, n_yz.y, fade_xyz.x); 
  return 2.2 * n_xyz;
}

//-------- Holographic Effect Functions ------------

mat3 rotation3dY(float angle) {
  float s = sin(angle);
  float c = cos(angle);
  return mat3(c, 0.0, -s, 0.0, 1.0, 0.0, s, 0.0, c);
}

mat3 rotation3dX(float angle) {
  float s = sin(angle);
  float c = cos(angle);
  return mat3(1.0, 0.0, 0.0, 0.0, c, s, 0.0, -s, c);
}

vec3 rotateY(vec3 v, float angle) { return rotation3dY(angle) * v; }
vec3 rotateX(vec3 v, float angle) { return rotation3dX(angle) * v; }

varying vec3 vNormal;
varying float displacement;
varying vec3 vPos;
varying float vDistort;
varying vec2 vUv;
varying float vHolographicIntensity;
varying float vCosmicWave;

uniform float uTime;
uniform float uSpeed;
uniform float uLoadingTime;
uniform float uNoiseDensity;
uniform float uNoiseStrength;

#define STANDARD
varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif
#include <clipping_planes_pars_vertex>
#include <color_pars_vertex>
#include <common>
#include <displacementmap_pars_vertex>
#include <fog_pars_vertex>
#include <logdepthbuf_pars_vertex>
#include <morphtarget_pars_vertex>
#include <shadowmap_pars_vertex>
#include <skinning_pars_vertex>
#include <uv2_pars_vertex>
#include <uv_pars_vertex>

void main() {

  #include <beginnormal_vertex>
  #include <color_vertex>
  #include <defaultnormal_vertex>
  #include <morphnormal_vertex>
  #include <skinbase_vertex>
  #include <skinnormal_vertex>
  #include <uv2_vertex>
  #include <uv_vertex>
  #ifndef FLAT_SHADED
    vNormal = normalize(transformedNormal);
  #ifdef USE_TANGENT
    vTangent = normalize(transformedTangent);
    vBitangent = normalize(cross(vNormal, vTangent) * tangent.w);
  #endif
  #endif
  #include <begin_vertex>

  #include <clipping_planes_vertex>
  #include <displacementmap_vertex>
  #include <logdepthbuf_vertex>
  #include <morphtarget_vertex>
  #include <project_vertex>
  #include <skinning_vertex>
    vViewPosition = -mvPosition.xyz;
  #include <fog_vertex>
  #include <shadowmap_vertex>
  #include <worldpos_vertex>

  //-------- Cosmic Holographic Effect ------------
  vUv = uv;
  
  float t = uTime * uSpeed;
  
  // Create holographic interference patterns
  float holographicPattern = sin(position.x * 15.0 + t * 2.0) * 
                            sin(position.y * 12.0 + t * 1.5) * 0.1;
  
  // Cosmic wave distortion
  float cosmicWave = cnoise(position * uNoiseDensity * 0.5 + vec3(t * 0.3, t * 0.2, t * 0.4));
  vCosmicWave = cosmicWave;
  
  // Multi-layer noise for depth
  float noise1 = cnoise(position * uNoiseDensity * 2.0 + t * 0.8);
  float noise2 = cnoise(position * uNoiseDensity * 0.3 + t * 0.2) * 0.5;
  float noise3 = cnoise(position * uNoiseDensity * 4.0 + t * 1.2) * 0.25;
  
  float combinedNoise = noise1 + noise2 + noise3;
  
  // Holographic shimmer effect
  float shimmer = sin(position.x * 30.0 + t * 4.0) * 
                  cos(position.y * 25.0 + t * 3.0) * 0.05;
  
  // Calculate holographic intensity for fragment shader
  vHolographicIntensity = abs(holographicPattern) + abs(shimmer) * 2.0;
  
  // Apply displacement with holographic and cosmic effects
  float totalDisplacement = (combinedNoise + holographicPattern + shimmer) * uNoiseStrength * uLoadingTime;
  
  vec3 pos = position + normal * totalDisplacement;
  vPos = pos;
  
  // Add subtle rotation effect for cosmic feel
  pos = rotateY(pos, sin(t * 0.1) * 0.05);
  pos = rotateX(pos, cos(t * 0.07) * 0.03);

  gl_Position = projectionMatrix * modelViewMatrix * vec4(pos, 1.0);
}
`;

// node_modules/@shadergradient/react/dist/chunk-B5JTL55V.mjs
var a3 = {};
A(a3, { fragment: () => n13, vertex: () => e13 });
var n13 = `// Cosmic Sphere Fragment Shader - Nebula Particle Effect

#define STANDARD
#ifdef PHYSICAL
#define REFLECTIVITY
#define CLEARCOAT
#define TRANSMISSION
#endif

uniform vec3 diffuse;
uniform vec3 emissive;
uniform float roughness;
uniform float metalness;
uniform float opacity;

#ifdef TRANSMISSION
uniform float transmission;
#endif
#ifdef REFLECTIVITY
uniform float reflectivity;
#endif
#ifdef CLEARCOAT
uniform float clearcoat;
uniform float clearcoatRoughness;
#endif
#ifdef USE_SHEEN
uniform vec3 sheen;
#endif
varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif
#include <alphamap_pars_fragment>
#include <aomap_pars_fragment>
#include <color_pars_fragment>
#include <common>
#include <dithering_pars_fragment>
#include <emissivemap_pars_fragment>
#include <lightmap_pars_fragment>
#include <map_pars_fragment>
#include <packing>
#include <uv2_pars_fragment>
#include <uv_pars_fragment>
#include <bsdfs>
#include <bumpmap_pars_fragment>
#include <clearcoat_pars_fragment>
#include <clipping_planes_pars_fragment>
#include <cube_uv_reflection_fragment>
#include <envmap_common_pars_fragment>
#include <envmap_physical_pars_fragment>
#include <fog_pars_fragment>
#include <lights_pars_begin>
#include <lights_physical_pars_fragment>
#include <logdepthbuf_pars_fragment>
#include <metalnessmap_pars_fragment>
#include <normalmap_pars_fragment>
#include <roughnessmap_pars_fragment>
#include <shadowmap_pars_fragment>

varying vec3 vNormal;
varying float displacement;
varying vec3 vPos;
varying float vDistort;
varying vec2 vUv;
varying float vNebulaIntensity;
varying float vParticleDensity;
varying vec3 vCosmicSwirl;

uniform float uTime;
uniform float uSpeed;

uniform float uC1r;
uniform float uC1g;
uniform float uC1b;
uniform float uC2r;
uniform float uC2g;
uniform float uC2b;
uniform float uC3r;
uniform float uC3g;
uniform float uC3b;

// Nebula helper functions
float hash(vec2 p) {
    return fract(sin(dot(p, vec2(127.1, 311.7))) * 43758.5453123);
}

float noise2D(vec2 p) {
    vec2 i = floor(p);
    vec2 f = fract(p);
    vec2 u = f * f * (3.0 - 2.0 * f);
    
    return mix(mix(hash(i + vec2(0.0, 0.0)), 
                   hash(i + vec2(1.0, 0.0)), u.x),
               mix(hash(i + vec2(0.0, 1.0)), 
                   hash(i + vec2(1.0, 1.0)), u.x), u.y);
}

// Fractal Brownian Motion for complex nebula patterns
float fbm(vec2 p) {
    float value = 0.0;
    float amplitude = 0.5;
    float frequency = 1.0;
    
    for(int i = 0; i < 5; i++) {
        value += amplitude * noise2D(p * frequency);
        amplitude *= 0.5;
        frequency *= 2.0;
    }
    return value;
}

// Star field generation
float stars(vec2 p, float density) {
    vec2 n = floor(p * density);
    vec2 f = fract(p * density);
    
    float d = 1.0;
    for(int i = -1; i <= 1; i++) {
        for(int j = -1; j <= 1; j++) {
            vec2 g = vec2(float(i), float(j));
            vec2 o = hash(n + g) * vec2(1.0);
            vec2 r = g + o - f;
            d = min(d, dot(r, r));
        }
    }
    
    return 1.0 - smoothstep(0.0, 0.02, sqrt(d));
}

// for npm package, need to add this manually
float linearToRelativeLuminance2( const in vec3 color ) {
    vec3 weights = vec3( 0.2126, 0.7152, 0.0722 );
    return dot( weights, color.rgb );
}

void main() {

  //-------- Cosmic Nebula Gradient ------------
  vec3 color1 = vec3(uC1r, uC1g, uC1b);
  vec3 color2 = vec3(uC2r, uC2g, uC2b);
  vec3 color3 = vec3(uC3r, uC3g, uC3b);
  
  float clearcoat = 1.0;
  float clearcoatRoughness = 0.1; // Very reflective for cosmic shine

  #include <clipping_planes_fragment>

  float t = uTime * uSpeed;
  
  // Calculate distance from center for radial effects
  float distanceFromCenter = length(vPos);
  float angle = atan(vPos.y, vPos.x);
  
  // Create complex nebula patterns using FBM
  vec2 nebulaCoords = vPos.xy * 3.0 + vCosmicSwirl.xy;
  float nebulaPattern1 = fbm(nebulaCoords + t * 0.1);
  float nebulaPattern2 = fbm(nebulaCoords * 2.0 + t * 0.15);
  float nebulaPattern3 = fbm(nebulaCoords * 4.0 + t * 0.2);
  
  // Combine nebula patterns
  float combinedNebula = (nebulaPattern1 + nebulaPattern2 * 0.5 + nebulaPattern3 * 0.25) / 1.75;
  
  // Create particle-like bright spots
  float particleField = stars(vPos.xy * 20.0 + t * 0.5, 50.0);
  float microParticles = stars(vPos.xy * 80.0 + t * 1.0, 200.0) * 0.5;
  
  // Create cosmic dust clouds
  float dustClouds = fbm(vPos.xy * 8.0 + t * 0.05) * 0.3;
  
  // Energy streams
  float energyStream1 = sin(vPos.x * 15.0 + t * 3.0 + angle * 2.0) * 0.1;
  float energyStream2 = cos(vPos.y * 20.0 + t * 2.5 + distanceFromCenter * 5.0) * 0.1;
  
  // Cosmic gradient mixing with nebula influence
  float gradientX = smoothstep(-3.0, 3.0, vPos.x + combinedNebula * 2.0 + vCosmicSwirl.x * 3.0);
  float gradientY = smoothstep(-3.0, 3.0, vPos.y + vNebulaIntensity * 1.5 + vCosmicSwirl.y * 2.0);
  float gradientZ = smoothstep(-2.0, 2.0, vPos.z + dustClouds * 2.0);
  
  // Multi-layer color mixing
  vec3 baseGradient = mix(
    mix(color1, color2, gradientX), 
    color3, 
    gradientY * 0.6 + gradientZ * 0.4
  );
  
  // Add nebula color variations
  vec3 nebulaColor = baseGradient;
  nebulaColor.r += combinedNebula * 0.3 + energyStream1;
  nebulaColor.g += vNebulaIntensity * 0.2 + energyStream2;
  nebulaColor.b += dustClouds * 0.4 + abs(vCosmicSwirl.z) * 0.5;
  
  // Add particle brightness
  vec3 particleGlow = vec3(
    particleField * 0.8 + microParticles * 0.4,
    particleField * 0.6 + microParticles * 0.3,
    particleField * 0.9 + microParticles * 0.5
  );
  
  // Create pulsing cosmic energy
  float cosmicPulse = sin(t * 1.5 + distanceFromCenter * 3.0) * 0.1 + 1.0;
  
  // Combine all effects
  vec3 finalColor = (nebulaColor + particleGlow * 2.0) * cosmicPulse;
  
  // Add cosmic rim lighting effect
  float rimLight = pow(1.0 - abs(dot(normalize(vNormal), normalize(vViewPosition))), 2.0);
  finalColor += rimLight * 0.3 * (color1 + color2 + color3) / 3.0;
  
  // Enhance particle density areas
  finalColor = mix(finalColor, finalColor * 1.5, vParticleDensity * 0.5);
  
  // Add subtle color temperature variation
  float temperature = sin(angle * 3.0 + t * 0.8) * 0.1;
  finalColor.r += temperature * 0.1;
  finalColor.b -= temperature * 0.1;

  vec4 diffuseColor = vec4(finalColor, 1.0);

  //-------- Enhanced Materiality for Cosmic Effect ------------
  ReflectedLight reflectedLight = ReflectedLight(vec3(0.0), vec3(0.0), vec3(0.0), vec3(0.0));
  vec3 totalEmissiveRadiance = emissive + finalColor * 0.2; // Strong emission for nebula glow

  #ifdef TRANSMISSION
    float totalTransmission = transmission;
  #endif
  #include <logdepthbuf_fragment>
  #include <map_fragment>
  #include <color_fragment>
  #include <alphamap_fragment>
  #include <alphatest_fragment>
  #include <roughnessmap_fragment>
  #include <metalnessmap_fragment>
  #include <normal_fragment_begin>
  #include <normal_fragment_maps>
  #include <clearcoat_normal_fragment_begin>
  #include <clearcoat_normal_fragment_maps>
  #include <emissivemap_fragment>
  #include <lights_physical_fragment>
  #include <lights_fragment_begin>
  #include <lights_fragment_maps>
  #include <lights_fragment_end>
  #include <aomap_fragment>
  
  vec3 outgoingLight = reflectedLight.directDiffuse + reflectedLight.indirectDiffuse +
                      reflectedLight.directSpecular + reflectedLight.indirectSpecular +
                      totalEmissiveRadiance;

  #ifdef TRANSMISSION
    diffuseColor.a *= mix(saturate(1. - totalTransmission +
                        linearToRelativeLuminance2(reflectedLight.directSpecular +
                                                  reflectedLight.indirectSpecular)),
                1.0, metalness);
  #endif

  #include <tonemapping_fragment>
  #include <encodings_fragment>
  #include <fog_fragment>
  #include <premultiplied_alpha_fragment>
  #include <dithering_fragment>

  gl_FragColor = vec4(outgoingLight, diffuseColor.a);
}
`;
var e13 = `// Cosmic Sphere Vertex Shader - Nebula Effect
// #pragma glslify: cnoise3 = require(glsl-noise/classic/3d) 

// noise source from https://github.com/hughsk/glsl-noise/blob/master/periodic/3d.glsl

vec3 mod289(vec3 x)
{
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 mod289(vec4 x)
{
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 permute(vec4 x)
{
  return mod289(((x*34.0)+1.0)*x);
}

vec4 taylorInvSqrt(vec4 r)
{
  return 1.79284291400159 - 0.85373472095314 * r;
}

vec3 fade(vec3 t) {
  return t*t*t*(t*(t*6.0-15.0)+10.0);
}

float cnoise(vec3 P)
{
  vec3 Pi0 = floor(P); // Integer part for indexing
  vec3 Pi1 = Pi0 + vec3(1.0); // Integer part + 1
  Pi0 = mod289(Pi0);
  Pi1 = mod289(Pi1);
  vec3 Pf0 = fract(P); // Fractional part for interpolation
  vec3 Pf1 = Pf0 - vec3(1.0); // Fractional part - 1.0
  vec4 ix = vec4(Pi0.x, Pi1.x, Pi0.x, Pi1.x);
  vec4 iy = vec4(Pi0.yy, Pi1.yy);
  vec4 iz0 = Pi0.zzzz;
  vec4 iz1 = Pi1.zzzz;

  vec4 ixy = permute(permute(ix) + iy);
  vec4 ixy0 = permute(ixy + iz0);
  vec4 ixy1 = permute(ixy + iz1);

  vec4 gx0 = ixy0 * (1.0 / 7.0);
  vec4 gy0 = fract(floor(gx0) * (1.0 / 7.0)) - 0.5;
  gx0 = fract(gx0);
  vec4 gz0 = vec4(0.5) - abs(gx0) - abs(gy0);
  vec4 sz0 = step(gz0, vec4(0.0));
  gx0 -= sz0 * (step(0.0, gx0) - 0.5);
  gy0 -= sz0 * (step(0.0, gy0) - 0.5);

  vec4 gx1 = ixy1 * (1.0 / 7.0);
  vec4 gy1 = fract(floor(gx1) * (1.0 / 7.0)) - 0.5;
  gx1 = fract(gx1);
  vec4 gz1 = vec4(0.5) - abs(gx1) - abs(gy1);
  vec4 sz1 = step(gz1, vec4(0.0));
  gx1 -= sz1 * (step(0.0, gx1) - 0.5);
  gy1 -= sz1 * (step(0.0, gy1) - 0.5);

  vec3 g000 = vec3(gx0.x,gy0.x,gz0.x);
  vec3 g100 = vec3(gx0.y,gy0.y,gz0.y);
  vec3 g010 = vec3(gx0.z,gy0.z,gz0.z);
  vec3 g110 = vec3(gx0.w,gy0.w,gz0.w);
  vec3 g001 = vec3(gx1.x,gy1.x,gz1.x);
  vec3 g101 = vec3(gx1.y,gy1.y,gz1.y);
  vec3 g011 = vec3(gx1.z,gy1.z,gz1.z);
  vec3 g111 = vec3(gx1.w,gy1.w,gz1.w);

  vec4 norm0 = taylorInvSqrt(vec4(dot(g000, g000), dot(g010, g010), dot(g100, g100), dot(g110, g110)));
  g000 *= norm0.x;
  g010 *= norm0.y;
  g100 *= norm0.z;
  g110 *= norm0.w;
  vec4 norm1 = taylorInvSqrt(vec4(dot(g001, g001), dot(g011, g011), dot(g101, g101), dot(g111, g111)));
  g001 *= norm1.x;
  g011 *= norm1.y;
  g101 *= norm1.z;
  g111 *= norm1.w;

  float n000 = dot(g000, Pf0);
  float n100 = dot(g100, vec3(Pf1.x, Pf0.yz));
  float n010 = dot(g010, vec3(Pf0.x, Pf1.y, Pf0.z));
  float n110 = dot(g110, vec3(Pf1.xy, Pf0.z));
  float n001 = dot(g001, vec3(Pf0.xy, Pf1.z));
  float n101 = dot(g101, vec3(Pf1.x, Pf0.y, Pf1.z));
  float n011 = dot(g011, vec3(Pf0.x, Pf1.yz));
  float n111 = dot(g111, Pf1);

  vec3 fade_xyz = fade(Pf0);
  vec4 n_z = mix(vec4(n000, n100, n010, n110), vec4(n001, n101, n011, n111), fade_xyz.z);
  vec2 n_yz = mix(n_z.xy, n_z.zw, fade_xyz.y);
  float n_xyz = mix(n_yz.x, n_yz.y, fade_xyz.x); 
  return 2.2 * n_xyz;
}

//-------- Nebula Effect Functions ------------

mat3 rotation3dY(float angle) {
  float s = sin(angle);
  float c = cos(angle);
  return mat3(c, 0.0, -s, 0.0, 1.0, 0.0, s, 0.0, c);
}

mat3 rotation3dX(float angle) {
  float s = sin(angle);
  float c = cos(angle);
  return mat3(1.0, 0.0, 0.0, 0.0, c, s, 0.0, -s, c);
}

mat3 rotation3dZ(float angle) {
  float s = sin(angle);
  float c = cos(angle);
  return mat3(c, s, 0.0, -s, c, 0.0, 0.0, 0.0, 1.0);
}

vec3 rotateY(vec3 v, float angle) { return rotation3dY(angle) * v; }
vec3 rotateX(vec3 v, float angle) { return rotation3dX(angle) * v; }
vec3 rotateZ(vec3 v, float angle) { return rotation3dZ(angle) * v; }

varying vec3 vNormal;
varying float displacement;
varying vec3 vPos;
varying float vDistort;
varying vec2 vUv;
varying float vNebulaIntensity;
varying float vParticleDensity;
varying vec3 vCosmicSwirl;

uniform float uTime;
uniform float uSpeed;
uniform float uLoadingTime;
uniform float uNoiseDensity;
uniform float uNoiseStrength;

#define STANDARD
varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif
#include <clipping_planes_pars_vertex>
#include <color_pars_vertex>
#include <common>
#include <displacementmap_pars_vertex>
#include <fog_pars_vertex>
#include <logdepthbuf_pars_vertex>
#include <morphtarget_pars_vertex>
#include <shadowmap_pars_vertex>
#include <skinning_pars_vertex>
#include <uv2_pars_vertex>
#include <uv_pars_vertex>

void main() {

  #include <beginnormal_vertex>
  #include <color_vertex>
  #include <defaultnormal_vertex>
  #include <morphnormal_vertex>
  #include <skinbase_vertex>
  #include <skinnormal_vertex>
  #include <uv2_vertex>
  #include <uv_vertex>
  #ifndef FLAT_SHADED
    vNormal = normalize(transformedNormal);
  #ifdef USE_TANGENT
    vTangent = normalize(transformedTangent);
    vBitangent = normalize(cross(vNormal, vTangent) * tangent.w);
  #endif
  #endif
  #include <begin_vertex>

  #include <clipping_planes_vertex>
  #include <displacementmap_vertex>
  #include <logdepthbuf_vertex>
  #include <morphtarget_vertex>
  #include <project_vertex>
  #include <skinning_vertex>
    vViewPosition = -mvPosition.xyz;
  #include <fog_vertex>
  #include <shadowmap_vertex>
  #include <worldpos_vertex>

  //-------- Cosmic Nebula Effect ------------
  vUv = uv;
  
  float t = uTime * uSpeed;
  
  // Create swirling nebula patterns
  vec3 swirlCenter = vec3(0.0, 0.0, 0.0);
  vec3 toCenter = position - swirlCenter;
  float distanceFromCenter = length(toCenter);
  
  // Create spiral motion
  float angle = atan(toCenter.y, toCenter.x);
  float spiralAngle = angle + distanceFromCenter * 2.0 + t * 0.5;
  
  // Multi-octave noise for nebula density
  float nebula1 = cnoise(position * uNoiseDensity * 0.8 + vec3(t * 0.2, t * 0.3, t * 0.1));
  float nebula2 = cnoise(position * uNoiseDensity * 1.5 + vec3(t * 0.4, t * 0.2, t * 0.5)) * 0.7;
  float nebula3 = cnoise(position * uNoiseDensity * 3.0 + vec3(t * 0.8, t * 0.6, t * 0.9)) * 0.4;
  float nebula4 = cnoise(position * uNoiseDensity * 6.0 + vec3(t * 1.2, t * 1.0, t * 1.4)) * 0.2;
  
  // Combine nebula layers for complexity
  float nebulaPattern = nebula1 + nebula2 + nebula3 + nebula4;
  vNebulaIntensity = abs(nebulaPattern);
  
  // Create particle-like density variations
  float particleDensity = cnoise(position * uNoiseDensity * 8.0 + vec3(t * 2.0, t * 1.5, t * 2.5));
  vParticleDensity = smoothstep(-0.3, 0.8, particleDensity);
  
  // Create cosmic swirl effect
  vec3 swirl = vec3(
    sin(spiralAngle + t * 0.3) * distanceFromCenter * 0.1,
    cos(spiralAngle + t * 0.2) * distanceFromCenter * 0.1,
    sin(distanceFromCenter * 3.0 + t * 0.4) * 0.05
  );
  vCosmicSwirl = swirl;
  
  // Create pulsing effect for cosmic energy
  float pulse = sin(t * 2.0 + distanceFromCenter * 5.0) * 0.1 + 1.0;
  
  // Apply complex displacement
  float totalDisplacement = nebulaPattern * uNoiseStrength * uLoadingTime * pulse;
  
  // Add swirl displacement
  vec3 pos = position + normal * totalDisplacement + swirl * 0.3;
  vPos = pos;
  
  // Add cosmic rotation for dynamic feel
  pos = rotateY(pos, sin(t * 0.1 + distanceFromCenter) * 0.1);
  pos = rotateX(pos, cos(t * 0.08 + angle) * 0.08);
  pos = rotateZ(pos, sin(t * 0.05 + spiralAngle) * 0.05);

  gl_Position = projectionMatrix * modelViewMatrix * vec4(pos, 1.0);
}
`;

// node_modules/@shadergradient/react/dist/chunk-N4CAJBCU.mjs
var a4 = {};
A(a4, { fragment: () => n14, vertex: () => e14 });
var n14 = `// Cosmic WaterPlane Fragment Shader - Aurora Wave Effect

#define STANDARD
#ifdef PHYSICAL
#define REFLECTIVITY
#define CLEARCOAT
#define TRANSMISSION
#endif

uniform vec3 diffuse;
uniform vec3 emissive;
uniform float roughness;
uniform float metalness;
uniform float opacity;

#ifdef TRANSMISSION
uniform float transmission;
#endif
#ifdef REFLECTIVITY
uniform float reflectivity;
#endif
#ifdef CLEARCOAT
uniform float clearcoat;
uniform float clearcoatRoughness;
#endif
#ifdef USE_SHEEN
uniform vec3 sheen;
#endif
varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif
#include <alphamap_pars_fragment>
#include <aomap_pars_fragment>
#include <color_pars_fragment>
#include <common>
#include <dithering_pars_fragment>
#include <emissivemap_pars_fragment>
#include <lightmap_pars_fragment>
#include <map_pars_fragment>
#include <packing>
#include <uv2_pars_fragment>
#include <uv_pars_fragment>
#include <bsdfs>
#include <bumpmap_pars_fragment>
#include <clearcoat_pars_fragment>
#include <clipping_planes_pars_fragment>
#include <cube_uv_reflection_fragment>
#include <envmap_common_pars_fragment>
#include <envmap_physical_pars_fragment>
#include <fog_pars_fragment>
#include <lights_pars_begin>
#include <lights_physical_pars_fragment>
#include <logdepthbuf_pars_fragment>
#include <metalnessmap_pars_fragment>
#include <normalmap_pars_fragment>
#include <roughnessmap_pars_fragment>
#include <shadowmap_pars_fragment>

varying vec3 vNormal;
varying float displacement;
varying vec3 vPos;
varying float vDistort;
varying vec2 vUv;
varying float vAuroraIntensity;
varying float vWaveHeight;
varying vec3 vFlowDirection;

uniform float uTime;
uniform float uSpeed;

uniform float uC1r;
uniform float uC1g;
uniform float uC1b;
uniform float uC2r;
uniform float uC2g;
uniform float uC2b;
uniform float uC3r;
uniform float uC3g;
uniform float uC3b;

// Aurora helper functions
float hash(vec2 p) {
    return fract(sin(dot(p, vec2(127.1, 311.7))) * 43758.5453123);
}

float noise2D(vec2 p) {
    vec2 i = floor(p);
    vec2 f = fract(p);
    vec2 u = f * f * (3.0 - 2.0 * f);
    
    return mix(mix(hash(i + vec2(0.0, 0.0)), 
                   hash(i + vec2(1.0, 0.0)), u.x),
               mix(hash(i + vec2(0.0, 1.0)), 
                   hash(i + vec2(1.0, 1.0)), u.x), u.y);
}

// Fractal Brownian Motion for aurora patterns
float fbm(vec2 p) {
    float value = 0.0;
    float amplitude = 0.5;
    float frequency = 1.0;
    
    for(int i = 0; i < 4; i++) {
        value += amplitude * noise2D(p * frequency);
        amplitude *= 0.5;
        frequency *= 2.0;
    }
    return value;
}

// Aurora curtain effect
float aurora(vec2 p, float time) {
    vec2 q = vec2(fbm(p + vec2(0.0, time * 0.1)),
                  fbm(p + vec2(5.2, time * 0.15)));
    
    vec2 r = vec2(fbm(p + 4.0 * q + vec2(1.7, time * 0.2)),
                  fbm(p + 4.0 * q + vec2(8.3, time * 0.18)));
    
    return fbm(p + 4.0 * r);
}

// Water caustics effect
float caustics(vec2 p, float time) {
    vec2 uv = p * 4.0;
    vec2 p0 = uv + vec2(time * 0.3, time * 0.2);
    vec2 p1 = uv + vec2(time * -0.4, time * 0.3);
    
    float c1 = sin(length(p0) * 8.0 - time * 2.0) * 0.5 + 0.5;
    float c2 = sin(length(p1) * 6.0 - time * 1.5) * 0.5 + 0.5;
    
    return (c1 + c2) * 0.5;
}

// for npm package, need to add this manually
float linearToRelativeLuminance2( const in vec3 color ) {
    vec3 weights = vec3( 0.2126, 0.7152, 0.0722 );
    return dot( weights, color.rgb );
}

void main() {

  //-------- Cosmic Aurora Water Gradient ------------
  vec3 color1 = vec3(uC1r, uC1g, uC1b);
  vec3 color2 = vec3(uC2r, uC2g, uC2b);
  vec3 color3 = vec3(uC3r, uC3g, uC3b);
  
  float clearcoat = 1.0;
  float clearcoatRoughness = 0.05; // Very smooth for water-like reflection

  #include <clipping_planes_fragment>

  float t = uTime * uSpeed;
  
  // Create aurora patterns
  vec2 auroraCoords = vPos.xy * 2.0 + vFlowDirection.xy * t * 0.5;
  float auroraPattern1 = aurora(auroraCoords, t);
  float auroraPattern2 = aurora(auroraCoords * 1.5 + vec2(3.0, 1.0), t * 1.2);
  float auroraPattern3 = aurora(auroraCoords * 0.7 + vec2(-2.0, 4.0), t * 0.8);
  
  // Combine aurora layers
  float combinedAurora = (auroraPattern1 + auroraPattern2 * 0.7 + auroraPattern3 * 0.5) / 2.2;
  
  // Create water caustics
  float causticsPattern = caustics(vPos.xy, t);
  
  // Create flowing light streams
  float lightStream1 = sin(vPos.x * 8.0 + t * 2.0 + combinedAurora * 3.0) * 0.2;
  float lightStream2 = cos(vPos.y * 6.0 + t * 1.5 + vWaveHeight * 4.0) * 0.15;
  float lightStream3 = sin((vPos.x + vPos.y) * 10.0 + t * 2.5) * 0.1;
  
  // Create cosmic energy waves
  float distanceFromCenter = length(vPos.xy);
  float energyWave = sin(distanceFromCenter * 5.0 - t * 3.0) * 
                     exp(-distanceFromCenter * 0.05) * 0.3;
  
  // Aurora color shifting effect
  vec3 auroraShift = vec3(
    sin(combinedAurora * 6.28 + t * 1.0) * 0.2,
    sin(combinedAurora * 6.28 + t * 1.0 + 2.094) * 0.2,  // 120 degrees
    sin(combinedAurora * 6.28 + t * 1.0 + 4.188) * 0.2   // 240 degrees
  );
  
  // Enhanced gradient mixing with aurora and water effects
  float gradientX = smoothstep(-4.0, 4.0, vPos.x + combinedAurora * 3.0 + vFlowDirection.x * 2.0);
  float gradientY = smoothstep(-4.0, 4.0, vPos.y + vWaveHeight * 2.0 + lightStream1 * 3.0);
  float gradientZ = smoothstep(-3.0, 3.0, vPos.z + causticsPattern * 2.0);
  
  // Multi-layer color mixing
  vec3 baseGradient = mix(
    mix(color1, color2, gradientX), 
    color3, 
    gradientY * 0.7 + gradientZ * 0.3
  );
  
  // Apply aurora color shifts
  vec3 auroraColor = baseGradient + auroraShift;
  
  // Add water caustics coloring
  vec3 causticsColor = vec3(
    causticsPattern * 0.3,
    causticsPattern * 0.4,
    causticsPattern * 0.5
  );
  
  // Add light streams
  vec3 lightStreams = vec3(
    abs(lightStream1) * 0.4,
    abs(lightStream2) * 0.3,
    abs(lightStream3) * 0.5
  );
  
  // Aurora intensity modulation
  float auroraIntensityMod = 1.0 + vAuroraIntensity * 0.8 + abs(combinedAurora) * 0.6;
  
  // Combine all effects
  vec3 finalColor = (auroraColor + causticsColor + lightStreams + vec3(energyWave * 0.2)) * auroraIntensityMod;
  
  // Add water-like shimmer
  float shimmer = sin(vPos.x * 20.0 + t * 4.0) * 
                  cos(vPos.y * 18.0 + t * 3.5) * 
                  vWaveHeight * 0.1;
  finalColor += vec3(shimmer * 0.3, shimmer * 0.4, shimmer * 0.6);
  
  // Add aurora dancing effect
  float auroraMovement = sin(vPos.x * 3.0 + t * 1.2 + combinedAurora * 2.0) * 
                         cos(vPos.y * 2.5 + t * 0.9) * 0.15;
  finalColor.g += abs(auroraMovement) * 0.4;
  finalColor.b += abs(auroraMovement) * 0.2;
  
  // Add cosmic depth variation
  float depthVariation = noise2D(vPos.xy * 5.0 + t * 0.3) * 0.1;
  finalColor *= (1.0 + depthVariation);

  vec4 diffuseColor = vec4(finalColor, 1.0);

  //-------- Enhanced Materiality for Water Aurora Effect ------------
  ReflectedLight reflectedLight = ReflectedLight(vec3(0.0), vec3(0.0), vec3(0.0), vec3(0.0));
  vec3 totalEmissiveRadiance = emissive + finalColor * 0.15; // Moderate emission for aurora glow

  #ifdef TRANSMISSION
    float totalTransmission = transmission;
  #endif
  #include <logdepthbuf_fragment>
  #include <map_fragment>
  #include <color_fragment>
  #include <alphamap_fragment>
  #include <alphatest_fragment>
  #include <roughnessmap_fragment>
  #include <metalnessmap_fragment>
  #include <normal_fragment_begin>
  #include <normal_fragment_maps>
  #include <clearcoat_normal_fragment_begin>
  #include <clearcoat_normal_fragment_maps>
  #include <emissivemap_fragment>
  #include <lights_physical_fragment>
  #include <lights_fragment_begin>
  #include <lights_fragment_maps>
  #include <lights_fragment_end>
  #include <aomap_fragment>
  
  vec3 outgoingLight = reflectedLight.directDiffuse + reflectedLight.indirectDiffuse +
                      reflectedLight.directSpecular + reflectedLight.indirectSpecular +
                      totalEmissiveRadiance;

  #ifdef TRANSMISSION
    diffuseColor.a *= mix(saturate(1. - totalTransmission +
                        linearToRelativeLuminance2(reflectedLight.directSpecular +
                                                  reflectedLight.indirectSpecular)),
                1.0, metalness);
  #endif

  #include <tonemapping_fragment>
  #include <encodings_fragment>
  #include <fog_fragment>
  #include <premultiplied_alpha_fragment>
  #include <dithering_fragment>

  gl_FragColor = vec4(outgoingLight, diffuseColor.a);
}
`;
var e14 = `// Cosmic WaterPlane Vertex Shader - Aurora Wave Effect
// #pragma glslify: cnoise3 = require(glsl-noise/classic/3d) 

// noise source from https://github.com/hughsk/glsl-noise/blob/master/periodic/3d.glsl

vec3 mod289(vec3 x)
{
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 mod289(vec4 x)
{
  return x - floor(x * (1.0 / 289.0)) * 289.0;
}

vec4 permute(vec4 x)
{
  return mod289(((x*34.0)+1.0)*x);
}

vec4 taylorInvSqrt(vec4 r)
{
  return 1.79284291400159 - 0.85373472095314 * r;
}

vec3 fade(vec3 t) {
  return t*t*t*(t*(t*6.0-15.0)+10.0);
}

float cnoise(vec3 P)
{
  vec3 Pi0 = floor(P); // Integer part for indexing
  vec3 Pi1 = Pi0 + vec3(1.0); // Integer part + 1
  Pi0 = mod289(Pi0);
  Pi1 = mod289(Pi1);
  vec3 Pf0 = fract(P); // Fractional part for interpolation
  vec3 Pf1 = Pf0 - vec3(1.0); // Fractional part - 1.0
  vec4 ix = vec4(Pi0.x, Pi1.x, Pi0.x, Pi1.x);
  vec4 iy = vec4(Pi0.yy, Pi1.yy);
  vec4 iz0 = Pi0.zzzz;
  vec4 iz1 = Pi1.zzzz;

  vec4 ixy = permute(permute(ix) + iy);
  vec4 ixy0 = permute(ixy + iz0);
  vec4 ixy1 = permute(ixy + iz1);

  vec4 gx0 = ixy0 * (1.0 / 7.0);
  vec4 gy0 = fract(floor(gx0) * (1.0 / 7.0)) - 0.5;
  gx0 = fract(gx0);
  vec4 gz0 = vec4(0.5) - abs(gx0) - abs(gy0);
  vec4 sz0 = step(gz0, vec4(0.0));
  gx0 -= sz0 * (step(0.0, gx0) - 0.5);
  gy0 -= sz0 * (step(0.0, gy0) - 0.5);

  vec4 gx1 = ixy1 * (1.0 / 7.0);
  vec4 gy1 = fract(floor(gx1) * (1.0 / 7.0)) - 0.5;
  gx1 = fract(gx1);
  vec4 gz1 = vec4(0.5) - abs(gx1) - abs(gy1);
  vec4 sz1 = step(gz1, vec4(0.0));
  gx1 -= sz1 * (step(0.0, gx1) - 0.5);
  gy1 -= sz1 * (step(0.0, gy1) - 0.5);

  vec3 g000 = vec3(gx0.x,gy0.x,gz0.x);
  vec3 g100 = vec3(gx0.y,gy0.y,gz0.y);
  vec3 g010 = vec3(gx0.z,gy0.z,gz0.z);
  vec3 g110 = vec3(gx0.w,gy0.w,gz0.w);
  vec3 g001 = vec3(gx1.x,gy1.x,gz1.x);
  vec3 g101 = vec3(gx1.y,gy1.y,gz1.y);
  vec3 g011 = vec3(gx1.z,gy1.z,gz1.z);
  vec3 g111 = vec3(gx1.w,gy1.w,gz1.w);

  vec4 norm0 = taylorInvSqrt(vec4(dot(g000, g000), dot(g010, g010), dot(g100, g100), dot(g110, g110)));
  g000 *= norm0.x;
  g010 *= norm0.y;
  g100 *= norm0.z;
  g110 *= norm0.w;
  vec4 norm1 = taylorInvSqrt(vec4(dot(g001, g001), dot(g011, g011), dot(g101, g101), dot(g111, g111)));
  g001 *= norm1.x;
  g011 *= norm1.y;
  g101 *= norm1.z;
  g111 *= norm1.w;

  float n000 = dot(g000, Pf0);
  float n100 = dot(g100, vec3(Pf1.x, Pf0.yz));
  float n010 = dot(g010, vec3(Pf0.x, Pf1.y, Pf0.z));
  float n110 = dot(g110, vec3(Pf1.xy, Pf0.z));
  float n001 = dot(g001, vec3(Pf0.xy, Pf1.z));
  float n101 = dot(g101, vec3(Pf1.x, Pf0.y, Pf1.z));
  float n011 = dot(g011, vec3(Pf0.x, Pf1.yz));
  float n111 = dot(g111, Pf1);

  vec3 fade_xyz = fade(Pf0);
  vec4 n_z = mix(vec4(n000, n100, n010, n110), vec4(n001, n101, n011, n111), fade_xyz.z);
  vec2 n_yz = mix(n_z.xy, n_z.zw, fade_xyz.y);
  float n_xyz = mix(n_yz.x, n_yz.y, fade_xyz.x); 
  return 2.2 * n_xyz;
}

//-------- Aurora Wave Effect Functions ------------

mat3 rotation3dY(float angle) {
  float s = sin(angle);
  float c = cos(angle);
  return mat3(c, 0.0, -s, 0.0, 1.0, 0.0, s, 0.0, c);
}

vec3 rotateY(vec3 v, float angle) { return rotation3dY(angle) * v; }

varying vec3 vNormal;
varying float displacement;
varying vec3 vPos;
varying float vDistort;
varying vec2 vUv;
varying float vAuroraIntensity;
varying float vWaveHeight;
varying vec3 vFlowDirection;

uniform float uTime;
uniform float uSpeed;
uniform float uLoadingTime;
uniform float uNoiseDensity;
uniform float uNoiseStrength;

#define STANDARD
varying vec3 vViewPosition;
#ifndef FLAT_SHADED
#ifdef USE_TANGENT
varying vec3 vTangent;
varying vec3 vBitangent;
#endif
#endif
#include <clipping_planes_pars_vertex>
#include <color_pars_vertex>
#include <common>
#include <displacementmap_pars_vertex>
#include <fog_pars_vertex>
#include <logdepthbuf_pars_vertex>
#include <morphtarget_pars_vertex>
#include <shadowmap_pars_vertex>
#include <skinning_pars_vertex>
#include <uv2_pars_vertex>
#include <uv_pars_vertex>

void main() {

  #include <beginnormal_vertex>
  #include <color_vertex>
  #include <defaultnormal_vertex>
  #include <morphnormal_vertex>
  #include <skinbase_vertex>
  #include <skinnormal_vertex>
  #include <uv2_vertex>
  #include <uv_vertex>
  #ifndef FLAT_SHADED
    vNormal = normalize(transformedNormal);
  #ifdef USE_TANGENT
    vTangent = normalize(transformedTangent);
    vBitangent = normalize(cross(vNormal, vTangent) * tangent.w);
  #endif
  #endif
  #include <begin_vertex>

  #include <clipping_planes_vertex>
  #include <displacementmap_vertex>
  #include <logdepthbuf_vertex>
  #include <morphtarget_vertex>
  #include <project_vertex>
  #include <skinning_vertex>
    vViewPosition = -mvPosition.xyz;
  #include <fog_vertex>
  #include <shadowmap_vertex>
  #include <worldpos_vertex>

  //-------- Cosmic Aurora Wave Effect ------------
  vUv = uv;
  
  float t = uTime * uSpeed;
  
  // Create flowing aurora patterns
  float auroraFlow1 = sin(position.x * 5.0 + t * 1.5) * cos(position.y * 3.0 + t * 1.0);
  float auroraFlow2 = sin(position.x * 8.0 + t * 2.0) * sin(position.y * 6.0 + t * 1.8);
  float auroraFlow3 = cos(position.x * 12.0 + t * 2.5) * cos(position.y * 9.0 + t * 2.2);
  
  // Combine aurora flows
  float auroraPattern = (auroraFlow1 + auroraFlow2 * 0.7 + auroraFlow3 * 0.4) / 2.1;
  vAuroraIntensity = abs(auroraPattern);
  
  // Create multi-layered waves
  float wave1 = cnoise(vec3(position.xy * uNoiseDensity * 0.5, t * 0.3));
  float wave2 = cnoise(vec3(position.xy * uNoiseDensity * 1.2, t * 0.5)) * 0.6;
  float wave3 = cnoise(vec3(position.xy * uNoiseDensity * 2.5, t * 0.8)) * 0.3;
  float wave4 = cnoise(vec3(position.xy * uNoiseDensity * 5.0, t * 1.2)) * 0.15;
  
  // Combine waves for complex water surface
  float combinedWaves = wave1 + wave2 + wave3 + wave4;
  vWaveHeight = combinedWaves;
  
  // Create flowing current patterns
  vec2 flowDirection = vec2(
    sin(position.x * 2.0 + t * 0.8) + cos(position.y * 1.5 + t * 0.6),
    cos(position.x * 1.8 + t * 0.7) + sin(position.y * 2.2 + t * 0.9)
  );
  vFlowDirection = vec3(normalize(flowDirection), 0.0);
  
  // Aurora-influenced wave distortion
  float auroraWave = sin(position.x * 15.0 + t * 3.0 + auroraPattern * 5.0) * 
                     cos(position.y * 12.0 + t * 2.5 + auroraPattern * 4.0) * 0.2;
  
  // Create cosmic energy ripples
  float distanceFromCenter = length(position.xy);
  float cosmicRipple = sin(distanceFromCenter * 8.0 - t * 4.0) * 
                       exp(-distanceFromCenter * 0.1) * 0.3;
  
  // Pulsing effect for cosmic energy
  float cosmicPulse = sin(t * 1.5 + distanceFromCenter * 2.0) * 0.1 + 1.0;
  
  // Apply complex displacement
  float totalDisplacement = (combinedWaves + auroraWave + cosmicRipple) * 
                           uNoiseStrength * uLoadingTime * cosmicPulse;
  
  vec3 pos = position + normal * totalDisplacement;
  vPos = pos;
  
  // Add subtle rotation for cosmic flow
  pos = rotateY(pos, sin(t * 0.05 + distanceFromCenter * 0.1) * 0.02);

  gl_Position = projectionMatrix * modelViewMatrix * vec4(pos, 1.0);
}
`;

// node_modules/@shadergradient/react/dist/chunk-CLJIQSIC.mjs
var p5 = {};
A(p5, { plane: () => o5, sphere: () => a3, waterPlane: () => a4 });

// node_modules/@shadergradient/react/dist/chunk-RHJWCM3L.mjs
var t5 = {};
A(t5, { cosmic: () => p5, defaults: () => p2, glass: () => p3, positionMix: () => p4 });

// node_modules/@shadergradient/react/dist/chunk-HKGRTUU2.mjs
var r3 = { performance: false, render: true };
var o6 = { enable: (e19) => {
  r3[e19] = true;
}, disable: (e19) => {
  r3[e19] = false;
}, enableAll: () => {
  Object.keys(r3).forEach((e19) => {
    r3[e19] = true;
  });
}, disableAll: () => {
  Object.keys(r3).forEach((e19) => {
    r3[e19] = false;
  });
}, performance: (...e19) => {
  r3.performance && console.log("[Performance]", ...e19);
}, render: (...e19) => {
  r3.render && console.log("[Render]", ...e19);
} };
typeof window != "undefined" && (window.debug = o6);

// node_modules/@shadergradient/react/dist/chunk-H5PDSFZE.mjs
function R(e19) {
  return e19 / 180 * Math.PI;
}
function y2(e19) {
  return e19.map((r10) => R(r10));
}
function I(e19) {
  return e19.replace("http://localhost:3001/customize", "").replace("https://shadergradient.co/customize", "").replace("https://www.shadergradient.co/customize", "");
}
function M(T4) {
  var o11 = T4, { position: e19, rotation: r10, cameraAngle: i9, noise: p10, canvas: a11 } = o11, s10 = y(o11, ["position", "rotation", "cameraAngle", "noise", "canvas"]);
  let { positionX: u5, positionY: f5, positionZ: d5 } = e19 || {}, { rotationX: m4, rotationY: l7, rotationZ: h7 } = r10 || {}, { cAzimuthAngle: P5, cPolarAngle: g6 } = i9 || {}, { uDensity: w4, uStrength: x5 } = p10 || {}, { pixelDensity: z3, fov: A4, preserveDrawingBuffer: D3, powerPreference: t15 } = a11 || {};
  return w({ positionX: u5, positionY: f5, positionZ: d5, rotationX: m4, rotationY: l7, rotationZ: h7, cAzimuthAngle: P5, cPolarAngle: g6, uDensity: w4, uStrength: x5, pixelDensity: z3, fov: A4, preserveDrawingBuffer: D3, powerPreference: t15 && t15 !== "default" ? t15 : void 0 }, s10);
}

// node_modules/@shadergradient/react/dist/chunk-6MZB7NYQ.mjs
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
function J({ animate: d5, range: h7, rangeStart: y8, rangeEnd: v9, loop: t15, loopDuration: n17, positionX: T4, positionY: F4, positionZ: I5, rotationX: M5, rotationY: S3, rotationZ: b8, type: i9, color1: a11, color2: s10, color3: m4, reflection: C2, uTime: D3, uSpeed: L, uDensity: R4, uStrength: w4, uFrequency: x5, uAmplitude: A4, shader: o11 }) {
  let { vertex: q4, fragment: E4 } = t5[o11][i9], N5 = { colors: [a11, s10, m4], uTime: D3, uSpeed: L, uLoadingTime: 1, uNoiseDensity: R4, uNoiseStrength: w4, uFrequency: x5, uAmplitude: A4, uIntensity: 0.5, uLoop: t15 === "on" ? 1 : 0, uLoopDuration: n17 || 5 }, U4 = o11 === "glass" ? { uColor1: s(a11), uColor2: s(s10), uColor3: s(m4), uTransparency: 0.1, uRefraction: 1.5, uChromaticAberration: 0.1, uFresnelPower: 2, uReflectivity: 0.9, uWaveAmplitude: 0.02, uWaveFrequency: 5, uDistortion: 0.1, uFlowSpeed: 0.1, uFlowDirection: { x: 1, y: 0.5 }, uLiquidEffect: 0.5, uFoamIntensity: 0.3, envMapIntensity: 1 } : {}, W = w(w({}, N5), U4);
  return (0, import_jsx_runtime3.jsxs)("mesh", { name: "shadergradient-mesh", position: [T4, F4, I5], rotation: y2([M5, S3, b8]), children: [(0, import_jsx_runtime3.jsx)(n, { type: i9 }), (0, import_jsx_runtime3.jsx)(A2, { animate: d5, range: h7, rangeStart: y8, rangeEnd: v9, loop: t15, loopDuration: n17, reflection: C2, shader: o11, uniforms: W, vertexShader: q4, fragmentShader: E4, onInit: (G3) => {
    o6.performance("material (onInit)", G3);
  } })] });
}

// node_modules/@shadergradient/react/dist/chunk-YRKK26G7.mjs
var r4 = class {
  constructor() {
    this.enabled = true, this.needsSwap = true, this.clear = false, this.renderToScreen = false;
  }
  setSize() {
  }
  render() {
    console.error("THREE.Pass: .render() must be implemented in derived pass.");
  }
};
var h3 = new OrthographicCamera(-1, 1, 1, -1, 0, 1);
var t6 = new BufferGeometry();
t6.setAttribute("position", new Float32BufferAttribute([-1, 3, 0, -1, -1, 0, 3, -1, 0], 3));
t6.setAttribute("uv", new Float32BufferAttribute([0, 2, 0, 0, 2, 0], 2));
var s3 = class {
  constructor(e19) {
    this._mesh = new Mesh(t6, e19);
  }
  dispose() {
    this._mesh.geometry.dispose();
  }
  render(e19) {
    e19.render(this._mesh, h3);
  }
  get material() {
    return this._mesh.material;
  }
  set material(e19) {
    this._mesh.material = e19;
  }
};

// node_modules/@shadergradient/react/dist/chunk-QYDWPRFC.mjs
var s4 = class extends r4 {
  constructor(l7, i9, t15, a11, e19) {
    super(), this.scene = l7, this.camera = i9, this.overrideMaterial = t15, this.clearColor = a11, this.clearAlpha = e19 !== void 0 ? e19 : 0, this.clear = true, this.clearDepth = false, this.needsSwap = false, this._oldClearColor = new Color();
  }
  render(l7, i9, t15) {
    let a11 = l7.autoClear;
    l7.autoClear = false;
    let e19, o11;
    this.overrideMaterial !== void 0 && (o11 = this.scene.overrideMaterial, this.scene.overrideMaterial = this.overrideMaterial), this.clearColor && (l7.getClearColor(this._oldClearColor), e19 = l7.getClearAlpha(), l7.setClearColor(this.clearColor, this.clearAlpha)), this.clearDepth && l7.clearDepth(), l7.setRenderTarget(this.renderToScreen ? null : t15), this.clear && l7.clear(l7.autoClearColor, l7.autoClearDepth, l7.autoClearStencil), l7.render(this.scene, this.camera), this.clearColor && l7.setClearColor(this._oldClearColor, e19), this.overrideMaterial !== void 0 && (this.scene.overrideMaterial = o11), l7.autoClear = a11;
  }
};

// node_modules/@shadergradient/react/dist/chunk-66B4V7BU.mjs
var f = class extends r4 {
  constructor(t15, i9) {
    super(), this.textureID = i9 !== void 0 ? i9 : "tDiffuse", t15 instanceof ShaderMaterial ? (this.uniforms = t15.uniforms, this.material = t15) : t15 && (this.uniforms = UniformsUtils.clone(t15.uniforms), this.material = new ShaderMaterial({ defines: Object.assign({}, t15.defines), uniforms: this.uniforms, vertexShader: t15.vertexShader, fragmentShader: t15.fragmentShader })), this.fsQuad = new s3(this.material);
  }
  render(t15, i9, n17) {
    this.uniforms[this.textureID] && (this.uniforms[this.textureID].value = n17.texture), this.fsQuad.material = this.material, this.renderToScreen ? (t15.setRenderTarget(null), this.fsQuad.render(t15)) : (t15.setRenderTarget(i9), this.clear && t15.clear(t15.autoClearColor, t15.autoClearDepth, t15.autoClearStencil), this.fsQuad.render(t15));
  }
};

// node_modules/@shadergradient/react/dist/chunk-LHZKBITZ.mjs
var e15 = { uniforms: { tDiffuse: { value: null }, opacity: { value: 1 } }, vertexShader: `

		varying vec2 vUv;

		void main() {

			vUv = uv;
			gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );

		}`, fragmentShader: `

		uniform float opacity;

		uniform sampler2D tDiffuse;

		varying vec2 vUv;

		void main() {

			vec4 texel = texture2D( tDiffuse, vUv );
			gl_FragColor = opacity * texel;

		}` };

// node_modules/@shadergradient/react/dist/chunk-AYXY7L4E.mjs
var a5 = class extends r4 {
  constructor(s10, f5) {
    super(), this.scene = s10, this.camera = f5, this.clear = true, this.needsSwap = false, this.inverse = false;
  }
  render(s10, f5, n17) {
    let t15 = s10.getContext(), e19 = s10.state;
    e19.buffers.color.setMask(false), e19.buffers.depth.setMask(false), e19.buffers.color.setLocked(true), e19.buffers.depth.setLocked(true);
    let c3, l7;
    this.inverse ? (c3 = 0, l7 = 1) : (c3 = 1, l7 = 0), e19.buffers.stencil.setTest(true), e19.buffers.stencil.setOp(t15.REPLACE, t15.REPLACE, t15.REPLACE), e19.buffers.stencil.setFunc(t15.ALWAYS, c3, 4294967295), e19.buffers.stencil.setClear(l7), e19.buffers.stencil.setLocked(true), s10.setRenderTarget(n17), this.clear && s10.clear(), s10.render(this.scene, this.camera), s10.setRenderTarget(f5), this.clear && s10.clear(), s10.render(this.scene, this.camera), e19.buffers.color.setLocked(false), e19.buffers.depth.setLocked(false), e19.buffers.stencil.setLocked(false), e19.buffers.stencil.setFunc(t15.EQUAL, 1, 4294967295), e19.buffers.stencil.setOp(t15.KEEP, t15.KEEP, t15.KEEP), e19.buffers.stencil.setLocked(true);
  }
};
var i5 = class extends r4 {
  constructor() {
    super(), this.needsSwap = false;
  }
  render(s10) {
    s10.state.buffers.stencil.setLocked(false), s10.state.buffers.stencil.setTest(false);
  }
};

// node_modules/@shadergradient/react/dist/chunk-WASVT75G.mjs
var g2 = class {
  constructor(e19, t15) {
    if (this.renderer = e19, t15 === void 0) {
      let i9 = { minFilter: LinearFilter, magFilter: LinearFilter, format: RGBAFormat }, s10 = e19.getSize(new Vector2());
      this._pixelRatio = e19.getPixelRatio(), this._width = s10.width, this._height = s10.height, t15 = new WebGLRenderTarget(this._width * this._pixelRatio, this._height * this._pixelRatio, i9), t15.texture.name = "EffectComposer.rt1";
    } else this._pixelRatio = 1, this._width = t15.width, this._height = t15.height;
    this.renderTarget1 = t15, this.renderTarget2 = t15.clone(), this.renderTarget2.texture.name = "EffectComposer.rt2", this.writeBuffer = this.renderTarget1, this.readBuffer = this.renderTarget2, this.renderToScreen = true, this.passes = [], e15 === void 0 && console.error("THREE.EffectComposer relies on CopyShader"), f === void 0 && console.error("THREE.EffectComposer relies on ShaderPass"), this.copyPass = new f(e15), this.clock = new Clock();
  }
  swapBuffers() {
    let e19 = this.readBuffer;
    this.readBuffer = this.writeBuffer, this.writeBuffer = e19;
  }
  addPass(e19) {
    this.passes.push(e19), e19.setSize(this._width * this._pixelRatio, this._height * this._pixelRatio);
  }
  insertPass(e19, t15) {
    this.passes.splice(t15, 0, e19), e19.setSize(this._width * this._pixelRatio, this._height * this._pixelRatio);
  }
  removePass(e19) {
    let t15 = this.passes.indexOf(e19);
    t15 !== -1 && this.passes.splice(t15, 1);
  }
  isLastEnabledPass(e19) {
    for (let t15 = e19 + 1; t15 < this.passes.length; t15++) if (this.passes[t15].enabled) return false;
    return true;
  }
  render(e19) {
    e19 === void 0 && (e19 = this.clock.getDelta());
    let t15 = this.renderer.getRenderTarget(), i9 = false;
    for (let s10 = 0, h7 = this.passes.length; s10 < h7; s10++) {
      let r10 = this.passes[s10];
      if (r10.enabled !== false) {
        if (r10.renderToScreen = this.renderToScreen && this.isLastEnabledPass(s10), r10.render(this.renderer, this.writeBuffer, this.readBuffer, e19, i9), r10.needsSwap) {
          if (i9) {
            let l7 = this.renderer.getContext(), c3 = this.renderer.state.buffers.stencil;
            c3.setFunc(l7.NOTEQUAL, 1, 4294967295), this.copyPass.render(this.renderer, this.writeBuffer, this.readBuffer, e19), c3.setFunc(l7.EQUAL, 1, 4294967295);
          }
          this.swapBuffers();
        }
        a5 !== void 0 && (r10 instanceof a5 ? i9 = true : r10 instanceof i5 && (i9 = false));
      }
    }
    this.renderer.setRenderTarget(t15);
  }
  reset(e19) {
    if (e19 === void 0) {
      let t15 = this.renderer.getSize(new Vector2());
      this._pixelRatio = this.renderer.getPixelRatio(), this._width = t15.width, this._height = t15.height, e19 = this.renderTarget1.clone(), e19.setSize(this._width * this._pixelRatio, this._height * this._pixelRatio);
    }
    this.renderTarget1.dispose(), this.renderTarget2.dispose(), this.renderTarget1 = e19, this.renderTarget2 = e19.clone(), this.writeBuffer = this.renderTarget1, this.readBuffer = this.renderTarget2;
  }
  setSize(e19, t15) {
    this._width = e19, this._height = t15;
    let i9 = this._width * this._pixelRatio, s10 = this._height * this._pixelRatio;
    this.renderTarget1.setSize(i9, s10), this.renderTarget2.setSize(i9, s10);
    for (let h7 = 0; h7 < this.passes.length; h7++) this.passes[h7].setSize(i9, s10);
  }
  setPixelRatio(e19) {
    this._pixelRatio = e19, this.setSize(this._width, this._height);
  }
};
var T = new OrthographicCamera(-1, 1, 1, -1, 0, 1);
var f2 = new BufferGeometry();
f2.setAttribute("position", new Float32BufferAttribute([-1, 3, 0, -1, -1, 0, 3, -1, 0], 3));
f2.setAttribute("uv", new Float32BufferAttribute([0, 2, 0, 0, 2, 0], 2));

// node_modules/@shadergradient/react/dist/chunk-VJZMGGI7.mjs
var e16 = { uniforms: { tDiffuse: { value: null }, shape: { value: 1 }, radius: { value: 2 }, rotateR: { value: Math.PI / 12 * 1 }, rotateG: { value: Math.PI / 12 * 2 }, rotateB: { value: Math.PI / 12 * 3 }, scatter: { value: 1 }, width: { value: 20 }, height: { value: 20 }, blending: { value: 1 }, blendingMode: { value: 1 }, greyscale: { value: false }, disable: { value: false } }, vertexShader: `

		varying vec2 vUV;
		varying vec3 vPosition;

		void main() {

			vUV = uv;
			vPosition = position;

			gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);

		}`, fragmentShader: `

		#define SQRT2_MINUS_ONE 0.41421356
		#define SQRT2_HALF_MINUS_ONE 0.20710678
		#define PI2 6.28318531
		#define SHAPE_DOT 1
		#define SHAPE_ELLIPSE 2
		#define SHAPE_LINE 3
		#define SHAPE_SQUARE 4
		#define BLENDING_LINEAR 1
		#define BLENDING_MULTIPLY 2
		#define BLENDING_ADD 3
		#define BLENDING_LIGHTER 4
		#define BLENDING_DARKER 5
		uniform sampler2D tDiffuse;
		uniform float radius;
		uniform float rotateR;
		uniform float rotateG;
		uniform float rotateB;
		uniform float scatter;
		uniform float width;
		uniform float height;
		uniform int shape;
		uniform bool disable;
		uniform float blending;
		uniform int blendingMode;
		varying vec2 vUV;
		varying vec3 vPosition;
		uniform bool greyscale;
		const int samples = 8;

		float blend( float a, float b, float t ) {

		// linear blend
			return a * ( 1.0 - t ) + b * t;

		}

		float hypot( float x, float y ) {

		// vector magnitude
			return sqrt( x * x + y * y );

		}

		float rand( vec2 seed ){

		// get pseudo-random number
			return fract( sin( dot( seed.xy, vec2( 12.9898, 78.233 ) ) ) * 43758.5453 );

		}

		float distanceToDotRadius( float channel, vec2 coord, vec2 normal, vec2 p, float angle, float rad_max ) {

		// apply shape-specific transforms
			float dist = hypot( coord.x - p.x, coord.y - p.y );
			float rad = channel;

			if ( shape == SHAPE_DOT ) {

				rad = pow( abs( rad ), 1.125 ) * rad_max;

			} else if ( shape == SHAPE_ELLIPSE ) {

				rad = pow( abs( rad ), 1.125 ) * rad_max;

				if ( dist != 0.0 ) {
					float dot_p = abs( ( p.x - coord.x ) / dist * normal.x + ( p.y - coord.y ) / dist * normal.y );
					dist = ( dist * ( 1.0 - SQRT2_HALF_MINUS_ONE ) ) + dot_p * dist * SQRT2_MINUS_ONE;
				}

			} else if ( shape == SHAPE_LINE ) {

				rad = pow( abs( rad ), 1.5) * rad_max;
				float dot_p = ( p.x - coord.x ) * normal.x + ( p.y - coord.y ) * normal.y;
				dist = hypot( normal.x * dot_p, normal.y * dot_p );

			} else if ( shape == SHAPE_SQUARE ) {

				float theta = atan( p.y - coord.y, p.x - coord.x ) - angle;
				float sin_t = abs( sin( theta ) );
				float cos_t = abs( cos( theta ) );
				rad = pow( abs( rad ), 1.4 );
				rad = rad_max * ( rad + ( ( sin_t > cos_t ) ? rad - sin_t * rad : rad - cos_t * rad ) );

			}

			return rad - dist;

		}

		struct Cell {

		// grid sample positions
			vec2 normal;
			vec2 p1;
			vec2 p2;
			vec2 p3;
			vec2 p4;
			float samp2;
			float samp1;
			float samp3;
			float samp4;

		};

		vec4 getSample( vec2 point ) {

		// multi-sampled point
			vec4 tex = texture2D( tDiffuse, vec2( point.x / width, point.y / height ) );
			float base = rand( vec2( floor( point.x ), floor( point.y ) ) ) * PI2;
			float step = PI2 / float( samples );
			// float dist = radius * 0.66;
			float dist = radius * 0.0;

			for ( int i = 0; i < samples; ++i ) {

				float r = base + step * float( i );
				vec2 coord = point + vec2( cos( r ) * dist, sin( r ) * dist );
				tex += texture2D( tDiffuse, vec2( coord.x / width, coord.y / height ) );

			}

			tex /= float( samples ) + 1.0;
			return tex;

		}

		float getDotColour( Cell c, vec2 p, int channel, float angle, float aa ) {

		// get colour for given point
			float dist_c_1, dist_c_2, dist_c_3, dist_c_4, res;

			if ( channel == 0 ) {

				c.samp1 = getSample( c.p1 ).r;
				c.samp2 = getSample( c.p2 ).r;
				c.samp3 = getSample( c.p3 ).r;
				c.samp4 = getSample( c.p4 ).r;

			} else if (channel == 1) {

				c.samp1 = getSample( c.p1 ).g;
				c.samp2 = getSample( c.p2 ).g;
				c.samp3 = getSample( c.p3 ).g;
				c.samp4 = getSample( c.p4 ).g;

			} else {

				c.samp1 = getSample( c.p1 ).b;
				c.samp3 = getSample( c.p3 ).b;
				c.samp2 = getSample( c.p2 ).b;
				c.samp4 = getSample( c.p4 ).b;

			}

			dist_c_1 = distanceToDotRadius( c.samp1, c.p1, c.normal, p, angle, radius );
			dist_c_2 = distanceToDotRadius( c.samp2, c.p2, c.normal, p, angle, radius );
			dist_c_3 = distanceToDotRadius( c.samp3, c.p3, c.normal, p, angle, radius );
			dist_c_4 = distanceToDotRadius( c.samp4, c.p4, c.normal, p, angle, radius );
			res = ( dist_c_1 > 0.0 ) ? clamp( dist_c_1 / aa, 0.0, 1.0 ) : 0.0;
			// res = 0.0;
			res += ( dist_c_2 > 0.0 ) ? clamp( dist_c_2 / aa, 0.0, 1.0 ) : 0.0;
			res += ( dist_c_3 > 0.0 ) ? clamp( dist_c_3 / aa, 0.0, 1.0 ) : 0.0;
			res += ( dist_c_4 > 0.0 ) ? clamp( dist_c_4 / aa, 0.0, 1.0 ) : 0.0;
			res = clamp( res, 0.0, 1.0 );

			return res;
			// return 2

		}

		Cell getReferenceCell( vec2 p, vec2 origin, float grid_angle, float step ) {

		// get containing cell
			Cell c;

		// calc grid
			vec2 n = vec2( cos( grid_angle ), sin( grid_angle ) );
			float threshold = step * 0.5;
			float dot_normal = n.x * ( p.x - origin.x ) + n.y * ( p.y - origin.y );
			float dot_line = -n.y * ( p.x - origin.x ) + n.x * ( p.y - origin.y );
			vec2 offset = vec2( n.x * dot_normal, n.y * dot_normal );
			float offset_normal = mod( hypot( offset.x, offset.y ), step );
			float normal_dir = ( dot_normal < 0.0 ) ? 1.0 : -1.0;
			float normal_scale = ( ( offset_normal < threshold ) ? -offset_normal : step - offset_normal ) * normal_dir;
			float offset_line = mod( hypot( ( p.x - offset.x ) - origin.x, ( p.y - offset.y ) - origin.y ), step );
			float line_dir = ( dot_line < 0.0 ) ? 1.0 : -1.0;
			float line_scale = ( ( offset_line < threshold ) ? -offset_line : step - offset_line ) * line_dir;

		// get closest corner
			c.normal = n;
			c.p1.x = p.x - n.x * normal_scale + n.y * line_scale;
			c.p1.y = p.y - n.y * normal_scale - n.x * line_scale;

		// scatter
			if ( scatter != 0.0 ) {

				float off_mag = scatter * threshold * 0.5;
				float off_angle = rand( vec2( floor( c.p1.x ), floor( c.p1.y ) ) ) * PI2;
				c.p1.x += cos( off_angle ) * off_mag;
				c.p1.y += sin( off_angle ) * off_mag;

			}

		// find corners
			float normal_step = normal_dir * ( ( offset_normal < threshold ) ? step : -step );
			float line_step = line_dir * ( ( offset_line < threshold ) ? step : -step );
			c.p2.x = c.p1.x - n.x * normal_step;
			c.p2.y = c.p1.y - n.y * normal_step;
			c.p3.x = c.p1.x + n.y * line_step;
			c.p3.y = c.p1.y - n.x * line_step;
			c.p4.x = c.p1.x - n.x * normal_step + n.y * line_step;
			c.p4.y = c.p1.y - n.y * normal_step - n.x * line_step;

			return c;

		}

		float blendColour( float a, float b, float t ) {

		// blend colours
			if ( blendingMode == BLENDING_LINEAR ) {
				return blend( a, b, 1.0 - t );
			} else if ( blendingMode == BLENDING_ADD ) {
				return blend( a, min( 1.0, a + b ), t );
			} else if ( blendingMode == BLENDING_MULTIPLY ) {
				return blend( a, max( 0.0, a * b ), t );
			} else if ( blendingMode == BLENDING_LIGHTER ) {
				return blend( a, max( a, b ), t );
			} else if ( blendingMode == BLENDING_DARKER ) {
				return blend( a, min( a, b ), t );
			} else {
				return blend( a, b, 1.0 - t );
			}

		}

		void main() {

			if ( ! disable ) {

		// setup
				vec2 p = vec2( vUV.x * width, vUV.y * height ) - vec2(vPosition.x, vPosition.y) * 3.0; // - position values to remove black borders.
				vec2 origin = vec2( 0, 0 );
				float aa = ( radius < 2.5 ) ? radius * 0.5 : 1.25;
				// float aa = 0.0;

		// get channel samples
				Cell cell_r = getReferenceCell( p, origin, rotateR, radius );
				Cell cell_g = getReferenceCell( p, origin, rotateG, radius );
				Cell cell_b = getReferenceCell( p, origin, rotateB, radius );
				float r = getDotColour( cell_r, p, 0, rotateR, aa );
				float g = getDotColour( cell_g, p, 1, rotateG, aa );
				float b = getDotColour( cell_b, p, 2, rotateB, aa );

		// blend with original
				vec4 colour = texture2D( tDiffuse, vUV );
				
				// add masking before blendColour
				if (colour.r == 0.0) {
					r = 0.0;
				} else {
					r = blendColour( r, colour.r, blending );
				}

				if (colour.g == 0.0) {
					g = 0.0;
				} else {
					g = blendColour( g, colour.g, blending );
				}

				if (colour.b == 0.0) {
					b = 0.0;
				} else {
					b = blendColour( b, colour.b, blending );
				}
				
				
				

				if ( greyscale ) {
					r = g = b = (r + b + g) / 3.0;
				}

				// add alpha channel to each r, g, b colors
				vec4 vR;
				vec4 vG;
				vec4 vB;
	
				// apply transparent to outside of mesh
				if (r == 0.0 && colour.r == 0.0) {
					vR = vec4( 0, 0, 0, 0 );
				} else {
					vR = vec4( r, 0, 0, 1 );
				}
	
				if (g == 0.0 && colour.g == 0.0) {
					vG = vec4( 0, 0, 0, 0 );
				} else {
					vG = vec4( 0, g, 0, 1 );
				}
	
				if (b == 0.0 && colour.b == 0.0) {
					vB = vec4( 0, 0, 0, 0 );
				} else {
					vB = vec4( 0, 0, b, 1 );
				}

				// gl_FragColor = vec4( r, g, b, 1.0 );
				gl_FragColor = vR + vG + vB;

			} else {

				gl_FragColor = texture2D( tDiffuse, vUV );

			}

		}` };

// node_modules/@shadergradient/react/dist/chunk-RE2GTNAW.mjs
var r5 = class {
  constructor() {
    this.enabled = true, this.needsSwap = true, this.clear = false, this.renderToScreen = false;
  }
  setSize() {
  }
  render() {
    console.error("THREE.Pass: .render() must be implemented in derived pass.");
  }
};
var h4 = new OrthographicCamera(-1, 1, 1, -1, 0, 1);
var t7 = new BufferGeometry();
t7.setAttribute("position", new Float32BufferAttribute([-1, 3, 0, -1, -1, 0, 3, -1, 0], 3));
t7.setAttribute("uv", new Float32BufferAttribute([0, 2, 0, 0, 2, 0], 2));
var s5 = class {
  constructor(e19) {
    this._mesh = new Mesh(t7, e19);
  }
  dispose() {
    this._mesh.geometry.dispose();
  }
  render(e19) {
    e19.render(this._mesh, h4);
  }
  get material() {
    return this._mesh.material;
  }
  set material(e19) {
    this._mesh.material = e19;
  }
};

// node_modules/@shadergradient/react/dist/chunk-E7V5LYA3.mjs
var E = { SKIP: 0, ADD: 1, ALPHA: 2, AVERAGE: 3, COLOR_BURN: 4, COLOR_DODGE: 5, DARKEN: 6, DIFFERENCE: 7, EXCLUSION: 8, LIGHTEN: 9, MULTIPLY: 10, DIVIDE: 11, NEGATION: 12, NORMAL: 13, OVERLAY: 14, REFLECT: 15, SCREEN: 16, SOFT_LIGHT: 17, SUBTRACT: 18 };

// node_modules/@shadergradient/react/dist/chunk-TJDK6R5K.mjs
var o7 = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {

	return min(x + y, 1.0) * opacity + x * (1.0 - opacity);

}
`;
var e17 = `vec3 blend(const in vec3 x, const in vec3 y, const in float opacity) {

	return y * opacity + x * (1.0 - opacity);

}

vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {

	float a = min(y.a, opacity);

	return vec4(blend(x.rgb, y.rgb, a), max(x.a, a));

}
`;
var c = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {

	return (x + y) * 0.5 * opacity + x * (1.0 - opacity);

}
`;
var i6 = `float blend(const in float x, const in float y) {

	return (y == 0.0) ? y : max(1.0 - (1.0 - x) / y, 0.0);

}

vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {

	vec4 z = vec4(
		blend(x.r, y.r),
		blend(x.g, y.g),
		blend(x.b, y.b),
		blend(x.a, y.a)
	);

	return z * opacity + x * (1.0 - opacity);

}
`;
var a6 = `float blend(const in float x, const in float y) {

	return (y == 1.0) ? y : min(x / (1.0 - y), 1.0);

}

vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {

	vec4 z = vec4(
		blend(x.r, y.r),
		blend(x.g, y.g),
		blend(x.b, y.b),
		blend(x.a, y.a)
	);

	return z * opacity + x * (1.0 - opacity);

}
`;
var r6 = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {

	return min(x, y) * opacity + x * (1.0 - opacity);

}
`;
var l3 = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {

	return abs(x - y) * opacity + x * (1.0 - opacity);

}
`;
var y3 = `float blend(const in float x, const in float y) {

	return (y > 0.0) ? min(x / y, 1.0) : 1.0;

}

vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {

	vec4 z = vec4(
		blend(x.r, y.r),
		blend(x.g, y.g),
		blend(x.b, y.b),
		blend(x.a, y.a)
	);

	return z * opacity + x * (1.0 - opacity);

}
`;
var x2 = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {

	return (x + y - 2.0 * x * y) * opacity + x * (1.0 - opacity);

}
`;
var d = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {

	return max(x, y) * opacity + x * (1.0 - opacity);

}
`;
var p6 = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {

	return x * y * opacity + x * (1.0 - opacity);

}
`;
var s6 = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {

	return (1.0 - abs(1.0 - x - y)) * opacity + x * (1.0 - opacity);

}
`;
var f3 = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {

	return y * opacity + x * (1.0 - opacity);

}
`;
var u2 = `float blend(const in float x, const in float y) {

	return (x < 0.5) ? (2.0 * x * y) : (1.0 - 2.0 * (1.0 - x) * (1.0 - y));

}

vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {

	vec4 z = vec4(
		blend(x.r, y.r),
		blend(x.g, y.g),
		blend(x.b, y.b),
		blend(x.a, y.a)
	);

	return z * opacity + x * (1.0 - opacity);

}
`;
var v2 = `float blend(const in float x, const in float y) {

	return (y == 1.0) ? y : min(x * x / (1.0 - y), 1.0);

}

vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {

	vec4 z = vec4(
		blend(x.r, y.r),
		blend(x.g, y.g),
		blend(x.b, y.b),
		blend(x.a, y.a)
	);

	return z * opacity + x * (1.0 - opacity);

}
`;
var b = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {

	return (1.0 - (1.0 - x) * (1.0 - y)) * opacity + x * (1.0 - opacity);

}
`;
var m2 = `float blend(const in float x, const in float y) {

	return (y < 0.5) ?
		(2.0 * x * y + x * x * (1.0 - 2.0 * y)) :
		(sqrt(x) * (2.0 * y - 1.0) + 2.0 * x * (1.0 - y));

}

vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {

	vec4 z = vec4(
		blend(x.r, y.r),
		blend(x.g, y.g),
		blend(x.b, y.b),
		blend(x.a, y.a)
	);

	return z * opacity + x * (1.0 - opacity);

}
`;
var F = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {

	return max(x + y - 1.0, 0.0) * opacity + x * (1.0 - opacity);

}
`;
var z2 = /* @__PURE__ */ new Map([[E.SKIP, null], [E.ADD, o7], [E.ALPHA, e17], [E.AVERAGE, c], [E.COLOR_BURN, i6], [E.COLOR_DODGE, a6], [E.DARKEN, r6], [E.DIFFERENCE, l3], [E.EXCLUSION, x2], [E.LIGHTEN, d], [E.MULTIPLY, p6], [E.DIVIDE, y3], [E.NEGATION, s6], [E.NORMAL, f3], [E.OVERLAY, u2], [E.REFLECT, v2], [E.SCREEN, b], [E.SOFT_LIGHT, m2], [E.SUBTRACT, F]]);
var g3 = class extends EventDispatcher {
  constructor(t15, B5 = 1) {
    super(), this.blendFunction = t15, this.opacity = new Uniform(B5);
  }
  getBlendFunction() {
    return this.blendFunction;
  }
  setBlendFunction(t15) {
    this.blendFunction = t15, this.dispatchEvent({ type: "change" });
  }
  getShaderCode() {
    return z2.get(this.blendFunction);
  }
};

// node_modules/@shadergradient/react/dist/chunk-DG5TTWXJ.mjs
var h5 = true;
var d2 = class extends r5 {
  constructor(e19, r10, i9) {
    super(), e16 === void 0 && console.error("THREE.HalftonePass requires HalftoneShader"), this.uniforms = UniformsUtils.clone(e16.uniforms), this.material = new ShaderMaterial({ uniforms: this.uniforms, fragmentShader: e16.fragmentShader, vertexShader: e16.vertexShader }), h5 && (this.uniforms.width.value = e19, this.uniforms.height.value = r10), this.uniforms.disable.value = i9.disable, this.fsQuad = new s5(this.material), this.blendMode = new g3(E.SCREEN), this.extensions = null;
  }
  render(e19, r10, i9) {
    this.material.uniforms.tDiffuse.value = i9.texture, this.renderToScreen ? (e19.setRenderTarget(null), this.fsQuad.render(e19)) : (e19.setRenderTarget(r10), this.clear && e19.clear(), this.fsQuad.render(e19));
  }
  setSize(e19, r10) {
    h5 && (this.uniforms.width.value = e19, this.uniforms.height.value = r10);
  }
  initialize(e19, r10, i9) {
  }
  addEventListener() {
  }
  getAttributes() {
    return this.attributes;
  }
  getFragmentShader() {
    return e16.fragmentShader;
  }
  getVertexShader() {
    return e16.vertexShader;
  }
  update(e19, r10, i9) {
  }
};

// node_modules/@shadergradient/react/dist/chunk-SOFAB2VP.mjs
var import_react2 = __toESM(require_react(), 1);
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
function k3({ disable: u5 = false }) {
  let { gl: c3, scene: m4, camera: d5, size: t15 } = useThree(), n17 = (0, import_react2.useRef)(null), o11 = (0, import_react2.useRef)(null), p10 = (0, import_react2.useMemo)(() => ({ shape: 1, radius: 2, rotateR: Math.PI / 12, rotateB: Math.PI / 12 * 2, rotateG: Math.PI / 12 * 3, scatter: 1, blending: 1, blendingMode: 1, greyscale: false }), []);
  return (0, import_react2.useEffect)(() => {
    let e19 = new g2(c3), r10 = new s4(m4, d5), s10 = new d2(t15.width, t15.height, x(w({}, p10), { disable: u5 }));
    return e19.addPass(r10), e19.addPass(s10), n17.current = e19, o11.current = s10, () => {
      var h7, P5, a11, g6, f5, w4, i9, C2;
      (h7 = r10.dispose) == null || h7.call(r10), (P5 = s10.fsQuad) != null && P5.dispose && s10.fsQuad.dispose(), (g6 = (a11 = s10.material) == null ? void 0 : a11.dispose) == null || g6.call(a11), (w4 = (f5 = e19.renderTarget1) == null ? void 0 : f5.dispose) == null || w4.call(f5), (C2 = (i9 = e19.renderTarget2) == null ? void 0 : i9.dispose) == null || C2.call(i9), n17.current = null, o11.current = null;
    };
  }, [d5, c3, p10, m4]), (0, import_react2.useEffect)(() => {
    var r10;
    let e19 = n17.current;
    e19 && (e19.setSize(t15.width, t15.height), (r10 = o11.current) == null || r10.setSize(t15.width, t15.height));
  }, [t15.height, t15.width]), (0, import_react2.useEffect)(() => {
    var e19, r10;
    (r10 = (e19 = o11.current) == null ? void 0 : e19.uniforms) != null && r10.disable && (o11.current.uniforms.disable.value = u5);
  }, [u5]), useFrame((e19, r10) => {
    let s10 = n17.current;
    s10 && (c3.autoClear = true, s10.render(r10));
  }, 1), (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, {});
}

// node_modules/@shadergradient/react/dist/chunk-CPUZJ7YV.mjs
var r7 = (e19, o11, t15) => ({ dpr: e19, camera: { fov: o11 }, linear: true, flat: true, gl: { preserveDrawingBuffer: t15 == null ? void 0 : t15.preserveDrawingBuffer, powerPreference: t15 == null ? void 0 : t15.powerPreference } });
var s7 = 1;
var a7 = 14;
var p7 = { zoom: 1, distance: 14 };
var c2 = { zoom: 5, distance: 14 };
var h6 = "https://ruucm.github.io/shadergradient/ui@0.0.0/assets/hdr/";

// node_modules/@shadergradient/react/dist/chunk-YIJC6OD2.mjs
var import_react3 = __toESM(require_react(), 1);
function v4({ type: u5, cAzimuthAngle: z3, cPolarAngle: y8, cDistance: h7, cameraZoom: R4, zoomOut: S3, enableTransition: f5 = true }) {
  let e19 = (0, import_react3.useRef)();
  return useFrame((m4, F4) => e19.current.update(F4)), (0, import_react3.useEffect)(() => {
    let m4 = e19.current;
    m4 == null || m4.rotateTo(R(z3), R(y8), f5);
  }, [e19, z3, y8, f5]), (0, import_react3.useEffect)(() => {
    let m4 = e19.current;
    S3 ? u5 === "sphere" ? (m4 == null || m4.dollyTo(c2.distance, f5), m4 == null || m4.zoomTo(c2.zoom, f5)) : (m4 == null || m4.dollyTo(p7.distance, f5), m4 == null || m4.zoomTo(p7.zoom, f5)) : u5 === "sphere" ? (m4 == null || m4.zoomTo(R4, f5), m4 == null || m4.dollyTo(a7, f5)) : (m4 == null || m4.dollyTo(h7, f5), m4 == null || m4.zoomTo(s7, f5));
  }, [e19, S3, u5, R4, h7, f5]), e19;
}

// node_modules/@shadergradient/react/dist/chunk-6DNZ3I5B.mjs
var import_react4 = __toESM(require_react(), 1);
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
var P2 = { LEFT: 1, RIGHT: 2, MIDDLE: 4 };
var i7 = Object.freeze({ NONE: 0, ROTATE: 1, TRUCK: 2, OFFSET: 4, DOLLY: 8, ZOOM: 16, TOUCH_ROTATE: 32, TOUCH_TRUCK: 64, TOUCH_OFFSET: 128, TOUCH_DOLLY: 256, TOUCH_ZOOM: 512, TOUCH_DOLLY_TRUCK: 1024, TOUCH_DOLLY_OFFSET: 2048, TOUCH_DOLLY_ROTATE: 4096, TOUCH_ZOOM_TRUCK: 8192, TOUCH_ZOOM_OFFSET: 16384, TOUCH_ZOOM_ROTATE: 32768 });
var N = { NONE: 0, IN: 1, OUT: -1 };
function k4(u5) {
  return u5.isPerspectiveCamera;
}
function H(u5) {
  return u5.isOrthographicCamera;
}
var Z = Math.PI * 2;
var pt = Math.PI / 2;
var Ct = 1e-5;
var K = Math.PI / 180;
function M2(u5, t15, e19) {
  return Math.max(t15, Math.min(e19, u5));
}
function D(u5, t15 = Ct) {
  return Math.abs(u5) < t15;
}
function x3(u5, t15, e19 = Ct) {
  return D(u5 - t15, e19);
}
function ut(u5, t15) {
  return Math.round(u5 / t15) * t15;
}
function X(u5) {
  return isFinite(u5) ? u5 : u5 < 0 ? -Number.MAX_VALUE : Number.MAX_VALUE;
}
function Q(u5) {
  return Math.abs(u5) < Number.MAX_VALUE ? u5 : u5 * (1 / 0);
}
function J2(u5, t15, e19, s10, n17 = 1 / 0, r10) {
  s10 = Math.max(1e-4, s10);
  let a11 = 2 / s10, l7 = a11 * r10, m4 = 1 / (1 + l7 + 0.48 * l7 * l7 + 0.235 * l7 * l7 * l7), h7 = u5 - t15, v9 = t15, g6 = n17 * s10;
  h7 = M2(h7, -g6, g6), t15 = u5 - h7;
  let C2 = (e19.value + a11 * h7) * r10;
  e19.value = (e19.value - a11 * C2) * m4;
  let f5 = t15 + (h7 + C2) * m4;
  return v9 - u5 > 0 == f5 > v9 && (f5 = v9, e19.value = (f5 - v9) / r10), f5;
}
function ft(u5, t15, e19, s10, n17 = 1 / 0, r10, a11) {
  s10 = Math.max(1e-4, s10);
  let l7 = 2 / s10, m4 = l7 * r10, h7 = 1 / (1 + m4 + 0.48 * m4 * m4 + 0.235 * m4 * m4 * m4), v9 = t15.x, g6 = t15.y, C2 = t15.z, f5 = u5.x - v9, L = u5.y - g6, T4 = u5.z - C2, A4 = v9, o11 = g6, c3 = C2, d5 = n17 * s10, _ = d5 * d5, E4 = f5 * f5 + L * L + T4 * T4;
  if (E4 > _) {
    let st = Math.sqrt(E4);
    f5 = f5 / st * d5, L = L / st * d5, T4 = T4 / st * d5;
  }
  v9 = u5.x - f5, g6 = u5.y - L, C2 = u5.z - T4;
  let O3 = (e19.x + l7 * f5) * r10, S3 = (e19.y + l7 * L) * r10, z3 = (e19.z + l7 * T4) * r10;
  e19.x = (e19.x - l7 * O3) * h7, e19.y = (e19.y - l7 * S3) * h7, e19.z = (e19.z - l7 * z3) * h7, a11.x = v9 + (f5 + O3) * h7, a11.y = g6 + (L + S3) * h7, a11.z = C2 + (T4 + z3) * h7;
  let j = A4 - u5.x, W = o11 - u5.y, vt = c3 - u5.z, xt = a11.x - A4, Dt = a11.y - o11, Lt = a11.z - c3;
  return j * xt + W * Dt + vt * Lt > 0 && (a11.x = A4, a11.y = o11, a11.z = c3, e19.x = (a11.x - A4) / r10, e19.y = (a11.y - o11) / r10, e19.z = (a11.z - c3) / r10), a11;
}
function ot(u5, t15) {
  t15.set(0, 0), u5.forEach((e19) => {
    t15.x += e19.clientX, t15.y += e19.clientY;
  }), t15.x /= u5.length, t15.y /= u5.length;
}
function rt(u5, t15) {
  return H(u5) ? (console.warn(`${t15} is not supported in OrthographicCamera`), true) : false;
}
var _t = class {
  constructor() {
    this._listeners = {};
  }
  addEventListener(t15, e19) {
    let s10 = this._listeners;
    s10[t15] === void 0 && (s10[t15] = []), s10[t15].indexOf(e19) === -1 && s10[t15].push(e19);
  }
  hasEventListener(t15, e19) {
    let s10 = this._listeners;
    return s10[t15] !== void 0 && s10[t15].indexOf(e19) !== -1;
  }
  removeEventListener(t15, e19) {
    let n17 = this._listeners[t15];
    if (n17 !== void 0) {
      let r10 = n17.indexOf(e19);
      r10 !== -1 && n17.splice(r10, 1);
    }
  }
  removeAllEventListeners(t15) {
    if (!t15) {
      this._listeners = {};
      return;
    }
    Array.isArray(this._listeners[t15]) && (this._listeners[t15].length = 0);
  }
  dispatchEvent(t15) {
    let s10 = this._listeners[t15.type];
    if (s10 !== void 0) {
      t15.target = this;
      let n17 = s10.slice(0);
      for (let r10 = 0, a11 = n17.length; r10 < a11; r10++) n17[r10].call(this, t15);
    }
  }
};
var nt;
var At = "2.9.0";
var $ = 1 / 8;
var St = /Mac/.test((nt = globalThis == null ? void 0 : globalThis.navigator) === null || nt === void 0 ? void 0 : nt.platform);
var p8;
var gt;
var tt;
var at;
var F2;
var y4;
var U2;
var V2;
var q2;
var I2;
var b3;
var Y;
var Ot;
var yt;
var R2;
var G;
var B2;
var Tt;
var ht;
var Et;
var lt;
var ct;
var et;
var w2 = class u3 extends _t {
  static install(t15) {
    p8 = t15.THREE, gt = Object.freeze(new p8.Vector3(0, 0, 0)), tt = Object.freeze(new p8.Vector3(0, 1, 0)), at = Object.freeze(new p8.Vector3(0, 0, 1)), F2 = new p8.Vector2(), y4 = new p8.Vector3(), U2 = new p8.Vector3(), V2 = new p8.Vector3(), q2 = new p8.Vector3(), I2 = new p8.Vector3(), b3 = new p8.Vector3(), Y = new p8.Vector3(), Ot = new p8.Vector3(), yt = new p8.Vector3(), R2 = new p8.Spherical(), G = new p8.Spherical(), B2 = new p8.Box3(), Tt = new p8.Box3(), ht = new p8.Sphere(), Et = new p8.Quaternion(), lt = new p8.Quaternion(), ct = new p8.Matrix4(), et = new p8.Raycaster();
  }
  static get ACTION() {
    return i7;
  }
  constructor(t15, e19) {
    super(), this.minPolarAngle = 0, this.maxPolarAngle = Math.PI, this.minAzimuthAngle = -1 / 0, this.maxAzimuthAngle = 1 / 0, this.minDistance = Number.EPSILON, this.maxDistance = 1 / 0, this.infinityDolly = false, this.minZoom = 0.01, this.maxZoom = 1 / 0, this.smoothTime = 0.25, this.draggingSmoothTime = 0.125, this.maxSpeed = 1 / 0, this.azimuthRotateSpeed = 1, this.polarRotateSpeed = 1, this.dollySpeed = 1, this.dollyDragInverted = false, this.truckSpeed = 2, this.dollyToCursor = false, this.dragToOffset = false, this.verticalDragToForward = false, this.boundaryFriction = 0, this.restThreshold = 0.01, this.colliderMeshes = [], this.cancel = () => {
    }, this._enabled = true, this._state = i7.NONE, this._viewport = null, this._changedDolly = 0, this._changedZoom = 0, this._hasRested = true, this._boundaryEnclosesCamera = false, this._needsUpdate = true, this._updatedLastTime = false, this._elementRect = new DOMRect(), this._isDragging = false, this._dragNeedsUpdate = true, this._activePointers = [], this._lockedPointer = null, this._interactiveArea = new DOMRect(0, 0, 1, 1), this._isUserControllingRotate = false, this._isUserControllingDolly = false, this._isUserControllingTruck = false, this._isUserControllingOffset = false, this._isUserControllingZoom = false, this._lastDollyDirection = N.NONE, this._thetaVelocity = { value: 0 }, this._phiVelocity = { value: 0 }, this._radiusVelocity = { value: 0 }, this._targetVelocity = new p8.Vector3(), this._focalOffsetVelocity = new p8.Vector3(), this._zoomVelocity = { value: 0 }, this._truckInternal = (o11, c3, d5) => {
      let _, E4;
      if (k4(this._camera)) {
        let O3 = y4.copy(this._camera.position).sub(this._target), S3 = this._camera.getEffectiveFOV() * K, z3 = O3.length() * Math.tan(S3 * 0.5);
        _ = this.truckSpeed * o11 * z3 / this._elementRect.height, E4 = this.truckSpeed * c3 * z3 / this._elementRect.height;
      } else if (H(this._camera)) {
        let O3 = this._camera;
        _ = o11 * (O3.right - O3.left) / O3.zoom / this._elementRect.width, E4 = c3 * (O3.top - O3.bottom) / O3.zoom / this._elementRect.height;
      } else return;
      this.verticalDragToForward ? (d5 ? this.setFocalOffset(this._focalOffsetEnd.x + _, this._focalOffsetEnd.y, this._focalOffsetEnd.z, true) : this.truck(_, 0, true), this.forward(-E4, true)) : d5 ? this.setFocalOffset(this._focalOffsetEnd.x + _, this._focalOffsetEnd.y + E4, this._focalOffsetEnd.z, true) : this.truck(_, E4, true);
    }, this._rotateInternal = (o11, c3) => {
      let d5 = Z * this.azimuthRotateSpeed * o11 / this._elementRect.height, _ = Z * this.polarRotateSpeed * c3 / this._elementRect.height;
      this.rotate(d5, _, true);
    }, this._dollyInternal = (o11, c3, d5) => {
      let _ = Math.pow(0.95, -o11 * this.dollySpeed), E4 = this._sphericalEnd.radius, O3 = this._sphericalEnd.radius * _, S3 = M2(O3, this.minDistance, this.maxDistance), z3 = S3 - O3;
      this.infinityDolly && this.dollyToCursor ? this._dollyToNoClamp(O3, true) : this.infinityDolly && !this.dollyToCursor ? (this.dollyInFixed(z3, true), this._dollyToNoClamp(S3, true)) : this._dollyToNoClamp(S3, true), this.dollyToCursor && (this._changedDolly += (this.infinityDolly ? O3 : S3) - E4, this._dollyControlCoord.set(c3, d5)), this._lastDollyDirection = Math.sign(-o11);
    }, this._zoomInternal = (o11, c3, d5) => {
      let _ = Math.pow(0.95, o11 * this.dollySpeed), E4 = this._zoom, O3 = this._zoom * _;
      this.zoomTo(O3, true), this.dollyToCursor && (this._changedZoom += O3 - E4, this._dollyControlCoord.set(c3, d5));
    }, typeof p8 == "undefined" && console.error("camera-controls: `THREE` is undefined. You must first run `CameraControls.install( { THREE: THREE } )`. Check the docs for further information."), this._camera = t15, this._yAxisUpSpace = new p8.Quaternion().setFromUnitVectors(this._camera.up, tt), this._yAxisUpSpaceInverse = this._yAxisUpSpace.clone().invert(), this._state = i7.NONE, this._target = new p8.Vector3(), this._targetEnd = this._target.clone(), this._focalOffset = new p8.Vector3(), this._focalOffsetEnd = this._focalOffset.clone(), this._spherical = new p8.Spherical().setFromVector3(y4.copy(this._camera.position).applyQuaternion(this._yAxisUpSpace)), this._sphericalEnd = this._spherical.clone(), this._lastDistance = this._spherical.radius, this._zoom = this._camera.zoom, this._zoomEnd = this._zoom, this._lastZoom = this._zoom, this._nearPlaneCorners = [new p8.Vector3(), new p8.Vector3(), new p8.Vector3(), new p8.Vector3()], this._updateNearPlaneCorners(), this._boundary = new p8.Box3(new p8.Vector3(-1 / 0, -1 / 0, -1 / 0), new p8.Vector3(1 / 0, 1 / 0, 1 / 0)), this._cameraUp0 = this._camera.up.clone(), this._target0 = this._target.clone(), this._position0 = this._camera.position.clone(), this._zoom0 = this._zoom, this._focalOffset0 = this._focalOffset.clone(), this._dollyControlCoord = new p8.Vector2(), this.mouseButtons = { left: i7.ROTATE, middle: i7.DOLLY, right: i7.TRUCK, wheel: k4(this._camera) ? i7.DOLLY : H(this._camera) ? i7.ZOOM : i7.NONE }, this.touches = { one: i7.TOUCH_ROTATE, two: k4(this._camera) ? i7.TOUCH_DOLLY_TRUCK : H(this._camera) ? i7.TOUCH_ZOOM_TRUCK : i7.NONE, three: i7.TOUCH_TRUCK };
    let s10 = new p8.Vector2(), n17 = new p8.Vector2(), r10 = new p8.Vector2(), a11 = (o11) => {
      if (!this._enabled || !this._domElement) return;
      if (this._interactiveArea.left !== 0 || this._interactiveArea.top !== 0 || this._interactiveArea.width !== 1 || this._interactiveArea.height !== 1) {
        let _ = this._domElement.getBoundingClientRect(), E4 = o11.clientX / _.width, O3 = o11.clientY / _.height;
        if (E4 < this._interactiveArea.left || E4 > this._interactiveArea.right || O3 < this._interactiveArea.top || O3 > this._interactiveArea.bottom) return;
      }
      let c3 = o11.pointerType !== "mouse" ? null : (o11.buttons & P2.LEFT) === P2.LEFT ? P2.LEFT : (o11.buttons & P2.MIDDLE) === P2.MIDDLE ? P2.MIDDLE : (o11.buttons & P2.RIGHT) === P2.RIGHT ? P2.RIGHT : null;
      if (c3 !== null) {
        let _ = this._findPointerByMouseButton(c3);
        _ && this._disposePointer(_);
      }
      if ((o11.buttons & P2.LEFT) === P2.LEFT && this._lockedPointer) return;
      let d5 = { pointerId: o11.pointerId, clientX: o11.clientX, clientY: o11.clientY, deltaX: 0, deltaY: 0, mouseButton: c3 };
      this._activePointers.push(d5), this._domElement.ownerDocument.removeEventListener("pointermove", l7, { passive: false }), this._domElement.ownerDocument.removeEventListener("pointerup", m4), this._domElement.ownerDocument.addEventListener("pointermove", l7, { passive: false }), this._domElement.ownerDocument.addEventListener("pointerup", m4), this._isDragging = true, C2(o11);
    }, l7 = (o11) => {
      o11.cancelable && o11.preventDefault();
      let c3 = o11.pointerId, d5 = this._lockedPointer || this._findPointerById(c3);
      if (d5) {
        if (d5.clientX = o11.clientX, d5.clientY = o11.clientY, d5.deltaX = o11.movementX, d5.deltaY = o11.movementY, this._state = 0, o11.pointerType === "touch") switch (this._activePointers.length) {
          case 1:
            this._state = this.touches.one;
            break;
          case 2:
            this._state = this.touches.two;
            break;
          case 3:
            this._state = this.touches.three;
            break;
        }
        else (!this._isDragging && this._lockedPointer || this._isDragging && (o11.buttons & P2.LEFT) === P2.LEFT) && (this._state = this._state | this.mouseButtons.left), this._isDragging && (o11.buttons & P2.MIDDLE) === P2.MIDDLE && (this._state = this._state | this.mouseButtons.middle), this._isDragging && (o11.buttons & P2.RIGHT) === P2.RIGHT && (this._state = this._state | this.mouseButtons.right);
        f5();
      }
    }, m4 = (o11) => {
      let c3 = this._findPointerById(o11.pointerId);
      if (!(c3 && c3 === this._lockedPointer)) {
        if (c3 && this._disposePointer(c3), o11.pointerType === "touch") switch (this._activePointers.length) {
          case 0:
            this._state = i7.NONE;
            break;
          case 1:
            this._state = this.touches.one;
            break;
          case 2:
            this._state = this.touches.two;
            break;
          case 3:
            this._state = this.touches.three;
            break;
        }
        else this._state = i7.NONE;
        L();
      }
    }, h7 = -1, v9 = (o11) => {
      if (!this._domElement || !this._enabled || this.mouseButtons.wheel === i7.NONE) return;
      if (this._interactiveArea.left !== 0 || this._interactiveArea.top !== 0 || this._interactiveArea.width !== 1 || this._interactiveArea.height !== 1) {
        let O3 = this._domElement.getBoundingClientRect(), S3 = o11.clientX / O3.width, z3 = o11.clientY / O3.height;
        if (S3 < this._interactiveArea.left || S3 > this._interactiveArea.right || z3 < this._interactiveArea.top || z3 > this._interactiveArea.bottom) return;
      }
      if (o11.preventDefault(), this.dollyToCursor || this.mouseButtons.wheel === i7.ROTATE || this.mouseButtons.wheel === i7.TRUCK) {
        let O3 = performance.now();
        h7 - O3 < 1e3 && this._getClientRect(this._elementRect), h7 = O3;
      }
      let c3 = St ? -1 : -3, d5 = o11.deltaMode === 1 ? o11.deltaY / c3 : o11.deltaY / (c3 * 10), _ = this.dollyToCursor ? (o11.clientX - this._elementRect.x) / this._elementRect.width * 2 - 1 : 0, E4 = this.dollyToCursor ? (o11.clientY - this._elementRect.y) / this._elementRect.height * -2 + 1 : 0;
      switch (this.mouseButtons.wheel) {
        case i7.ROTATE: {
          this._rotateInternal(o11.deltaX, o11.deltaY), this._isUserControllingRotate = true;
          break;
        }
        case i7.TRUCK: {
          this._truckInternal(o11.deltaX, o11.deltaY, false), this._isUserControllingTruck = true;
          break;
        }
        case i7.OFFSET: {
          this._truckInternal(o11.deltaX, o11.deltaY, true), this._isUserControllingOffset = true;
          break;
        }
        case i7.DOLLY: {
          this._dollyInternal(-d5, _, E4), this._isUserControllingDolly = true;
          break;
        }
        case i7.ZOOM: {
          this._zoomInternal(-d5, _, E4), this._isUserControllingZoom = true;
          break;
        }
      }
      this.dispatchEvent({ type: "control" });
    }, g6 = (o11) => {
      if (!(!this._domElement || !this._enabled)) {
        if (this.mouseButtons.right === u3.ACTION.NONE) {
          let c3 = o11 instanceof PointerEvent ? o11.pointerId : 0, d5 = this._findPointerById(c3);
          d5 && this._disposePointer(d5), this._domElement.ownerDocument.removeEventListener("pointermove", l7, { passive: false }), this._domElement.ownerDocument.removeEventListener("pointerup", m4);
          return;
        }
        o11.preventDefault();
      }
    }, C2 = (o11) => {
      if (!this._enabled) return;
      if (ot(this._activePointers, F2), this._getClientRect(this._elementRect), s10.copy(F2), n17.copy(F2), this._activePointers.length >= 2) {
        let d5 = F2.x - this._activePointers[1].clientX, _ = F2.y - this._activePointers[1].clientY, E4 = Math.sqrt(d5 * d5 + _ * _);
        r10.set(0, E4);
        let O3 = (this._activePointers[0].clientX + this._activePointers[1].clientX) * 0.5, S3 = (this._activePointers[0].clientY + this._activePointers[1].clientY) * 0.5;
        n17.set(O3, S3);
      }
      if (this._state = 0, !o11) this._lockedPointer && (this._state = this._state | this.mouseButtons.left);
      else if ("pointerType" in o11 && o11.pointerType === "touch") switch (this._activePointers.length) {
        case 1:
          this._state = this.touches.one;
          break;
        case 2:
          this._state = this.touches.two;
          break;
        case 3:
          this._state = this.touches.three;
          break;
      }
      else !this._lockedPointer && (o11.buttons & P2.LEFT) === P2.LEFT && (this._state = this._state | this.mouseButtons.left), (o11.buttons & P2.MIDDLE) === P2.MIDDLE && (this._state = this._state | this.mouseButtons.middle), (o11.buttons & P2.RIGHT) === P2.RIGHT && (this._state = this._state | this.mouseButtons.right);
      ((this._state & i7.ROTATE) === i7.ROTATE || (this._state & i7.TOUCH_ROTATE) === i7.TOUCH_ROTATE || (this._state & i7.TOUCH_DOLLY_ROTATE) === i7.TOUCH_DOLLY_ROTATE || (this._state & i7.TOUCH_ZOOM_ROTATE) === i7.TOUCH_ZOOM_ROTATE) && (this._sphericalEnd.theta = this._spherical.theta, this._sphericalEnd.phi = this._spherical.phi, this._thetaVelocity.value = 0, this._phiVelocity.value = 0), ((this._state & i7.TRUCK) === i7.TRUCK || (this._state & i7.TOUCH_TRUCK) === i7.TOUCH_TRUCK || (this._state & i7.TOUCH_DOLLY_TRUCK) === i7.TOUCH_DOLLY_TRUCK || (this._state & i7.TOUCH_ZOOM_TRUCK) === i7.TOUCH_ZOOM_TRUCK) && (this._targetEnd.copy(this._target), this._targetVelocity.set(0, 0, 0)), ((this._state & i7.DOLLY) === i7.DOLLY || (this._state & i7.TOUCH_DOLLY) === i7.TOUCH_DOLLY || (this._state & i7.TOUCH_DOLLY_TRUCK) === i7.TOUCH_DOLLY_TRUCK || (this._state & i7.TOUCH_DOLLY_OFFSET) === i7.TOUCH_DOLLY_OFFSET || (this._state & i7.TOUCH_DOLLY_ROTATE) === i7.TOUCH_DOLLY_ROTATE) && (this._sphericalEnd.radius = this._spherical.radius, this._radiusVelocity.value = 0), ((this._state & i7.ZOOM) === i7.ZOOM || (this._state & i7.TOUCH_ZOOM) === i7.TOUCH_ZOOM || (this._state & i7.TOUCH_ZOOM_TRUCK) === i7.TOUCH_ZOOM_TRUCK || (this._state & i7.TOUCH_ZOOM_OFFSET) === i7.TOUCH_ZOOM_OFFSET || (this._state & i7.TOUCH_ZOOM_ROTATE) === i7.TOUCH_ZOOM_ROTATE) && (this._zoomEnd = this._zoom, this._zoomVelocity.value = 0), ((this._state & i7.OFFSET) === i7.OFFSET || (this._state & i7.TOUCH_OFFSET) === i7.TOUCH_OFFSET || (this._state & i7.TOUCH_DOLLY_OFFSET) === i7.TOUCH_DOLLY_OFFSET || (this._state & i7.TOUCH_ZOOM_OFFSET) === i7.TOUCH_ZOOM_OFFSET) && (this._focalOffsetEnd.copy(this._focalOffset), this._focalOffsetVelocity.set(0, 0, 0)), this.dispatchEvent({ type: "controlstart" });
    }, f5 = () => {
      if (!this._enabled || !this._dragNeedsUpdate) return;
      this._dragNeedsUpdate = false, ot(this._activePointers, F2);
      let c3 = this._domElement && this._domElement.ownerDocument.pointerLockElement === this._domElement ? this._lockedPointer || this._activePointers[0] : null, d5 = c3 ? -c3.deltaX : n17.x - F2.x, _ = c3 ? -c3.deltaY : n17.y - F2.y;
      if (n17.copy(F2), ((this._state & i7.ROTATE) === i7.ROTATE || (this._state & i7.TOUCH_ROTATE) === i7.TOUCH_ROTATE || (this._state & i7.TOUCH_DOLLY_ROTATE) === i7.TOUCH_DOLLY_ROTATE || (this._state & i7.TOUCH_ZOOM_ROTATE) === i7.TOUCH_ZOOM_ROTATE) && (this._rotateInternal(d5, _), this._isUserControllingRotate = true), (this._state & i7.DOLLY) === i7.DOLLY || (this._state & i7.ZOOM) === i7.ZOOM) {
        let E4 = this.dollyToCursor ? (s10.x - this._elementRect.x) / this._elementRect.width * 2 - 1 : 0, O3 = this.dollyToCursor ? (s10.y - this._elementRect.y) / this._elementRect.height * -2 + 1 : 0, S3 = this.dollyDragInverted ? -1 : 1;
        (this._state & i7.DOLLY) === i7.DOLLY ? (this._dollyInternal(S3 * _ * $, E4, O3), this._isUserControllingDolly = true) : (this._zoomInternal(S3 * _ * $, E4, O3), this._isUserControllingZoom = true);
      }
      if ((this._state & i7.TOUCH_DOLLY) === i7.TOUCH_DOLLY || (this._state & i7.TOUCH_ZOOM) === i7.TOUCH_ZOOM || (this._state & i7.TOUCH_DOLLY_TRUCK) === i7.TOUCH_DOLLY_TRUCK || (this._state & i7.TOUCH_ZOOM_TRUCK) === i7.TOUCH_ZOOM_TRUCK || (this._state & i7.TOUCH_DOLLY_OFFSET) === i7.TOUCH_DOLLY_OFFSET || (this._state & i7.TOUCH_ZOOM_OFFSET) === i7.TOUCH_ZOOM_OFFSET || (this._state & i7.TOUCH_DOLLY_ROTATE) === i7.TOUCH_DOLLY_ROTATE || (this._state & i7.TOUCH_ZOOM_ROTATE) === i7.TOUCH_ZOOM_ROTATE) {
        let E4 = F2.x - this._activePointers[1].clientX, O3 = F2.y - this._activePointers[1].clientY, S3 = Math.sqrt(E4 * E4 + O3 * O3), z3 = r10.y - S3;
        r10.set(0, S3);
        let j = this.dollyToCursor ? (n17.x - this._elementRect.x) / this._elementRect.width * 2 - 1 : 0, W = this.dollyToCursor ? (n17.y - this._elementRect.y) / this._elementRect.height * -2 + 1 : 0;
        (this._state & i7.TOUCH_DOLLY) === i7.TOUCH_DOLLY || (this._state & i7.TOUCH_DOLLY_ROTATE) === i7.TOUCH_DOLLY_ROTATE || (this._state & i7.TOUCH_DOLLY_TRUCK) === i7.TOUCH_DOLLY_TRUCK || (this._state & i7.TOUCH_DOLLY_OFFSET) === i7.TOUCH_DOLLY_OFFSET ? (this._dollyInternal(z3 * $, j, W), this._isUserControllingDolly = true) : (this._zoomInternal(z3 * $, j, W), this._isUserControllingZoom = true);
      }
      ((this._state & i7.TRUCK) === i7.TRUCK || (this._state & i7.TOUCH_TRUCK) === i7.TOUCH_TRUCK || (this._state & i7.TOUCH_DOLLY_TRUCK) === i7.TOUCH_DOLLY_TRUCK || (this._state & i7.TOUCH_ZOOM_TRUCK) === i7.TOUCH_ZOOM_TRUCK) && (this._truckInternal(d5, _, false), this._isUserControllingTruck = true), ((this._state & i7.OFFSET) === i7.OFFSET || (this._state & i7.TOUCH_OFFSET) === i7.TOUCH_OFFSET || (this._state & i7.TOUCH_DOLLY_OFFSET) === i7.TOUCH_DOLLY_OFFSET || (this._state & i7.TOUCH_ZOOM_OFFSET) === i7.TOUCH_ZOOM_OFFSET) && (this._truckInternal(d5, _, true), this._isUserControllingOffset = true), this.dispatchEvent({ type: "control" });
    }, L = () => {
      ot(this._activePointers, F2), n17.copy(F2), this._dragNeedsUpdate = false, (this._activePointers.length === 0 || this._activePointers.length === 1 && this._activePointers[0] === this._lockedPointer) && (this._isDragging = false), this._activePointers.length === 0 && this._domElement && (this._domElement.ownerDocument.removeEventListener("pointermove", l7, { passive: false }), this._domElement.ownerDocument.removeEventListener("pointerup", m4), this.dispatchEvent({ type: "controlend" }));
    };
    this.lockPointer = () => {
      !this._enabled || !this._domElement || (this.cancel(), this._lockedPointer = { pointerId: -1, clientX: 0, clientY: 0, deltaX: 0, deltaY: 0, mouseButton: null }, this._activePointers.push(this._lockedPointer), this._domElement.ownerDocument.removeEventListener("pointermove", l7, { passive: false }), this._domElement.ownerDocument.removeEventListener("pointerup", m4), this._domElement.requestPointerLock(), this._domElement.ownerDocument.addEventListener("pointerlockchange", T4), this._domElement.ownerDocument.addEventListener("pointerlockerror", A4), this._domElement.ownerDocument.addEventListener("pointermove", l7, { passive: false }), this._domElement.ownerDocument.addEventListener("pointerup", m4), C2());
    }, this.unlockPointer = () => {
      var o11, c3, d5;
      this._lockedPointer !== null && (this._disposePointer(this._lockedPointer), this._lockedPointer = null), (o11 = this._domElement) === null || o11 === void 0 || o11.ownerDocument.exitPointerLock(), (c3 = this._domElement) === null || c3 === void 0 || c3.ownerDocument.removeEventListener("pointerlockchange", T4), (d5 = this._domElement) === null || d5 === void 0 || d5.ownerDocument.removeEventListener("pointerlockerror", A4), this.cancel();
    };
    let T4 = () => {
      this._domElement && this._domElement.ownerDocument.pointerLockElement === this._domElement || this.unlockPointer();
    }, A4 = () => {
      this.unlockPointer();
    };
    this._addAllEventListeners = (o11) => {
      this._domElement = o11, this._domElement.style.touchAction = "none", this._domElement.style.userSelect = "none", this._domElement.style.webkitUserSelect = "none", this._domElement.addEventListener("pointerdown", a11), this._domElement.addEventListener("pointercancel", m4), this._domElement.addEventListener("wheel", v9, { passive: false }), this._domElement.addEventListener("contextmenu", g6);
    }, this._removeAllEventListeners = () => {
      this._domElement && (this._domElement.style.touchAction = "", this._domElement.style.userSelect = "", this._domElement.style.webkitUserSelect = "", this._domElement.removeEventListener("pointerdown", a11), this._domElement.removeEventListener("pointercancel", m4), this._domElement.removeEventListener("wheel", v9, { passive: false }), this._domElement.removeEventListener("contextmenu", g6), this._domElement.ownerDocument.removeEventListener("pointermove", l7, { passive: false }), this._domElement.ownerDocument.removeEventListener("pointerup", m4), this._domElement.ownerDocument.removeEventListener("pointerlockchange", T4), this._domElement.ownerDocument.removeEventListener("pointerlockerror", A4));
    }, this.cancel = () => {
      this._state !== i7.NONE && (this._state = i7.NONE, this._activePointers.length = 0, L());
    }, e19 && this.connect(e19), this.update(0);
  }
  get camera() {
    return this._camera;
  }
  set camera(t15) {
    this._camera = t15, this.updateCameraUp(), this._camera.updateProjectionMatrix(), this._updateNearPlaneCorners(), this._needsUpdate = true;
  }
  get enabled() {
    return this._enabled;
  }
  set enabled(t15) {
    this._enabled = t15, this._domElement && (t15 ? (this._domElement.style.touchAction = "none", this._domElement.style.userSelect = "none", this._domElement.style.webkitUserSelect = "none") : (this.cancel(), this._domElement.style.touchAction = "", this._domElement.style.userSelect = "", this._domElement.style.webkitUserSelect = ""));
  }
  get active() {
    return !this._hasRested;
  }
  get currentAction() {
    return this._state;
  }
  get distance() {
    return this._spherical.radius;
  }
  set distance(t15) {
    this._spherical.radius === t15 && this._sphericalEnd.radius === t15 || (this._spherical.radius = t15, this._sphericalEnd.radius = t15, this._needsUpdate = true);
  }
  get azimuthAngle() {
    return this._spherical.theta;
  }
  set azimuthAngle(t15) {
    this._spherical.theta === t15 && this._sphericalEnd.theta === t15 || (this._spherical.theta = t15, this._sphericalEnd.theta = t15, this._needsUpdate = true);
  }
  get polarAngle() {
    return this._spherical.phi;
  }
  set polarAngle(t15) {
    this._spherical.phi === t15 && this._sphericalEnd.phi === t15 || (this._spherical.phi = t15, this._sphericalEnd.phi = t15, this._needsUpdate = true);
  }
  get boundaryEnclosesCamera() {
    return this._boundaryEnclosesCamera;
  }
  set boundaryEnclosesCamera(t15) {
    this._boundaryEnclosesCamera = t15, this._needsUpdate = true;
  }
  set interactiveArea(t15) {
    this._interactiveArea.width = M2(t15.width, 0, 1), this._interactiveArea.height = M2(t15.height, 0, 1), this._interactiveArea.x = M2(t15.x, 0, 1 - this._interactiveArea.width), this._interactiveArea.y = M2(t15.y, 0, 1 - this._interactiveArea.height);
  }
  addEventListener(t15, e19) {
    super.addEventListener(t15, e19);
  }
  removeEventListener(t15, e19) {
    super.removeEventListener(t15, e19);
  }
  rotate(t15, e19, s10 = false) {
    return this.rotateTo(this._sphericalEnd.theta + t15, this._sphericalEnd.phi + e19, s10);
  }
  rotateAzimuthTo(t15, e19 = false) {
    return this.rotateTo(t15, this._sphericalEnd.phi, e19);
  }
  rotatePolarTo(t15, e19 = false) {
    return this.rotateTo(this._sphericalEnd.theta, t15, e19);
  }
  rotateTo(t15, e19, s10 = false) {
    this._isUserControllingRotate = false;
    let n17 = M2(t15, this.minAzimuthAngle, this.maxAzimuthAngle), r10 = M2(e19, this.minPolarAngle, this.maxPolarAngle);
    this._sphericalEnd.theta = n17, this._sphericalEnd.phi = r10, this._sphericalEnd.makeSafe(), this._needsUpdate = true, s10 || (this._spherical.theta = this._sphericalEnd.theta, this._spherical.phi = this._sphericalEnd.phi);
    let a11 = !s10 || x3(this._spherical.theta, this._sphericalEnd.theta, this.restThreshold) && x3(this._spherical.phi, this._sphericalEnd.phi, this.restThreshold);
    return this._createOnRestPromise(a11);
  }
  dolly(t15, e19 = false) {
    return this.dollyTo(this._sphericalEnd.radius - t15, e19);
  }
  dollyTo(t15, e19 = false) {
    return this._isUserControllingDolly = false, this._lastDollyDirection = N.NONE, this._changedDolly = 0, this._dollyToNoClamp(M2(t15, this.minDistance, this.maxDistance), e19);
  }
  _dollyToNoClamp(t15, e19 = false) {
    let s10 = this._sphericalEnd.radius;
    if (this.colliderMeshes.length >= 1) {
      let a11 = this._collisionTest(), l7 = x3(a11, this._spherical.radius);
      if (!(s10 > t15) && l7) return Promise.resolve();
      this._sphericalEnd.radius = Math.min(t15, a11);
    } else this._sphericalEnd.radius = t15;
    this._needsUpdate = true, e19 || (this._spherical.radius = this._sphericalEnd.radius);
    let r10 = !e19 || x3(this._spherical.radius, this._sphericalEnd.radius, this.restThreshold);
    return this._createOnRestPromise(r10);
  }
  dollyInFixed(t15, e19 = false) {
    this._targetEnd.add(this._getCameraDirection(q2).multiplyScalar(t15)), e19 || this._target.copy(this._targetEnd);
    let s10 = !e19 || x3(this._target.x, this._targetEnd.x, this.restThreshold) && x3(this._target.y, this._targetEnd.y, this.restThreshold) && x3(this._target.z, this._targetEnd.z, this.restThreshold);
    return this._createOnRestPromise(s10);
  }
  zoom(t15, e19 = false) {
    return this.zoomTo(this._zoomEnd + t15, e19);
  }
  zoomTo(t15, e19 = false) {
    this._isUserControllingZoom = false, this._zoomEnd = M2(t15, this.minZoom, this.maxZoom), this._needsUpdate = true, e19 || (this._zoom = this._zoomEnd);
    let s10 = !e19 || x3(this._zoom, this._zoomEnd, this.restThreshold);
    return this._changedZoom = 0, this._createOnRestPromise(s10);
  }
  pan(t15, e19, s10 = false) {
    return console.warn("`pan` has been renamed to `truck`"), this.truck(t15, e19, s10);
  }
  truck(t15, e19, s10 = false) {
    this._camera.updateMatrix(), I2.setFromMatrixColumn(this._camera.matrix, 0), b3.setFromMatrixColumn(this._camera.matrix, 1), I2.multiplyScalar(t15), b3.multiplyScalar(-e19);
    let n17 = y4.copy(I2).add(b3), r10 = U2.copy(this._targetEnd).add(n17);
    return this.moveTo(r10.x, r10.y, r10.z, s10);
  }
  forward(t15, e19 = false) {
    y4.setFromMatrixColumn(this._camera.matrix, 0), y4.crossVectors(this._camera.up, y4), y4.multiplyScalar(t15);
    let s10 = U2.copy(this._targetEnd).add(y4);
    return this.moveTo(s10.x, s10.y, s10.z, e19);
  }
  elevate(t15, e19 = false) {
    return y4.copy(this._camera.up).multiplyScalar(t15), this.moveTo(this._targetEnd.x + y4.x, this._targetEnd.y + y4.y, this._targetEnd.z + y4.z, e19);
  }
  moveTo(t15, e19, s10, n17 = false) {
    this._isUserControllingTruck = false;
    let r10 = y4.set(t15, e19, s10).sub(this._targetEnd);
    this._encloseToBoundary(this._targetEnd, r10, this.boundaryFriction), this._needsUpdate = true, n17 || this._target.copy(this._targetEnd);
    let a11 = !n17 || x3(this._target.x, this._targetEnd.x, this.restThreshold) && x3(this._target.y, this._targetEnd.y, this.restThreshold) && x3(this._target.z, this._targetEnd.z, this.restThreshold);
    return this._createOnRestPromise(a11);
  }
  lookInDirectionOf(t15, e19, s10, n17 = false) {
    let l7 = y4.set(t15, e19, s10).sub(this._targetEnd).normalize().multiplyScalar(-this._sphericalEnd.radius).add(this._targetEnd);
    return this.setPosition(l7.x, l7.y, l7.z, n17);
  }
  fitToBox(t15, e19, { cover: s10 = false, paddingLeft: n17 = 0, paddingRight: r10 = 0, paddingBottom: a11 = 0, paddingTop: l7 = 0 } = {}) {
    let m4 = [], h7 = t15.isBox3 ? B2.copy(t15) : B2.setFromObject(t15);
    h7.isEmpty() && (console.warn("camera-controls: fitTo() cannot be used with an empty box. Aborting"), Promise.resolve());
    let v9 = ut(this._sphericalEnd.theta, pt), g6 = ut(this._sphericalEnd.phi, pt);
    m4.push(this.rotateTo(v9, g6, e19));
    let C2 = y4.setFromSpherical(this._sphericalEnd).normalize(), f5 = Et.setFromUnitVectors(C2, at), L = x3(Math.abs(C2.y), 1);
    L && f5.multiply(lt.setFromAxisAngle(tt, v9)), f5.multiply(this._yAxisUpSpaceInverse);
    let T4 = Tt.makeEmpty();
    U2.copy(h7.min).applyQuaternion(f5), T4.expandByPoint(U2), U2.copy(h7.min).setX(h7.max.x).applyQuaternion(f5), T4.expandByPoint(U2), U2.copy(h7.min).setY(h7.max.y).applyQuaternion(f5), T4.expandByPoint(U2), U2.copy(h7.max).setZ(h7.min.z).applyQuaternion(f5), T4.expandByPoint(U2), U2.copy(h7.min).setZ(h7.max.z).applyQuaternion(f5), T4.expandByPoint(U2), U2.copy(h7.max).setY(h7.min.y).applyQuaternion(f5), T4.expandByPoint(U2), U2.copy(h7.max).setX(h7.min.x).applyQuaternion(f5), T4.expandByPoint(U2), U2.copy(h7.max).applyQuaternion(f5), T4.expandByPoint(U2), T4.min.x -= n17, T4.min.y -= a11, T4.max.x += r10, T4.max.y += l7, f5.setFromUnitVectors(at, C2), L && f5.premultiply(lt.invert()), f5.premultiply(this._yAxisUpSpace);
    let A4 = T4.getSize(y4), o11 = T4.getCenter(U2).applyQuaternion(f5);
    if (k4(this._camera)) {
      let c3 = this.getDistanceToFitBox(A4.x, A4.y, A4.z, s10);
      m4.push(this.moveTo(o11.x, o11.y, o11.z, e19)), m4.push(this.dollyTo(c3, e19)), m4.push(this.setFocalOffset(0, 0, 0, e19));
    } else if (H(this._camera)) {
      let c3 = this._camera, d5 = c3.right - c3.left, _ = c3.top - c3.bottom, E4 = s10 ? Math.max(d5 / A4.x, _ / A4.y) : Math.min(d5 / A4.x, _ / A4.y);
      m4.push(this.moveTo(o11.x, o11.y, o11.z, e19)), m4.push(this.zoomTo(E4, e19)), m4.push(this.setFocalOffset(0, 0, 0, e19));
    }
    return Promise.all(m4);
  }
  fitToSphere(t15, e19) {
    let s10 = [], r10 = "isObject3D" in t15 ? u3.createBoundingSphere(t15, ht) : ht.copy(t15);
    if (s10.push(this.moveTo(r10.center.x, r10.center.y, r10.center.z, e19)), k4(this._camera)) {
      let a11 = this.getDistanceToFitSphere(r10.radius);
      s10.push(this.dollyTo(a11, e19));
    } else if (H(this._camera)) {
      let a11 = this._camera.right - this._camera.left, l7 = this._camera.top - this._camera.bottom, m4 = 2 * r10.radius, h7 = Math.min(a11 / m4, l7 / m4);
      s10.push(this.zoomTo(h7, e19));
    }
    return s10.push(this.setFocalOffset(0, 0, 0, e19)), Promise.all(s10);
  }
  setLookAt(t15, e19, s10, n17, r10, a11, l7 = false) {
    this._isUserControllingRotate = false, this._isUserControllingDolly = false, this._isUserControllingTruck = false, this._lastDollyDirection = N.NONE, this._changedDolly = 0;
    let m4 = U2.set(n17, r10, a11), h7 = y4.set(t15, e19, s10);
    this._targetEnd.copy(m4), this._sphericalEnd.setFromVector3(h7.sub(m4).applyQuaternion(this._yAxisUpSpace)), this.normalizeRotations(), this._needsUpdate = true, l7 || (this._target.copy(this._targetEnd), this._spherical.copy(this._sphericalEnd));
    let v9 = !l7 || x3(this._target.x, this._targetEnd.x, this.restThreshold) && x3(this._target.y, this._targetEnd.y, this.restThreshold) && x3(this._target.z, this._targetEnd.z, this.restThreshold) && x3(this._spherical.theta, this._sphericalEnd.theta, this.restThreshold) && x3(this._spherical.phi, this._sphericalEnd.phi, this.restThreshold) && x3(this._spherical.radius, this._sphericalEnd.radius, this.restThreshold);
    return this._createOnRestPromise(v9);
  }
  lerpLookAt(t15, e19, s10, n17, r10, a11, l7, m4, h7, v9, g6, C2, f5, L = false) {
    this._isUserControllingRotate = false, this._isUserControllingDolly = false, this._isUserControllingTruck = false, this._lastDollyDirection = N.NONE, this._changedDolly = 0;
    let T4 = y4.set(n17, r10, a11), A4 = U2.set(t15, e19, s10);
    R2.setFromVector3(A4.sub(T4).applyQuaternion(this._yAxisUpSpace));
    let o11 = V2.set(v9, g6, C2), c3 = U2.set(l7, m4, h7);
    G.setFromVector3(c3.sub(o11).applyQuaternion(this._yAxisUpSpace)), this._targetEnd.copy(T4.lerp(o11, f5));
    let d5 = G.theta - R2.theta, _ = G.phi - R2.phi, E4 = G.radius - R2.radius;
    this._sphericalEnd.set(R2.radius + E4 * f5, R2.phi + _ * f5, R2.theta + d5 * f5), this.normalizeRotations(), this._needsUpdate = true, L || (this._target.copy(this._targetEnd), this._spherical.copy(this._sphericalEnd));
    let O3 = !L || x3(this._target.x, this._targetEnd.x, this.restThreshold) && x3(this._target.y, this._targetEnd.y, this.restThreshold) && x3(this._target.z, this._targetEnd.z, this.restThreshold) && x3(this._spherical.theta, this._sphericalEnd.theta, this.restThreshold) && x3(this._spherical.phi, this._sphericalEnd.phi, this.restThreshold) && x3(this._spherical.radius, this._sphericalEnd.radius, this.restThreshold);
    return this._createOnRestPromise(O3);
  }
  setPosition(t15, e19, s10, n17 = false) {
    return this.setLookAt(t15, e19, s10, this._targetEnd.x, this._targetEnd.y, this._targetEnd.z, n17);
  }
  setTarget(t15, e19, s10, n17 = false) {
    let r10 = this.getPosition(y4), a11 = this.setLookAt(r10.x, r10.y, r10.z, t15, e19, s10, n17);
    return this._sphericalEnd.phi = M2(this._sphericalEnd.phi, this.minPolarAngle, this.maxPolarAngle), a11;
  }
  setFocalOffset(t15, e19, s10, n17 = false) {
    this._isUserControllingOffset = false, this._focalOffsetEnd.set(t15, e19, s10), this._needsUpdate = true, n17 || this._focalOffset.copy(this._focalOffsetEnd);
    let r10 = !n17 || x3(this._focalOffset.x, this._focalOffsetEnd.x, this.restThreshold) && x3(this._focalOffset.y, this._focalOffsetEnd.y, this.restThreshold) && x3(this._focalOffset.z, this._focalOffsetEnd.z, this.restThreshold);
    return this._createOnRestPromise(r10);
  }
  setOrbitPoint(t15, e19, s10) {
    this._camera.updateMatrixWorld(), I2.setFromMatrixColumn(this._camera.matrixWorldInverse, 0), b3.setFromMatrixColumn(this._camera.matrixWorldInverse, 1), Y.setFromMatrixColumn(this._camera.matrixWorldInverse, 2);
    let n17 = y4.set(t15, e19, s10), r10 = n17.distanceTo(this._camera.position), a11 = n17.sub(this._camera.position);
    I2.multiplyScalar(a11.x), b3.multiplyScalar(a11.y), Y.multiplyScalar(a11.z), y4.copy(I2).add(b3).add(Y), y4.z = y4.z + r10, this.dollyTo(r10, false), this.setFocalOffset(-y4.x, y4.y, -y4.z, false), this.moveTo(t15, e19, s10, false);
  }
  setBoundary(t15) {
    if (!t15) {
      this._boundary.min.set(-1 / 0, -1 / 0, -1 / 0), this._boundary.max.set(1 / 0, 1 / 0, 1 / 0), this._needsUpdate = true;
      return;
    }
    this._boundary.copy(t15), this._boundary.clampPoint(this._targetEnd, this._targetEnd), this._needsUpdate = true;
  }
  setViewport(t15, e19, s10, n17) {
    if (t15 === null) {
      this._viewport = null;
      return;
    }
    this._viewport = this._viewport || new p8.Vector4(), typeof t15 == "number" ? this._viewport.set(t15, e19, s10, n17) : this._viewport.copy(t15);
  }
  getDistanceToFitBox(t15, e19, s10, n17 = false) {
    if (rt(this._camera, "getDistanceToFitBox")) return this._spherical.radius;
    let r10 = t15 / e19, a11 = this._camera.getEffectiveFOV() * K, l7 = this._camera.aspect;
    return ((n17 ? r10 > l7 : r10 < l7) ? e19 : t15 / l7) * 0.5 / Math.tan(a11 * 0.5) + s10 * 0.5;
  }
  getDistanceToFitSphere(t15) {
    if (rt(this._camera, "getDistanceToFitSphere")) return this._spherical.radius;
    let e19 = this._camera.getEffectiveFOV() * K, s10 = Math.atan(Math.tan(e19 * 0.5) * this._camera.aspect) * 2, n17 = 1 < this._camera.aspect ? e19 : s10;
    return t15 / Math.sin(n17 * 0.5);
  }
  getTarget(t15, e19 = true) {
    return (t15 && t15.isVector3 ? t15 : new p8.Vector3()).copy(e19 ? this._targetEnd : this._target);
  }
  getPosition(t15, e19 = true) {
    return (t15 && t15.isVector3 ? t15 : new p8.Vector3()).setFromSpherical(e19 ? this._sphericalEnd : this._spherical).applyQuaternion(this._yAxisUpSpaceInverse).add(e19 ? this._targetEnd : this._target);
  }
  getSpherical(t15, e19 = true) {
    return (t15 || new p8.Spherical()).copy(e19 ? this._sphericalEnd : this._spherical);
  }
  getFocalOffset(t15, e19 = true) {
    return (t15 && t15.isVector3 ? t15 : new p8.Vector3()).copy(e19 ? this._focalOffsetEnd : this._focalOffset);
  }
  normalizeRotations() {
    this._sphericalEnd.theta = this._sphericalEnd.theta % Z, this._sphericalEnd.theta < 0 && (this._sphericalEnd.theta += Z), this._spherical.theta += Z * Math.round((this._sphericalEnd.theta - this._spherical.theta) / Z);
  }
  stop() {
    this._focalOffset.copy(this._focalOffsetEnd), this._target.copy(this._targetEnd), this._spherical.copy(this._sphericalEnd), this._zoom = this._zoomEnd;
  }
  reset(t15 = false) {
    if (!x3(this._camera.up.x, this._cameraUp0.x) || !x3(this._camera.up.y, this._cameraUp0.y) || !x3(this._camera.up.z, this._cameraUp0.z)) {
      this._camera.up.copy(this._cameraUp0);
      let s10 = this.getPosition(y4);
      this.updateCameraUp(), this.setPosition(s10.x, s10.y, s10.z);
    }
    let e19 = [this.setLookAt(this._position0.x, this._position0.y, this._position0.z, this._target0.x, this._target0.y, this._target0.z, t15), this.setFocalOffset(this._focalOffset0.x, this._focalOffset0.y, this._focalOffset0.z, t15), this.zoomTo(this._zoom0, t15)];
    return Promise.all(e19);
  }
  saveState() {
    this._cameraUp0.copy(this._camera.up), this.getTarget(this._target0), this.getPosition(this._position0), this._zoom0 = this._zoom, this._focalOffset0.copy(this._focalOffset);
  }
  updateCameraUp() {
    this._yAxisUpSpace.setFromUnitVectors(this._camera.up, tt), this._yAxisUpSpaceInverse.copy(this._yAxisUpSpace).invert();
  }
  applyCameraUp() {
    let t15 = y4.subVectors(this._target, this._camera.position).normalize(), e19 = U2.crossVectors(t15, this._camera.up);
    this._camera.up.crossVectors(e19, t15).normalize(), this._camera.updateMatrixWorld();
    let s10 = this.getPosition(y4);
    this.updateCameraUp(), this.setPosition(s10.x, s10.y, s10.z);
  }
  update(t15) {
    let e19 = this._sphericalEnd.theta - this._spherical.theta, s10 = this._sphericalEnd.phi - this._spherical.phi, n17 = this._sphericalEnd.radius - this._spherical.radius, r10 = Ot.subVectors(this._targetEnd, this._target), a11 = yt.subVectors(this._focalOffsetEnd, this._focalOffset), l7 = this._zoomEnd - this._zoom;
    if (D(e19)) this._thetaVelocity.value = 0, this._spherical.theta = this._sphericalEnd.theta;
    else {
      let g6 = this._isUserControllingRotate ? this.draggingSmoothTime : this.smoothTime;
      this._spherical.theta = J2(this._spherical.theta, this._sphericalEnd.theta, this._thetaVelocity, g6, 1 / 0, t15), this._needsUpdate = true;
    }
    if (D(s10)) this._phiVelocity.value = 0, this._spherical.phi = this._sphericalEnd.phi;
    else {
      let g6 = this._isUserControllingRotate ? this.draggingSmoothTime : this.smoothTime;
      this._spherical.phi = J2(this._spherical.phi, this._sphericalEnd.phi, this._phiVelocity, g6, 1 / 0, t15), this._needsUpdate = true;
    }
    if (D(n17)) this._radiusVelocity.value = 0, this._spherical.radius = this._sphericalEnd.radius;
    else {
      let g6 = this._isUserControllingDolly ? this.draggingSmoothTime : this.smoothTime;
      this._spherical.radius = J2(this._spherical.radius, this._sphericalEnd.radius, this._radiusVelocity, g6, this.maxSpeed, t15), this._needsUpdate = true;
    }
    if (D(r10.x) && D(r10.y) && D(r10.z)) this._targetVelocity.set(0, 0, 0), this._target.copy(this._targetEnd);
    else {
      let g6 = this._isUserControllingTruck ? this.draggingSmoothTime : this.smoothTime;
      ft(this._target, this._targetEnd, this._targetVelocity, g6, this.maxSpeed, t15, this._target), this._needsUpdate = true;
    }
    if (D(a11.x) && D(a11.y) && D(a11.z)) this._focalOffsetVelocity.set(0, 0, 0), this._focalOffset.copy(this._focalOffsetEnd);
    else {
      let g6 = this._isUserControllingOffset ? this.draggingSmoothTime : this.smoothTime;
      ft(this._focalOffset, this._focalOffsetEnd, this._focalOffsetVelocity, g6, this.maxSpeed, t15, this._focalOffset), this._needsUpdate = true;
    }
    if (D(l7)) this._zoomVelocity.value = 0, this._zoom = this._zoomEnd;
    else {
      let g6 = this._isUserControllingZoom ? this.draggingSmoothTime : this.smoothTime;
      this._zoom = J2(this._zoom, this._zoomEnd, this._zoomVelocity, g6, 1 / 0, t15);
    }
    if (this.dollyToCursor) {
      if (k4(this._camera) && this._changedDolly !== 0) {
        let g6 = this._spherical.radius - this._lastDistance, C2 = this._camera, f5 = this._getCameraDirection(q2), L = y4.copy(f5).cross(C2.up).normalize();
        L.lengthSq() === 0 && (L.x = 1);
        let T4 = U2.crossVectors(L, f5), A4 = this._sphericalEnd.radius * Math.tan(C2.getEffectiveFOV() * K * 0.5), c3 = (this._sphericalEnd.radius - g6 - this._sphericalEnd.radius) / this._sphericalEnd.radius, d5 = V2.copy(this._targetEnd).add(L.multiplyScalar(this._dollyControlCoord.x * A4 * C2.aspect)).add(T4.multiplyScalar(this._dollyControlCoord.y * A4)), _ = y4.copy(this._targetEnd).lerp(d5, c3), E4 = this._lastDollyDirection === N.IN && this._spherical.radius <= this.minDistance, O3 = this._lastDollyDirection === N.OUT && this.maxDistance <= this._spherical.radius;
        if (this.infinityDolly && (E4 || O3)) {
          this._sphericalEnd.radius -= g6, this._spherical.radius -= g6;
          let z3 = U2.copy(f5).multiplyScalar(-g6);
          _.add(z3);
        }
        this._boundary.clampPoint(_, _);
        let S3 = U2.subVectors(_, this._targetEnd);
        this._targetEnd.copy(_), this._target.add(S3), this._changedDolly -= g6, D(this._changedDolly) && (this._changedDolly = 0);
      } else if (H(this._camera) && this._changedZoom !== 0) {
        let g6 = this._zoom - this._lastZoom, C2 = this._camera, f5 = y4.set(this._dollyControlCoord.x, this._dollyControlCoord.y, (C2.near + C2.far) / (C2.near - C2.far)).unproject(C2), L = U2.set(0, 0, -1).applyQuaternion(C2.quaternion), T4 = V2.copy(f5).add(L.multiplyScalar(-f5.dot(C2.up))), o11 = -(this._zoom - g6 - this._zoom) / this._zoom, c3 = this._getCameraDirection(q2), d5 = this._targetEnd.dot(c3), _ = y4.copy(this._targetEnd).lerp(T4, o11), E4 = _.dot(c3), O3 = c3.multiplyScalar(E4 - d5);
        _.sub(O3), this._boundary.clampPoint(_, _);
        let S3 = U2.subVectors(_, this._targetEnd);
        this._targetEnd.copy(_), this._target.add(S3), this._changedZoom -= g6, D(this._changedZoom) && (this._changedZoom = 0);
      }
    }
    this._camera.zoom !== this._zoom && (this._camera.zoom = this._zoom, this._camera.updateProjectionMatrix(), this._updateNearPlaneCorners(), this._needsUpdate = true), this._dragNeedsUpdate = true;
    let m4 = this._collisionTest();
    this._spherical.radius = Math.min(this._spherical.radius, m4), this._spherical.makeSafe(), this._camera.position.setFromSpherical(this._spherical).applyQuaternion(this._yAxisUpSpaceInverse).add(this._target), this._camera.lookAt(this._target), (!D(this._focalOffset.x) || !D(this._focalOffset.y) || !D(this._focalOffset.z)) && (this._camera.updateMatrixWorld(), I2.setFromMatrixColumn(this._camera.matrix, 0), b3.setFromMatrixColumn(this._camera.matrix, 1), Y.setFromMatrixColumn(this._camera.matrix, 2), I2.multiplyScalar(this._focalOffset.x), b3.multiplyScalar(-this._focalOffset.y), Y.multiplyScalar(this._focalOffset.z), y4.copy(I2).add(b3).add(Y), this._camera.position.add(y4)), this._boundaryEnclosesCamera && this._encloseToBoundary(this._camera.position.copy(this._target), y4.setFromSpherical(this._spherical).applyQuaternion(this._yAxisUpSpaceInverse), 1);
    let v9 = this._needsUpdate;
    return v9 && !this._updatedLastTime ? (this._hasRested = false, this.dispatchEvent({ type: "wake" }), this.dispatchEvent({ type: "update" })) : v9 ? (this.dispatchEvent({ type: "update" }), D(e19, this.restThreshold) && D(s10, this.restThreshold) && D(n17, this.restThreshold) && D(r10.x, this.restThreshold) && D(r10.y, this.restThreshold) && D(r10.z, this.restThreshold) && D(a11.x, this.restThreshold) && D(a11.y, this.restThreshold) && D(a11.z, this.restThreshold) && D(l7, this.restThreshold) && !this._hasRested && (this._hasRested = true, this.dispatchEvent({ type: "rest" }))) : !v9 && this._updatedLastTime && this.dispatchEvent({ type: "sleep" }), this._lastDistance = this._spherical.radius, this._lastZoom = this._zoom, this._updatedLastTime = v9, this._needsUpdate = false, v9;
  }
  toJSON() {
    return JSON.stringify({ enabled: this._enabled, minDistance: this.minDistance, maxDistance: X(this.maxDistance), minZoom: this.minZoom, maxZoom: X(this.maxZoom), minPolarAngle: this.minPolarAngle, maxPolarAngle: X(this.maxPolarAngle), minAzimuthAngle: X(this.minAzimuthAngle), maxAzimuthAngle: X(this.maxAzimuthAngle), smoothTime: this.smoothTime, draggingSmoothTime: this.draggingSmoothTime, dollySpeed: this.dollySpeed, truckSpeed: this.truckSpeed, dollyToCursor: this.dollyToCursor, verticalDragToForward: this.verticalDragToForward, target: this._targetEnd.toArray(), position: y4.setFromSpherical(this._sphericalEnd).add(this._targetEnd).toArray(), zoom: this._zoomEnd, focalOffset: this._focalOffsetEnd.toArray(), target0: this._target0.toArray(), position0: this._position0.toArray(), zoom0: this._zoom0, focalOffset0: this._focalOffset0.toArray() });
  }
  fromJSON(t15, e19 = false) {
    let s10 = JSON.parse(t15);
    this.enabled = s10.enabled, this.minDistance = s10.minDistance, this.maxDistance = Q(s10.maxDistance), this.minZoom = s10.minZoom, this.maxZoom = Q(s10.maxZoom), this.minPolarAngle = s10.minPolarAngle, this.maxPolarAngle = Q(s10.maxPolarAngle), this.minAzimuthAngle = Q(s10.minAzimuthAngle), this.maxAzimuthAngle = Q(s10.maxAzimuthAngle), this.smoothTime = s10.smoothTime, this.draggingSmoothTime = s10.draggingSmoothTime, this.dollySpeed = s10.dollySpeed, this.truckSpeed = s10.truckSpeed, this.dollyToCursor = s10.dollyToCursor, this.verticalDragToForward = s10.verticalDragToForward, this._target0.fromArray(s10.target0), this._position0.fromArray(s10.position0), this._zoom0 = s10.zoom0, this._focalOffset0.fromArray(s10.focalOffset0), this.moveTo(s10.target[0], s10.target[1], s10.target[2], e19), R2.setFromVector3(y4.fromArray(s10.position).sub(this._targetEnd).applyQuaternion(this._yAxisUpSpace)), this.rotateTo(R2.theta, R2.phi, e19), this.dollyTo(R2.radius, e19), this.zoomTo(s10.zoom, e19), this.setFocalOffset(s10.focalOffset[0], s10.focalOffset[1], s10.focalOffset[2], e19), this._needsUpdate = true;
  }
  connect(t15) {
    if (this._domElement) {
      console.warn("camera-controls is already connected.");
      return;
    }
    t15.setAttribute("data-camera-controls-version", At), this._addAllEventListeners(t15), this._getClientRect(this._elementRect);
  }
  disconnect() {
    this.cancel(), this._removeAllEventListeners(), this._domElement && (this._domElement.removeAttribute("data-camera-controls-version"), this._domElement = void 0);
  }
  dispose() {
    this.removeAllEventListeners(), this.disconnect();
  }
  _getTargetDirection(t15) {
    return t15.setFromSpherical(this._spherical).divideScalar(this._spherical.radius).applyQuaternion(this._yAxisUpSpaceInverse);
  }
  _getCameraDirection(t15) {
    return this._getTargetDirection(t15).negate();
  }
  _findPointerById(t15) {
    return this._activePointers.find((e19) => e19.pointerId === t15);
  }
  _findPointerByMouseButton(t15) {
    return this._activePointers.find((e19) => e19.mouseButton === t15);
  }
  _disposePointer(t15) {
    this._activePointers.splice(this._activePointers.indexOf(t15), 1);
  }
  _encloseToBoundary(t15, e19, s10) {
    let n17 = e19.lengthSq();
    if (n17 === 0) return t15;
    let r10 = U2.copy(e19).add(t15), l7 = this._boundary.clampPoint(r10, V2).sub(r10), m4 = l7.lengthSq();
    if (m4 === 0) return t15.add(e19);
    if (m4 === n17) return t15;
    if (s10 === 0) return t15.add(e19).add(l7);
    {
      let h7 = 1 + s10 * m4 / e19.dot(l7);
      return t15.add(U2.copy(e19).multiplyScalar(h7)).add(l7.multiplyScalar(1 - s10));
    }
  }
  _updateNearPlaneCorners() {
    if (k4(this._camera)) {
      let t15 = this._camera, e19 = t15.near, s10 = t15.getEffectiveFOV() * K, n17 = Math.tan(s10 * 0.5) * e19, r10 = n17 * t15.aspect;
      this._nearPlaneCorners[0].set(-r10, -n17, 0), this._nearPlaneCorners[1].set(r10, -n17, 0), this._nearPlaneCorners[2].set(r10, n17, 0), this._nearPlaneCorners[3].set(-r10, n17, 0);
    } else if (H(this._camera)) {
      let t15 = this._camera, e19 = 1 / t15.zoom, s10 = t15.left * e19, n17 = t15.right * e19, r10 = t15.top * e19, a11 = t15.bottom * e19;
      this._nearPlaneCorners[0].set(s10, r10, 0), this._nearPlaneCorners[1].set(n17, r10, 0), this._nearPlaneCorners[2].set(n17, a11, 0), this._nearPlaneCorners[3].set(s10, a11, 0);
    }
  }
  _collisionTest() {
    let t15 = 1 / 0;
    if (!(this.colliderMeshes.length >= 1) || rt(this._camera, "_collisionTest")) return t15;
    let s10 = this._getTargetDirection(q2);
    ct.lookAt(gt, s10, this._camera.up);
    for (let n17 = 0; n17 < 4; n17++) {
      let r10 = U2.copy(this._nearPlaneCorners[n17]);
      r10.applyMatrix4(ct);
      let a11 = V2.addVectors(this._target, r10);
      et.set(a11, s10), et.far = this._spherical.radius + 1;
      let l7 = et.intersectObjects(this.colliderMeshes);
      l7.length !== 0 && l7[0].distance < t15 && (t15 = l7[0].distance);
    }
    return t15;
  }
  _getClientRect(t15) {
    if (!this._domElement) return;
    let e19 = this._domElement.getBoundingClientRect();
    return t15.x = e19.left, t15.y = e19.top, this._viewport ? (t15.x += this._viewport.x, t15.y += e19.height - this._viewport.w - this._viewport.y, t15.width = this._viewport.z, t15.height = this._viewport.w) : (t15.width = e19.width, t15.height = e19.height), t15;
  }
  _createOnRestPromise(t15) {
    return t15 ? Promise.resolve() : (this._hasRested = false, this.dispatchEvent({ type: "transitionstart" }), new Promise((e19) => {
      let s10 = () => {
        this.removeEventListener("rest", s10), e19();
      };
      this.addEventListener("rest", s10);
    }));
  }
  _addAllEventListeners(t15) {
  }
  _removeAllEventListeners() {
  }
  get dampingFactor() {
    return console.warn(".dampingFactor has been deprecated. use smoothTime (in seconds) instead."), 0;
  }
  set dampingFactor(t15) {
    console.warn(".dampingFactor has been deprecated. use smoothTime (in seconds) instead.");
  }
  get draggingDampingFactor() {
    return console.warn(".draggingDampingFactor has been deprecated. use draggingSmoothTime (in seconds) instead."), 0;
  }
  set draggingDampingFactor(t15) {
    console.warn(".draggingDampingFactor has been deprecated. use draggingSmoothTime (in seconds) instead.");
  }
  static createBoundingSphere(t15, e19 = new p8.Sphere()) {
    let s10 = e19, n17 = s10.center;
    B2.makeEmpty(), t15.traverseVisible((a11) => {
      a11.isMesh && B2.expandByObject(a11);
    }), B2.getCenter(n17);
    let r10 = 0;
    return t15.traverseVisible((a11) => {
      if (!a11.isMesh) return;
      let l7 = a11, m4 = l7.geometry.clone();
      m4.applyMatrix4(l7.matrixWorld);
      let v9 = m4.attributes.position;
      for (let g6 = 0, C2 = v9.count; g6 < C2; g6++) y4.fromBufferAttribute(v9, g6), r10 = Math.max(r10, n17.distanceToSquared(y4));
    }), s10.radius = Math.sqrt(r10), s10;
  }
};
function Yt(e19) {
  var s10 = e19, { smoothTime: u5 = 0.05 } = s10, t15 = y(s10, ["smoothTime"]);
  w2.install({ THREE: three_module_exports }), extend({ CameraControls: w2 });
  let n17 = useThree((h7) => h7.camera), r10 = useThree((h7) => h7.gl), a11 = v4(t15), [l7, m4] = (0, import_react4.useState)(false);
  return (0, import_react4.useEffect)(() => {
    let h7 = a11.current;
    if (!h7) return;
    let { type: v9, onCameraUpdate: g6 } = t15 || {};
    if (!g6) return;
    let C2 = (o11) => Math.round(o11 * 180 / Math.PI), f5 = () => ({ cAzimuthAngle: C2(h7.azimuthAngle), cPolarAngle: C2(h7.polarAngle) }), L = () => {
      var c3;
      let o11 = {};
      if (v9 === "sphere") {
        let d5 = h7 == null ? void 0 : h7.zoom;
        if (Number.isFinite(d5)) o11.cameraZoom = Number(d5.toFixed(2));
        else {
          let _ = (c3 = h7 == null ? void 0 : h7.camera) == null ? void 0 : c3.zoom;
          Number.isFinite(_) && (o11.cameraZoom = Number(_.toFixed(2)));
        }
      } else Number.isFinite(h7.distance) && (o11.cDistance = Number(h7.distance.toFixed(2)));
      return o11;
    }, T4 = () => {
      m4(true);
    }, A4 = () => {
      m4(false), g6(w(w({}, f5()), L()));
    };
    return h7.addEventListener("controlstart", T4), h7.addEventListener("rest", A4), () => {
      h7.removeEventListener("controlstart", T4), h7.removeEventListener("rest", A4);
    };
  }, [a11, t15]), (0, import_jsx_runtime5.jsx)("cameraControls", { ref: a11, args: [n17, r10.domElement], smoothTime: l7 ? 0 : u5, zoomSpeed: 10, dollySpeed: 5, maxDistance: 1e3, restThreshold: 0.01, mouseButtons: { left: w2.ACTION.ROTATE, middle: t15.type === "sphere" ? w2.ACTION.ZOOM : w2.ACTION.DOLLY, right: w2.ACTION.NONE, wheel: t15.type === "sphere" ? w2.ACTION.ZOOM : w2.ACTION.DOLLY }, touches: { one: w2.ACTION.ROTATE, two: w2.ACTION.NONE, three: w2.ACTION.NONE } });
}

// node_modules/@shadergradient/react/dist/chunk-MNG3BBZ4.mjs
var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
function l5(t15) {
  return (0, import_jsx_runtime6.jsx)(import_jsx_runtime6.Fragment, { children: (0, import_jsx_runtime6.jsx)(Yt, w({}, t15)) });
}

// node_modules/@shadergradient/react/dist/chunk-DTOHMYM5.mjs
var ie = class extends DataTextureLoader {
  constructor(e19) {
    super(e19), this.type = HalfFloatType;
  }
  parse(e19) {
    let a11 = function(n17, m4) {
      switch (n17) {
        case 1:
          throw new Error("THREE.RGBELoader: Read Error: " + (m4 || ""));
        case 2:
          throw new Error("THREE.RGBELoader: Write Error: " + (m4 || ""));
        case 3:
          throw new Error("THREE.RGBELoader: Bad File Format: " + (m4 || ""));
        default:
        case 4:
          throw new Error("THREE.RGBELoader: Memory Error: " + (m4 || ""));
      }
    }, b8 = `
`, L = function(n17, m4, l7) {
      m4 = m4 || 1024;
      let u5 = n17.pos, c3 = -1, s10 = 0, h7 = "", p10 = String.fromCharCode.apply(null, new Uint16Array(n17.subarray(u5, u5 + 128)));
      for (; 0 > (c3 = p10.indexOf(b8)) && s10 < m4 && u5 < n17.byteLength; ) h7 += p10, s10 += p10.length, u5 += 128, p10 += String.fromCharCode.apply(null, new Uint16Array(n17.subarray(u5, u5 + 128)));
      return -1 < c3 ? (l7 !== false && (n17.pos += s10 + c3 + 1), h7 + p10.slice(0, c3)) : false;
    }, C2 = function(n17) {
      let m4 = /^#\?(\S+)/, l7 = /^\s*GAMMA\s*=\s*(\d+(\.\d+)?)\s*$/, d5 = /^\s*EXPOSURE\s*=\s*(\d+(\.\d+)?)\s*$/, u5 = /^\s*FORMAT=(\S+)\s*$/, c3 = /^\s*\-Y\s+(\d+)\s+\+X\s+(\d+)\s*$/, s10 = { valid: 0, string: "", comments: "", programtype: "RGBE", format: "", gamma: 1, exposure: 1, width: 0, height: 0 }, h7, p10;
      for ((n17.pos >= n17.byteLength || !(h7 = L(n17))) && a11(1, "no header found"), (p10 = h7.match(m4)) || a11(3, "bad initial token"), s10.valid |= 1, s10.programtype = p10[1], s10.string += h7 + `
`; h7 = L(n17), h7 !== false; ) {
        if (s10.string += h7 + `
`, h7.charAt(0) === "#") {
          s10.comments += h7 + `
`;
          continue;
        }
        if ((p10 = h7.match(l7)) && (s10.gamma = parseFloat(p10[1])), (p10 = h7.match(d5)) && (s10.exposure = parseFloat(p10[1])), (p10 = h7.match(u5)) && (s10.valid |= 2, s10.format = p10[1]), (p10 = h7.match(c3)) && (s10.valid |= 4, s10.height = parseInt(p10[1], 10), s10.width = parseInt(p10[2], 10)), s10.valid & 2 && s10.valid & 4) break;
      }
      return s10.valid & 2 || a11(3, "missing format specifier"), s10.valid & 4 || a11(3, "missing image size specifier"), s10;
    }, ee = function(n17, m4, l7) {
      let d5 = m4;
      if (d5 < 8 || d5 > 32767 || n17[0] !== 2 || n17[1] !== 2 || n17[2] & 128) return new Uint8Array(n17);
      d5 !== (n17[2] << 8 | n17[3]) && a11(3, "wrong scanline width");
      let u5 = new Uint8Array(4 * m4 * l7);
      u5.length || a11(4, "unable to allocate buffer space");
      let c3 = 0, s10 = 0, h7 = 4 * d5, p10 = new Uint8Array(4), z3 = new Uint8Array(h7), pe = l7;
      for (; pe > 0 && s10 < n17.byteLength; ) {
        s10 + 4 > n17.byteLength && a11(1), p10[0] = n17[s10++], p10[1] = n17[s10++], p10[2] = n17[s10++], p10[3] = n17[s10++], (p10[0] != 2 || p10[1] != 2 || (p10[2] << 8 | p10[3]) != d5) && a11(3, "bad rgbe scanline format");
        let V6 = 0, R4;
        for (; V6 < h7 && s10 < n17.byteLength; ) {
          R4 = n17[s10++];
          let T4 = R4 > 128;
          if (T4 && (R4 -= 128), (R4 === 0 || V6 + R4 > h7) && a11(3, "bad scanline data"), T4) {
            let D3 = n17[s10++];
            for (let de = 0; de < R4; de++) z3[V6++] = D3;
          } else z3.set(n17.subarray(s10, s10 + R4), V6), V6 += R4, s10 += R4;
        }
        let Ee = d5;
        for (let T4 = 0; T4 < Ee; T4++) {
          let D3 = 0;
          u5[c3] = z3[T4 + D3], D3 += d5, u5[c3 + 1] = z3[T4 + D3], D3 += d5, u5[c3 + 2] = z3[T4 + D3], D3 += d5, u5[c3 + 3] = z3[T4 + D3], c3 += 4;
        }
        pe--;
      }
      return u5;
    }, le = function(n17, m4, l7, d5) {
      let u5 = n17[m4 + 3], c3 = Math.pow(2, u5 - 128) / 255;
      l7[d5 + 0] = n17[m4 + 0] * c3, l7[d5 + 1] = n17[m4 + 1] * c3, l7[d5 + 2] = n17[m4 + 2] * c3, l7[d5 + 3] = 1;
    }, k6 = function(n17, m4, l7, d5) {
      let u5 = n17[m4 + 3], c3 = Math.pow(2, u5 - 128) / 255;
      l7[d5 + 0] = DataUtils.toHalfFloat(Math.min(n17[m4 + 0] * c3, 65504)), l7[d5 + 1] = DataUtils.toHalfFloat(Math.min(n17[m4 + 1] * c3, 65504)), l7[d5 + 2] = DataUtils.toHalfFloat(Math.min(n17[m4 + 2] * c3, 65504)), l7[d5 + 3] = DataUtils.toHalfFloat(1);
    }, U4 = new Uint8Array(e19);
    U4.pos = 0;
    let G3 = C2(U4), j = G3.width, B5 = G3.height, _ = ee(U4.subarray(U4.pos), j, B5), te, re, F4;
    switch (this.type) {
      case FloatType:
        F4 = _.length / 4;
        let n17 = new Float32Array(F4 * 4);
        for (let l7 = 0; l7 < F4; l7++) le(_, l7 * 4, n17, l7 * 4);
        te = n17, re = FloatType;
        break;
      case HalfFloatType:
        F4 = _.length / 4;
        let m4 = new Uint16Array(F4 * 4);
        for (let l7 = 0; l7 < F4; l7++) k6(_, l7 * 4, m4, l7 * 4);
        te = m4, re = HalfFloatType;
        break;
      default:
        throw new Error("THREE.RGBELoader: Unsupported type: " + this.type);
    }
    return { width: j, height: B5, data: te, header: G3.string, gamma: G3.gamma, exposure: G3.exposure, type: re };
  }
  setDataType(e19) {
    return this.type = e19, this;
  }
  load(e19, t15, i9, r10) {
    function o11(a11, f5) {
      switch (a11.type) {
        case FloatType:
        case HalfFloatType:
          "colorSpace" in a11 ? a11.colorSpace = "srgb-linear" : a11.encoding = 3e3, a11.minFilter = LinearFilter, a11.magFilter = LinearFilter, a11.generateMipmaps = false, a11.flipY = true;
          break;
      }
      t15 && t15(a11, f5);
    }
    return super.load(e19, o11, i9, r10);
  }
};
var Se = new Box3();
var Y2 = new Vector3();
var $2 = parseInt(REVISION.replace(/\D+/g, ""));
var ne = $2 >= 125 ? "uv1" : "uv2";
var ae = new Vector4();
var xe = new Vector3();
var be = new Vector3();
var g4 = new Vector4();
var v5 = new Vector4();
var E3 = new Vector4();
var se = new Vector3();
var me = new Matrix4();
var y5 = new Line3();
var _e = new Vector3();
var J3 = new Box3();
var Z2 = new Sphere();
var M3 = new Vector4();

// node_modules/@shadergradient/react/dist/chunk-CRHASVN5.mjs
function s8(t15, { path: r10 }) {
  return useLoader(ie, t15, (u5) => u5.setPath(r10));
}

// node_modules/@shadergradient/react/dist/chunk-6MX7M6OR.mjs
var import_react5 = __toESM(require_react(), 1);
function v6(e19 = true, t15 = 0.1, r10 = "0px") {
  let [i9, u5] = (0, import_react5.useState)(!e19), n17 = (0, import_react5.useRef)(null);
  return (0, import_react5.useEffect)(() => {
    if (!e19) return;
    let s10 = new IntersectionObserver(([c3]) => {
      u5(c3.isIntersecting);
    }, { threshold: t15, rootMargin: r10 });
    return n17.current && s10.observe(n17.current), () => s10.disconnect();
  }, [e19, t15, r10]), { isInView: i9, containerRef: n17 };
}

// node_modules/@shadergradient/react/dist/chunk-O3EDKAOT.mjs
var import_react6 = __toESM(require_react(), 1);
var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);
var v7 = (0, import_react6.createContext)({});
var N2 = () => (0, import_react6.useContext)(v7);
function V3({ children: C2, style: u5 = {}, pixelDensity: n17 = 1, fov: a11 = 45, pointerEvents: c3, className: f5, envBasePath: o11, lazyLoad: s10 = true, threshold: p10 = 0.1, rootMargin: m4 = "0px", preserveDrawingBuffer: x5, powerPreference: S3 }) {
  let { isInView: g6, containerRef: l7 } = v6(s10, p10, m4), E4 = (0, import_react6.useMemo)(() => ({ envBasePath: o11 }), [o11]);
  return b4(), (0, import_jsx_runtime7.jsx)("div", { ref: l7, style: w({ width: "100%", height: "100%" }, u5), children: (!s10 || g6) && (0, import_jsx_runtime7.jsx)(v7.Provider, { value: E4, children: (0, import_jsx_runtime7.jsx)(Canvas, x(w({ id: "gradientCanvas", style: { pointerEvents: c3 }, resize: { offsetSize: true }, className: f5 }, r7(n17, a11, { preserveDrawingBuffer: x5, powerPreference: S3 })), { children: C2 }), n17 + a11) }) });
}
function b4() {
  (0, import_react6.useEffect)(() => {
    ShaderChunk.uv2_pars_vertex = "", ShaderChunk.uv2_vertex = "", ShaderChunk.uv2_pars_fragment = "", ShaderChunk.encodings_fragment = "";
  }, []);
}

// node_modules/@shadergradient/react/dist/chunk-V6TQH2YK.mjs
var import_react7 = __toESM(require_react(), 1);
var y6 = (e19) => e19.current && e19.current.isScene;
var v8 = (e19) => y6(e19) ? e19.current : e19;
function B3({ background: e19 = false, envPreset: i9 }) {
  let { envBasePath: u5 } = N2(), r10 = u5 || h6, l7 = s8("city.hdr", { path: r10 }), f5 = s8("dawn.hdr", { path: r10 }), m4 = s8("lobby.hdr", { path: r10 }), n17 = { city: l7, dawn: f5, lobby: m4 }[i9], a11 = useThree((t15) => t15.scene);
  import_react7.default.useLayoutEffect(() => {
    if (n17) {
      let t15 = v8(a11), h7 = t15.background, b8 = t15.environment;
      return e19 !== "only" && (t15.environment = n17), e19 && (t15.background = n17), () => {
        e19 !== "only" && (t15.environment = b8), e19 && (t15.background = "black");
      };
    }
  }, [a11, n17, e19]);
  let p10 = n17;
  return p10.mapping = EquirectangularReflectionMapping, null;
}

// node_modules/@shadergradient/react/dist/chunk-R5BBYD62.mjs
var import_react8 = __toESM(require_react(), 1);
var import_jsx_runtime8 = __toESM(require_jsx_runtime(), 1);
function p9({ lightType: t15, brightness: e19, envPreset: o11 }) {
  return (0, import_jsx_runtime8.jsxs)(import_jsx_runtime8.Fragment, { children: [t15 === "3d" && (0, import_jsx_runtime8.jsx)("ambientLight", { intensity: (e19 || 1) * Math.PI }), t15 === "env" && (0, import_jsx_runtime8.jsx)(import_react8.Suspense, { fallback: (0, import_jsx_runtime8.jsx)(a8, {}), children: (0, import_jsx_runtime8.jsx)(B3, { envPreset: o11, background: false, loadingCallback: () => {
  } }) })] });
}
function a8() {
  return (0, import_jsx_runtime8.jsx)("ambientLight", { intensity: 0.4 });
}

// node_modules/@shadergradient/react/dist/chunk-OM36AWEE.mjs
var o9 = __toESM(require_react(), 1);
function b5(r10, e19) {
  let n17 = useThree((t15) => t15.pointer), [c3] = o9.useState(() => {
    let t15 = new Raycaster();
    return e19 && applyProps(t15, e19, {}), function(p10, s10) {
      t15.setFromCamera(n17, r10 instanceof Camera ? r10 : r10.current);
      let a11 = this.constructor.prototype.raycast.bind(this);
      a11 && a11(t15, s10);
    };
  });
  return c3;
}

// node_modules/@shadergradient/react/dist/chunk-VL2CUAE4.mjs
var t9 = __toESM(require_react(), 1);
function d4(n17, m4, T4) {
  let { gl: c3, size: p10, viewport: u5 } = useThree(), o11 = typeof n17 == "number" ? n17 : p10.width * u5.dpr, a11 = typeof m4 == "number" ? m4 : p10.height * u5.dpr, g6 = (typeof n17 == "number" ? T4 : n17) || {}, { samples: r10 } = g6, f5 = y(g6, ["samples"]), s10 = t9.useMemo(() => {
    let i9;
    return i9 = new WebGLRenderTarget(o11, a11, w({ minFilter: LinearFilter, magFilter: LinearFilter, encoding: c3.outputEncoding, type: HalfFloatType }, f5)), i9.samples = r10, i9;
  }, []);
  return t9.useLayoutEffect(() => {
    s10.setSize(o11, a11), r10 && (s10.samples = r10);
  }, [r10, s10, o11, a11]), t9.useEffect(() => () => s10.dispose(), []), s10;
}

// node_modules/@shadergradient/react/dist/chunk-GXQ2EBSK.mjs
function t10(e19, n17) {
  if (typeof e19 == "function") return e19(n17);
  e19 && (e19.current = n17);
}
function u4(e19) {
  return (n17) => {
    for (let f5 of e19) t10(f5, n17);
  };
}

// node_modules/@shadergradient/react/dist/chunk-ST3I7JZX.mjs
var r9 = __toESM(require_react(), 1);
var import_jsx_runtime9 = __toESM(require_jsx_runtime(), 1);
var F3 = (a11) => typeof a11 == "function";
var N3 = r9.forwardRef((v9, H3) => {
  var R4 = v9, { envMap: a11, resolution: x5 = 256, frames: s10 = 1 / 0, children: n17, makeDefault: l7 } = R4, c3 = y(R4, ["envMap", "resolution", "frames", "children", "makeDefault"]);
  let u5 = useThree(({ set: e19 }) => e19), O3 = useThree(({ camera: e19 }) => e19), o11 = useThree(({ size: e19 }) => e19), t15 = r9.useRef(null), i9 = r9.useRef(null), p10 = d4(x5);
  r9.useLayoutEffect(() => {
    c3.manual || t15.current.updateProjectionMatrix();
  }, [o11, c3]), r9.useLayoutEffect(() => {
    t15.current.updateProjectionMatrix();
  }), r9.useLayoutEffect(() => {
    if (l7) {
      let e19 = O3;
      return u5(() => ({ camera: t15.current })), () => u5(() => ({ camera: e19 }));
    }
  }, [t15, l7, u5]);
  let g6 = 0, h7 = null, m4 = F3(n17);
  return useFrame((e19) => {
    m4 && (s10 === 1 / 0 || g6 < s10) && (i9.current.visible = false, e19.gl.setRenderTarget(p10), h7 = e19.scene.background, a11 && (e19.scene.background = a11), e19.gl.render(e19.scene, t15.current), e19.scene.background = h7, e19.gl.setRenderTarget(null), i9.current.visible = true, g6++);
  }), (0, import_jsx_runtime9.jsxs)(import_jsx_runtime9.Fragment, { children: [(0, import_jsx_runtime9.jsx)("orthographicCamera", x(w({ left: o11.width / -2, right: o11.width / 2, top: o11.height / 2, bottom: o11.height / -2, ref: u4([t15, H3]) }, c3), { children: !m4 && n17 })), (0, import_jsx_runtime9.jsx)("group", { ref: i9, children: m4 && n17(p10.texture) })] });
});

// node_modules/@shadergradient/react/dist/chunk-HHLYZM5K.mjs
var t11 = __toESM(require_react(), 1);
var import_jsx_runtime10 = __toESM(require_jsx_runtime(), 1);
var I4 = t11.createContext({});
var pt2 = () => t11.useContext(I4);
var N4 = 2 * Math.PI;
var O2 = new Object3D();
var q3 = new Matrix4();
var [a9, b6] = [new Quaternion(), new Quaternion()];
var S2 = new Vector3();
var T3 = new Vector3();
var Z3 = (n17) => "minPolarAngle" in n17;
var lt2 = ({ alignment: n17 = "bottom-right", margin: M5 = [80, 80], renderPriority: W = 0, autoClear: Q2 = true, onUpdate: h7, onTarget: s10, children: A4 }) => {
  let i9 = useThree(({ size: e19 }) => e19), r10 = useThree(({ camera: e19 }) => e19), o11 = useThree(({ controls: e19 }) => e19), f5 = useThree(({ gl: e19 }) => e19), u5 = useThree(({ scene: e19 }) => e19), d5 = useThree(({ invalidate: e19 }) => e19), p10 = t11.useRef(), C2 = t11.useRef(), l7 = t11.useRef(null), [R4] = t11.useState(() => new Scene()), y8 = t11.useRef(false), g6 = t11.useRef(0), x5 = t11.useRef(new Vector3(0, 0, 0)), w4 = t11.useRef(new Vector3(0, 0, 0));
  t11.useEffect(() => {
    w4.current.copy(r10.up);
  }, [r10]);
  let P5 = t11.useCallback((e19) => {
    y8.current = true, (o11 || s10) && (x5.current = (o11 == null ? void 0 : o11.target) || (s10 == null ? void 0 : s10())), g6.current = r10.position.distanceTo(S2), a9.copy(r10.quaternion), T3.copy(e19).multiplyScalar(g6.current).add(S2), O2.lookAt(T3), b6.copy(O2.quaternion), d5();
  }, [o11, r10, s10, d5]);
  t11.useEffect(() => (u5.background && (p10.current = u5.background, u5.background = null, R4.background = p10.current), () => {
    p10.current && (u5.background = p10.current);
  }), []), useFrame((e19, U4) => {
    var G3;
    if (l7.current && C2.current) {
      if (y8.current) if (a9.angleTo(b6) < 0.01) y8.current = false, Z3(o11) && r10.up.copy(w4.current);
      else {
        let Y3 = U4 * N4;
        a9.rotateTowards(b6, Y3), r10.position.set(0, 0, 1).applyQuaternion(a9).multiplyScalar(g6.current).add(x5.current), r10.up.set(0, 1, 0).applyQuaternion(a9).normalize(), r10.quaternion.copy(a9), h7 ? h7() : o11 && o11.update(), d5();
      }
      q3.copy(r10.matrix).invert(), (G3 = C2.current) == null || G3.quaternion.setFromRotationMatrix(q3), Q2 && (f5.autoClear = false), f5.clearDepth(), f5.render(R4, l7.current);
    }
  }, W);
  let D3 = b5(l7), F4 = t11.useMemo(() => ({ tweenCamera: P5, raycast: D3 }), [P5]), [z3, v9] = M5, X2 = n17.endsWith("-center") ? 0 : n17.endsWith("-left") ? -i9.width / 2 + z3 : i9.width / 2 - z3, J5 = n17.startsWith("center-") ? 0 : n17.startsWith("top-") ? i9.height / 2 - v9 : -i9.height / 2 + v9;
  return createPortal((0, import_jsx_runtime10.jsxs)(I4.Provider, { value: F4, children: [(0, import_jsx_runtime10.jsx)(N3, { ref: l7, position: [0, 0, 200] }), (0, import_jsx_runtime10.jsx)("group", { ref: C2, position: [X2, J5, 0], children: A4 })] }), R4);
};

// node_modules/@shadergradient/react/dist/chunk-MNIYBOZ2.mjs
var b7 = __toESM(require_react(), 1);
var import_jsx_runtime11 = __toESM(require_jsx_runtime(), 1);
function A3({ scale: c3 = [0.8, 0.05, 0.05], color: s10, rotation: r10 }) {
  return (0, import_jsx_runtime11.jsx)("group", { rotation: r10, children: (0, import_jsx_runtime11.jsxs)("mesh", { position: [0.4, 0, 0], children: [(0, import_jsx_runtime11.jsx)("boxGeometry", { args: c3 }), (0, import_jsx_runtime11.jsx)("meshBasicMaterial", { color: s10, toneMapped: false })] }) });
}
function l6(S3) {
  var h7 = S3, { onClick: c3, font: s10, disabled: r10, arcStyle: g6, label: i9, labelColor: d5, axisHeadScale: p10 = 1 } = h7, u5 = y(h7, ["onClick", "font", "disabled", "arcStyle", "label", "labelColor", "axisHeadScale"]);
  let f5 = useThree((o11) => o11.gl), M5 = b7.useMemo(() => {
    let o11 = document.createElement("canvas");
    o11.width = 64, o11.height = 64;
    let e19 = o11.getContext("2d");
    return e19.beginPath(), e19.arc(32, 32, 16, 0, 2 * Math.PI), e19.closePath(), e19.fillStyle = g6, e19.fill(), i9 && (e19.font = s10, e19.textAlign = "center", e19.fillStyle = d5, e19.fillText(i9, 32, 41)), new CanvasTexture(o11);
  }, [g6, i9, d5, s10]), [P5, a11] = b7.useState(false), m4 = (i9 ? 1 : 0.75) * (P5 ? 1.2 : 1) * p10;
  return (0, import_jsx_runtime11.jsx)("sprite", x(w({ scale: m4, onPointerOver: r10 ? void 0 : (o11) => {
    o11.stopPropagation(), a11(true);
  }, onPointerOut: r10 ? void 0 : c3 || ((o11) => {
    o11.stopPropagation(), a11(false);
  }) }, u5), { children: (0, import_jsx_runtime11.jsx)("spriteMaterial", { map: M5, "map-encoding": f5.outputEncoding, "map-anisotropy": f5.capabilities.getMaxAnisotropy() || 1, alphaTest: 0.3, opacity: i9 ? 1 : 0.75, toneMapped: false }) }));
}
var V5 = (M5) => {
  var P5 = M5, { hideNegativeAxes: c3, hideAxisHeads: s10, disabled: r10, font: g6 = "18px Inter var, Arial, sans-serif", axisColors: i9 = ["#ff2060", "#20df80", "#2080ff"], axisHeadScale: d5 = 1, axisScale: p10, labels: u5 = ["X", "Y", "Z"], labelColor: S3 = "#000", onClick: h7 } = P5, f5 = y(P5, ["hideNegativeAxes", "hideAxisHeads", "disabled", "font", "axisColors", "axisHeadScale", "axisScale", "labels", "labelColor", "onClick"]);
  let [a11, m4, v9] = i9, { tweenCamera: T4, raycast: o11 } = pt2(), e19 = { font: g6, disabled: r10, labelColor: S3, raycast: o11, onClick: h7, axisHeadScale: d5, onPointerDown: r10 ? void 0 : (C2) => {
    T4(C2.object.position), C2.stopPropagation();
  } };
  return (0, import_jsx_runtime11.jsxs)("group", x(w({ scale: 40 }, f5), { children: [(0, import_jsx_runtime11.jsx)(A3, { color: a11, rotation: [0, 0, 0], scale: p10 }), (0, import_jsx_runtime11.jsx)(A3, { color: m4, rotation: [0, 0, Math.PI / 2], scale: p10 }), (0, import_jsx_runtime11.jsx)(A3, { color: v9, rotation: [0, -Math.PI / 2, 0], scale: p10 }), !s10 && (0, import_jsx_runtime11.jsxs)(import_jsx_runtime11.Fragment, { children: [(0, import_jsx_runtime11.jsx)(l6, w({ arcStyle: a11, position: [1, 0, 0], label: u5[0] }, e19)), (0, import_jsx_runtime11.jsx)(l6, w({ arcStyle: m4, position: [0, 1, 0], label: u5[1] }, e19)), (0, import_jsx_runtime11.jsx)(l6, w({ arcStyle: v9, position: [0, 0, 1], label: u5[2] }, e19)), !c3 && (0, import_jsx_runtime11.jsxs)(import_jsx_runtime11.Fragment, { children: [(0, import_jsx_runtime11.jsx)(l6, w({ arcStyle: a11, position: [-1, 0, 0] }, e19)), (0, import_jsx_runtime11.jsx)(l6, w({ arcStyle: m4, position: [0, -1, 0] }, e19)), (0, import_jsx_runtime11.jsx)(l6, w({ arcStyle: v9, position: [0, 0, -1] }, e19))] })] }), (0, import_jsx_runtime11.jsx)("ambientLight", { intensity: 0.5 }), (0, import_jsx_runtime11.jsx)("pointLight", { position: [10, 10, 10], intensity: 0.5 })] }));
};

// node_modules/@shadergradient/react/dist/chunk-4UW34ZH3.mjs
var import_jsx_runtime12 = __toESM(require_jsx_runtime(), 1);
function a10({ margin: o11 = [65, 110] }) {
  return (0, import_jsx_runtime12.jsx)(import_jsx_runtime12.Fragment, { children: (0, import_jsx_runtime12.jsx)(lt2, { alignment: "bottom-right", margin: o11, renderPriority: 2, children: (0, import_jsx_runtime12.jsx)(V5, { axisColors: ["#FF430A", "#FF430A", "#FF430A"], labelColor: "white", hideNegativeAxes: true, axisHeadScale: 0.8 }) }) });
}

// node_modules/@shadergradient/react/dist/chunk-PMGWUTR2.mjs
var e18 = { halo: { title: "Halo", color: "white", props: { type: "plane", uAmplitude: 1, uDensity: 1.3, uSpeed: 0.4, uStrength: 4, uTime: 0, uFrequency: 5.5, range: "disabled", rangeStart: 0, rangeEnd: 40, frameRate: 10, destination: "onCanvas", format: "gif", axesHelper: "off", brightness: 1.2, cAzimuthAngle: 180, cDistance: 3.6, cPolarAngle: 90, cameraZoom: 1, color1: "#ff5005", color2: "#dbba95", color3: "#d0bce1", embedMode: "off", envPreset: "city", gizmoHelper: "hide", grain: "on", lightType: "3d", pixelDensity: 1, fov: 45, positionX: -1.4, positionY: 0, positionZ: 0, reflection: 0.1, rotationX: 0, rotationY: 10, rotationZ: 50, shader: "defaults", animate: "on", wireframe: false } }, pensive: { title: "Pensive", color: "white", props: { range: "disabled", rangeStart: 0, rangeEnd: 40, frameRate: 10, destination: "onCanvas", format: "gif", animate: "on", axesHelper: "off", brightness: 1.5, cAzimuthAngle: 250, cDistance: 1.5, cPolarAngle: 140, cameraZoom: 12.5, color1: "#809bd6", color2: "#910aff", color3: "#af38ff", embedMode: "off", envPreset: "city", gizmoHelper: "hide", grain: "on", lightType: "3d", pixelDensity: 1, fov: 45, positionX: 0, positionY: 0, positionZ: 0, reflection: 0.5, rotationX: 0, rotationY: 0, rotationZ: 140, shader: "defaults", type: "sphere", uAmplitude: 7, uDensity: 0.8, uFrequency: 5.5, uSpeed: 0.3, uStrength: 0.4, uTime: 0, wireframe: false } }, mint: { title: "Mint", color: "white", props: { range: "disabled", rangeStart: 0, rangeEnd: 40, frameRate: 10, destination: "onCanvas", format: "gif", animate: "on", axesHelper: "off", brightness: 1.2, cAzimuthAngle: 170, cDistance: 4.4, cPolarAngle: 70, cameraZoom: 1, color1: "#94ffd1", color2: "#6bf5ff", color3: "#ffffff", embedMode: "off", envPreset: "city", gizmoHelper: "hide", grain: "off", lightType: "3d", pixelDensity: 1, fov: 45, positionX: 0, positionY: 0.9, positionZ: -0.3, reflection: 0.1, rotationX: 45, rotationY: 0, rotationZ: 0, shader: "defaults", type: "waterPlane", uAmplitude: 0, uDensity: 1.2, uFrequency: 0, uSpeed: 0.2, uStrength: 3.4, uTime: 0, wireframe: false } }, interstella: { title: "Interstella", color: "white", props: { range: "disabled", rangeStart: 0, rangeEnd: 40, frameRate: 10, destination: "onCanvas", format: "gif", animate: "on", axesHelper: "off", brightness: 0.8, cAzimuthAngle: 270, cDistance: 0.5, cPolarAngle: 180, cameraZoom: 15.1, color1: "#73bfc4", color2: "#ff810a", color3: "#8da0ce", embedMode: "off", envPreset: "city", gizmoHelper: "hide", grain: "on", lightType: "env", pixelDensity: 1, fov: 45, positionX: -0.1, positionY: 0, positionZ: 0, reflection: 0.4, rotationX: 0, rotationY: 130, rotationZ: 70, shader: "defaults", type: "sphere", uAmplitude: 3.2, uDensity: 0.8, uFrequency: 5.5, uSpeed: 0.3, uStrength: 0.3, uTime: 0, wireframe: false } }, nightyNight: { title: "Nighty night", color: "white", props: { range: "disabled", rangeStart: 0, rangeEnd: 40, frameRate: 10, destination: "onCanvas", format: "gif", animate: "on", axesHelper: "off", brightness: 1, cAzimuthAngle: 180, cDistance: 2.8, cPolarAngle: 80, cameraZoom: 9.1, color1: "#606080", color2: "#8d7dca", color3: "#212121", embedMode: "off", envPreset: "city", gizmoHelper: "hide", grain: "on", lightType: "3d", pixelDensity: 1, fov: 45, positionX: 0, positionY: 0, positionZ: 0, reflection: 0.1, rotationX: 50, rotationY: 0, rotationZ: -60, shader: "defaults", type: "waterPlane", uAmplitude: 0, uDensity: 1.5, uFrequency: 0, uSpeed: 0.3, uStrength: 1.5, uTime: 8, wireframe: false } }, violaOrientalis: { title: "Viola", color: "white", props: { range: "disabled", rangeStart: 0, rangeEnd: 40, frameRate: 10, destination: "onCanvas", format: "gif", animate: "on", axesHelper: "on", brightness: 1.1, cAzimuthAngle: 0, cDistance: 7.1, cPolarAngle: 140, cameraZoom: 17.3, color1: "#ffffff", color2: "#ffbb00", color3: "#0700ff", embedMode: "off", envPreset: "city", grain: "off", lightType: "3d", pixelDensity: 1, fov: 45, positionX: 0, positionY: 0, positionZ: 0, reflection: 0.1, rotationX: 0, rotationY: 0, rotationZ: 0, shader: "defaults", type: "sphere", uAmplitude: 1.4, uDensity: 1.1, uSpeed: 0.1, uStrength: 1, uTime: 0, uFrequency: 5.5, wireframe: false } }, universe: { title: "Universe", color: "white", props: { range: "disabled", rangeStart: 0, rangeEnd: 40, frameRate: 10, destination: "onCanvas", format: "gif", animate: "on", axesHelper: "on", brightness: 1.1, cAzimuthAngle: 180, cDistance: 3.9, cPolarAngle: 115, cameraZoom: 1, color1: "#5606ff", color2: "#fe8989", color3: "#000000", embedMode: "off", envPreset: "city", grain: "off", lightType: "3d", pixelDensity: 1, fov: 45, positionX: -0.5, positionY: 0.1, positionZ: 0, reflection: 0.1, rotationX: 0, rotationY: 0, rotationZ: 235, shader: "defaults", type: "waterPlane", uAmplitude: 0, uDensity: 1.1, uSpeed: 0.1, uStrength: 2.4, uTime: 0.2, uFrequency: 5.5, wireframe: false } }, sunset: { title: "Sunset", color: "white", props: { range: "disabled", rangeStart: 0, rangeEnd: 40, frameRate: 10, destination: "onCanvas", format: "gif", animate: "on", axesHelper: "on", bgColor1: "#000000", bgColor2: "#000000", brightness: 1.5, cAzimuthAngle: 60, cDistance: 7.1, cPolarAngle: 90, cameraZoom: 15.3, color1: "#ff7a33", color2: "#33a0ff", color3: "#ffc53d", embedMode: "off", envPreset: "dawn", grain: "off", lightType: "3d", pixelDensity: 1, fov: 45, positionX: 0, positionY: -0.15, positionZ: 0, reflection: 0.1, rotationX: 0, rotationY: 0, rotationZ: 0, shader: "defaults", type: "sphere", uAmplitude: 1.4, uDensity: 1.1, uSpeed: 0.1, uStrength: 0.4, uTime: 0, uFrequency: 5.5, wireframe: false } }, mandarin: { title: "Mandarin", color: "white", props: { range: "disabled", rangeStart: 0, rangeEnd: 40, frameRate: 10, destination: "onCanvas", format: "gif", animate: "on", axesHelper: "on", bgColor1: "#000000", bgColor2: "#000000", brightness: 1.2, cAzimuthAngle: 180, cDistance: 2.4, cPolarAngle: 95, cameraZoom: 1, color1: "#ff6a1a", color2: "#c73c00", color3: "#FD4912", embedMode: "off", envPreset: "city", grain: "off", lightType: "3d", pixelDensity: 1, fov: 45, positionX: 0, positionY: -2.1, positionZ: 0, reflection: 0.1, rotationX: 0, rotationY: 0, rotationZ: 225, shader: "defaults", type: "waterPlane", uAmplitude: 0, uDensity: 1.8, uSpeed: 0.2, uStrength: 3, uTime: 0.2, uFrequency: 5.5, wireframe: false } }, cottonCandy: { title: "Cotton Candy", color: "white", props: { range: "disabled", rangeStart: 0, rangeEnd: 40, frameRate: 10, destination: "onCanvas", format: "gif", animate: "on", axesHelper: "off", brightness: 1.2, cAzimuthAngle: 180, cDistance: 2.9, cPolarAngle: 120, cameraZoom: 1, color1: "#ebedff", color2: "#f3f2f8", color3: "#dbf8ff", embedMode: "off", envPreset: "city", grain: "off", lightType: "3d", pixelDensity: 1, fov: 45, positionX: 0, positionY: 1.8, positionZ: 0, reflection: 0.1, rotationX: 0, rotationY: 0, rotationZ: -90, shader: "defaults", type: "waterPlane", uAmplitude: 0, uDensity: 1, uSpeed: 0.3, uStrength: 3, uTime: 0.2, uFrequency: 5.5, wireframe: false } } };
var t14 = 0;
var o10 = Object.values(e18);

// node_modules/@shadergradient/react/dist/chunk-KLJOXLT7.mjs
var import_jsx_runtime13 = __toESM(require_jsx_runtime(), 1);
var U3 = z((dr, w4) => {
  "use strict";
  w4.exports = (r10) => encodeURIComponent(r10).replace(/[!'()*]/g, (e19) => `%${e19.charCodeAt(0).toString(16).toUpperCase()}`);
});
var D2 = z((or, T4) => {
  "use strict";
  var q4 = "%[a-f0-9]{2}", $4 = new RegExp("(" + q4 + ")|([^%]+?)", "gi"), I5 = new RegExp("(" + q4 + ")+", "gi");
  function h7(r10, e19) {
    try {
      return [decodeURIComponent(r10.join(""))];
    } catch (a11) {
    }
    if (r10.length === 1) return r10;
    e19 = e19 || 1;
    var t15 = r10.slice(0, e19), n17 = r10.slice(e19);
    return Array.prototype.concat.call([], h7(t15), h7(n17));
  }
  function Z4(r10) {
    try {
      return decodeURIComponent(r10);
    } catch (n17) {
      for (var e19 = r10.match($4) || [], t15 = 1; t15 < e19.length; t15++) r10 = h7(e19, t15).join(""), e19 = r10.match($4) || [];
      return r10;
    }
  }
  function k6(r10) {
    for (var e19 = { "%FE%FF": "��", "%FF%FE": "��" }, t15 = I5.exec(r10); t15; ) {
      try {
        e19[t15[0]] = decodeURIComponent(t15[0]);
      } catch (i9) {
        var n17 = Z4(t15[0]);
        n17 !== t15[0] && (e19[t15[0]] = n17);
      }
      t15 = I5.exec(r10);
    }
    e19["%C2"] = "�";
    for (var a11 = Object.keys(e19), s10 = 0; s10 < a11.length; s10++) {
      var f5 = a11[s10];
      r10 = r10.replace(new RegExp(f5, "g"), e19[f5]);
    }
    return r10;
  }
  T4.exports = function(r10) {
    if (typeof r10 != "string") throw new TypeError("Expected `encodedURI` to be of type `string`, got `" + typeof r10 + "`");
    try {
      return r10 = r10.replace(/\+/g, " "), decodeURIComponent(r10);
    } catch (e19) {
      return k6(r10);
    }
  };
});
var B4 = z((mr, R4) => {
  "use strict";
  R4.exports = (r10, e19) => {
    if (!(typeof r10 == "string" && typeof e19 == "string")) throw new TypeError("Expected the arguments to be of type `string`");
    if (e19 === "") return [r10];
    let t15 = r10.indexOf(e19);
    return t15 === -1 ? [r10] : [r10.slice(0, t15), r10.slice(t15 + e19.length)];
  };
});
var M4 = z((ur, L) => {
  "use strict";
  L.exports = function(r10, e19) {
    for (var t15 = {}, n17 = Object.keys(r10), a11 = Array.isArray(e19), s10 = 0; s10 < n17.length; s10++) {
      var f5 = n17[s10], i9 = r10[f5];
      (a11 ? e19.indexOf(f5) !== -1 : e19(f5, i9, r10)) && (t15[f5] = i9);
    }
    return t15;
  };
});
var J4 = z((d5) => {
  "use strict";
  var v9 = U3(), rr = D2(), V6 = B4(), er = M4(), tr = (r10) => r10 == null, y8 = Symbol("encodeFragmentIdentifier");
  function nr(r10) {
    switch (r10.arrayFormat) {
      case "index":
        return (e19) => (t15, n17) => {
          let a11 = t15.length;
          return n17 === void 0 || r10.skipNull && n17 === null || r10.skipEmptyString && n17 === "" ? t15 : n17 === null ? [...t15, [c3(e19, r10), "[", a11, "]"].join("")] : [...t15, [c3(e19, r10), "[", c3(a11, r10), "]=", c3(n17, r10)].join("")];
        };
      case "bracket":
        return (e19) => (t15, n17) => n17 === void 0 || r10.skipNull && n17 === null || r10.skipEmptyString && n17 === "" ? t15 : n17 === null ? [...t15, [c3(e19, r10), "[]"].join("")] : [...t15, [c3(e19, r10), "[]=", c3(n17, r10)].join("")];
      case "colon-list-separator":
        return (e19) => (t15, n17) => n17 === void 0 || r10.skipNull && n17 === null || r10.skipEmptyString && n17 === "" ? t15 : n17 === null ? [...t15, [c3(e19, r10), ":list="].join("")] : [...t15, [c3(e19, r10), ":list=", c3(n17, r10)].join("")];
      case "comma":
      case "separator":
      case "bracket-separator": {
        let e19 = r10.arrayFormat === "bracket-separator" ? "[]=" : "=";
        return (t15) => (n17, a11) => a11 === void 0 || r10.skipNull && a11 === null || r10.skipEmptyString && a11 === "" ? n17 : (a11 = a11 === null ? "" : a11, n17.length === 0 ? [[c3(t15, r10), e19, c3(a11, r10)].join("")] : [[n17, c3(a11, r10)].join(r10.arrayFormatSeparator)]);
      }
      default:
        return (e19) => (t15, n17) => n17 === void 0 || r10.skipNull && n17 === null || r10.skipEmptyString && n17 === "" ? t15 : n17 === null ? [...t15, c3(e19, r10)] : [...t15, [c3(e19, r10), "=", c3(n17, r10)].join("")];
    }
  }
  function ar(r10) {
    let e19;
    switch (r10.arrayFormat) {
      case "index":
        return (t15, n17, a11) => {
          if (e19 = /\[(\d*)\]$/.exec(t15), t15 = t15.replace(/\[\d*\]$/, ""), !e19) {
            a11[t15] = n17;
            return;
          }
          a11[t15] === void 0 && (a11[t15] = {}), a11[t15][e19[1]] = n17;
        };
      case "bracket":
        return (t15, n17, a11) => {
          if (e19 = /(\[\])$/.exec(t15), t15 = t15.replace(/\[\]$/, ""), !e19) {
            a11[t15] = n17;
            return;
          }
          if (a11[t15] === void 0) {
            a11[t15] = [n17];
            return;
          }
          a11[t15] = [].concat(a11[t15], n17);
        };
      case "colon-list-separator":
        return (t15, n17, a11) => {
          if (e19 = /(:list)$/.exec(t15), t15 = t15.replace(/:list$/, ""), !e19) {
            a11[t15] = n17;
            return;
          }
          if (a11[t15] === void 0) {
            a11[t15] = [n17];
            return;
          }
          a11[t15] = [].concat(a11[t15], n17);
        };
      case "comma":
      case "separator":
        return (t15, n17, a11) => {
          let s10 = typeof n17 == "string" && n17.includes(r10.arrayFormatSeparator), f5 = typeof n17 == "string" && !s10 && o11(n17, r10).includes(r10.arrayFormatSeparator);
          n17 = f5 ? o11(n17, r10) : n17;
          let i9 = s10 || f5 ? n17.split(r10.arrayFormatSeparator).map((l7) => o11(l7, r10)) : n17 === null ? n17 : o11(n17, r10);
          a11[t15] = i9;
        };
      case "bracket-separator":
        return (t15, n17, a11) => {
          let s10 = /(\[\])$/.test(t15);
          if (t15 = t15.replace(/\[\]$/, ""), !s10) {
            a11[t15] = n17 && o11(n17, r10);
            return;
          }
          let f5 = n17 === null ? [] : n17.split(r10.arrayFormatSeparator).map((i9) => o11(i9, r10));
          if (a11[t15] === void 0) {
            a11[t15] = f5;
            return;
          }
          a11[t15] = [].concat(a11[t15], f5);
        };
      default:
        return (t15, n17, a11) => {
          if (a11[t15] === void 0) {
            a11[t15] = n17;
            return;
          }
          a11[t15] = [].concat(a11[t15], n17);
        };
    }
  }
  function G3(r10) {
    if (typeof r10 != "string" || r10.length !== 1) throw new TypeError("arrayFormatSeparator must be single character string");
  }
  function c3(r10, e19) {
    return e19.encode ? e19.strict ? v9(r10) : encodeURIComponent(r10) : r10;
  }
  function o11(r10, e19) {
    return e19.decode ? rr(r10) : r10;
  }
  function H3(r10) {
    return Array.isArray(r10) ? r10.sort() : typeof r10 == "object" ? H3(Object.keys(r10)).sort((e19, t15) => Number(e19) - Number(t15)).map((e19) => r10[e19]) : r10;
  }
  function Q2(r10) {
    let e19 = r10.indexOf("#");
    return e19 !== -1 && (r10 = r10.slice(0, e19)), r10;
  }
  function fr(r10) {
    let e19 = "", t15 = r10.indexOf("#");
    return t15 !== -1 && (e19 = r10.slice(t15)), e19;
  }
  function _(r10) {
    r10 = Q2(r10);
    let e19 = r10.indexOf("?");
    return e19 === -1 ? "" : r10.slice(e19 + 1);
  }
  function P5(r10, e19) {
    return e19.parseNumbers && !Number.isNaN(Number(r10)) && typeof r10 == "string" && r10.trim() !== "" ? r10 = Number(r10) : e19.parseBooleans && r10 !== null && (r10.toLowerCase() === "true" || r10.toLowerCase() === "false") && (r10 = r10.toLowerCase() === "true"), r10;
  }
  function z3(r10, e19) {
    e19 = Object.assign({ decode: true, sort: true, arrayFormat: "none", arrayFormatSeparator: ",", parseNumbers: false, parseBooleans: false }, e19), G3(e19.arrayFormatSeparator);
    let t15 = ar(e19), n17 = /* @__PURE__ */ Object.create(null);
    if (typeof r10 != "string" || (r10 = r10.trim().replace(/^[?#&]/, ""), !r10)) return n17;
    for (let a11 of r10.split("&")) {
      if (a11 === "") continue;
      let [s10, f5] = V6(e19.decode ? a11.replace(/\+/g, " ") : a11, "=");
      f5 = f5 === void 0 ? null : ["comma", "separator", "bracket-separator"].includes(e19.arrayFormat) ? f5 : o11(f5, e19), t15(o11(s10, e19), f5, n17);
    }
    for (let a11 of Object.keys(n17)) {
      let s10 = n17[a11];
      if (typeof s10 == "object" && s10 !== null) for (let f5 of Object.keys(s10)) s10[f5] = P5(s10[f5], e19);
      else n17[a11] = P5(s10, e19);
    }
    return e19.sort === false ? n17 : (e19.sort === true ? Object.keys(n17).sort() : Object.keys(n17).sort(e19.sort)).reduce((a11, s10) => {
      let f5 = n17[s10];
      return f5 && typeof f5 == "object" && !Array.isArray(f5) ? a11[s10] = H3(f5) : a11[s10] = f5, a11;
    }, /* @__PURE__ */ Object.create(null));
  }
  d5.extract = _;
  d5.parse = z3;
  d5.stringify = (r10, e19) => {
    if (!r10) return "";
    e19 = Object.assign({ encode: true, strict: true, arrayFormat: "none", arrayFormatSeparator: "," }, e19), G3(e19.arrayFormatSeparator);
    let t15 = (f5) => e19.skipNull && tr(r10[f5]) || e19.skipEmptyString && r10[f5] === "", n17 = nr(e19), a11 = {};
    for (let f5 of Object.keys(r10)) t15(f5) || (a11[f5] = r10[f5]);
    let s10 = Object.keys(a11);
    return e19.sort !== false && s10.sort(e19.sort), s10.map((f5) => {
      let i9 = r10[f5];
      return i9 === void 0 ? "" : i9 === null ? c3(f5, e19) : Array.isArray(i9) ? i9.length === 0 && e19.arrayFormat === "bracket-separator" ? c3(f5, e19) + "[]" : i9.reduce(n17(f5), []).join("&") : c3(f5, e19) + "=" + c3(i9, e19);
    }).filter((f5) => f5.length > 0).join("&");
  };
  d5.parseUrl = (r10, e19) => {
    e19 = Object.assign({ decode: true }, e19);
    let [t15, n17] = V6(r10, "#");
    return Object.assign({ url: t15.split("?")[0] || "", query: z3(_(r10), e19) }, e19 && e19.parseFragmentIdentifier && n17 ? { fragmentIdentifier: o11(n17, e19) } : {});
  };
  d5.stringifyUrl = (r10, e19) => {
    e19 = Object.assign({ encode: true, strict: true, [y8]: true }, e19);
    let t15 = Q2(r10.url).split("?")[0] || "", n17 = d5.extract(r10.url), a11 = d5.parse(n17, { sort: false }), s10 = Object.assign(a11, r10.query), f5 = d5.stringify(s10, e19);
    f5 && (f5 = `?${f5}`);
    let i9 = fr(r10.url);
    return r10.fragmentIdentifier && (i9 = `#${e19[y8] ? c3(r10.fragmentIdentifier, e19) : r10.fragmentIdentifier}`), `${t15}${f5}${i9}`;
  };
  d5.pick = (r10, e19, t15) => {
    t15 = Object.assign({ parseFragmentIdentifier: true, [y8]: false }, t15);
    let { url: n17, query: a11, fragmentIdentifier: s10 } = d5.parseUrl(r10, t15);
    return d5.stringifyUrl({ url: n17, query: er(a11, e19), fragmentIdentifier: s10 }, t15);
  };
  d5.exclude = (r10, e19, t15) => {
    let n17 = Array.isArray(e19) ? (a11) => !e19.includes(a11) : (a11, s10) => !e19(a11, s10);
    return d5.pick(r10, n17, t15);
  };
});
var K2 = B(J4());
function Sr(r10) {
  let p10 = w(w({}, e18.halo.props), r10), { control: e19, urlString: t15, onCameraUpdate: n17 } = p10, s10 = y(p10, ["control", "urlString", "onCameraUpdate"]);
  e19 === "query" && (s10 = K2.parse(I(t15), { parseNumbers: true, parseBooleans: true, arrayFormat: "index" }));
  let b8 = s10, { lightType: f5, envPreset: i9, brightness: l7, grain: W, toggleAxis: X2 } = b8, ir = y(b8, ["lightType", "envPreset", "brightness", "grain", "toggleAxis"]);
  return (0, import_jsx_runtime13.jsxs)(import_jsx_runtime13.Fragment, { children: [(0, import_jsx_runtime13.jsx)(J, w({}, s10)), (0, import_jsx_runtime13.jsx)(p9, { lightType: f5, brightness: l7, envPreset: i9 }), W !== "off" && (0, import_jsx_runtime13.jsx)(k3, {}), X2 && (0, import_jsx_runtime13.jsx)(a10, {}), (0, import_jsx_runtime13.jsx)(l5, x(w({}, s10), { onCameraUpdate: n17 }))] });
}
export {
  Sr as ShaderGradient,
  V3 as ShaderGradientCanvas,
  M as formatFramerProps,
  I as formatUrlString,
  t14 as initialActivePreset,
  e18 as presets,
  o10 as presetsArray,
  i as propertyControls,
  N2 as useShaderGradientCanvasContext
};
/*! Bundled license information:

@shadergradient/react/dist/chunk-6DNZ3I5B.mjs:
  (*! Bundled license information:
  
  camera-controls/dist/camera-controls.module.js:
    (*!
     * camera-controls
     * https://github.com/yomotsu/camera-controls
     * (c) 2017 @yomotsu
     * Released under the MIT License.
     *)
  *)
*/
//# sourceMappingURL=@shadergradient_react.js.map
