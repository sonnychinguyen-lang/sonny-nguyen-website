import Controller, { ControllerProps } from "./controller.js";
import ViewState, { type ConstraintContext } from "./view-state.js";
import type Viewport from "../viewports/viewport.js";
import LinearInterpolator from "../transitions/linear-interpolator.js";
import type { MjolnirGestureEvent } from 'mjolnir.js';
export type OrthographicStateProps = {
    width: number;
    height: number;
    target?: number[];
    zoom?: number | number[];
    zoomX?: number;
    zoomY?: number;
    zoomAxis?: 'X' | 'Y' | 'all';
    /** Viewport constraints */
    maxZoomX?: number;
    minZoomX?: number;
    maxZoomY?: number;
    minZoomY?: number;
    maxBounds?: ControllerProps['maxBounds'];
    /** Padding inside the viewport used when fitting `maxBounds`. Defaults to `0`. */
    maxBoundsPadding?: ControllerProps['maxBoundsPadding'];
    /** Enables elastic bounds and zoom constraints during interaction. Defaults to `false`. */
    rubberBand?: boolean;
};
type OrthographicStateInternal = {
    startPanPosition?: number[];
    startZoomPosition?: [number, number];
    startZoom?: number[];
};
export declare class OrthographicState extends ViewState<OrthographicState, OrthographicStateProps, OrthographicStateInternal> {
    constructor(options: OrthographicStateProps & OrthographicStateInternal & {
        maxZoom?: number;
        minZoom?: number;
        makeViewport: (props: Record<string, any>) => Viewport;
        constraintContext?: ConstraintContext;
    });
    /**
     * Start panning
     * @param {[Number, Number]} pos - position on screen where the pointer grabs
     */
    panStart({ pos }: {
        pos: [number, number];
    }, constraintContext?: ConstraintContext): OrthographicState;
    /**
     * Pan
     * @param {[Number, Number]} pos - position on screen where the pointer is
     */
    pan({ pos, startPosition }: {
        pos: [number, number];
        startPosition?: number[];
    }, constraintContext?: ConstraintContext): OrthographicState;
    /**
     * End panning
     * Must call if `panStart()` was called
     */
    panEnd(constraintContext?: ConstraintContext): OrthographicState;
    /**
     * Start rotating
     */
    rotateStart(): OrthographicState;
    /**
     * Rotate
     */
    rotate(): OrthographicState;
    /**
     * End rotating
     */
    rotateEnd(): OrthographicState;
    shortestPathFrom(viewState: OrthographicState): OrthographicStateProps;
    /**
     * Start zooming
     * @param {[Number, Number]} pos - position on screen where the pointer grabs
     */
    zoomStart({ pos }: {
        pos: [number, number];
    }, constraintContext?: ConstraintContext): OrthographicState;
    /**
     * Zoom
     * @param {[Number, Number]} pos - position on screen where the current target is
     * @param {[Number, Number]} startPos - the target position at
     *   the start of the operation. Must be supplied of `zoomStart()` was not called
     * @param {Number} scale - a number between [0, 1] specifying the accumulated
     *   relative scale.
     */
    zoom({ pos, startPos, scale }: {
        pos: [number, number];
        startPos?: [number, number];
        scale: number;
    }, constraintContext?: ConstraintContext): OrthographicState;
    /**
     * End zooming
     * Must call if `zoomStart()` was called
     */
    zoomEnd(constraintContext?: ConstraintContext): OrthographicState;
    zoomIn(speed?: number, constraintContext?: ConstraintContext): OrthographicState;
    zoomOut(speed?: number, constraintContext?: ConstraintContext): OrthographicState;
    moveLeft(speed?: number, constraintContext?: ConstraintContext): OrthographicState;
    moveRight(speed?: number, constraintContext?: ConstraintContext): OrthographicState;
    moveUp(speed?: number, constraintContext?: ConstraintContext): OrthographicState;
    moveDown(speed?: number, constraintContext?: ConstraintContext): OrthographicState;
    rotateLeft(speed?: number): OrthographicState;
    rotateRight(speed?: number): OrthographicState;
    rotateUp(speed?: number): OrthographicState;
    rotateDown(speed?: number): OrthographicState;
    _project(pos: number[]): number[];
    _unproject(pos: number[]): [number, number];
    _calculateNewZoom({ scale, startZoom }: {
        scale: number;
        startZoom?: number[];
    }): {
        zoomX: number;
        zoomY: number;
    };
    _panFromCenter(offset: any, constraintContext?: ConstraintContext): OrthographicState;
    _getUpdatedState(newProps: any, constraintContext?: ConstraintContext): OrthographicState;
    applyConstraints(props: Required<OrthographicStateProps>, constraintContext?: ConstraintContext): Required<OrthographicStateProps>;
    _constrainZoom({ zoomX, zoomY }: {
        zoomX: number;
        zoomY: number;
    }, props?: Required<OrthographicStateProps>): {
        zoomX: number;
        zoomY: number;
    };
}
export default class OrthographicController extends Controller<OrthographicState> {
    ControllerState: typeof OrthographicState;
    transition: {
        transitionDuration: number;
        transitionInterpolator: LinearInterpolator;
    };
    dragMode: 'pan' | 'rotate';
    setProps(props: ControllerProps & OrthographicStateProps): void;
    protected _onMultiPanStart(event: MjolnirGestureEvent): boolean;
    _onPanRotate(): boolean;
}
export {};
//# sourceMappingURL=orthographic-controller.d.ts.map