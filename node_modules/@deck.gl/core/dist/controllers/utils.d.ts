import type { CommonViewProps } from "../views/view.js";
import type Viewport from "../viewports/viewport.js";
export type MaxBoundsPadding = CommonViewProps<any>['padding'];
export type ResolvedPadding = {
    left: number;
    right: number;
    top: number;
    bottom: number;
};
export type MaxBoundsRect = {
    x: number;
    y: number;
    width: number;
    height: number;
};
export type MaxBoundsExtents = ResolvedPadding;
/** Applies asymptotic resistance to a value beyond its constrained limit. */
export declare function applyRubberBand(value: number, constrainedValue: number, range: number): number;
/** Returns the on-screen rectangle available for fitting max bounds. */
export declare function getMaxBoundsRect(width: number, height: number, padding: MaxBoundsPadding): MaxBoundsRect;
/** Measures the available screen space on each side of the projected semantic target. */
export declare function getMaxBoundsExtents(viewport: Viewport, target: number[], rect: MaxBoundsRect): MaxBoundsExtents;
//# sourceMappingURL=utils.d.ts.map