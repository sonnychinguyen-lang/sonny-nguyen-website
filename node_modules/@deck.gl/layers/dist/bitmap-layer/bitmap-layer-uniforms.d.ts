import type { Texture } from '@luma.gl/core';
export type BitmapProps = {
    bounds: [number, number, number, number];
    coordinateConversion: number;
    desaturate: number;
    tintColor: [number, number, number];
    transparentColor: [number, number, number, number];
    bitmapTexture: Texture;
};
export declare const bitmapUniforms: {
    readonly name: "bitmap";
    readonly source: "\nstruct BitmapUniforms {\n  bounds: vec4<f32>,\n  coordinateConversion: f32,\n  desaturate: f32,\n  tintColor: vec3<f32>,\n  transparentColor: vec4<f32>,\n};\n\n@group(0) @binding(auto) var<uniform> bitmap: BitmapUniforms;\n@group(0) @binding(auto) var bitmapTexture: texture_2d<f32>;\n@group(0) @binding(auto) var bitmapTextureSampler: sampler;\n";
    readonly vs: "layout(std140) uniform bitmapUniforms {\n  vec4 bounds;\n  float coordinateConversion;\n  float desaturate;\n  vec3 tintColor;\n  vec4 transparentColor;\n} bitmap;\n";
    readonly fs: "layout(std140) uniform bitmapUniforms {\n  vec4 bounds;\n  float coordinateConversion;\n  float desaturate;\n  vec3 tintColor;\n  vec4 transparentColor;\n} bitmap;\n";
    readonly uniformTypes: {
        readonly bounds: "vec4<f32>";
        readonly coordinateConversion: "f32";
        readonly desaturate: "f32";
        readonly tintColor: "vec3<f32>";
        readonly transparentColor: "vec4<f32>";
    };
};
//# sourceMappingURL=bitmap-layer-uniforms.d.ts.map