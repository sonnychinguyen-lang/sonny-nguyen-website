import type { Matrix4 } from '@math.gl/core';
export type ScenegraphProps = {
    sizeScale: number;
    sizeMinPixels: number;
    sizeMaxPixels: number;
    sceneModelMatrix: Matrix4;
    composeModelMatrix: number;
};
export declare const scenegraphUniforms: {
    readonly name: "scenegraph";
    readonly source: "struct ScenegraphUniforms {\n  sizeScale: f32,\n  sizeMinPixels: f32,\n  sizeMaxPixels: f32,\n  sceneModelMatrix: mat4x4<f32>,\n  composeModelMatrix: f32,\n};\n\n@group(0) @binding(auto)\nvar<uniform> scenegraph: ScenegraphUniforms;\n";
    readonly vs: "layout(std140) uniform scenegraphUniforms {\n  float sizeScale;\n  float sizeMinPixels;\n  float sizeMaxPixels;\n  mat4 sceneModelMatrix;\n  float composeModelMatrix;\n} scenegraph;\n";
    readonly fs: "layout(std140) uniform scenegraphUniforms {\n  float sizeScale;\n  float sizeMinPixels;\n  float sizeMaxPixels;\n  mat4 sceneModelMatrix;\n  float composeModelMatrix;\n} scenegraph;\n";
    readonly uniformTypes: {
        readonly sizeScale: "f32";
        readonly sizeMinPixels: "f32";
        readonly sizeMaxPixels: "f32";
        readonly sceneModelMatrix: "mat4x4<f32>";
        readonly composeModelMatrix: "f32";
    };
};
//# sourceMappingURL=scenegraph-layer-uniforms.d.ts.map