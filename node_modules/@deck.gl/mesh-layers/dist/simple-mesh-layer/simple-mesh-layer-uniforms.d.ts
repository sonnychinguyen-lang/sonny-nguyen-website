import type { Texture } from '@luma.gl/core';
export type SimpleMeshProps = {
    sizeScale?: number;
    composeModelMatrix?: boolean;
    hasTexture?: boolean;
    flatShading?: boolean;
    sampler?: Texture;
    simpleMeshTexture?: Texture;
};
export declare const simpleMeshUniforms: {
    readonly name: "simpleMesh";
    readonly source: "struct SimpleMeshUniforms {\n  sizeScale: f32,\n  composeModelMatrix: f32,\n  hasTexture: f32,\n  flatShading: f32,\n};\n\n@group(0) @binding(auto) var<uniform> simpleMesh: SimpleMeshUniforms;\n@group(0) @binding(auto) var simpleMeshTexture: texture_2d<f32>;\n@group(0) @binding(auto) var simpleMeshTextureSampler: sampler;\n";
    readonly vs: "layout(std140) uniform simpleMeshUniforms {\n  float sizeScale;\n  bool composeModelMatrix;\n  bool hasTexture;\n  bool flatShading;\n} simpleMesh;\n";
    readonly fs: "layout(std140) uniform simpleMeshUniforms {\n  float sizeScale;\n  bool composeModelMatrix;\n  bool hasTexture;\n  bool flatShading;\n} simpleMesh;\n";
    readonly uniformTypes: {
        readonly sizeScale: "f32";
        readonly composeModelMatrix: "f32";
        readonly hasTexture: "f32";
        readonly flatShading: "f32";
    };
};
//# sourceMappingURL=simple-mesh-layer-uniforms.d.ts.map