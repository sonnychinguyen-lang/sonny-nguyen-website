import { jsx as _jsx } from "preact/jsx-runtime";
import { useMemo } from 'preact/hooks';
import { getCSSMask } from "../data-url.js";
import { Tooltip } from "./tooltip.js";
/** Renders a button component with widget CSS */
export const IconButton = (props) => {
    const { className = '', style, color, icon, label, tooltip, onClick, children } = props;
    const tooltipContent = tooltip === false ? undefined : (tooltip ?? label);
    const iconStyle = useMemo(() => {
        const css = getCSSMask(icon);
        if (!color)
            return css;
        return { ...css, backgroundColor: color };
    }, [color, icon]);
    const button = (_jsx("button", { className: `deck-widget-icon-button ${className}`, type: "button", onClick: onClick, "aria-label": label, children: children ? children : _jsx("div", { className: "deck-widget-icon", style: iconStyle }) }));
    return (_jsx("div", { className: "deck-widget-button", style: style, children: tooltipContent ? _jsx(Tooltip, { content: tooltipContent, children: button }) : button }));
};
//# sourceMappingURL=icon-button.js.map