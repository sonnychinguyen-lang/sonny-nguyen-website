import { Geometry, type GeometryAttribute } from "../geometry/geometry.js";
import type { Model } from "../model/model.js";
/** Portable vertex attributes supported by glTF-style morph target deformation. */
export type MorphTargetAttributes = {
    POSITION?: Float32Array;
    NORMAL?: Float32Array;
    TANGENT?: Float32Array;
};
/** Decodes one immutable vertex attribute into its shader-facing floating-point values. */
export declare function decodeMorphTargetAttribute(attribute: GeometryAttribute): Float32Array;
/** Applies weighted morph deltas without modifying the immutable source vertex attributes. */
export declare function applyMorphTargets(baseAttributes: Readonly<MorphTargetAttributes>, targets: readonly Readonly<MorphTargetAttributes>[], weights: readonly number[]): MorphTargetAttributes;
/** Rewrites an existing model vertex buffer while preserving its canonical interleaved layout. */
export declare function updateMorphTargetBuffers(model: Model, geometry: Geometry, targets: readonly Readonly<MorphTargetAttributes>[], weights: readonly number[]): void;
//# sourceMappingURL=morph-targets.d.ts.map