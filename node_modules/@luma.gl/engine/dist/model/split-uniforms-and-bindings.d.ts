import type { Binding, CompositeShaderType, UniformValue } from '@luma.gl/core';
import type { ShaderModuleUniformValue } from '@luma.gl/shadertools';
import type { TextureBindingSource } from "../dynamic-texture/texture-binding-source.js";
/** Shader input value routed through the binding map. */
type ShaderInputBinding = Binding | TextureBindingSource;
export declare function isUniformValue(value: unknown): value is UniformValue;
type UniformsAndBindings = {
    bindings: Record<string, ShaderInputBinding>;
    uniforms: Record<string, ShaderModuleUniformValue>;
};
export declare function splitUniformsAndBindings(uniforms: Record<string, ShaderInputBinding | ShaderModuleUniformValue>, uniformTypes?: Readonly<Record<string, CompositeShaderType>>): UniformsAndBindings;
export {};
//# sourceMappingURL=split-uniforms-and-bindings.d.ts.map