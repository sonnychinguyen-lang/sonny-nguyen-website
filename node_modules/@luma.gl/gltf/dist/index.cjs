var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// dist/index.js
var index_exports = {};
__export(index_exports, {
  GLTFAnimatedCrowd: () => GLTFAnimatedCrowd,
  GLTFAnimationClip: () => GLTFAnimationClip,
  GLTFAnimator: () => GLTFAnimator,
  GLTFCrowdActor: () => GLTFCrowdActor,
  GLTFMaterialVariants: () => GLTFMaterialVariants,
  GLTFSkinController: () => GLTFSkinController,
  assertSupportedGLTFExtensions: () => assertSupportedGLTFExtensions,
  convertGLTFSampler: () => convertSampler,
  convertSamplerToGLTF: () => convertSamplerToGLTF,
  createGLTFAnimatedCrowd: () => createGLTFAnimatedCrowd,
  createGLTFTexture: () => createGLTFTexture,
  createScenegraphsFromGLTF: () => createScenegraphsFromGLTF,
  exportGLTF: () => exportGLTF,
  generateGLTFLODLevels: () => generateGLTFLODLevels,
  getGLTFExtensionSupport: () => getGLTFExtensionSupport,
  getGLTFExtensionSupportSummary: () => getGLTFExtensionSupportSummary,
  getGLTFNodeInstancing: () => getGLTFNodeInstancing,
  getGLTFNodeLODs: () => getGLTFNodeLODs,
  getRegisteredGLTFExtensions: () => getRegisteredGLTFExtensions,
  getTextureTransformMatrix: () => getTextureTransformMatrix,
  getTextureTransformSlotDefinition: () => getTextureTransformSlotDefinition,
  getTextureTransformSlotDefinitions: () => getTextureTransformSlotDefinitions,
  getUnsupportedRequiredGLTFExtensions: () => getUnsupportedRequiredGLTFExtensions,
  loadPBREnvironment: () => loadPBREnvironment,
  parseGLTFAnimations: () => parseGLTFAnimations,
  parseGLTFLights: () => parseGLTFLights,
  parsePBRMaterial: () => parsePBRMaterial,
  resolveGLTFSkinIndex: () => resolveGLTFSkinIndex,
  resolveTextureCoordinateSet: () => resolveTextureCoordinateSet,
  resolveTextureTransform: () => resolveTextureTransform,
  setGLTFMorphWeights: () => setGLTFMorphWeights
});
module.exports = __toCommonJS(index_exports);

// dist/gltf/create-scenegraph-from-gltf.js
var import_engine7 = require("@luma.gl/engine");

// dist/parsers/parse-gltf.js
var import_engine3 = require("@luma.gl/engine");
var import_shadertools2 = require("@luma.gl/shadertools");

// dist/gltf/create-gltf-model.js
var import_core = require("@luma.gl/core");
var import_engine = require("@luma.gl/engine");
var import_shadertools = require("@luma.gl/shadertools");
var SHADER = (
  /* WGSL */
  `
struct VertexInputs {
  @location(0) positions: vec3f,
#ifdef HAS_NORMALS
  @location(1) normals: vec3f,
#endif
#ifdef HAS_TANGENTS
  @location(2) TANGENT: vec4f,
#endif
#ifdef HAS_UV
  @location(3) texCoords: vec2f,
#endif
#ifdef HAS_UV_1
  @location(4) texCoords1: vec2f,
#endif
#ifdef HAS_SKIN
  @location(5) JOINTS_0: vec4u,
  @location(6) WEIGHTS_0: vec4f,
#endif
#ifdef HAS_GLTF_INSTANCING
  @location(8) instanceModelMatrixCol0: vec4f,
  @location(9) instanceModelMatrixCol1: vec4f,
  @location(10) instanceModelMatrixCol2: vec4f,
  @location(11) instanceModelMatrixCol3: vec4f,
  @builtin(instance_index) instanceIndex: u32,
#endif
#ifdef HAS_GPU_CROWD_ANIMATION
  @location(12) instanceAnimationFrames: vec4f,
  @location(13) instanceAnimationBlend: vec4f,
#endif
#ifdef HAS_INSTANCED_MORPH
  @builtin(vertex_index) vertexIndex: u32,
#endif
};

struct FragmentInputs {
  @builtin(position) position: vec4f,
  @location(0) pbrPosition: vec3f,
  @location(1) pbrUV0: vec2f,
  @location(2) pbrUV1: vec2f,
  @location(3) pbrNormal: vec3f,
#ifdef HAS_TANGENTS
  @location(4) pbrTangent: vec4f,
#endif
};

#ifdef HAS_GLTF_INSTANCING
fn getGLTFInstanceNormalMatrix(matrix: mat3x3f) -> mat3x3f {
  let firstCofactor = cross(matrix[1], matrix[2]);
  let inverseDeterminant = 1.0 / dot(matrix[0], firstCofactor);
  return mat3x3f(
    firstCofactor,
    cross(matrix[2], matrix[0]),
    cross(matrix[0], matrix[1])
  ) * inverseDeterminant;
}
#endif

@vertex
fn vertexMain(inputs: VertexInputs) -> FragmentInputs {
  var outputs: FragmentInputs;
  var position = vec4f(inputs.positions, 1.0);
  var normal = vec3f(0.0, 0.0, 1.0);
  var tangent = vec4f(1.0, 0.0, 0.0, 1.0);
  var uv0 = vec2f(0.0, 0.0);
  var uv1 = vec2f(0.0, 0.0);

#ifdef HAS_NORMALS
  normal = inputs.normals;
#endif
#ifdef HAS_UV
  uv0 = inputs.texCoords;
#endif
#ifdef HAS_UV_1
  uv1 = inputs.texCoords1;
#endif
#ifdef HAS_TANGENTS
  tangent = inputs.TANGENT;
#endif

#ifdef HAS_INSTANCED_MORPH
  var animationFrames = vec4f(0.0);
  var animationBlend = vec4f(0.0);
#ifdef HAS_GPU_CROWD_ANIMATION
  animationFrames = inputs.instanceAnimationFrames;
  animationBlend = inputs.instanceAnimationBlend;
#endif
  position = vec4f(
    position.xyz + getGPUCrowdMorphDelta(
      inputs.instanceIndex,
      inputs.vertexIndex,
      0u,
      u32(CROWD_MORPH_VERTEX_COUNT),
      u32(CROWD_MORPH_TARGET_COUNT),
      u32(CROWD_ANIMATION_JOINT_COUNT),
      animationFrames,
      animationBlend,
      u32(CROWD_ANIMATION_FRAME_STRIDE)
    ),
    1.0
  );
#ifdef HAS_NORMALS
  normal = normalize(normal + getGPUCrowdMorphDelta(
    inputs.instanceIndex,
    inputs.vertexIndex,
    1u,
    u32(CROWD_MORPH_VERTEX_COUNT),
    u32(CROWD_MORPH_TARGET_COUNT),
    u32(CROWD_ANIMATION_JOINT_COUNT),
    animationFrames,
    animationBlend,
    u32(CROWD_ANIMATION_FRAME_STRIDE)
  ));
#endif
#ifdef HAS_TANGENTS
  tangent = vec4f(normalize(tangent.xyz + getGPUCrowdMorphDelta(
    inputs.instanceIndex,
    inputs.vertexIndex,
    2u,
    u32(CROWD_MORPH_VERTEX_COUNT),
    u32(CROWD_MORPH_TARGET_COUNT),
    u32(CROWD_ANIMATION_JOINT_COUNT),
    animationFrames,
    animationBlend,
    u32(CROWD_ANIMATION_FRAME_STRIDE)
  )), tangent.w);
#endif
#endif

#ifdef HAS_SKIN
#ifdef HAS_GPU_CROWD_ANIMATION
  let skinMatrix = getGPUAnimatedSkinMatrix(
    inputs.WEIGHTS_0,
    inputs.JOINTS_0,
    inputs.instanceAnimationFrames,
    inputs.instanceAnimationBlend,
    u32(CROWD_ANIMATION_FRAME_STRIDE)
  );
#else
#ifdef HAS_INSTANCED_SKIN
  let skinMatrix = getInstancedSkinMatrix(
    inputs.WEIGHTS_0,
    inputs.JOINTS_0,
    inputs.instanceIndex,
    u32(CROWD_JOINTS_PER_INSTANCE)
  );
#else
  let skinMatrix = getSkinMatrix(inputs.WEIGHTS_0, inputs.JOINTS_0);
#endif
#endif
  position = skinMatrix * position;
  normal = normalize((skinMatrix * vec4f(normal, 0.0)).xyz);
#ifdef HAS_TANGENTS
  tangent = vec4f(normalize((skinMatrix * vec4f(tangent.xyz, 0.0)).xyz), tangent.w);
#endif
#endif

#ifdef HAS_GLTF_INSTANCING
  var instanceMatrix = mat4x4f(
    inputs.instanceModelMatrixCol0,
    inputs.instanceModelMatrixCol1,
    inputs.instanceModelMatrixCol2,
    inputs.instanceModelMatrixCol3
  );
#ifdef HAS_GPU_CROWD_ANIMATION
  instanceMatrix *= sampleGPUAnimationMatrix(
    inputs.instanceAnimationFrames,
    inputs.instanceAnimationBlend,
    0u,
    u32(CROWD_ANIMATION_FRAME_STRIDE)
  );
#endif
  position = instanceMatrix * position;
  normal = normalize(getGLTFInstanceNormalMatrix(mat3x3f(
    instanceMatrix[0].xyz,
    instanceMatrix[1].xyz,
    instanceMatrix[2].xyz
  )) * normal);
#ifdef HAS_TANGENTS
  tangent = vec4f(normalize((instanceMatrix * vec4f(tangent.xyz, 0.0)).xyz), tangent.w);
#endif
#endif

  let worldPosition = pbrProjection.modelMatrix * position;

#ifdef HAS_NORMALS
  normal = normalize((pbrProjection.normalMatrix * vec4f(normal, 0.0)).xyz);
#endif
#ifdef HAS_TANGENTS
  let worldTangent = normalize((pbrProjection.modelMatrix * vec4f(tangent.xyz, 0.0)).xyz);
  outputs.pbrTangent = vec4f(worldTangent, tangent.w);
#endif

  outputs.position = pbrProjection.modelViewProjectionMatrix * position;
  outputs.pbrPosition = worldPosition.xyz / worldPosition.w;
  outputs.pbrUV0 = uv0;
  outputs.pbrUV1 = uv1;
  outputs.pbrNormal = normal;
  return outputs;
}

@fragment
fn fragmentMain(inputs: FragmentInputs) -> @location(0) vec4f {
  fragmentInputs.pbr_vPosition = inputs.pbrPosition;
  fragmentInputs.pbr_vUV0 = inputs.pbrUV0;
  fragmentInputs.pbr_vUV1 = inputs.pbrUV1;
  fragmentInputs.pbr_vNormal = inputs.pbrNormal;
#ifdef HAS_TANGENTS
  let tangent = normalize(inputs.pbrTangent.xyz);
  let bitangent = normalize(cross(inputs.pbrNormal, tangent)) * inputs.pbrTangent.w;
  fragmentInputs.pbr_vTBN = mat3x3f(tangent, bitangent, inputs.pbrNormal);
#endif
  return pbr_filterColor(vec4f(1.0));
}
`
);
var vs = (
  /* glsl */
  `#version 300 es

  // in vec4 POSITION;
  in vec4 positions;

  #ifdef HAS_NORMALS
    // in vec4 NORMAL;
    in vec4 normals;
  #endif

  #ifdef HAS_TANGENTS
    in vec4 TANGENT;
  #endif

  #ifdef HAS_UV
    // in vec2 TEXCOORD_0;
    in vec2 texCoords;
  #endif

  #ifdef HAS_UV_1
    in vec2 texCoords1;
  #endif

  #ifdef HAS_SKIN
    in uvec4 JOINTS_0;
    in vec4 WEIGHTS_0;
  #endif

  #ifdef HAS_GLTF_INSTANCING
    in vec4 instanceModelMatrixCol0;
    in vec4 instanceModelMatrixCol1;
    in vec4 instanceModelMatrixCol2;
    in vec4 instanceModelMatrixCol3;
  #endif

  #ifdef HAS_GPU_CROWD_ANIMATION
    in vec4 instanceAnimationFrames;
    in vec4 instanceAnimationBlend;
  #endif

  void main(void) {
    vec4 _NORMAL = vec4(0.);
    vec4 _TANGENT = vec4(0.);
    vec2 _TEXCOORD_0 = vec2(0.);
    vec2 _TEXCOORD_1 = vec2(0.);

    #ifdef HAS_NORMALS
      _NORMAL = normals;
    #endif

    #ifdef HAS_TANGENTS
      _TANGENT = TANGENT;
    #endif

    #ifdef HAS_UV
      _TEXCOORD_0 = texCoords;
    #endif

    #ifdef HAS_UV_1
      _TEXCOORD_1 = texCoords1;
    #endif

    vec4 pos = positions;

    #ifdef HAS_INSTANCED_MORPH
      vec4 animationFrames = vec4(0.0);
      vec4 animationBlend = vec4(0.0);
      #ifdef HAS_GPU_CROWD_ANIMATION
        animationFrames = instanceAnimationFrames;
        animationBlend = instanceAnimationBlend;
      #endif
      pos.xyz += getGPUCrowdMorphDelta(
        uint(gl_InstanceID),
        uint(gl_VertexID),
        0u,
        uint(CROWD_MORPH_TARGET_COUNT),
        uint(CROWD_ANIMATION_JOINT_COUNT),
        animationFrames,
        animationBlend
      );
      #ifdef HAS_NORMALS
        _NORMAL.xyz = normalize(_NORMAL.xyz + getGPUCrowdMorphDelta(
          uint(gl_InstanceID),
          uint(gl_VertexID),
          1u,
          uint(CROWD_MORPH_TARGET_COUNT),
          uint(CROWD_ANIMATION_JOINT_COUNT),
          animationFrames,
          animationBlend
        ));
      #endif
      #ifdef HAS_TANGENTS
        _TANGENT.xyz = normalize(_TANGENT.xyz + getGPUCrowdMorphDelta(
          uint(gl_InstanceID),
          uint(gl_VertexID),
          2u,
          uint(CROWD_MORPH_TARGET_COUNT),
          uint(CROWD_ANIMATION_JOINT_COUNT),
          animationFrames,
          animationBlend
        ));
      #endif
    #endif

    #ifdef HAS_SKIN
      #ifdef HAS_GPU_CROWD_ANIMATION
        mat4 skinMat = getGPUAnimatedSkinMatrix(
          WEIGHTS_0,
          JOINTS_0,
          instanceAnimationFrames,
          instanceAnimationBlend
        );
      #else
      #ifdef HAS_INSTANCED_SKIN
        mat4 skinMat = getInstancedSkinMatrix(
          WEIGHTS_0,
          JOINTS_0,
          uint(gl_InstanceID),
          uint(CROWD_JOINTS_PER_INSTANCE)
        );
      #else
      mat4 skinMat = getSkinMatrix(WEIGHTS_0, JOINTS_0);
      #endif
      #endif
      pos = skinMat * pos;
      _NORMAL = skinMat * _NORMAL;
      _TANGENT = vec4((skinMat * vec4(_TANGENT.xyz, 0.)).xyz, _TANGENT.w);
    #endif

    #ifdef HAS_GLTF_INSTANCING
      mat4 instanceMatrix = mat4(
        instanceModelMatrixCol0,
        instanceModelMatrixCol1,
        instanceModelMatrixCol2,
        instanceModelMatrixCol3
      );
      #ifdef HAS_GPU_CROWD_ANIMATION
        instanceMatrix *= sampleGPUAnimationMatrix(
          instanceAnimationFrames,
          instanceAnimationBlend,
          0
        );
      #endif
      pos = instanceMatrix * pos;
      _NORMAL = vec4(normalize(transpose(inverse(mat3(instanceMatrix))) * _NORMAL.xyz), 0.0);
      _TANGENT = vec4(normalize(mat3(instanceMatrix) * _TANGENT.xyz), _TANGENT.w);
    #endif

    pbr_setPositionNormalTangentUV(pos, _NORMAL, _TANGENT, _TEXCOORD_0, _TEXCOORD_1);
    gl_Position = pbrProjection.modelViewProjectionMatrix * pos;
  }
`
);
var fs = (
  /* glsl */
  `#version 300 es
  out vec4 fragmentColor;

  void main(void) {
    vec3 pos = pbr_vPosition;
    fragmentColor = pbr_filterColor(vec4(1.0));
  }
`
);
function createGLTFMaterial(device, options) {
  const materialFactory = options.materialFactory || new import_engine.MaterialFactory(device, { modules: [import_shadertools.pbrMaterial] });
  const pbrMaterialProps = { ...options.parsedPPBRMaterial.uniforms };
  delete pbrMaterialProps.camera;
  const materialBindings = Object.fromEntries(Object.entries({
    ...pbrMaterialProps,
    ...options.parsedPPBRMaterial.bindings
  }).filter(([name, value]) => materialFactory.ownsBinding(name) && isMaterialBindingResource(value)));
  const material = materialFactory.createMaterial({
    id: options.id,
    bindings: materialBindings
  });
  material.setProps({ pbrMaterial: pbrMaterialProps });
  return material;
}
function createGLTFModel(device, options) {
  var _a, _b;
  const { id, geometry, parsedPPBRMaterial, vertexCount, modelOptions = {}, instanceMatrices, morphTargets = [] } = options;
  const crowd = (_a = modelOptions.userData) == null ? void 0 : _a["gltfAnimatedCrowd"];
  if (crowd && instanceMatrices) {
    throw new Error("Nested glTF crowd instancing is unsupported");
  }
  import_core.log.info(4, "createGLTFModel defines: ", parsedPPBRMaterial.defines)();
  const managedResources = [];
  const parameters = {
    depthWriteEnabled: true,
    depthCompare: "less",
    depthFormat: "depth24plus",
    cullMode: "back"
  };
  const instanceAttributes = {};
  const instanceBufferLayout = [];
  const transformBuffers = [];
  const transformColumns = [];
  const hasGPUAnimation = Boolean(crowd == null ? void 0 : crowd.gpuAnimation);
  if (instanceMatrices || crowd) {
    for (let columnIndex = 0; columnIndex < 4; columnIndex++) {
      const values = new Float32Array(((crowd == null ? void 0 : crowd.capacity) || (instanceMatrices == null ? void 0 : instanceMatrices.length) || 0) * 4);
      instanceMatrices == null ? void 0 : instanceMatrices.forEach((matrix, instanceIndex) => {
        for (let rowIndex = 0; rowIndex < 4; rowIndex++) {
          values[instanceIndex * 4 + rowIndex] = matrix[columnIndex * 4 + rowIndex];
        }
      });
      const attributeName = `instanceModelMatrixCol${columnIndex}`;
      const buffer = device.createBuffer({
        id: `${id || "gltf"}-${attributeName}`,
        data: values,
        usage: import_core.Buffer.VERTEX | import_core.Buffer.COPY_DST
      });
      instanceAttributes[attributeName] = buffer;
      instanceBufferLayout.push({ name: attributeName, format: "float32x4", stepMode: "instance" });
      managedResources.push(buffer);
      transformBuffers.push(buffer);
      transformColumns.push(values);
    }
  }
  let animationParameters;
  let animationParameterBuffer;
  let animationBlend;
  let animationBlendBuffer;
  if (crowd && hasGPUAnimation) {
    animationParameters = new Float32Array(crowd.capacity * 4);
    animationBlend = new Float32Array(crowd.capacity * 4);
    for (const [attributeName, values] of [
      ["instanceAnimationFrames", animationParameters],
      ["instanceAnimationBlend", animationBlend]
    ]) {
      const buffer = device.createBuffer({
        id: `${id || "gltf"}-${attributeName}`,
        data: values,
        usage: import_core.Buffer.VERTEX | import_core.Buffer.COPY_DST
      });
      instanceAttributes[attributeName] = buffer;
      instanceBufferLayout.push({ name: attributeName, format: "float32x4", stepMode: "instance" });
      managedResources.push(buffer);
      if (attributeName === "instanceAnimationFrames") {
        animationParameterBuffer = buffer;
      } else {
        animationBlendBuffer = buffer;
      }
    }
  }
  const hasSkin = Boolean(parsedPPBRMaterial.defines["HAS_SKIN"]);
  const hasInstancedSkin = Boolean(crowd && hasSkin && !hasGPUAnimation);
  let skinJointMatrices;
  let jointMatrices;
  if (crowd && hasInstancedSkin) {
    jointMatrices = new Float32Array(crowd.capacity * crowd.jointsPerInstance * 16);
    skinJointMatrices = device.type === "webgpu" ? device.createBuffer({
      id: `${id || "gltf"}-crowd-joint-matrices`,
      byteLength: jointMatrices.byteLength,
      usage: import_core.Buffer.STORAGE | import_core.Buffer.COPY_DST
    }) : device.createTexture({
      id: `${id || "gltf"}-crowd-joint-matrices`,
      format: "rgba32float",
      width: crowd.jointsPerInstance * 4,
      height: crowd.capacity,
      usage: import_core.Texture.SAMPLE | import_core.Texture.COPY_DST,
      sampler: { minFilter: "nearest", magFilter: "nearest", mipmapFilter: "nearest" }
    });
    managedResources.push(skinJointMatrices);
  }
  const morphTargetCount = crowd ? morphTargets.length : 0;
  const morphVertexCount = Math.floor((((_b = geometry.attributes["POSITION"]) == null ? void 0 : _b.value.length) || 0) / 3);
  let morphTargetData;
  let morphWeights;
  let morphWeightData;
  if (crowd && morphTargetCount > 0 && morphVertexCount > 0) {
    const targetValues = new Float32Array(morphTargetCount * 3 * morphVertexCount * 4);
    for (const [targetIndex, target] of morphTargets.entries()) {
      for (const [attributeIndex, attributeName] of ["POSITION", "NORMAL", "TANGENT"].entries()) {
        const values = target[attributeName];
        if (!values) {
          continue;
        }
        const componentCount = attributeName === "TANGENT" && values.length === morphVertexCount * 4 ? 4 : 3;
        for (let vertexIndex = 0; vertexIndex < morphVertexCount; vertexIndex++) {
          const destinationOffset = ((targetIndex * 3 + attributeIndex) * morphVertexCount + vertexIndex) * 4;
          const sourceOffset = vertexIndex * componentCount;
          targetValues[destinationOffset] = values[sourceOffset] || 0;
          targetValues[destinationOffset + 1] = values[sourceOffset + 1] || 0;
          targetValues[destinationOffset + 2] = values[sourceOffset + 2] || 0;
        }
      }
    }
    morphTargetData = createGPUAnimationResource(device, `${id || "gltf"}-crowd-morph-targets`, targetValues, morphVertexCount, morphTargetCount * 3);
    managedResources.push(morphTargetData);
    if (!hasGPUAnimation) {
      const packedTargetCount = Math.ceil(morphTargetCount / 4);
      morphWeights = new Float32Array(crowd.capacity * packedTargetCount * 4);
      morphWeightData = createGPUAnimationResource(device, `${id || "gltf"}-crowd-morph-weights`, morphWeights, packedTargetCount, crowd.capacity);
      managedResources.push(morphWeightData);
    }
  }
  const animationJointCount = hasSkin && crowd ? crowd.jointsPerInstance : 0;
  const animationFrameStride = 4 + animationJointCount * 4 + morphTargetCount;
  let animationFrames;
  let animationFrameValues;
  if (crowd == null ? void 0 : crowd.gpuAnimation) {
    animationFrameValues = new Float32Array(crowd.gpuAnimation.frameCount * animationFrameStride * 4);
    animationFrames = createGPUAnimationResource(device, `${id || "gltf"}-crowd-animation-frames`, animationFrameValues, animationFrameStride, crowd.gpuAnimation.frameCount);
    managedResources.push(animationFrames);
  }
  let source = SHADER;
  for (const [name, value] of [
    ["CROWD_JOINTS_PER_INSTANCE", (crowd == null ? void 0 : crowd.jointsPerInstance) || 0],
    ["CROWD_MORPH_VERTEX_COUNT", morphVertexCount],
    ["CROWD_MORPH_TARGET_COUNT", morphTargetCount],
    ["CROWD_ANIMATION_JOINT_COUNT", animationJointCount],
    ["CROWD_ANIMATION_FRAME_STRIDE", animationFrameStride]
  ]) {
    source = source.replaceAll(`u32(${name})`, `u32(${value})`);
  }
  const modelProps = {
    id,
    source,
    vs,
    fs,
    geometry,
    topology: geometry.topology,
    vertexCount,
    modules: [import_shadertools.pbrMaterial, import_shadertools.skin, ...crowd ? [import_shadertools.gpuAnimation] : []],
    ...modelOptions,
    ...instanceMatrices || crowd ? {
      attributes: { ...modelOptions.attributes, ...instanceAttributes },
      bufferLayout: [...modelOptions.bufferLayout || [], ...instanceBufferLayout],
      instanceCount: (instanceMatrices == null ? void 0 : instanceMatrices.length) || 0,
      isInstanced: true
    } : {},
    defines: {
      ...parsedPPBRMaterial.defines,
      ...modelOptions.defines,
      ...instanceMatrices || crowd ? { HAS_GLTF_INSTANCING: true } : {},
      ...hasInstancedSkin ? { HAS_INSTANCED_SKIN: true, CROWD_JOINTS_PER_INSTANCE: crowd.jointsPerInstance } : {},
      ...hasGPUAnimation ? { HAS_GPU_CROWD_ANIMATION: true, CROWD_ANIMATION_FRAME_STRIDE: animationFrameStride } : {},
      ...morphTargetData ? { HAS_INSTANCED_MORPH: true, CROWD_MORPH_TARGET_COUNT: morphTargetCount } : {},
      ...crowd ? { CROWD_ANIMATION_JOINT_COUNT: animationJointCount } : {}
    },
    parameters: { ...parameters, ...parsedPPBRMaterial.parameters, ...modelOptions.parameters }
  };
  const material = options.material || createGLTFMaterial(device, {
    id: id ? `${id}-material` : void 0,
    parsedPPBRMaterial
  });
  modelProps.material = material;
  const model = new import_engine.Model(device, modelProps);
  const sceneShaderInputValues = {
    ...parsedPPBRMaterial.uniforms,
    ...modelOptions.uniforms,
    ...parsedPPBRMaterial.bindings,
    ...modelOptions.bindings
  };
  const sceneShaderInputProps = getSceneShaderInputProps(model.shaderInputs.getModules(), material, sceneShaderInputValues);
  model.shaderInputs.setProps(sceneShaderInputProps);
  if (skinJointMatrices) {
    model.shaderInputs.setProps({ skin: { jointMatrices: [], skinJointMatrices } });
  }
  if (animationFrames || morphTargetData || morphWeightData) {
    model.shaderInputs.setProps({
      gpuAnimation: {
        ...animationFrames ? { gpuAnimationFrames: animationFrames } : {},
        ...morphTargetData ? { gpuMorphTargets: morphTargetData } : {},
        ...morphWeightData ? { gpuMorphWeights: morphWeightData } : {}
      }
    });
  }
  const modelNode = new import_engine.ModelNode({
    managedResources,
    model,
    bounds: options.bounds,
    instanceMatrices
  });
  if (crowd) {
    modelNode.userData["gltfAnimatedCrowd"] = {
      transformBuffers,
      transformColumns,
      skinJointMatrices,
      jointMatrices,
      jointsPerInstance: crowd.jointsPerInstance,
      morphTargetCount,
      morphTargetData,
      morphWeights,
      morphWeightData,
      animationFrames,
      animationFrameValues,
      animationFrameStride,
      animationJointCount,
      animationParameters,
      animationParameterBuffer,
      animationBlend,
      animationBlendBuffer
    };
  }
  return modelNode;
}
function createGPUAnimationResource(device, id, values, width, height) {
  if (device.type === "webgpu") {
    return device.createBuffer({
      id,
      data: values,
      usage: import_core.Buffer.STORAGE | import_core.Buffer.COPY_DST
    });
  }
  const texture = device.createTexture({
    id,
    format: "rgba32float",
    width,
    height,
    usage: import_core.Texture.SAMPLE | import_core.Texture.COPY_DST,
    sampler: { minFilter: "nearest", magFilter: "nearest", mipmapFilter: "nearest" }
  });
  texture.writeData(values, { width, height });
  return texture;
}
function isMaterialBindingResource(value) {
  return value instanceof import_core.Buffer || value instanceof import_engine.DynamicTexture || value instanceof import_core.Sampler || value instanceof import_core.Texture || value instanceof import_core.TextureView;
}
function getSceneShaderInputProps(modules, material, shaderInputValues) {
  const propertyToModuleNameMap = /* @__PURE__ */ new Map();
  for (const module2 of modules) {
    for (const uniformName of Object.keys(module2.uniformTypes || {})) {
      propertyToModuleNameMap.set(uniformName, module2.name);
    }
    for (const binding of module2.bindingLayout || []) {
      propertyToModuleNameMap.set(binding.name, module2.name);
    }
  }
  const sceneShaderInputProps = {};
  for (const [propertyName, value] of Object.entries(shaderInputValues)) {
    if (value === void 0) {
      continue;
    }
    const moduleName = propertyToModuleNameMap.get(propertyName);
    if (!moduleName || material.ownsModule(moduleName)) {
      continue;
    }
    sceneShaderInputProps[moduleName] ||= {};
    sceneShaderInputProps[moduleName][propertyName] = value;
  }
  return sceneShaderInputProps;
}

// dist/gltf/gltf-instancing.js
var import_core2 = require("@math.gl/core");
function getGLTFNodeInstancing(gltf, node) {
  var _a, _b;
  const sourceAttributes = (_b = (_a = node.extensions) == null ? void 0 : _a["EXT_mesh_gpu_instancing"]) == null ? void 0 : _b.attributes;
  if (!sourceAttributes || typeof sourceAttributes !== "object") {
    return null;
  }
  const attributes = {};
  let instanceCount;
  for (const [attributeName, accessorReference] of Object.entries(sourceAttributes)) {
    const accessor = typeof accessorReference === "number" ? gltf.accessors[accessorReference] : accessorReference;
    if (!accessor || !ArrayBuffer.isView(accessor.value)) {
      throw new Error(`Invalid glTF instance accessor for ${attributeName}`);
    }
    if (instanceCount !== void 0 && accessor.count !== instanceCount) {
      throw new Error("glTF instance attributes must have matching accessor counts");
    }
    instanceCount = accessor.count;
    attributes[attributeName] = {
      value: accessor.value,
      size: accessor.components || getAccessorComponentCount(accessor.type),
      count: accessor.count,
      normalized: Boolean(accessor.normalized)
    };
  }
  const matrices = [];
  for (let instanceIndex = 0; instanceIndex < (instanceCount || 0); instanceIndex++) {
    const translation = getInstanceValues(attributes["TRANSLATION"], instanceIndex, [0, 0, 0]);
    const rotation = getInstanceValues(attributes["ROTATION"], instanceIndex, [0, 0, 0, 1]);
    const scale = getInstanceValues(attributes["SCALE"], instanceIndex, [1, 1, 1]);
    const rotationLength = Math.hypot(...rotation);
    if (rotationLength > 0) {
      for (let component = 0; component < rotation.length; component++) {
        rotation[component] /= rotationLength;
      }
    }
    matrices.push(new import_core2.Matrix4().translate(translation).multiplyRight(new import_core2.Matrix4().fromQuaternion(rotation)).scale(scale));
  }
  return { matrices, attributes };
}
function getInstanceValues(attribute, instanceIndex, defaultValues) {
  if (!attribute) {
    return [...defaultValues];
  }
  const values = attribute.value;
  return defaultValues.map((defaultValue, componentIndex) => {
    const component = values[instanceIndex * attribute.size + componentIndex];
    if (component === void 0) {
      return defaultValue;
    }
    if (!attribute.normalized) {
      return component;
    }
    if (attribute.value instanceof Int8Array) {
      return Math.max(component / 127, -1);
    }
    if (attribute.value instanceof Int16Array) {
      return Math.max(component / 32767, -1);
    }
    if (attribute.value instanceof Uint8Array) {
      return component / 255;
    }
    if (attribute.value instanceof Uint16Array) {
      return component / 65535;
    }
    return component;
  });
}
function getAccessorComponentCount(type) {
  switch (type) {
    case "VEC2":
      return 2;
    case "VEC3":
      return 3;
    case "VEC4":
      return 4;
    default:
      return 1;
  }
}

// dist/gltf/morph-targets.js
var import_engine2 = require("@luma.gl/engine");
function setGLTFMorphWeights(node, weights) {
  node.userData["morphWeights"] = [...weights];
  const meshes = node.userData["morphMeshes"] || [];
  for (const mesh of meshes) {
    mesh.preorderTraversal((child) => {
      if (!(child instanceof import_engine2.ModelNode)) {
        return;
      }
      const state = child.userData["morphTargets"];
      if (!state) {
        return;
      }
      (0, import_engine2.updateMorphTargetBuffers)(child.model, state.geometry, state.targets, weights);
      child.userData["morphWeights"] = [...weights];
    });
  }
}

// dist/webgl-to-webgpu/gltf-webgl-constants.js
var GLEnum;
(function(GLEnum2) {
  GLEnum2[GLEnum2["POINTS"] = 0] = "POINTS";
  GLEnum2[GLEnum2["LINES"] = 1] = "LINES";
  GLEnum2[GLEnum2["LINE_LOOP"] = 2] = "LINE_LOOP";
  GLEnum2[GLEnum2["LINE_STRIP"] = 3] = "LINE_STRIP";
  GLEnum2[GLEnum2["TRIANGLES"] = 4] = "TRIANGLES";
  GLEnum2[GLEnum2["TRIANGLE_STRIP"] = 5] = "TRIANGLE_STRIP";
  GLEnum2[GLEnum2["TRIANGLE_FAN"] = 6] = "TRIANGLE_FAN";
  GLEnum2[GLEnum2["ONE"] = 1] = "ONE";
  GLEnum2[GLEnum2["SRC_ALPHA"] = 770] = "SRC_ALPHA";
  GLEnum2[GLEnum2["ONE_MINUS_SRC_ALPHA"] = 771] = "ONE_MINUS_SRC_ALPHA";
  GLEnum2[GLEnum2["FUNC_ADD"] = 32774] = "FUNC_ADD";
  GLEnum2[GLEnum2["LINEAR"] = 9729] = "LINEAR";
  GLEnum2[GLEnum2["NEAREST"] = 9728] = "NEAREST";
  GLEnum2[GLEnum2["NEAREST_MIPMAP_NEAREST"] = 9984] = "NEAREST_MIPMAP_NEAREST";
  GLEnum2[GLEnum2["LINEAR_MIPMAP_NEAREST"] = 9985] = "LINEAR_MIPMAP_NEAREST";
  GLEnum2[GLEnum2["NEAREST_MIPMAP_LINEAR"] = 9986] = "NEAREST_MIPMAP_LINEAR";
  GLEnum2[GLEnum2["LINEAR_MIPMAP_LINEAR"] = 9987] = "LINEAR_MIPMAP_LINEAR";
  GLEnum2[GLEnum2["TEXTURE_MAG_FILTER"] = 10240] = "TEXTURE_MAG_FILTER";
  GLEnum2[GLEnum2["TEXTURE_MIN_FILTER"] = 10241] = "TEXTURE_MIN_FILTER";
  GLEnum2[GLEnum2["TEXTURE_WRAP_S"] = 10242] = "TEXTURE_WRAP_S";
  GLEnum2[GLEnum2["TEXTURE_WRAP_T"] = 10243] = "TEXTURE_WRAP_T";
  GLEnum2[GLEnum2["REPEAT"] = 10497] = "REPEAT";
  GLEnum2[GLEnum2["CLAMP_TO_EDGE"] = 33071] = "CLAMP_TO_EDGE";
  GLEnum2[GLEnum2["MIRRORED_REPEAT"] = 33648] = "MIRRORED_REPEAT";
  GLEnum2[GLEnum2["UNPACK_FLIP_Y_WEBGL"] = 37440] = "UNPACK_FLIP_Y_WEBGL";
})(GLEnum || (GLEnum = {}));

// dist/webgl-to-webgpu/convert-webgl-topology.js
function convertGLDrawModeToTopology(drawMode) {
  switch (drawMode) {
    case GLEnum.POINTS:
      return "point-list";
    case GLEnum.LINES:
      return "line-list";
    case GLEnum.LINE_STRIP:
      return "line-strip";
    case GLEnum.TRIANGLES:
      return "triangle-list";
    case GLEnum.TRIANGLE_STRIP:
      return "triangle-strip";
    default:
      throw new Error(String(drawMode));
  }
}
function normalizeGLTFTopology(drawMode, sourceIndices, vertexCount) {
  if (drawMode !== GLEnum.LINE_LOOP && drawMode !== GLEnum.TRIANGLE_FAN) {
    return { topology: convertGLDrawModeToTopology(drawMode) };
  }
  const sourceVertexCount = (sourceIndices == null ? void 0 : sourceIndices.length) ?? vertexCount;
  const indexCount = drawMode === GLEnum.LINE_LOOP ? sourceVertexCount >= 2 ? sourceVertexCount * 2 : 0 : sourceVertexCount >= 3 ? (sourceVertexCount - 2) * 3 : 0;
  const IndexArray = sourceIndices instanceof Uint32Array || !sourceIndices && vertexCount > 65536 ? Uint32Array : Uint16Array;
  const indices = new IndexArray(indexCount);
  const getSourceIndex = (index) => (sourceIndices == null ? void 0 : sourceIndices[index]) ?? index;
  if (drawMode === GLEnum.LINE_LOOP) {
    for (let index = 0; index < sourceVertexCount; index++) {
      indices[index * 2] = getSourceIndex(index);
      indices[index * 2 + 1] = getSourceIndex((index + 1) % sourceVertexCount);
    }
    return { topology: "line-list", indices };
  }
  for (let triangleIndex = 0; triangleIndex < sourceVertexCount - 2; triangleIndex++) {
    indices[triangleIndex * 3] = getSourceIndex(0);
    indices[triangleIndex * 3 + 1] = getSourceIndex(triangleIndex + 1);
    indices[triangleIndex * 3 + 2] = getSourceIndex(triangleIndex + 2);
  }
  return { topology: "triangle-list", indices };
}

// dist/parsers/parse-pbr-material.js
var import_core4 = require("@luma.gl/core");

// dist/pbr/texture-transform.js
var import_core3 = require("@math.gl/core");
var TEXTURE_TRANSFORM_SLOT_DEFINITIONS = [
  createTextureTransformSlotDefinition("baseColor", "pbr_baseColorSampler", "baseColorTexture", [
    "pbrMetallicRoughness",
    "baseColorTexture"
  ]),
  createTextureTransformSlotDefinition("metallicRoughness", "pbr_metallicRoughnessSampler", "metallicRoughnessTexture", ["pbrMetallicRoughness", "metallicRoughnessTexture"]),
  createTextureTransformSlotDefinition("normal", "pbr_normalSampler", "normalTexture", [
    "normalTexture"
  ]),
  createTextureTransformSlotDefinition("occlusion", "pbr_occlusionSampler", "occlusionTexture", [
    "occlusionTexture"
  ]),
  createTextureTransformSlotDefinition("emissive", "pbr_emissiveSampler", "emissiveTexture", [
    "emissiveTexture"
  ]),
  createTextureTransformSlotDefinition("specularColor", "pbr_specularColorSampler", "KHR_materials_specular.specularColorTexture", ["extensions", "KHR_materials_specular", "specularColorTexture"]),
  createTextureTransformSlotDefinition("specularIntensity", "pbr_specularIntensitySampler", "KHR_materials_specular.specularTexture", ["extensions", "KHR_materials_specular", "specularTexture"]),
  createTextureTransformSlotDefinition("transmission", "pbr_transmissionSampler", "KHR_materials_transmission.transmissionTexture", ["extensions", "KHR_materials_transmission", "transmissionTexture"]),
  createTextureTransformSlotDefinition("thickness", "pbr_thicknessSampler", "KHR_materials_volume.thicknessTexture", ["extensions", "KHR_materials_volume", "thicknessTexture"]),
  createTextureTransformSlotDefinition("clearcoat", "pbr_clearcoatSampler", "KHR_materials_clearcoat.clearcoatTexture", ["extensions", "KHR_materials_clearcoat", "clearcoatTexture"]),
  createTextureTransformSlotDefinition("clearcoatRoughness", "pbr_clearcoatRoughnessSampler", "KHR_materials_clearcoat.clearcoatRoughnessTexture", ["extensions", "KHR_materials_clearcoat", "clearcoatRoughnessTexture"]),
  createTextureTransformSlotDefinition("clearcoatNormal", "pbr_clearcoatNormalSampler", "KHR_materials_clearcoat.clearcoatNormalTexture", ["extensions", "KHR_materials_clearcoat", "clearcoatNormalTexture"]),
  createTextureTransformSlotDefinition("sheenColor", "pbr_sheenColorSampler", "KHR_materials_sheen.sheenColorTexture", ["extensions", "KHR_materials_sheen", "sheenColorTexture"]),
  createTextureTransformSlotDefinition("sheenRoughness", "pbr_sheenRoughnessSampler", "KHR_materials_sheen.sheenRoughnessTexture", ["extensions", "KHR_materials_sheen", "sheenRoughnessTexture"]),
  createTextureTransformSlotDefinition("iridescence", "pbr_iridescenceSampler", "KHR_materials_iridescence.iridescenceTexture", ["extensions", "KHR_materials_iridescence", "iridescenceTexture"]),
  createTextureTransformSlotDefinition("iridescenceThickness", "pbr_iridescenceThicknessSampler", "KHR_materials_iridescence.iridescenceThicknessTexture", ["extensions", "KHR_materials_iridescence", "iridescenceThicknessTexture"]),
  createTextureTransformSlotDefinition("anisotropy", "pbr_anisotropySampler", "KHR_materials_anisotropy.anisotropyTexture", ["extensions", "KHR_materials_anisotropy", "anisotropyTexture"]),
  createTextureTransformSlotDefinition("bump", "pbr_bumpSampler", "EXT_materials_bump.bumpTexture", ["extensions", "EXT_materials_bump", "bumpTexture"]),
  createTextureTransformSlotDefinition("diffuseTransmission", "pbr_diffuseTransmissionSampler", "KHR_materials_diffuse_transmission.diffuseTransmissionTexture", ["extensions", "KHR_materials_diffuse_transmission", "diffuseTransmissionTexture"]),
  createTextureTransformSlotDefinition("diffuseTransmissionColor", "pbr_diffuseTransmissionColorSampler", "KHR_materials_diffuse_transmission.diffuseTransmissionColorTexture", ["extensions", "KHR_materials_diffuse_transmission", "diffuseTransmissionColorTexture"]),
  createTextureTransformSlotDefinition("multiscatterColor", "pbr_multiscatterColorSampler", "KHR_materials_volume_scatter.multiscatterColorTexture", ["extensions", "KHR_materials_volume_scatter", "multiscatterColorTexture"])
];
var TEXTURE_TRANSFORM_SLOT_DEFINITION_MAP = new Map(TEXTURE_TRANSFORM_SLOT_DEFINITIONS.map((definition) => [definition.slot, definition]));
function createTextureTransformSlotDefinition(slot, binding, displayName, pathSegments) {
  return {
    slot,
    binding,
    displayName,
    pathSegments,
    colorSpace: slot === "baseColor" || slot === "emissive" || slot === "specularColor" || slot === "sheenColor" || slot === "diffuseTransmissionColor" || slot === "multiscatterColor" ? "srgb" : "linear",
    uvSetUniform: `${slot}UVSet`,
    uvTransformUniform: `${slot}UVTransform`
  };
}
function getTextureTransformSlotDefinitions() {
  return TEXTURE_TRANSFORM_SLOT_DEFINITIONS;
}
function getTextureTransformSlotDefinition(slot) {
  const definition = TEXTURE_TRANSFORM_SLOT_DEFINITION_MAP.get(slot);
  if (!definition) {
    throw new Error(`Unknown PBR texture transform slot ${slot}`);
  }
  return definition;
}
function resolveTextureTransform(textureInfo) {
  var _a;
  const extensionTextureTransform = (_a = textureInfo == null ? void 0 : textureInfo["extensions"]) == null ? void 0 : _a["KHR_texture_transform"];
  return {
    offset: (extensionTextureTransform == null ? void 0 : extensionTextureTransform.offset) ? [extensionTextureTransform.offset[0], extensionTextureTransform.offset[1]] : [0, 0],
    rotation: (extensionTextureTransform == null ? void 0 : extensionTextureTransform.rotation) ?? 0,
    scale: (extensionTextureTransform == null ? void 0 : extensionTextureTransform.scale) ? [extensionTextureTransform.scale[0], extensionTextureTransform.scale[1]] : [1, 1]
  };
}
function resolveTextureCoordinateSet(textureInfo) {
  var _a;
  const extensionTextureTransform = (_a = textureInfo == null ? void 0 : textureInfo["extensions"]) == null ? void 0 : _a["KHR_texture_transform"];
  return (extensionTextureTransform == null ? void 0 : extensionTextureTransform["texCoord"]) ?? (textureInfo == null ? void 0 : textureInfo["texCoord"]) ?? 0;
}
function resolveTextureTransformSlot(pointerSegments) {
  return TEXTURE_TRANSFORM_SLOT_DEFINITIONS.find((definition) => definition.pathSegments.length === pointerSegments.length && definition.pathSegments.every((segment, index) => pointerSegments[index] === segment)) || null;
}
function getTextureTransformMatrix(transform) {
  const translationMatrix = new import_core3.Matrix3().set(1, 0, 0, 0, 1, 0, transform.offset[0], transform.offset[1], 1);
  const rotationMatrix = new import_core3.Matrix3().set(Math.cos(transform.rotation), Math.sin(transform.rotation), 0, -Math.sin(transform.rotation), Math.cos(transform.rotation), 0, 0, 0, 1);
  const scaleMatrix = new import_core3.Matrix3().set(transform.scale[0], 0, 0, 0, transform.scale[1], 0, 0, 0, 1);
  return Array.from(translationMatrix.multiplyRight(rotationMatrix).multiplyRight(scaleMatrix));
}
function getTextureTransformDeltaMatrix(baseTransform, currentTransform) {
  const baseMatrix = new import_core3.Matrix3(getTextureTransformMatrix(baseTransform));
  const currentMatrix = new import_core3.Matrix3(getTextureTransformMatrix(currentTransform));
  const inverseBaseMatrix = new import_core3.Matrix3(baseMatrix).invert();
  return Array.from(currentMatrix.multiplyRight(inverseBaseMatrix));
}

// dist/webgl-to-webgpu/convert-webgl-sampler.js
function convertSampler(gltfSampler = {}) {
  var _a, _b, _c, _d;
  const wrapS = gltfSampler.wrapS ?? ((_a = gltfSampler.parameters) == null ? void 0 : _a[GLEnum.TEXTURE_WRAP_S]);
  const wrapT = gltfSampler.wrapT ?? ((_b = gltfSampler.parameters) == null ? void 0 : _b[GLEnum.TEXTURE_WRAP_T]);
  const magFilter = gltfSampler.magFilter ?? ((_c = gltfSampler.parameters) == null ? void 0 : _c[GLEnum.TEXTURE_MAG_FILTER]);
  const minFilter = gltfSampler.minFilter ?? ((_d = gltfSampler.parameters) == null ? void 0 : _d[GLEnum.TEXTURE_MIN_FILTER]);
  const addressModeU = convertSamplerWrapMode(wrapS);
  const addressModeV = convertSamplerWrapMode(wrapT);
  const magnificationFilter = convertSamplerMagFilter(magFilter);
  return {
    ...addressModeU ? { addressModeU } : {},
    ...addressModeV ? { addressModeV } : {},
    ...magnificationFilter ? { magFilter: magnificationFilter } : {},
    ...convertSamplerMinFilter(minFilter)
  };
}
function convertSamplerToGLTF(sampler) {
  const wrapS = convertAddressModeToGLTF(sampler.addressModeU);
  const wrapT = convertAddressModeToGLTF(sampler.addressModeV);
  const magFilter = convertFilterModeToGLTF(sampler.magFilter);
  const minFilter = convertMinFilterToGLTF(sampler.minFilter, sampler.mipmapFilter);
  return {
    ...wrapS !== void 0 ? { wrapS } : {},
    ...wrapT !== void 0 ? { wrapT } : {},
    ...magFilter !== void 0 ? { magFilter } : {},
    ...minFilter !== void 0 ? { minFilter } : {}
  };
}
function convertSamplerWrapMode(mode) {
  switch (mode) {
    case GLEnum.CLAMP_TO_EDGE:
      return "clamp-to-edge";
    case GLEnum.REPEAT:
      return "repeat";
    case GLEnum.MIRRORED_REPEAT:
      return "mirror-repeat";
    default:
      return void 0;
  }
}
function convertAddressModeToGLTF(addressMode) {
  switch (addressMode) {
    case "clamp-to-edge":
      return GLEnum.CLAMP_TO_EDGE;
    case "repeat":
      return GLEnum.REPEAT;
    case "mirror-repeat":
      return GLEnum.MIRRORED_REPEAT;
    default:
      return void 0;
  }
}
function convertFilterModeToGLTF(filter) {
  switch (filter) {
    case "nearest":
      return GLEnum.NEAREST;
    case "linear":
      return GLEnum.LINEAR;
    default:
      return void 0;
  }
}
function convertMinFilterToGLTF(minFilter, mipmapFilter) {
  if (!minFilter) {
    return void 0;
  }
  if (mipmapFilter === "nearest") {
    return minFilter === "nearest" ? GLEnum.NEAREST_MIPMAP_NEAREST : GLEnum.LINEAR_MIPMAP_NEAREST;
  }
  if (mipmapFilter === "linear") {
    return minFilter === "nearest" ? GLEnum.NEAREST_MIPMAP_LINEAR : GLEnum.LINEAR_MIPMAP_LINEAR;
  }
  return minFilter === "nearest" ? GLEnum.NEAREST : GLEnum.LINEAR;
}
function convertSamplerMagFilter(mode) {
  switch (mode) {
    case GLEnum.NEAREST:
      return "nearest";
    case GLEnum.LINEAR:
      return "linear";
    default:
      return void 0;
  }
}
function convertSamplerMinFilter(mode) {
  switch (mode) {
    case GLEnum.NEAREST:
      return { minFilter: "nearest" };
    case GLEnum.LINEAR:
      return { minFilter: "linear" };
    case GLEnum.NEAREST_MIPMAP_NEAREST:
      return { minFilter: "nearest", mipmapFilter: "nearest" };
    case GLEnum.LINEAR_MIPMAP_NEAREST:
      return { minFilter: "linear", mipmapFilter: "nearest" };
    case GLEnum.NEAREST_MIPMAP_LINEAR:
      return { minFilter: "nearest", mipmapFilter: "linear" };
    case GLEnum.LINEAR_MIPMAP_LINEAR:
      return { minFilter: "linear", mipmapFilter: "linear" };
    default:
      return {};
  }
}

// dist/parsers/parse-pbr-material.js
var GLTF_ATTRIBUTE_ALIASES = {
  NORMAL: ["NORMAL", "normals"],
  TANGENT: ["TANGENT"],
  TEXCOORD_0: ["TEXCOORD_0", "texCoords"],
  TEXCOORD_1: ["TEXCOORD_1", "texCoords1"],
  JOINTS_0: ["JOINTS_0"],
  WEIGHTS_0: ["WEIGHTS_0"],
  COLOR_0: ["COLOR_0", "colors"]
};
function parsePBRMaterial(device, material, attributes, options) {
  const parsedMaterial = {
    defines: {
      // TODO: Use EXT_sRGB if available (Standard in WebGL 2.0)
      MANUAL_SRGB: true
    },
    bindings: {},
    uniforms: {
      // TODO: find better values?
      camera: [0, 0, 0],
      // Model should override
      metallicRoughnessValues: [1, 1]
      // Default is 1 and 1
    },
    parameters: {},
    glParameters: {},
    generatedTextures: []
  };
  parsedMaterial.defines["USE_TEX_LOD"] = true;
  const { imageBasedLightingEnvironment } = options;
  if (imageBasedLightingEnvironment) {
    parsedMaterial.bindings.pbr_diffuseEnvSampler = imageBasedLightingEnvironment.diffuseEnvSampler.texture;
    parsedMaterial.bindings.pbr_specularEnvSampler = imageBasedLightingEnvironment.specularEnvSampler.texture;
    parsedMaterial.bindings.pbr_brdfLUT = imageBasedLightingEnvironment.brdfLutTexture.texture;
    parsedMaterial.uniforms.IBLenabled = true;
    parsedMaterial.uniforms.scaleIBLAmbient = [1, 1];
  }
  if (options == null ? void 0 : options.pbrDebug) {
    parsedMaterial.defines["PBR_DEBUG"] = true;
    parsedMaterial.uniforms.scaleDiffBaseMR = [0, 0, 0, 0];
    parsedMaterial.uniforms.scaleFGDSpec = [0, 0, 0, 0];
  }
  if (hasGLTFAttribute(attributes, "NORMAL"))
    parsedMaterial.defines["HAS_NORMALS"] = true;
  if (hasGLTFAttribute(attributes, "TANGENT") && (options == null ? void 0 : options.useTangents)) {
    parsedMaterial.defines["HAS_TANGENTS"] = true;
  }
  if (hasGLTFAttribute(attributes, "TEXCOORD_0"))
    parsedMaterial.defines["HAS_UV"] = true;
  if (hasGLTFAttribute(attributes, "TEXCOORD_1"))
    parsedMaterial.defines["HAS_UV_1"] = true;
  if (hasGLTFAttribute(attributes, "JOINTS_0") && hasGLTFAttribute(attributes, "WEIGHTS_0")) {
    parsedMaterial.defines["HAS_SKIN"] = true;
  }
  if (hasGLTFAttribute(attributes, "COLOR_0"))
    parsedMaterial.defines["HAS_COLORS"] = true;
  if (options == null ? void 0 : options.imageBasedLightingEnvironment)
    parsedMaterial.defines["USE_IBL"] = true;
  if (options == null ? void 0 : options.lights)
    parsedMaterial.defines["USE_LIGHTS"] = true;
  if (material) {
    if (options.validateAttributes !== false) {
      warnOnMissingExpectedAttributes(material, attributes);
    }
    parseMaterial(device, material, parsedMaterial, attributes, options.gltf);
  }
  return parsedMaterial;
}
function warnOnMissingExpectedAttributes(material, attributes) {
  var _a;
  const uvDependentTextureSlots = getUvDependentTextureSlots(material, 0);
  if (uvDependentTextureSlots.length > 0 && !hasGLTFAttribute(attributes, "TEXCOORD_0")) {
    import_core4.log.warn(`glTF material uses ${uvDependentTextureSlots.join(", ")} but primitive is missing TEXCOORD_0; textured shading will sample the default UV coordinates`)();
  }
  const uv1DependentTextureSlots = getUvDependentTextureSlots(material, 1);
  if (uv1DependentTextureSlots.length > 0 && !hasGLTFAttribute(attributes, "TEXCOORD_1")) {
    import_core4.log.warn(`glTF material uses ${uv1DependentTextureSlots.join(", ")} with TEXCOORD_1 but primitive is missing TEXCOORD_1; those textures will be skipped`)();
  }
  const isUnlitMaterial = Boolean(material.unlit || ((_a = material.extensions) == null ? void 0 : _a.KHR_materials_unlit));
  if (isUnlitMaterial || hasGLTFAttribute(attributes, "NORMAL")) {
    return;
  }
  const missingNormalReason = material.normalTexture ? "lit PBR shading with normalTexture" : "lit PBR shading";
  import_core4.log.warn(`glTF primitive is missing NORMAL while using ${missingNormalReason}; shading will fall back to geometric normals`)();
}
function getUvDependentTextureSlots(material, textureCoordinateSet) {
  const uvDependentTextureSlots = [];
  for (const slotDefinition of getTextureTransformSlotDefinitions()) {
    const textureInfo = getNestedTextureInfo(material, slotDefinition.pathSegments);
    if (!textureInfo) {
      continue;
    }
    if (resolveTextureCoordinateSet(textureInfo) === textureCoordinateSet) {
      uvDependentTextureSlots.push(slotDefinition.displayName);
    }
  }
  return uvDependentTextureSlots;
}
function hasGLTFAttribute(attributes, attributeName) {
  return GLTF_ATTRIBUTE_ALIASES[attributeName].some((alias) => Boolean(attributes[alias]));
}
function getNestedTextureInfo(material, pathSegments) {
  let value = material;
  for (const pathSegment of pathSegments) {
    value = value == null ? void 0 : value[pathSegment];
    if (!value) {
      return null;
    }
  }
  return value;
}
function parseMaterial(device, material, parsedMaterial, attributes, gltf) {
  var _a;
  parsedMaterial.uniforms.unlit = Boolean(material.unlit || ((_a = material.extensions) == null ? void 0 : _a.KHR_materials_unlit));
  if (material.pbrMetallicRoughness) {
    parsePbrMetallicRoughness(device, material.pbrMetallicRoughness, parsedMaterial, attributes, gltf);
  }
  if (material.normalTexture) {
    addTexture(device, material.normalTexture, "pbr_normalSampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_NORMALMAP",
        enabledUniformName: "normalMapEnabled"
      },
      gltf,
      attributes,
      textureTransformSlot: "normal"
    });
    const { scale = 1 } = material.normalTexture;
    parsedMaterial.uniforms.normalScale = scale;
  }
  if (material.occlusionTexture) {
    addTexture(device, material.occlusionTexture, "pbr_occlusionSampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_OCCLUSIONMAP",
        enabledUniformName: "occlusionMapEnabled"
      },
      gltf,
      attributes,
      textureTransformSlot: "occlusion"
    });
    const { strength = 1 } = material.occlusionTexture;
    parsedMaterial.uniforms.occlusionStrength = strength;
  }
  parsedMaterial.uniforms.emissiveFactor = material.emissiveFactor || [0, 0, 0];
  if (material.emissiveTexture) {
    addTexture(device, material.emissiveTexture, "pbr_emissiveSampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_EMISSIVEMAP",
        enabledUniformName: "emissiveMapEnabled"
      },
      gltf,
      attributes,
      textureTransformSlot: "emissive"
    });
  }
  parseMaterialExtensions(device, material.extensions, parsedMaterial, gltf, attributes);
  switch (material.alphaMode || "OPAQUE") {
    case "OPAQUE":
      break;
    case "MASK": {
      const { alphaCutoff = 0.5 } = material;
      parsedMaterial.defines["ALPHA_CUTOFF"] = true;
      parsedMaterial.uniforms.alphaCutoffEnabled = true;
      parsedMaterial.uniforms.alphaCutoff = alphaCutoff;
      break;
    }
    case "BLEND":
      import_core4.log.warn("glTF BLEND alphaMode might not work well because it requires mesh sorting")();
      applyAlphaBlendParameters(parsedMaterial);
      break;
  }
}
function applyAlphaBlendParameters(parsedMaterial) {
  parsedMaterial.parameters.blend = true;
  parsedMaterial.parameters.blendColorOperation = "add";
  parsedMaterial.parameters.blendColorSrcFactor = "src-alpha";
  parsedMaterial.parameters.blendColorDstFactor = "one-minus-src-alpha";
  parsedMaterial.parameters.blendAlphaOperation = "add";
  parsedMaterial.parameters.blendAlphaSrcFactor = "one";
  parsedMaterial.parameters.blendAlphaDstFactor = "one-minus-src-alpha";
  parsedMaterial.glParameters["blend"] = true;
  parsedMaterial.glParameters["blendEquation"] = GLEnum.FUNC_ADD;
  parsedMaterial.glParameters["blendFunc"] = [
    GLEnum.SRC_ALPHA,
    GLEnum.ONE_MINUS_SRC_ALPHA,
    GLEnum.ONE,
    GLEnum.ONE_MINUS_SRC_ALPHA
  ];
}
function applyTransmissionBlendApproximation(parsedMaterial) {
  parsedMaterial.parameters.blend = true;
  parsedMaterial.parameters.depthWriteEnabled = false;
  parsedMaterial.parameters.blendColorOperation = "add";
  parsedMaterial.parameters.blendColorSrcFactor = "one";
  parsedMaterial.parameters.blendColorDstFactor = "one-minus-src-alpha";
  parsedMaterial.parameters.blendAlphaOperation = "add";
  parsedMaterial.parameters.blendAlphaSrcFactor = "one";
  parsedMaterial.parameters.blendAlphaDstFactor = "one-minus-src-alpha";
  parsedMaterial.glParameters["blend"] = true;
  parsedMaterial.glParameters["depthMask"] = false;
  parsedMaterial.glParameters["blendEquation"] = GLEnum.FUNC_ADD;
  parsedMaterial.glParameters["blendFunc"] = [
    GLEnum.ONE,
    GLEnum.ONE_MINUS_SRC_ALPHA,
    GLEnum.ONE,
    GLEnum.ONE_MINUS_SRC_ALPHA
  ];
}
function parsePbrMetallicRoughness(device, pbrMetallicRoughness, parsedMaterial, attributes, gltf) {
  if (pbrMetallicRoughness.baseColorTexture) {
    addTexture(device, pbrMetallicRoughness.baseColorTexture, "pbr_baseColorSampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_BASECOLORMAP",
        enabledUniformName: "baseColorMapEnabled"
      },
      gltf,
      attributes,
      textureTransformSlot: "baseColor"
    });
  }
  parsedMaterial.uniforms.baseColorFactor = pbrMetallicRoughness.baseColorFactor || [1, 1, 1, 1];
  if (pbrMetallicRoughness.metallicRoughnessTexture) {
    addTexture(device, pbrMetallicRoughness.metallicRoughnessTexture, "pbr_metallicRoughnessSampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_METALROUGHNESSMAP",
        enabledUniformName: "metallicRoughnessMapEnabled"
      },
      gltf,
      attributes,
      textureTransformSlot: "metallicRoughness"
    });
  }
  const { metallicFactor = 1, roughnessFactor = 1 } = pbrMetallicRoughness;
  parsedMaterial.uniforms.metallicRoughnessValues = [metallicFactor, roughnessFactor];
}
function parseMaterialExtensions(device, extensions, parsedMaterial, gltf, attributes = {}) {
  if (!extensions) {
    return;
  }
  if (hasMaterialExtensionShading(extensions)) {
    parsedMaterial.defines["USE_MATERIAL_EXTENSIONS"] = true;
  }
  parseSpecularExtension(device, extensions.KHR_materials_specular, parsedMaterial, gltf, attributes);
  parseIorExtension(extensions.KHR_materials_ior, parsedMaterial);
  parseBumpExtension(device, extensions.EXT_materials_bump, parsedMaterial, gltf, attributes);
  parseTransmissionExtension(device, extensions.KHR_materials_transmission, parsedMaterial, gltf, attributes);
  parseDiffuseTransmissionExtension(device, extensions.KHR_materials_diffuse_transmission, parsedMaterial, gltf, attributes);
  parseVolumeExtension(device, extensions.KHR_materials_volume, parsedMaterial, gltf, attributes);
  parseVolumeScatterExtension(device, extensions.KHR_materials_volume_scatter, extensions.KHR_materials_volume, parsedMaterial, gltf, attributes);
  parseDispersionExtension(extensions.KHR_materials_dispersion, parsedMaterial);
  parseClearcoatExtension(device, extensions.KHR_materials_clearcoat, parsedMaterial, gltf, attributes);
  parseSheenExtension(device, extensions.KHR_materials_sheen, parsedMaterial, gltf, attributes);
  parseIridescenceExtension(device, extensions.KHR_materials_iridescence, parsedMaterial, gltf, attributes);
  parseAnisotropyExtension(device, extensions.KHR_materials_anisotropy, parsedMaterial, gltf, attributes);
  parseEmissiveStrengthExtension(extensions.KHR_materials_emissive_strength, parsedMaterial);
}
function hasMaterialExtensionShading(extensions) {
  return Boolean(extensions.KHR_materials_specular || extensions.KHR_materials_ior || extensions.EXT_materials_bump || extensions.KHR_materials_transmission || extensions.KHR_materials_diffuse_transmission || extensions.KHR_materials_volume || extensions.KHR_materials_volume_scatter || extensions.KHR_materials_dispersion || extensions.KHR_materials_clearcoat || extensions.KHR_materials_sheen || extensions.KHR_materials_iridescence || extensions.KHR_materials_anisotropy);
}
function parseSpecularExtension(device, extension, parsedMaterial, gltf, attributes = {}) {
  if (!extension) {
    return;
  }
  if (extension.specularColorFactor) {
    parsedMaterial.uniforms.specularColorFactor = extension.specularColorFactor;
  }
  if (extension.specularFactor !== void 0) {
    parsedMaterial.uniforms.specularIntensityFactor = extension.specularFactor;
  }
  if (extension.specularColorTexture) {
    addTexture(device, extension.specularColorTexture, "pbr_specularColorSampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_SPECULARCOLORMAP",
        enabledUniformName: "specularColorMapEnabled"
      },
      gltf,
      attributes,
      textureTransformSlot: "specularColor"
    });
  }
  if (extension.specularTexture) {
    addTexture(device, extension.specularTexture, "pbr_specularIntensitySampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_SPECULARINTENSITYMAP",
        enabledUniformName: "specularIntensityMapEnabled"
      },
      gltf,
      attributes,
      textureTransformSlot: "specularIntensity"
    });
  }
}
function parseIorExtension(extension, parsedMaterial) {
  if ((extension == null ? void 0 : extension.ior) !== void 0) {
    parsedMaterial.uniforms.ior = extension.ior;
  }
}
function parseTransmissionExtension(device, extension, parsedMaterial, gltf, attributes = {}) {
  if (!extension) {
    return;
  }
  if (extension.transmissionFactor !== void 0) {
    parsedMaterial.uniforms.transmissionFactor = extension.transmissionFactor;
  }
  if (extension.transmissionTexture) {
    addTexture(device, extension.transmissionTexture, "pbr_transmissionSampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_TRANSMISSIONMAP",
        enabledUniformName: "transmissionMapEnabled"
      },
      gltf,
      attributes,
      textureTransformSlot: "transmission"
    });
  }
  if ((extension.transmissionFactor ?? 0) > 0 || extension.transmissionTexture) {
    import_core4.log.warn("KHR_materials_transmission uses a premultiplied-alpha blending approximation and may require mesh sorting")();
    applyTransmissionBlendApproximation(parsedMaterial);
  }
}
function parseBumpExtension(device, extension, parsedMaterial, gltf, attributes = {}) {
  if (!extension) {
    return;
  }
  parsedMaterial.uniforms.bumpFactor = Math.max(extension.bumpFactor ?? 1, 0);
  if (extension.bumpTexture) {
    addTexture(device, extension.bumpTexture, "pbr_bumpSampler", parsedMaterial, {
      featureOptions: { define: "HAS_BUMPMAP", enabledUniformName: "bumpMapEnabled" },
      gltf,
      attributes,
      textureTransformSlot: "bump"
    });
  }
}
function parseDiffuseTransmissionExtension(device, extension, parsedMaterial, gltf, attributes = {}) {
  if (!extension) {
    return;
  }
  parsedMaterial.uniforms.diffuseTransmissionFactor = Math.min(Math.max(extension.diffuseTransmissionFactor ?? 0, 0), 1);
  parsedMaterial.uniforms.diffuseTransmissionColorFactor = extension.diffuseTransmissionColorFactor || [1, 1, 1];
  if (extension.diffuseTransmissionTexture) {
    addTexture(device, extension.diffuseTransmissionTexture, "pbr_diffuseTransmissionSampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_DIFFUSETRANSMISSIONMAP",
        enabledUniformName: "diffuseTransmissionMapEnabled"
      },
      gltf,
      attributes,
      textureTransformSlot: "diffuseTransmission"
    });
  }
  if (extension.diffuseTransmissionColorTexture) {
    addTexture(device, extension.diffuseTransmissionColorTexture, "pbr_diffuseTransmissionColorSampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_DIFFUSETRANSMISSIONCOLORMAP",
        enabledUniformName: "diffuseTransmissionColorMapEnabled"
      },
      gltf,
      attributes,
      textureTransformSlot: "diffuseTransmissionColor"
    });
  }
}
function parseVolumeExtension(device, extension, parsedMaterial, gltf, attributes = {}) {
  if (!extension) {
    return;
  }
  if (extension.thicknessFactor !== void 0) {
    parsedMaterial.uniforms.thicknessFactor = extension.thicknessFactor;
  }
  if (extension.thicknessTexture) {
    addTexture(device, extension.thicknessTexture, "pbr_thicknessSampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_THICKNESSMAP"
      },
      gltf,
      attributes,
      textureTransformSlot: "thickness"
    });
  }
  if (extension.attenuationDistance !== void 0) {
    parsedMaterial.uniforms.attenuationDistance = extension.attenuationDistance;
  }
  if (extension.attenuationColor) {
    parsedMaterial.uniforms.attenuationColor = extension.attenuationColor;
  }
}
function parseVolumeScatterExtension(device, extension, volumeExtension, parsedMaterial, gltf, attributes = {}) {
  if (!extension || !volumeExtension) {
    return;
  }
  parsedMaterial.uniforms.multiscatterColorFactor = extension.multiscatterColorFactor || extension.multiscatterColor || [0, 0, 0];
  parsedMaterial.uniforms.scatterAnisotropy = Math.min(Math.max(extension.scatterAnisotropy ?? 0, -0.999), 0.999);
  if (extension.multiscatterColorTexture) {
    addTexture(device, extension.multiscatterColorTexture, "pbr_multiscatterColorSampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_MULTISCATTERCOLORMAP",
        enabledUniformName: "multiscatterColorMapEnabled"
      },
      gltf,
      attributes,
      textureTransformSlot: "multiscatterColor"
    });
  }
}
function parseDispersionExtension(extension, parsedMaterial) {
  if ((extension == null ? void 0 : extension.dispersion) !== void 0) {
    parsedMaterial.uniforms.dispersion = Math.max(extension.dispersion, 0);
  }
}
function parseClearcoatExtension(device, extension, parsedMaterial, gltf, attributes = {}) {
  if (!extension) {
    return;
  }
  if (extension.clearcoatFactor !== void 0) {
    parsedMaterial.uniforms.clearcoatFactor = extension.clearcoatFactor;
  }
  if (extension.clearcoatRoughnessFactor !== void 0) {
    parsedMaterial.uniforms.clearcoatRoughnessFactor = extension.clearcoatRoughnessFactor;
  }
  if (extension.clearcoatTexture) {
    addTexture(device, extension.clearcoatTexture, "pbr_clearcoatSampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_CLEARCOATMAP",
        enabledUniformName: "clearcoatMapEnabled"
      },
      gltf,
      attributes,
      textureTransformSlot: "clearcoat"
    });
  }
  if (extension.clearcoatRoughnessTexture) {
    addTexture(device, extension.clearcoatRoughnessTexture, "pbr_clearcoatRoughnessSampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_CLEARCOATROUGHNESSMAP",
        enabledUniformName: "clearcoatRoughnessMapEnabled"
      },
      gltf,
      attributes,
      textureTransformSlot: "clearcoatRoughness"
    });
  }
  if (extension.clearcoatNormalTexture) {
    addTexture(device, extension.clearcoatNormalTexture, "pbr_clearcoatNormalSampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_CLEARCOATNORMALMAP"
      },
      gltf,
      attributes,
      textureTransformSlot: "clearcoatNormal"
    });
  }
}
function parseSheenExtension(device, extension, parsedMaterial, gltf, attributes = {}) {
  if (!extension) {
    return;
  }
  if (extension.sheenColorFactor) {
    parsedMaterial.uniforms.sheenColorFactor = extension.sheenColorFactor;
  }
  if (extension.sheenRoughnessFactor !== void 0) {
    parsedMaterial.uniforms.sheenRoughnessFactor = extension.sheenRoughnessFactor;
  }
  if (extension.sheenColorTexture) {
    addTexture(device, extension.sheenColorTexture, "pbr_sheenColorSampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_SHEENCOLORMAP",
        enabledUniformName: "sheenColorMapEnabled"
      },
      gltf,
      attributes,
      textureTransformSlot: "sheenColor"
    });
  }
  if (extension.sheenRoughnessTexture) {
    addTexture(device, extension.sheenRoughnessTexture, "pbr_sheenRoughnessSampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_SHEENROUGHNESSMAP",
        enabledUniformName: "sheenRoughnessMapEnabled"
      },
      gltf,
      attributes,
      textureTransformSlot: "sheenRoughness"
    });
  }
}
function parseIridescenceExtension(device, extension, parsedMaterial, gltf, attributes = {}) {
  if (!extension) {
    return;
  }
  if (extension.iridescenceFactor !== void 0) {
    parsedMaterial.uniforms.iridescenceFactor = extension.iridescenceFactor;
  }
  if (extension.iridescenceIor !== void 0) {
    parsedMaterial.uniforms.iridescenceIor = extension.iridescenceIor;
  }
  if (extension.iridescenceThicknessMinimum !== void 0 || extension.iridescenceThicknessMaximum !== void 0) {
    parsedMaterial.uniforms.iridescenceThicknessRange = [
      extension.iridescenceThicknessMinimum ?? 100,
      extension.iridescenceThicknessMaximum ?? 400
    ];
  }
  if (extension.iridescenceTexture) {
    addTexture(device, extension.iridescenceTexture, "pbr_iridescenceSampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_IRIDESCENCEMAP",
        enabledUniformName: "iridescenceMapEnabled"
      },
      gltf,
      attributes,
      textureTransformSlot: "iridescence"
    });
  }
  if (extension.iridescenceThicknessTexture) {
    addTexture(device, extension.iridescenceThicknessTexture, "pbr_iridescenceThicknessSampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_IRIDESCENCETHICKNESSMAP"
      },
      gltf,
      attributes,
      textureTransformSlot: "iridescenceThickness"
    });
  }
}
function parseAnisotropyExtension(device, extension, parsedMaterial, gltf, attributes = {}) {
  if (!extension) {
    return;
  }
  if (extension.anisotropyStrength !== void 0) {
    parsedMaterial.uniforms.anisotropyStrength = extension.anisotropyStrength;
  }
  if (extension.anisotropyRotation !== void 0) {
    parsedMaterial.uniforms.anisotropyRotation = extension.anisotropyRotation;
  }
  if (extension.anisotropyTexture) {
    addTexture(device, extension.anisotropyTexture, "pbr_anisotropySampler", parsedMaterial, {
      featureOptions: {
        define: "HAS_ANISOTROPYMAP",
        enabledUniformName: "anisotropyMapEnabled"
      },
      gltf,
      attributes,
      textureTransformSlot: "anisotropy"
    });
  }
}
function parseEmissiveStrengthExtension(extension, parsedMaterial) {
  if ((extension == null ? void 0 : extension.emissiveStrength) !== void 0) {
    parsedMaterial.uniforms.emissiveStrength = extension.emissiveStrength;
  }
}
function addTexture(device, gltfTexture, uniformName, parsedMaterial, textureParseOptions = {}) {
  var _a, _b;
  const { featureOptions = {}, gltf, attributes = {}, textureTransformSlot } = textureParseOptions;
  const { define, enabledUniformName } = featureOptions;
  const textureCoordinateSet = resolveTextureCoordinateSet(gltfTexture);
  if (textureCoordinateSet > 1) {
    import_core4.log.warn(`Skipping ${String(uniformName)} because ${textureCoordinateSet} is not supported; only TEXCOORD_0 and TEXCOORD_1 are currently available`)();
    return;
  }
  if (textureCoordinateSet === 1 && !hasGLTFAttribute(attributes, "TEXCOORD_1")) {
    import_core4.log.warn(`Skipping ${String(uniformName)} because it requires TEXCOORD_1 but the primitive does not provide TEXCOORD_1`)();
    return;
  }
  const resolvedTextureInfo = resolveTextureInfo(gltfTexture, gltf);
  const image = (_b = (_a = resolvedTextureInfo.texture) == null ? void 0 : _a.source) == null ? void 0 : _b.image;
  if (!image) {
    import_core4.log.warn(`Skipping unresolved glTF texture for ${String(uniformName)}`)();
    return;
  }
  const baseOptions = {
    id: resolvedTextureInfo.uniformName || resolvedTextureInfo.id,
    sampler: {
      addressModeU: "repeat",
      addressModeV: "repeat",
      minFilter: "linear",
      magFilter: "linear",
      ...convertSampler(resolvedTextureInfo.texture.sampler)
    }
  };
  const texture = createGLTFTexture(device, image, baseOptions);
  parsedMaterial.bindings[uniformName] = texture;
  if (define)
    parsedMaterial.defines[define] = true;
  if (enabledUniformName) {
    parsedMaterial.uniforms[enabledUniformName] = true;
  }
  if (textureTransformSlot) {
    const textureTransformSlotDefinition = getTextureTransformSlotDefinition(textureTransformSlot);
    parsedMaterial.uniforms[textureTransformSlotDefinition.uvSetUniform] = textureCoordinateSet;
    parsedMaterial.uniforms[textureTransformSlotDefinition.uvTransformUniform] = getTextureTransformMatrix(resolveTextureTransform(gltfTexture));
  }
  parsedMaterial.generatedTextures.push(texture);
}
function createGLTFTexture(device, image, options) {
  if ("compressed" in image) {
    return createCompressedTexture(device, image, {
      id: options.id,
      sampler: options.sampler
    });
  }
  const dimensions = options.width !== void 0 && options.height !== void 0 ? { width: options.width, height: options.height } : device.getExternalImageSize(image);
  const usesMipmaps = options.sampler.mipmapFilter === "nearest" || options.sampler.mipmapFilter === "linear";
  const mipLevels = usesMipmaps ? device.getMipLevelCount(dimensions.width, dimensions.height) : 1;
  const texture = device.createTexture({
    id: options.id,
    sampler: options.sampler,
    width: dimensions.width,
    height: dimensions.height,
    mipLevels,
    ...usesMipmaps ? { usage: import_core4.Texture.SAMPLE | import_core4.Texture.RENDER | import_core4.Texture.COPY_DST | import_core4.Texture.COPY_SRC } : {},
    ...options.colorSpace ? { format: options.colorSpace === "srgb" ? "rgba8unorm-srgb" : "rgba8unorm" } : {},
    data: image
  });
  if (mipLevels > 1) {
    if (device.type === "webgl") {
      texture.generateMipmapsWebGL();
    } else if (device.type === "webgpu") {
      device.generateMipmapsWebGPU(texture);
    }
  }
  return texture;
}
function resolveTextureInfo(gltfTexture, gltf) {
  if (gltfTexture.texture || gltfTexture.index === void 0 || !(gltf == null ? void 0 : gltf.textures)) {
    return gltfTexture;
  }
  const resolvedTextureEntry = gltf.textures[gltfTexture.index];
  if (!resolvedTextureEntry) {
    return gltfTexture;
  }
  if ("texture" in resolvedTextureEntry && resolvedTextureEntry.texture) {
    return {
      ...resolvedTextureEntry,
      ...gltfTexture,
      texture: resolvedTextureEntry.texture
    };
  }
  if (!("source" in resolvedTextureEntry)) {
    return gltfTexture;
  }
  return {
    ...gltfTexture,
    texture: resolvedTextureEntry
  };
}
function createCompressedTextureFallback(device, baseOptions) {
  return device.createTexture({
    ...baseOptions,
    format: "rgba8unorm",
    width: 1,
    height: 1,
    mipLevels: 1
  });
}
function resolveCompressedTextureFormat(level) {
  return level.textureFormat;
}
function getMaxCompressedMipLevels(baseWidth, baseHeight, format) {
  const { blockWidth = 1, blockHeight = 1 } = import_core4.textureFormatDecoder.getInfo(format);
  let count = 1;
  for (let i = 1; ; i++) {
    const w = Math.max(1, baseWidth >> i);
    const h = Math.max(1, baseHeight >> i);
    if (w < blockWidth || h < blockHeight)
      break;
    count++;
  }
  return count;
}
function createCompressedTexture(device, image, baseOptions) {
  var _a, _b;
  let levels;
  if (Array.isArray(image.data) && ((_a = image.data[0]) == null ? void 0 : _a.data)) {
    levels = image.data;
  } else if ("mipmaps" in image && Array.isArray(image.mipmaps)) {
    levels = image.mipmaps;
  } else {
    levels = [];
  }
  if (levels.length === 0 || !((_b = levels[0]) == null ? void 0 : _b.data)) {
    import_core4.log.warn("createCompressedTexture: compressed image has no valid mip levels, creating fallback")();
    return createCompressedTextureFallback(device, baseOptions);
  }
  const baseLevel = levels[0];
  const baseWidth = baseLevel.width ?? image.width ?? 0;
  const baseHeight = baseLevel.height ?? image.height ?? 0;
  if (baseWidth <= 0 || baseHeight <= 0) {
    import_core4.log.warn("createCompressedTexture: base level has invalid dimensions, creating fallback")();
    return createCompressedTextureFallback(device, baseOptions);
  }
  const format = resolveCompressedTextureFormat(baseLevel);
  if (!format) {
    import_core4.log.warn("createCompressedTexture: compressed image has no textureFormat, creating fallback")();
    return createCompressedTextureFallback(device, baseOptions);
  }
  if (!device.isTextureFormatSupported(format)) {
    import_core4.log.warn(`createCompressedTexture: ${device.type} device does not support '${format}', creating fallback`)();
    return createCompressedTextureFallback(device, baseOptions);
  }
  const maxMipLevels = getMaxCompressedMipLevels(baseWidth, baseHeight, format);
  const levelLimit = Math.min(levels.length, maxMipLevels);
  let validLevelCount = 1;
  for (let i = 1; i < levelLimit; i++) {
    const level = levels[i];
    if (!level.data || level.width <= 0 || level.height <= 0) {
      import_core4.log.warn(`createCompressedTexture: mip level ${i} has invalid data/dimensions, truncating`)();
      break;
    }
    const levelFormat = resolveCompressedTextureFormat(level);
    if (levelFormat && levelFormat !== format) {
      import_core4.log.warn(`createCompressedTexture: mip level ${i} format '${levelFormat}' differs from base '${format}', truncating`)();
      break;
    }
    const expectedW = Math.max(1, baseWidth >> i);
    const expectedH = Math.max(1, baseHeight >> i);
    if (level.width !== expectedW || level.height !== expectedH) {
      import_core4.log.warn(`createCompressedTexture: mip level ${i} dimensions ${level.width}x${level.height} don't match expected ${expectedW}x${expectedH}, truncating`)();
      break;
    }
    validLevelCount++;
  }
  const texture = device.createTexture({
    ...baseOptions,
    format,
    usage: import_core4.Texture.TEXTURE | import_core4.Texture.COPY_DST,
    width: baseWidth,
    height: baseHeight,
    mipLevels: validLevelCount,
    data: baseLevel.data
  });
  for (let i = 1; i < validLevelCount; i++) {
    texture.writeData(levels[i].data, {
      width: levels[i].width,
      height: levels[i].height,
      mipLevel: i
    });
  }
  return texture;
}

// dist/parsers/parse-gltf.js
var defaultOptions = {
  modelOptions: {},
  pbrDebug: false,
  imageBasedLightingEnvironment: void 0,
  lights: true,
  useTangents: false,
  useByteColors: true,
  strictExtensions: false
};
function parseGLTF(device, gltf, options = {}) {
  const generatedTextures = /* @__PURE__ */ new Set();
  const combinedOptions = {
    ...defaultOptions,
    ...options,
    generatedTextures
  };
  const materialFactory = new import_engine3.MaterialFactory(device, { modules: [import_shadertools2.pbrMaterial] });
  const materials = (gltf.materials || []).map((gltfMaterial, materialIndex) => createGLTFMaterial(device, {
    id: getGLTFMaterialId(gltfMaterial, materialIndex),
    parsedPPBRMaterial: parseOwnedPBRMaterial(device, gltfMaterial, {}, {
      ...combinedOptions,
      gltf,
      validateAttributes: false
    }),
    materialFactory
  }));
  const gltfMaterialIdToMaterialMap = /* @__PURE__ */ new Map();
  (gltf.materials || []).forEach((gltfMaterial, materialIndex) => {
    gltfMaterialIdToMaterialMap.set(gltfMaterial.id, materials[materialIndex]);
  });
  const gltfMeshIdToNodeMap = /* @__PURE__ */ new Map();
  gltf.meshes.forEach((gltfMesh, idx) => {
    const newMesh = createNodeForGLTFMesh(device, gltfMesh, gltf, gltfMaterialIdToMaterialMap, combinedOptions);
    gltfMeshIdToNodeMap.set(gltfMesh.id, newMesh);
  });
  const gltfNodeIndexToNodeMap = /* @__PURE__ */ new Map();
  const gltfNodeIdToNodeMap = /* @__PURE__ */ new Map();
  const assignedMorphMeshes = /* @__PURE__ */ new Set();
  const assignedMeshes = /* @__PURE__ */ new Set();
  const independentlySkinnedMeshes = /* @__PURE__ */ new Set();
  gltf.nodes.forEach((gltfNode, idx) => {
    const newNode = createNodeForGLTFNode(device, gltfNode, combinedOptions);
    gltfNodeIndexToNodeMap.set(idx, newNode);
    gltfNodeIdToNodeMap.set(gltfNode.id, newNode);
  });
  gltf.nodes.forEach((gltfNode, idx) => {
    var _a, _b, _c, _d;
    gltfNodeIndexToNodeMap.get(idx).add((gltfNode.children ?? []).map(({ id }) => {
      const child = gltfNodeIdToNodeMap.get(id);
      if (!child)
        throw new Error(`Cannot find child ${id} of node ${idx}`);
      return child;
    }));
    if (gltfNode.mesh) {
      const sourceMesh = gltfNode.mesh;
      const instancing = getGLTFNodeInstancing(gltf, gltfNode);
      const hasMorphTargets = sourceMesh.primitives.some((primitive) => {
        var _a2;
        return Boolean((_a2 = primitive.targets) == null ? void 0 : _a2.length);
      });
      const mesh = instancing || hasMorphTargets && assignedMorphMeshes.has(sourceMesh.id) ? createNodeForGLTFMesh(device, sourceMesh, gltf, gltfMaterialIdToMaterialMap, combinedOptions, instancing || void 0) : gltfMeshIdToNodeMap.get(sourceMesh.id);
      if (!mesh) {
        throw new Error(`Cannot find mesh child ${gltfNode.mesh.id} of node ${idx}`);
      }
      const node = gltfNodeIndexToNodeMap.get(idx);
      const sharedMesh = gltfMeshIdToNodeMap.get(sourceMesh.id);
      const needsIndependentSkin = assignedMeshes.has(sourceMesh.id) && (gltfNode.skin !== void 0 || independentlySkinnedMeshes.has(sourceMesh.id));
      const ownedMesh = needsIndependentSkin && mesh === sharedMesh ? createNodeForGLTFMesh(device, sourceMesh, gltf, gltfMaterialIdToMaterialMap, combinedOptions) : mesh;
      node.add(ownedMesh);
      node.userData["gltfMesh"] = ownedMesh;
      assignedMeshes.add(sourceMesh.id);
      if (gltfNode.skin !== void 0) {
        independentlySkinnedMeshes.add(sourceMesh.id);
      }
      if (hasMorphTargets) {
        assignedMorphMeshes.add(sourceMesh.id);
        const targetCount = ((_b = (_a = sourceMesh.primitives.find((primitive) => {
          var _a2;
          return (_a2 = primitive.targets) == null ? void 0 : _a2.length;
        })) == null ? void 0 : _a.targets) == null ? void 0 : _b.length) || 0;
        const weights = gltfNode.weights || sourceMesh.weights || new Array(targetCount).fill(0);
        node.userData["morphMeshes"] = [ownedMesh];
        if ((_d = (_c = combinedOptions.modelOptions) == null ? void 0 : _c.userData) == null ? void 0 : _d["gltfAnimatedCrowd"]) {
          node.userData["morphWeights"] = [...weights];
        } else {
          setGLTFMorphWeights(node, weights);
        }
      }
    }
  });
  const scenes = gltf.scenes.map((gltfScene) => {
    const children = (gltfScene.nodes || []).map(({ id }) => {
      const child = gltfNodeIdToNodeMap.get(id);
      if (!child)
        throw new Error(`Cannot find child ${id} of scene ${gltfScene.name || gltfScene.id}`);
      return child;
    });
    return new import_engine3.GroupNode({
      id: gltfScene.name || gltfScene.id,
      children
    });
  });
  return {
    scenes,
    materials,
    gltfMeshIdToNodeMap,
    gltfNodeIdToNodeMap,
    gltfNodeIndexToNodeMap,
    generatedTextures
  };
}
function parseOwnedPBRMaterial(device, material, attributes, options) {
  const parsedMaterial = parsePBRMaterial(device, material, attributes, options);
  for (const texture of parsedMaterial.generatedTextures) {
    options.generatedTextures.add(texture);
  }
  return parsedMaterial;
}
function createNodeForGLTFNode(device, gltfNode, options) {
  var _a, _b;
  return new import_engine3.GroupNode({
    id: gltfNode.name || gltfNode.id,
    children: [],
    matrix: gltfNode.matrix,
    display: ((_b = (_a = gltfNode.extensions) == null ? void 0 : _a["KHR_node_visibility"]) == null ? void 0 : _b.visible) !== false,
    position: gltfNode.translation,
    rotation: gltfNode.rotation,
    scale: gltfNode.scale
  });
}
function createNodeForGLTFMesh(device, gltfMesh, gltf, gltfMaterialIdToMaterialMap, options, instancing) {
  const gltfPrimitives = gltfMesh.primitives || [];
  const primitives = gltfPrimitives.map((gltfPrimitive, i) => createNodeForGLTFPrimitive({
    device,
    gltfPrimitive,
    primitiveIndex: i,
    gltfMesh,
    gltf,
    gltfMaterialIdToMaterialMap,
    options,
    instancing
  }));
  const mesh = new import_engine3.GroupNode({
    id: gltfMesh.name || gltfMesh.id,
    children: primitives
  });
  return mesh;
}
function createNodeForGLTFPrimitive({ device, gltfPrimitive, primitiveIndex, gltfMesh, gltf, gltfMaterialIdToMaterialMap, options, instancing }) {
  var _a, _b, _c;
  const id = gltfPrimitive.name || `${gltfMesh.name || gltfMesh.id}-primitive-${primitiveIndex}`;
  const sourceVertexCount = getVertexCount(gltfPrimitive.attributes);
  const normalizedTopology = normalizeGLTFTopology(gltfPrimitive.mode ?? 4, (_a = gltfPrimitive.indices) == null ? void 0 : _a.value, sourceVertexCount);
  const geometry = createGeometry(id, gltfPrimitive, normalizedTopology);
  const vertexCount = geometry.vertexCount;
  const morphTargetState = createGLTFMorphTargetState(gltfPrimitive, gltf, geometry);
  const parsedPPBRMaterial = parseOwnedPBRMaterial(device, gltfPrimitive.material, geometry.attributes, { ...options, gltf });
  const modelNode = createGLTFModel(device, {
    id,
    geometry,
    material: gltfPrimitive.material ? gltfMaterialIdToMaterialMap.get(gltfPrimitive.material.id) || null : null,
    parsedPPBRMaterial,
    modelOptions: options.modelOptions,
    vertexCount,
    bounds: [gltfPrimitive.attributes.POSITION.min, gltfPrimitive.attributes.POSITION.max],
    instanceMatrices: instancing == null ? void 0 : instancing.matrices,
    morphTargets: morphTargetState == null ? void 0 : morphTargetState.targets
  });
  if (instancing) {
    modelNode.userData["gltfInstancing"] = instancing;
  }
  const sourceVariantMappings = ((_c = (_b = gltfPrimitive.extensions) == null ? void 0 : _b["KHR_materials_variants"]) == null ? void 0 : _c.mappings) || [];
  if (sourceVariantMappings.length) {
    const mappings = /* @__PURE__ */ new Map();
    for (const mapping of sourceVariantMappings) {
      const sourceMaterial = typeof mapping.material === "number" ? gltf.materials[mapping.material] : mapping.material;
      const material = sourceMaterial && gltfMaterialIdToMaterialMap.get(sourceMaterial.id);
      if (!material) {
        continue;
      }
      const variantMaterial = parseOwnedPBRMaterial(device, sourceMaterial, geometry.attributes, { ...options, gltf });
      for (const variantIndex of mapping.variants || []) {
        mappings.set(variantIndex, {
          material,
          parameters: {
            ...modelNode.model.parameters,
            ...variantMaterial.parameters,
            depthWriteEnabled: sourceMaterial.alphaMode !== "BLEND",
            cullMode: sourceMaterial.doubleSided ? "none" : "back"
          }
        });
      }
    }
    modelNode.userData["gltfMaterialVariants"] = {
      defaultMaterial: modelNode.model.material,
      defaultParameters: { ...modelNode.model.parameters },
      mappings
    };
  }
  if (morphTargetState) {
    modelNode.userData["morphTargets"] = morphTargetState;
  }
  return modelNode;
}
function createGLTFMorphTargetState(gltfPrimitive, gltf, geometry) {
  var _a, _b;
  if (!((_a = gltfPrimitive.targets) == null ? void 0 : _a.length)) {
    return void 0;
  }
  const baseAttributes = {};
  for (const attributeName of ["POSITION", "NORMAL", "TANGENT"]) {
    const values = (_b = geometry.attributes[attributeName]) == null ? void 0 : _b.value;
    if (values instanceof Float32Array) {
      baseAttributes[attributeName] = new Float32Array(values);
    }
  }
  const targets = gltfPrimitive.targets.map((target) => {
    const attributes = {};
    for (const attributeName of ["POSITION", "NORMAL", "TANGENT"]) {
      const accessorReference = target[attributeName];
      const accessor = typeof accessorReference === "number" ? gltf.accessors[accessorReference] : accessorReference;
      if ((accessor == null ? void 0 : accessor.value) && ArrayBuffer.isView(accessor.value)) {
        attributes[attributeName] = (0, import_engine3.decodeMorphTargetAttribute)(accessor);
      }
    }
    return attributes;
  });
  return { geometry, baseAttributes, targets };
}
function getVertexCount(attributes) {
  let vertexCount = Infinity;
  for (const attribute of Object.values(attributes)) {
    if (attribute) {
      const { value, size, components } = attribute;
      const attributeSize = size ?? components;
      if ((value == null ? void 0 : value.length) !== void 0 && attributeSize >= 1) {
        vertexCount = Math.min(vertexCount, value.length / attributeSize);
      }
    }
  }
  if (!Number.isFinite(vertexCount)) {
    throw new Error("Could not determine vertex count from attributes");
  }
  return vertexCount;
}
function createGeometry(id, gltfPrimitive, normalizedTopology) {
  var _a, _b;
  const attributes = {};
  for (const [attributeName, attribute] of Object.entries(gltfPrimitive.attributes)) {
    const { components, size, value, normalized } = attribute;
    const isMorphAttribute = attributeName === "POSITION" || attributeName === "NORMAL" || attributeName === "TANGENT";
    const shouldDecode = Boolean(((_a = gltfPrimitive.targets) == null ? void 0 : _a.length) && isMorphAttribute);
    attributes[attributeName] = {
      size: size ?? components,
      value: shouldDecode ? (0, import_engine3.decodeMorphTargetAttribute)({ value, normalized }) : value,
      normalized: shouldDecode ? false : normalized
    };
  }
  return new import_engine3.Geometry({
    id,
    topology: normalizedTopology.topology,
    indices: normalizedTopology.indices ?? ((_b = gltfPrimitive.indices) == null ? void 0 : _b.value),
    attributes
  });
}
function getGLTFMaterialId(gltfMaterial, materialIndex) {
  return gltfMaterial.name || gltfMaterial.id || `material-${materialIndex}`;
}

// dist/parsers/parse-gltf-lights.js
var import_core5 = require("@math.gl/core");
var import_shadertools3 = require("@luma.gl/shadertools");
function parseGLTFLights(gltf, options = {}) {
  var _a, _b, _c, _d;
  const lightDefs = options.lightDefinitions || // `postProcessGLTF()` moves KHR_lights_punctual into `gltf.lights`.
  gltf.lights || ((_b = (_a = gltf.extensions) == null ? void 0 : _a["KHR_lights_punctual"]) == null ? void 0 : _b["lights"]);
  if (!lightDefs || !Array.isArray(lightDefs) || lightDefs.length === 0) {
    return [];
  }
  const lights = [];
  const parentNodeById = createParentNodeMap(gltf.nodes || []);
  const worldMatrixByNodeId = /* @__PURE__ */ new Map();
  for (const node of gltf.nodes || []) {
    if (!isNodeVisible(node, parentNodeById, options.nodeVisibility)) {
      continue;
    }
    const lightIndex = node.light ?? ((_d = (_c = node.extensions) == null ? void 0 : _c.KHR_lights_punctual) == null ? void 0 : _d.light);
    if (typeof lightIndex !== "number" || options.nodeIdentifiers && !options.nodeIdentifiers.has(node.id)) {
      continue;
    }
    const gltfLight = lightDefs[lightIndex];
    if (!gltfLight) {
      continue;
    }
    const color = normalizeGLTFLightColor(gltfLight.color || [1, 1, 1], options.useByteColors ?? true);
    const intensity = gltfLight.intensity ?? 1;
    const range = gltfLight.range;
    const worldMatrix = getNodeWorldMatrix(node, parentNodeById, worldMatrixByNodeId);
    switch (gltfLight.type) {
      case "directional":
        lights.push(parseDirectionalLight(worldMatrix, color, intensity));
        break;
      case "point":
        lights.push(parsePointLight(worldMatrix, color, intensity, range));
        break;
      case "spot":
        lights.push(parseSpotLight(worldMatrix, color, intensity, range, gltfLight.spot));
        break;
      default:
        break;
    }
  }
  return lights;
}
function isNodeVisible(node, parentNodeById, liveNodeVisibility) {
  var _a, _b;
  let currentNode = node;
  while (currentNode) {
    const liveNode = liveNodeVisibility == null ? void 0 : liveNodeVisibility.get(currentNode.id);
    if (liveNode ? !liveNode.display : ((_b = (_a = currentNode.extensions) == null ? void 0 : _a["KHR_node_visibility"]) == null ? void 0 : _b.visible) === false) {
      return false;
    }
    currentNode = parentNodeById.get(currentNode.id);
  }
  return true;
}
function normalizeGLTFLightColor(color, useByteColors) {
  if (useByteColors) {
    return color.map((component) => component * 255);
  }
  return (0, import_shadertools3.normalizeByteColor3)(color, false);
}
function parsePointLight(worldMatrix, color, intensity, range) {
  const position = getLightPosition(worldMatrix);
  let attenuation = [1, 0, 0];
  if (range !== void 0 && range > 0) {
    attenuation = [1, 0, 1 / (range * range)];
  }
  return {
    type: "point",
    position,
    color,
    intensity,
    attenuation
  };
}
function parseDirectionalLight(worldMatrix, color, intensity) {
  const direction = getLightDirection(worldMatrix);
  return {
    type: "directional",
    direction,
    color,
    intensity
  };
}
function parseSpotLight(worldMatrix, color, intensity, range, spot = {}) {
  const position = getLightPosition(worldMatrix);
  const direction = getLightDirection(worldMatrix);
  let attenuation = [1, 0, 0];
  if (range !== void 0 && range > 0) {
    attenuation = [1, 0, 1 / (range * range)];
  }
  return {
    type: "spot",
    position,
    direction,
    color,
    intensity,
    attenuation,
    innerConeAngle: spot.innerConeAngle ?? 0,
    outerConeAngle: spot.outerConeAngle ?? Math.PI / 4
  };
}
function createParentNodeMap(nodes) {
  const parentNodeById = /* @__PURE__ */ new Map();
  for (const node of nodes) {
    for (const childNode of node.children || []) {
      parentNodeById.set(childNode.id, node);
    }
  }
  return parentNodeById;
}
function getNodeWorldMatrix(node, parentNodeById, worldMatrixByNodeId) {
  const cachedWorldMatrix = worldMatrixByNodeId.get(node.id);
  if (cachedWorldMatrix) {
    return cachedWorldMatrix;
  }
  const localMatrix = getNodeLocalMatrix(node);
  const parentNode = parentNodeById.get(node.id);
  const worldMatrix = parentNode ? new import_core5.Matrix4(getNodeWorldMatrix(parentNode, parentNodeById, worldMatrixByNodeId)).multiplyRight(localMatrix) : localMatrix;
  worldMatrixByNodeId.set(node.id, worldMatrix);
  return worldMatrix;
}
function getNodeLocalMatrix(node) {
  if (node.matrix) {
    return new import_core5.Matrix4(node.matrix);
  }
  const matrix = new import_core5.Matrix4();
  if (node.translation) {
    matrix.translate(node.translation);
  }
  if (node.rotation) {
    matrix.multiplyRight(new import_core5.Matrix4().fromQuaternion(node.rotation));
  }
  if (node.scale) {
    matrix.scale(node.scale);
  }
  return matrix;
}
function getLightPosition(worldMatrix) {
  return worldMatrix.transformAsPoint([0, 0, 0]);
}
function getLightDirection(worldMatrix) {
  return worldMatrix.transformDirection([0, 0, -1]);
}

// dist/gltf/gltf-animator.js
var import_core6 = require("@luma.gl/core");
var import_engine4 = require("@luma.gl/engine");
var GLTFAnimationClip = class extends import_engine4.AnimationClipController {
  /** Animation definition being played. */
  animation;
  /** Target scenegraph lookup table. */
  gltfNodeIdToNodeMap;
  onVisibilityChange;
  cameras;
  lightDefinitions;
  onLightChange;
  /** Materials aligned with the source glTF materials array. */
  materials;
  /** Format-independent engine clip generated from the parsed glTF channels. */
  clip;
  /** Shared engine animation mixer. */
  mixer;
  /** Engine playback action controlling this animation clip. */
  action;
  /** Mutable runtime texture-transform state for animated material slots. */
  materialTextureTransformState = /* @__PURE__ */ new Map();
  /** Creates a single-animation controller. */
  constructor(props) {
    super({ name: props.animation.name || "unnamed" });
    this.animation = props.animation;
    this.gltfNodeIdToNodeMap = props.gltfNodeIdToNodeMap;
    this.onVisibilityChange = props.onVisibilityChange;
    this.cameras = props.cameras || [];
    this.lightDefinitions = props.lightDefinitions || [];
    this.onLightChange = props.onLightChange;
    this.materials = props.materials || [];
    this.animation.name ||= "unnamed";
    this.name = this.animation.name;
    if (this.animation.channels.some((channel) => channel.type === "material" || channel.type === "textureTransform") && !this.materials.length) {
      throw new Error(`Animation ${this.animation.name} targets materials, but GLTFAnimator was created without a materials array`);
    }
    this.mixer = props.mixer || new import_engine4.AnimationMixer();
    this.clip = new import_engine4.AnimationClip({
      name: this.name,
      tracks: this.animation.channels.map((channel) => this.createAnimationTrack(channel))
    });
    this.action = this.mixer.clipAction(this.clip).play();
  }
  /** Applies the resolved local clip time in seconds. */
  applyTime(time) {
    this.action.setTime(time);
    this.mixer.update(0);
  }
  createAnimationTrack(channel) {
    const interpolation = getAnimationInterpolation(channel.sampler.interpolation);
    if (channel.type === "node") {
      return new import_engine4.AnimationTrack({
        name: `${channel.targetNodeId}.${channel.path}`,
        times: channel.sampler.input,
        values: channel.sampler.output,
        interpolation,
        valueType: channel.path === "rotation" ? "quaternion" : "vector",
        binding: {
          id: `node:${channel.targetNodeId}:${channel.path}`,
          getValue: () => this.getNodeAnimationValue(channel.targetNodeId, channel.path),
          setValue: (value) => this.applyNodeAnimationValue(channel.targetNodeId, channel.path, value)
        }
      });
    }
    if (channel.type === "camera" || channel.type === "light") {
      return new import_engine4.AnimationTrack({
        name: channel.pointer,
        times: channel.sampler.input,
        values: channel.sampler.output,
        interpolation,
        binding: {
          id: channel.pointer,
          getValue: () => this.getSceneAnimationValue(channel),
          setValue: (value) => this.applySceneAnimationValue(channel, value)
        }
      });
    }
    const material = this.materials[channel.targetMaterialIndex];
    if (!material) {
      throw new Error(`Cannot find animation target material ${channel.targetMaterialIndex} for ${channel.pointer}`);
    }
    return new import_engine4.AnimationTrack({
      name: channel.pointer,
      times: channel.sampler.input,
      values: channel.sampler.output,
      interpolation,
      binding: {
        id: channel.pointer,
        getValue: channel.type === "material" ? () => getMaterialAnimationBindingValue(material, channel) : void 0,
        setValue: (value) => {
          if (channel.type === "material") {
            applyMaterialAnimationValue(material, channel, value);
          } else {
            applyTextureTransformAnimationValue(material, channel, value, this.materialTextureTransformState);
          }
        }
      }
    });
  }
  getNodeAnimationValue(targetNodeId, path) {
    const targetNode = this.getTargetNode(targetNodeId);
    switch (path) {
      case "translation":
        return Array.from(targetNode.position);
      case "rotation":
        return Array.from(targetNode.rotation);
      case "scale":
        return Array.from(targetNode.scale);
      case "weights":
        return Array.from(targetNode.userData["morphWeights"] || []);
      case "visibility":
        return [targetNode.display ? 1 : 0];
      default:
        return [];
    }
  }
  applyNodeAnimationValue(targetNodeId, path, value) {
    var _a;
    const targetNode = this.getTargetNode(targetNodeId);
    switch (path) {
      case "translation":
        targetNode.setPosition(value).updateMatrix();
        break;
      case "rotation":
        targetNode.setRotation(value).updateMatrix();
        break;
      case "scale":
        targetNode.setScale(value).updateMatrix();
        break;
      case "weights":
        setGLTFMorphWeights(targetNode, value);
        break;
      case "visibility":
        targetNode.setProps({ display: value[0] !== 0 });
        (_a = this.onVisibilityChange) == null ? void 0 : _a.call(this);
        break;
      default:
        import_core6.log.warn(`Bad animation path ${path}`)();
    }
  }
  getTargetNode(targetNodeId) {
    const targetNode = this.gltfNodeIdToNodeMap.get(targetNodeId);
    if (!targetNode) {
      throw new Error(`Cannot find animation target node ${targetNodeId}`);
    }
    return targetNode;
  }
  getSceneAnimationValue(channel) {
    var _a, _b;
    if (channel.type === "camera") {
      const camera = this.cameras[channel.targetCameraIndex];
      const value2 = (_a = camera == null ? void 0 : camera[channel.projection]) == null ? void 0 : _a[channel.property];
      return typeof value2 === "number" ? [value2] : [];
    }
    const light = this.lightDefinitions[channel.targetLightIndex];
    const value = channel.property === "innerConeAngle" || channel.property === "outerConeAngle" ? (_b = light == null ? void 0 : light["spot"]) == null ? void 0 : _b[channel.property] : light == null ? void 0 : light[channel.property];
    if (Array.isArray(value)) {
      return channel.component === void 0 ? [...value] : [value[channel.component]];
    }
    return typeof value === "number" ? [value] : [];
  }
  applySceneAnimationValue(channel, value) {
    var _a;
    if (channel.type === "camera") {
      const camera = this.cameras[channel.targetCameraIndex];
      if (camera == null ? void 0 : camera[channel.projection]) {
        camera[channel.projection][channel.property] = value[0];
      }
      return;
    }
    const light = this.lightDefinitions[channel.targetLightIndex];
    if (!light) {
      return;
    }
    if (channel.property === "innerConeAngle" || channel.property === "outerConeAngle") {
      light["spot"] ||= {};
      light["spot"][channel.property] = value[0];
    } else if (channel.component !== void 0) {
      const color = [...light[channel.property] || [1, 1, 1]];
      color[channel.component] = value[0];
      light[channel.property] = color;
    } else {
      light[channel.property] = value.length === 1 ? value[0] : [...value];
    }
    (_a = this.onLightChange) == null ? void 0 : _a.call(this);
  }
};
var GLTFAnimator = class extends import_engine4.Animator {
  /** Shared engine mixer containing every parsed glTF clip and target binding. */
  mixer;
  /** Name of the currently selected clip, when explicit selection has been requested. */
  activeClip;
  onUpdate;
  previousTimeSeconds;
  /** Creates an animator for the supplied glTF scenegraph. */
  constructor(props) {
    var _a;
    const mixer = new import_engine4.AnimationMixer();
    super(props.animations.map((animation, index) => {
      const name = animation.name || `Animation-${index}`;
      return new GLTFAnimationClip({
        gltfNodeIdToNodeMap: props.gltfNodeIdToNodeMap,
        onVisibilityChange: props.onVisibilityChange,
        cameras: props.cameras,
        lightDefinitions: props.lightDefinitions,
        onLightChange: props.onLightChange,
        materials: props.materials,
        mixer,
        animation: { name, channels: animation.channels }
      });
    }));
    this.mixer = mixer;
    this.onUpdate = props.onUpdate;
    this.activeClip = (_a = this.clips[0]) == null ? void 0 : _a.name;
    if (props.autoplay === false) {
      this.clips.forEach((clip) => {
        clip.playing = false;
        clip.action.stop();
      });
    } else if (props.autoplay === "first" && this.activeClip) {
      this.selectClip(this.activeClip);
    }
  }
  /** Registers the dependent scenegraph update performed once after each animation frame. */
  setUpdateHandler(callback) {
    this.onUpdate = callback;
    return this;
  }
  /** Resolves legacy wall-clock milliseconds while evaluating all clips in one mixer pass. */
  setTime(timeMs) {
    var _a;
    const absoluteTimeSeconds = timeMs / 1e3;
    const elapsedSeconds = this.previousTimeSeconds === void 0 ? 0 : absoluteTimeSeconds - this.previousTimeSeconds;
    this.previousTimeSeconds = absoluteTimeSeconds;
    const scaledElapsedSeconds = elapsedSeconds * this.mixer.timeScale;
    this.clips.forEach((clip) => {
      if (!clip.playing) {
        clip.action.stop();
        return;
      }
      if (clip.action.paused) {
        return;
      }
      clip.action.resume();
      const localTime = Math.max(0, absoluteTimeSeconds - clip.startTime) * clip.speed;
      clip.action.setTime(localTime - scaledElapsedSeconds * clip.action.timeScale);
    });
    this.mixer.update(elapsedSeconds);
    (_a = this.onUpdate) == null ? void 0 : _a.call(this);
  }
  /** Advances clips using delta seconds without converting the legacy millisecond clock. */
  update(deltaSeconds) {
    var _a;
    this.mixer.update(deltaSeconds);
    (_a = this.onUpdate) == null ? void 0 : _a.call(this);
  }
  /** Selects one imported clip and optionally crossfades from the previous selection. */
  selectClip(name, options = {}) {
    const nextClip = this.clips.find((clip) => clip.name === name);
    if (!nextClip) {
      throw new Error(`Unknown animation clip: ${name}`);
    }
    const previousClip = this.clips.find((clip) => clip.name === this.activeClip);
    const crossFadeDuration = options.crossFadeDuration || 0;
    for (const clip of this.clips) {
      if (clip !== nextClip && !(crossFadeDuration > 0 && clip === previousClip)) {
        clip.playing = false;
        clip.action.stop();
      }
    }
    nextClip.playing = true;
    if (crossFadeDuration > 0 && previousClip && previousClip !== nextClip) {
      previousClip.playing = true;
      previousClip.action.crossFadeTo(nextClip.action, crossFadeDuration);
    } else {
      nextClip.action.reset().setEffectiveWeight(1).play();
    }
    this.activeClip = name;
    return nextClip;
  }
};
function getAnimationInterpolation(interpolation) {
  switch (interpolation) {
    case "STEP":
    case "LINEAR":
    case "CUBICSPLINE":
      return interpolation;
    default:
      throw new Error(`Unsupported animation interpolation: ${interpolation}`);
  }
}
function getMaterialAnimationBindingValue(material, channel) {
  var _a;
  const uniformValues = material.shaderInputs.getUniformValues();
  const currentValue = (_a = uniformValues["pbrMaterial"]) == null ? void 0 : _a[channel.property];
  if (Array.isArray(currentValue)) {
    return channel.component === void 0 ? [...currentValue] : [currentValue[channel.component]];
  }
  return typeof currentValue === "number" ? [currentValue] : [];
}
function applyMaterialAnimationValue(material, channel, value) {
  const pbrMaterial3 = channel.component !== void 0 ? {
    [channel.property]: updateMaterialArrayComponent(getCurrentMaterialValue(material, channel.property), channel.component, value[0])
  } : {
    [channel.property]: value.length === 1 ? value[0] : value
  };
  material.setProps({ pbrMaterial: pbrMaterial3 });
}
function getCurrentMaterialValue(material, property) {
  var _a;
  const uniformValues = material.shaderInputs.getUniformValues();
  const currentValue = (_a = uniformValues["pbrMaterial"]) == null ? void 0 : _a[property];
  return Array.isArray(currentValue) ? [...currentValue] : [];
}
function updateMaterialArrayComponent(currentValue, component, nextValue) {
  const updatedValue = [...currentValue];
  updatedValue[component] = nextValue;
  return updatedValue;
}
function applyTextureTransformAnimationValue(material, channel, value, materialTextureTransformState) {
  const slotDefinition = getTextureTransformSlotDefinition(channel.textureSlot);
  const currentTransform = getCurrentTextureTransform(materialTextureTransformState, material, channel);
  switch (channel.path) {
    case "offset":
      if (channel.component !== void 0) {
        currentTransform.offset[channel.component] = value[0];
      } else {
        currentTransform.offset = [value[0], value[1]];
      }
      break;
    case "rotation":
      currentTransform.rotation = value[0];
      break;
    case "scale":
      if (channel.component !== void 0) {
        currentTransform.scale[channel.component] = value[0];
      } else {
        currentTransform.scale = [value[0], value[1]];
      }
      break;
  }
  material.setProps({
    pbrMaterial: {
      [slotDefinition.uvTransformUniform]: getTextureTransformDeltaMatrix(channel.baseTransform, currentTransform)
    }
  });
}
function getCurrentTextureTransform(materialTextureTransformState, material, channel) {
  const materialState = materialTextureTransformState.get(material) || {};
  let textureTransformState = materialState[channel.textureSlot];
  if (!textureTransformState) {
    textureTransformState = {
      offset: [...channel.baseTransform.offset],
      rotation: channel.baseTransform.rotation,
      scale: [...channel.baseTransform.scale]
    };
    materialState[channel.textureSlot] = textureTransformState;
    materialTextureTransformState.set(material, materialState);
  }
  return textureTransformState;
}

// dist/gltf/gltf-skin.js
var import_engine5 = require("@luma.gl/engine");
var import_core7 = require("@math.gl/core");
var GLTFSkinController = class {
  bindings;
  scenes;
  constructor(props) {
    this.scenes = props.scenes;
    this.bindings = makeSkinBindings(props);
    this.update();
  }
  /** Re-evaluates every source skin after its animated node transforms have changed. */
  update() {
    if (this.bindings.length === 0) {
      return;
    }
    const worldMatrices = /* @__PURE__ */ new Map();
    for (const scene of this.scenes) {
      scene.preorderTraversal((node, { worldMatrix }) => {
        if (node instanceof import_engine5.GroupNode) {
          worldMatrices.set(node, new import_core7.Matrix4(worldMatrix));
        }
      });
    }
    for (const binding of this.bindings) {
      (0, import_engine5.updateSkinJointMatrices)({
        joints: binding.joints,
        meshNode: binding.node,
        worldMatrices,
        inverseBindMatrices: binding.inverseBindMatrices,
        target: binding.jointMatrices
      });
      for (const modelNode of binding.models) {
        modelNode.model.shaderInputs.setProps({ skin: { jointMatrices: binding.jointMatrices } });
      }
    }
  }
  /** Finds the binding for an authored glTF node index or generated scenegraph node. */
  getBinding(node) {
    return this.bindings.find((binding) => typeof node === "number" ? binding.nodeIndex === node : binding.node === node);
  }
};
function makeSkinBindings(props) {
  var _a;
  const { gltf, gltfNodeIndexToNodeMap } = props;
  const bindings = [];
  const sourceSkins = gltf.skins || [];
  const reachableNodes = /* @__PURE__ */ new Set();
  for (const scene of props.scenes) {
    scene.preorderTraversal((node) => {
      if (node instanceof import_engine5.GroupNode) {
        reachableNodes.add(node);
      }
    });
  }
  for (const [nodeIndex, sourceNode] of gltf.nodes.entries()) {
    const sourceNodeSkin = sourceNode.skin;
    if (sourceNodeSkin === void 0 || !sourceNode.mesh) {
      continue;
    }
    const skinIndex = resolveGLTFSkinIndex(gltf, sourceNodeSkin);
    const sourceSkin = sourceSkins[skinIndex];
    const node = gltfNodeIndexToNodeMap.get(nodeIndex);
    if (!sourceSkin || !node || !reachableNodes.has(node)) {
      continue;
    }
    const joints = sourceSkin.joints.flatMap((jointIndex) => {
      const joint = gltfNodeIndexToNodeMap.get(jointIndex);
      return joint ? [joint] : [];
    });
    if (joints.length !== sourceSkin.joints.length) {
      continue;
    }
    const sourceMesh = sourceNode.mesh;
    const ownedMesh = node.userData["gltfMesh"];
    const meshNode = ownedMesh instanceof import_engine5.GroupNode ? ownedMesh : node.children.find((child) => child instanceof import_engine5.GroupNode && child.id === (sourceMesh.name || sourceMesh.id));
    if (!(meshNode instanceof import_engine5.GroupNode)) {
      continue;
    }
    const models = meshNode.children.flatMap((child) => child instanceof import_engine5.ModelNode ? [child] : []);
    const inverseBindMatrices = (_a = sourceSkin.inverseBindMatrices) == null ? void 0 : _a.value;
    bindings.push({
      nodeIndex,
      skinIndex,
      node,
      joints,
      ...inverseBindMatrices instanceof Float32Array ? { inverseBindMatrices } : {},
      jointMatrices: new Float32Array(joints.length * 16),
      models
    });
  }
  return bindings;
}
function resolveGLTFSkinIndex(gltf, sourceSkin) {
  if (typeof sourceSkin === "number") {
    return sourceSkin;
  }
  return (gltf.skins || []).findIndex((candidate) => {
    var _a;
    if (candidate === sourceSkin || sourceSkin.id && candidate.id === sourceSkin.id) {
      return true;
    }
    if (candidate.joints.length !== ((_a = sourceSkin.joints) == null ? void 0 : _a.length) || !candidate.joints.every((joint, index) => {
      var _a2;
      return joint === ((_a2 = sourceSkin.joints) == null ? void 0 : _a2[index]);
    })) {
      return false;
    }
    if (typeof sourceSkin.inverseBindMatrices === "number") {
      const accessor = gltf.accessors[sourceSkin.inverseBindMatrices];
      return !candidate.inverseBindMatrices || candidate.inverseBindMatrices === accessor;
    }
    return true;
  });
}

// dist/parsers/parse-gltf-animations.js
var import_core8 = require("@luma.gl/core");

// dist/gltf/gltf-extension-support.js
var UNKNOWN_EXTENSION_SUPPORT = {
  supportLevel: "none",
  standardStatus: "unknown",
  comment: "Not currently listed in the luma.gl glTF extension support registry."
};
var GLTF_EXTENSION_SUPPORT_REGISTRY = {
  KHR_draco_mesh_compression: {
    supportLevel: "built-in",
    standardStatus: "ratified",
    comment: "Decoded by loaders.gl before luma.gl builds the scenegraph."
  },
  EXT_meshopt_compression: {
    supportLevel: "built-in",
    standardStatus: "ratified",
    comment: "EXT meshopt-compressed buffer views are decoded by loaders.gl before rendering."
  },
  KHR_meshopt_compression: {
    supportLevel: "none",
    standardStatus: "release-candidate",
    comment: "The installed loaders.gl GLTFLoader supports EXT_meshopt_compression, not the KHR release candidate."
  },
  KHR_mesh_quantization: {
    supportLevel: "built-in",
    standardStatus: "ratified",
    comment: "Loader-materialized quantized accessors retain their typed values and normalization."
  },
  EXT_mesh_features: {
    supportLevel: "loader-only",
    standardStatus: "ratified",
    comment: "Feature identifiers are decoded by loaders.gl; automatic rendering and picking are application-owned."
  },
  EXT_structural_metadata: {
    supportLevel: "loader-only",
    standardStatus: "ratified",
    comment: "Structural metadata is decoded by loaders.gl; automatic rendering and querying are application-owned."
  },
  KHR_lights_punctual: {
    supportLevel: "built-in",
    standardStatus: "ratified",
    comment: "Parsed into luma.gl Light objects."
  },
  KHR_materials_unlit: {
    supportLevel: "built-in",
    standardStatus: "ratified",
    comment: "Unlit materials bypass the default lighting path."
  },
  KHR_materials_emissive_strength: {
    supportLevel: "built-in",
    standardStatus: "ratified",
    comment: "Applied by the stock PBR shader."
  },
  KHR_texture_basisu: {
    supportLevel: "built-in",
    standardStatus: "ratified",
    comment: "BasisU / KTX2 textures pass through when the device supports them."
  },
  KHR_texture_transform: {
    supportLevel: "built-in",
    standardStatus: "ratified",
    comment: "Per-slot UV transforms and animated pointers are applied at runtime; avoid duplicate legacy loader-side baking."
  },
  EXT_texture_webp: {
    supportLevel: "loader-only",
    standardStatus: "ratified",
    comment: "Texture source is resolved during load; final support depends on browser and device decode support."
  },
  EXT_texture_avif: {
    supportLevel: "none",
    standardStatus: "ratified",
    comment: "The image loader can decode supported AVIF images, but GLTFLoader does not select EXT_texture_avif sources."
  },
  KHR_materials_specular: {
    supportLevel: "built-in",
    standardStatus: "ratified",
    comment: "The stock shader now applies specular factors and textures to the dielectric F0 term."
  },
  KHR_materials_ior: {
    supportLevel: "built-in",
    standardStatus: "ratified",
    comment: "The stock shader now drives dielectric reflectance from the glTF IOR value."
  },
  KHR_materials_transmission: {
    supportLevel: "built-in",
    standardStatus: "ratified",
    comment: "The stock shader now applies transmission to the base layer and exposes transparency through alpha, without a scene-color refraction buffer."
  },
  KHR_materials_volume: {
    supportLevel: "built-in",
    standardStatus: "ratified",
    comment: "Thickness and attenuation now tint transmitted light in the stock shader."
  },
  KHR_materials_clearcoat: {
    supportLevel: "built-in",
    standardStatus: "ratified",
    comment: "The stock shader now adds a secondary clearcoat specular lobe."
  },
  KHR_materials_sheen: {
    supportLevel: "built-in",
    standardStatus: "ratified",
    comment: "The stock shader now adds a sheen lobe for cloth-like materials."
  },
  KHR_materials_iridescence: {
    supportLevel: "built-in",
    standardStatus: "ratified",
    comment: "The stock shader now tints specular response with a view-dependent thin-film iridescence approximation."
  },
  KHR_materials_anisotropy: {
    supportLevel: "built-in",
    standardStatus: "ratified",
    comment: "The stock shader now shapes highlights and IBL response with an anisotropy-direction approximation."
  },
  KHR_materials_pbrSpecularGlossiness: {
    supportLevel: "loader-only",
    standardStatus: "archived",
    comment: "Extension data can be loaded, but it is not translated into the default metallic-roughness material path."
  },
  KHR_materials_variants: {
    supportLevel: "parsed-and-wired",
    standardStatus: "ratified",
    comment: "Primitive material variants can be selected and restored on the generated scenegraph."
  },
  EXT_mesh_gpu_instancing: {
    supportLevel: "built-in",
    standardStatus: "ratified",
    comment: "Accessor-backed instance transforms use one instanced draw per source primitive."
  },
  KHR_node_visibility: {
    supportLevel: "parsed-and-wired",
    standardStatus: "ratified",
    comment: "Recursive node visibility controls rendered geometry, punctual lights, and animation."
  },
  KHR_animation_pointer: {
    supportLevel: "parsed-and-wired",
    standardStatus: "ratified",
    comment: "Node transforms, morph weights and visibility, material factors, texture transforms, camera projections, and punctual lights are wired to runtime updates."
  },
  EXT_materials_bump: {
    supportLevel: "built-in",
    standardStatus: "draft",
    comment: "The experimental bump-map draft perturbs the canonical surface normal from a linear height texture."
  },
  KHR_materials_diffuse_transmission: {
    supportLevel: "built-in",
    standardStatus: "release-candidate",
    comment: "The Khronos release candidate adds energy-conserving back-lit diffuse transmission and independent color/factor textures."
  },
  KHR_materials_dispersion: {
    supportLevel: "parsed-and-wired",
    standardStatus: "ratified",
    comment: "The canonical PBR shader separates red, green, and blue transmission using wavelength-dependent refraction."
  },
  KHR_materials_volume_scatter: {
    supportLevel: "parsed-and-wired",
    standardStatus: "draft",
    comment: "The unratified volume-scattering draft is approximated per surface; random-walk and screen-space diffusion are not implemented."
  },
  KHR_xmp: {
    supportLevel: "none",
    standardStatus: "archived",
    comment: "Metadata payloads remain in the loaded glTF, but luma.gl does not interpret them."
  },
  KHR_xmp_json_ld: {
    supportLevel: "none",
    standardStatus: "ratified",
    comment: "Metadata is preserved in the glTF, but luma.gl does not interpret it."
  },
  EXT_lights_image_based: {
    supportLevel: "none",
    standardStatus: "multi-vendor",
    comment: "Use loadPBREnvironment() or custom environment setup instead."
  },
  EXT_texture_video: {
    supportLevel: "none",
    standardStatus: "multi-vendor",
    comment: "Video textures are not created automatically by the stock pipeline."
  },
  MSFT_lod: {
    supportLevel: "parsed-and-wired",
    standardStatus: "vendor",
    comment: "Node levels are parsed and selected by opt-in animated crowds; material LOD and GPU-driven selection are not implemented."
  }
};
function getRegisteredGLTFExtensions() {
  return Object.entries(GLTF_EXTENSION_SUPPORT_REGISTRY).sort(([leftExtensionName], [rightExtensionName]) => leftExtensionName.localeCompare(rightExtensionName)).map(([extensionName, definition]) => ({
    extensionName,
    supported: isRuntimeSupported(definition.supportLevel),
    ...definition
  }));
}
function getGLTFExtensionSupportSummary() {
  const summary = {
    total: 0,
    supported: 0,
    bySupportLevel: { "built-in": 0, "parsed-and-wired": 0, "loader-only": 0, none: 0 },
    byStandardStatus: {
      ratified: 0,
      "release-candidate": 0,
      "multi-vendor": 0,
      vendor: 0,
      draft: 0,
      archived: 0,
      unknown: 0
    }
  };
  for (const extension of getRegisteredGLTFExtensions()) {
    summary.total++;
    summary.supported += extension.supported ? 1 : 0;
    summary.bySupportLevel[extension.supportLevel]++;
    summary.byStandardStatus[extension.standardStatus]++;
  }
  return summary;
}
function getGLTFExtensionSupport(gltf, device) {
  const extensionNames = Array.from(collectGLTFExtensionNames(gltf)).sort();
  const requiredExtensionNames = new Set(gltf.extensionsRequired || []);
  const extensionSupportEntries = extensionNames.map((extensionName) => {
    const extensionSupportDefinition = GLTF_EXTENSION_SUPPORT_REGISTRY[extensionName] || UNKNOWN_EXTENSION_SUPPORT;
    const deviceSupport = getDeviceExtensionSupport(extensionName, extensionSupportDefinition, gltf, device);
    return [
      extensionName,
      {
        extensionName,
        required: requiredExtensionNames.has(extensionName),
        supported: isRuntimeSupported(deviceSupport.supportLevel),
        supportLevel: deviceSupport.supportLevel,
        standardStatus: extensionSupportDefinition.standardStatus,
        comment: deviceSupport.comment
      }
    ];
  });
  return new Map(extensionSupportEntries);
}
function getUnsupportedRequiredGLTFExtensions(gltf, device) {
  return Array.from(getGLTFExtensionSupport(gltf, device).values()).filter((extension) => extension.required && !extension.supported);
}
function assertSupportedGLTFExtensions(gltf, device) {
  const unsupportedExtensions = getUnsupportedRequiredGLTFExtensions(gltf, device);
  if (unsupportedExtensions.length) {
    throw new Error(`Unsupported required glTF extensions: ${unsupportedExtensions.map((extension) => extension.extensionName).join(", ")}`);
  }
}
function getDeviceExtensionSupport(extensionName, definition, gltf, device) {
  if (extensionName !== "KHR_texture_basisu" || !device) {
    return definition;
  }
  const compressedFormats = collectCompressedTextureFormats(gltf);
  const unsupportedFormat = compressedFormats.find((format) => format === null || !device.isTextureFormatSupported(format));
  if (unsupportedFormat === void 0) {
    return definition;
  }
  return {
    supportLevel: "none",
    comment: unsupportedFormat === null ? `The ${device.type} device cannot use a BasisU texture whose transcoded GPU format is missing.` : `The ${device.type} device does not support the transcoded BasisU texture format '${unsupportedFormat}'.`
  };
}
function collectCompressedTextureFormats(gltf) {
  var _a;
  const formats = [];
  for (const texture of gltf.textures || []) {
    const image = (_a = texture == null ? void 0 : texture.source) == null ? void 0 : _a.image;
    if (!(image == null ? void 0 : image.compressed)) {
      continue;
    }
    const firstLevel = Array.isArray(image.data) ? image.data[0] : Array.isArray(image.mipmaps) ? image.mipmaps[0] : void 0;
    formats.push((firstLevel == null ? void 0 : firstLevel.textureFormat) ?? null);
  }
  return formats;
}
function getRegisteredGLTFExtensionSupport(extensionName) {
  return GLTF_EXTENSION_SUPPORT_REGISTRY[extensionName] || null;
}
function collectGLTFExtensionNames(gltf) {
  var _a;
  const gltfWithRemovedExtensions = gltf;
  const extensionNames = /* @__PURE__ */ new Set();
  addExtensionNames(extensionNames, gltf.extensionsUsed);
  addExtensionNames(extensionNames, gltf.extensionsRequired);
  addExtensionNames(extensionNames, gltfWithRemovedExtensions.extensionsRemoved);
  addExtensionNames(extensionNames, Object.keys(gltf.extensions || {}));
  if (((_a = gltfWithRemovedExtensions.lights) == null ? void 0 : _a.length) || (gltf.nodes || []).some((node) => "light" in node)) {
    extensionNames.add("KHR_lights_punctual");
  }
  if ((gltf.materials || []).some((material) => {
    var _a2;
    const gltfMaterial = material;
    return gltfMaterial.unlit || ((_a2 = gltfMaterial.extensions) == null ? void 0 : _a2.KHR_materials_unlit);
  })) {
    extensionNames.add("KHR_materials_unlit");
  }
  return extensionNames;
}
function addExtensionNames(extensionNames, newExtensionNames = []) {
  for (const extensionName of newExtensionNames) {
    extensionNames.add(extensionName);
  }
}
function isRuntimeSupported(supportLevel) {
  return supportLevel === "built-in" || supportLevel === "parsed-and-wired";
}

// dist/parsers/parse-gltf-animations.js
function parseGLTFAnimations(gltf) {
  const gltfAnimations = gltf.animations || [];
  const accessorCache1D = /* @__PURE__ */ new Map();
  const accessorCache2D = /* @__PURE__ */ new Map();
  return gltfAnimations.flatMap((animation, index) => {
    const name = animation.name || `Animation-${index}`;
    const samplerCache = /* @__PURE__ */ new Map();
    const channels = animation.channels.flatMap(({ sampler, target }) => {
      const morphTargetCount = getMorphTargetCount(gltf, target);
      const samplerIdentifier = `${sampler}:${morphTargetCount ?? 0}`;
      let parsedSampler = samplerCache.get(samplerIdentifier);
      if (!parsedSampler) {
        const gltfSampler = animation.samplers[sampler];
        if (!gltfSampler) {
          throw new Error(`Cannot find animation sampler ${sampler}`);
        }
        const { input, interpolation = "LINEAR", output } = gltfSampler;
        const keyframeTimes = accessorToJsArray1D(gltf.accessors[input], accessorCache1D);
        const keyframeValues = accessorToJsArray2D(gltf.accessors[output], accessorCache2D);
        parsedSampler = {
          input: keyframeTimes,
          interpolation,
          output: morphTargetCount !== void 0 ? groupMorphTargetValues(keyframeValues, keyframeTimes.length, interpolation, morphTargetCount) : keyframeValues
        };
        samplerCache.set(samplerIdentifier, parsedSampler);
      }
      const parsedChannel = parseAnimationChannel(gltf, target, parsedSampler);
      return parsedChannel ? [parsedChannel] : [];
    });
    return channels.length ? [{ name, channels }] : [];
  });
}
function parseAnimationChannel(gltf, target, sampler) {
  if (target.path === "pointer") {
    return parseAnimationPointerChannel(gltf, target, sampler);
  }
  const path = getNodeAnimationPath(target.path);
  if (!path) {
    return null;
  }
  const targetNode = gltf.nodes[target.node ?? 0];
  if (!targetNode) {
    throw new Error(`Cannot find animation target ${target.node}`);
  }
  return {
    type: "node",
    sampler,
    targetNodeId: targetNode.id,
    path
  };
}
function parseAnimationPointerChannel(gltf, target, sampler) {
  var _a, _b;
  const pointer = (_b = (_a = target.extensions) == null ? void 0 : _a["KHR_animation_pointer"]) == null ? void 0 : _b.pointer;
  if (typeof pointer !== "string" || !pointer.startsWith("/")) {
    import_core8.log.warn("KHR_animation_pointer channel is missing a valid JSON pointer and will be skipped")();
    return null;
  }
  const pointerSegments = splitJsonPointer(pointer);
  switch (pointerSegments[0]) {
    case "nodes":
      return parseNodePointerAnimationChannel(gltf, pointerSegments, sampler, pointer);
    case "materials":
      return parseMaterialPointerAnimationChannel(gltf, pointerSegments, sampler, pointer);
    case "cameras":
      return parseCameraPointerAnimationChannel(gltf, pointerSegments, sampler, pointer);
    case "extensions":
      if (pointerSegments[1] === "KHR_lights_punctual") {
        return parseLightPointerAnimationChannel(gltf, pointerSegments, sampler, pointer);
      }
      break;
    default:
      break;
  }
  warnUnsupportedAnimationPointer(pointer, `top-level target "${pointerSegments[0]}" has no runtime animation mapping`);
  return null;
}
function parseCameraPointerAnimationChannel(gltf, pointerSegments, sampler, pointer) {
  var _a;
  const cameraIndex = Number(pointerSegments[1]);
  const camera = (_a = gltf.cameras) == null ? void 0 : _a[cameraIndex];
  const projection = pointerSegments[2];
  const property = pointerSegments[3];
  const perspectiveProperties = ["aspectRatio", "yfov", "znear", "zfar"];
  const orthographicProperties = ["xmag", "ymag", "znear", "zfar"];
  if (pointerSegments.length !== 4 || !Number.isInteger(cameraIndex) || !camera || projection !== "perspective" && projection !== "orthographic" || camera.type !== projection || !(projection === "perspective" ? perspectiveProperties : orthographicProperties).includes(property)) {
    warnUnsupportedAnimationPointer(pointer, "camera pointers must target a supported projection property");
    return null;
  }
  return {
    type: "camera",
    sampler,
    pointer,
    targetCameraIndex: cameraIndex,
    projection,
    property
  };
}
function parseLightPointerAnimationChannel(gltf, pointerSegments, sampler, pointer) {
  var _a, _b;
  const lightIndex = Number(pointerSegments[3]);
  const lightDefinitions = gltf.lights || ((_b = (_a = gltf.extensions) == null ? void 0 : _a["KHR_lights_punctual"]) == null ? void 0 : _b["lights"]);
  const isSpotProperty = pointerSegments[4] === "spot";
  const property = isSpotProperty ? pointerSegments[5] : pointerSegments[4];
  const component = !isSpotProperty && property === "color" ? pointerSegments[5] : void 0;
  const allowedProperties = [
    "color",
    "intensity",
    "range",
    "innerConeAngle",
    "outerConeAngle"
  ];
  const expectedLength = isSpotProperty || component !== void 0 ? 6 : 5;
  if (pointerSegments[2] !== "lights" || pointerSegments.length !== expectedLength || !Number.isInteger(lightIndex) || !Array.isArray(lightDefinitions) || !lightDefinitions[lightIndex] || !allowedProperties.includes(property) || isSpotProperty && property !== "innerConeAngle" && property !== "outerConeAngle" || component !== void 0 && (!/^[0-2]$/.test(component) || property !== "color")) {
    warnUnsupportedAnimationPointer(pointer, "punctual-light pointers must target supported typed light properties");
    return null;
  }
  return {
    type: "light",
    sampler,
    pointer,
    targetLightIndex: lightIndex,
    property,
    ...component === void 0 ? {} : { component: Number(component) }
  };
}
function parseNodePointerAnimationChannel(gltf, pointerSegments, sampler, pointer) {
  const isVisibilityPointer = pointerSegments.length === 5 && pointerSegments[2] === "extensions" && pointerSegments[3] === "KHR_node_visibility" && pointerSegments[4] === "visible";
  if (pointerSegments.length !== 3 && !isVisibilityPointer) {
    warnUnsupportedAnimationPointer(pointer, "node pointers must target transforms, morph weights, or KHR_node_visibility.visible");
    return null;
  }
  const nodeIndex = Number(pointerSegments[1]);
  const targetNode = gltf.nodes[nodeIndex];
  if (!Number.isInteger(nodeIndex) || !targetNode) {
    import_core8.log.warn(`KHR_animation_pointer target ${pointer} references a missing node and will be skipped`)();
    return null;
  }
  if (isVisibilityPointer && sampler.interpolation !== "STEP") {
    warnUnsupportedAnimationPointer(pointer, "boolean visibility animation requires STEP interpolation");
    return null;
  }
  const path = isVisibilityPointer ? "visibility" : getNodeAnimationPath(pointerSegments[2]);
  if (!path) {
    warnUnsupportedAnimationPointer(pointer, `node property "${pointerSegments[2]}" has no runtime animation mapping`);
    return null;
  }
  return {
    type: "node",
    sampler,
    targetNodeId: targetNode.id,
    path
  };
}
function getMorphTargetCount(gltf, target) {
  var _a, _b, _c, _d, _e, _f, _g;
  let nodeIndex;
  if (target.path === "weights") {
    nodeIndex = target.node;
  } else if (target.path === "pointer") {
    const pointer = (_b = (_a = target.extensions) == null ? void 0 : _a["KHR_animation_pointer"]) == null ? void 0 : _b.pointer;
    const pointerMatch = typeof pointer === "string" ? /^\/nodes\/(\d+)\/weights$/.exec(pointer) : null;
    if (!pointerMatch) {
      return void 0;
    }
    nodeIndex = Number(pointerMatch[1]);
  } else {
    return void 0;
  }
  const node = gltf.nodes[nodeIndex ?? 0];
  const mesh = typeof (node == null ? void 0 : node.mesh) === "number" ? gltf.meshes[node.mesh] : node == null ? void 0 : node.mesh;
  return ((_c = node == null ? void 0 : node.weights) == null ? void 0 : _c.length) || ((_d = mesh == null ? void 0 : mesh.weights) == null ? void 0 : _d.length) || ((_g = (_f = (_e = mesh == null ? void 0 : mesh.primitives) == null ? void 0 : _e[0]) == null ? void 0 : _f.targets) == null ? void 0 : _g.length) || 1;
}
function groupMorphTargetValues(values, keyframeCount, interpolation, declaredTargetCount) {
  const valuesPerKeyframe = interpolation === "CUBICSPLINE" ? 3 : 1;
  const inferredTargetCount = values.length / (Math.max(keyframeCount, 1) * valuesPerKeyframe);
  const targetCount = declaredTargetCount > 1 ? declaredTargetCount : Number.isInteger(inferredTargetCount) && inferredTargetCount > 1 ? inferredTargetCount : declaredTargetCount;
  if (targetCount <= 1) {
    return values;
  }
  const scalarValues = values.flat();
  const groupedValues = [];
  for (let offset = 0; offset < scalarValues.length; offset += targetCount) {
    groupedValues.push(scalarValues.slice(offset, offset + targetCount));
  }
  return groupedValues;
}
function parseMaterialPointerAnimationChannel(gltf, pointerSegments, sampler, pointer) {
  if (pointerSegments.length < 3) {
    warnUnsupportedAnimationPointer(pointer, "material pointers must include a material index and target property path");
    return null;
  }
  const materialIndex = Number(pointerSegments[1]);
  const material = gltf.materials[materialIndex];
  if (!Number.isInteger(materialIndex) || !material) {
    import_core8.log.warn(`KHR_animation_pointer target ${pointer} references a missing material and will be skipped`)();
    return null;
  }
  const materialTarget = resolveMaterialAnimationTarget(material, pointerSegments.slice(2));
  if ("reason" in materialTarget) {
    warnUnsupportedAnimationPointer(pointer, materialTarget.reason);
    return null;
  }
  return {
    sampler,
    pointer,
    targetMaterialIndex: materialIndex,
    ...materialTarget
  };
}
function getNodeAnimationPath(path) {
  switch (path) {
    case "translation":
    case "rotation":
    case "scale":
    case "weights":
      return path;
    default:
      return null;
  }
}
function resolveMaterialAnimationTarget(material, pointerSegments) {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x;
  const textureTransformTarget = resolveTextureTransformAnimationTarget(material, pointerSegments);
  if (!("reason" in textureTransformTarget)) {
    return textureTransformTarget;
  }
  if (textureTransformTarget.reason !== "not-a-texture-transform-target") {
    return textureTransformTarget;
  }
  const pointerPath = pointerSegments.join("/");
  switch (pointerPath) {
    case "pbrMetallicRoughness/baseColorFactor":
      return material["pbrMetallicRoughness"] ? { type: "material", property: "baseColorFactor" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "pbrMetallicRoughness/metallicFactor":
      return material["pbrMetallicRoughness"] ? { type: "material", property: "metallicRoughnessValues", component: 0 } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "pbrMetallicRoughness/roughnessFactor":
      return material["pbrMetallicRoughness"] ? { type: "material", property: "metallicRoughnessValues", component: 1 } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "normalTexture/scale":
      return material["normalTexture"] ? { type: "material", property: "normalScale" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "occlusionTexture/strength":
      return material["occlusionTexture"] ? { type: "material", property: "occlusionStrength" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "emissiveFactor":
      return { type: "material", property: "emissiveFactor" };
    case "alphaCutoff":
      return { type: "material", property: "alphaCutoff" };
    case "extensions/KHR_materials_specular/specularFactor":
      return ((_a = material["extensions"]) == null ? void 0 : _a["KHR_materials_specular"]) ? { type: "material", property: "specularIntensityFactor" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_specular/specularColorFactor":
      return ((_b = material["extensions"]) == null ? void 0 : _b["KHR_materials_specular"]) ? { type: "material", property: "specularColorFactor" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_ior/ior":
      return ((_c = material["extensions"]) == null ? void 0 : _c["KHR_materials_ior"]) ? { type: "material", property: "ior" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/EXT_materials_bump/bumpFactor":
      return ((_d = material["extensions"]) == null ? void 0 : _d["EXT_materials_bump"]) ? { type: "material", property: "bumpFactor" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_diffuse_transmission/diffuseTransmissionFactor":
      return ((_e = material["extensions"]) == null ? void 0 : _e["KHR_materials_diffuse_transmission"]) ? { type: "material", property: "diffuseTransmissionFactor" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_diffuse_transmission/diffuseTransmissionColorFactor":
      return ((_f = material["extensions"]) == null ? void 0 : _f["KHR_materials_diffuse_transmission"]) ? { type: "material", property: "diffuseTransmissionColorFactor" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_volume_scatter/multiscatterColorFactor":
    case "extensions/KHR_materials_volume_scatter/multiscatterColor":
      return ((_g = material["extensions"]) == null ? void 0 : _g["KHR_materials_volume_scatter"]) ? { type: "material", property: "multiscatterColorFactor" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_volume_scatter/scatterAnisotropy":
      return ((_h = material["extensions"]) == null ? void 0 : _h["KHR_materials_volume_scatter"]) ? { type: "material", property: "scatterAnisotropy" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_dispersion/dispersion":
      return ((_i = material["extensions"]) == null ? void 0 : _i["KHR_materials_dispersion"]) ? { type: "material", property: "dispersion" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_transmission/transmissionFactor":
      return ((_j = material["extensions"]) == null ? void 0 : _j["KHR_materials_transmission"]) ? { type: "material", property: "transmissionFactor" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_volume/thicknessFactor":
      return ((_k = material["extensions"]) == null ? void 0 : _k["KHR_materials_volume"]) ? { type: "material", property: "thicknessFactor" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_volume/attenuationDistance":
      return ((_l = material["extensions"]) == null ? void 0 : _l["KHR_materials_volume"]) ? { type: "material", property: "attenuationDistance" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_volume/attenuationColor":
      return ((_m = material["extensions"]) == null ? void 0 : _m["KHR_materials_volume"]) ? { type: "material", property: "attenuationColor" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_clearcoat/clearcoatFactor":
      return ((_n = material["extensions"]) == null ? void 0 : _n["KHR_materials_clearcoat"]) ? { type: "material", property: "clearcoatFactor" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_clearcoat/clearcoatRoughnessFactor":
      return ((_o = material["extensions"]) == null ? void 0 : _o["KHR_materials_clearcoat"]) ? { type: "material", property: "clearcoatRoughnessFactor" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_sheen/sheenColorFactor":
      return ((_p = material["extensions"]) == null ? void 0 : _p["KHR_materials_sheen"]) ? { type: "material", property: "sheenColorFactor" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_sheen/sheenRoughnessFactor":
      return ((_q = material["extensions"]) == null ? void 0 : _q["KHR_materials_sheen"]) ? { type: "material", property: "sheenRoughnessFactor" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_iridescence/iridescenceFactor":
      return ((_r = material["extensions"]) == null ? void 0 : _r["KHR_materials_iridescence"]) ? { type: "material", property: "iridescenceFactor" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_iridescence/iridescenceIor":
      return ((_s = material["extensions"]) == null ? void 0 : _s["KHR_materials_iridescence"]) ? { type: "material", property: "iridescenceIor" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_iridescence/iridescenceThicknessMinimum":
      return ((_t = material["extensions"]) == null ? void 0 : _t["KHR_materials_iridescence"]) ? { type: "material", property: "iridescenceThicknessRange", component: 0 } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_iridescence/iridescenceThicknessMaximum":
      return ((_u = material["extensions"]) == null ? void 0 : _u["KHR_materials_iridescence"]) ? { type: "material", property: "iridescenceThicknessRange", component: 1 } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_anisotropy/anisotropyStrength":
      return ((_v = material["extensions"]) == null ? void 0 : _v["KHR_materials_anisotropy"]) ? { type: "material", property: "anisotropyStrength" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_anisotropy/anisotropyRotation":
      return ((_w = material["extensions"]) == null ? void 0 : _w["KHR_materials_anisotropy"]) ? { type: "material", property: "anisotropyRotation" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    case "extensions/KHR_materials_emissive_strength/emissiveStrength":
      return ((_x = material["extensions"]) == null ? void 0 : _x["KHR_materials_emissive_strength"]) ? { type: "material", property: "emissiveStrength" } : { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
    default:
      return { reason: getUnsupportedMaterialPointerReason(pointerSegments) };
  }
}
function resolveTextureTransformAnimationTarget(material, pointerSegments) {
  const extensionIndex = pointerSegments.lastIndexOf("extensions");
  if (extensionIndex < 0 || pointerSegments[extensionIndex + 1] !== "KHR_texture_transform" || extensionIndex < 1) {
    return { reason: "not-a-texture-transform-target" };
  }
  const textureSlotDefinition = resolveTextureTransformSlot(pointerSegments.slice(0, extensionIndex));
  if (!textureSlotDefinition) {
    return {
      reason: getUnsupportedTextureTransformSlotReason(pointerSegments.slice(0, extensionIndex))
    };
  }
  const textureInfo = getNestedMaterialValue(material, textureSlotDefinition.pathSegments);
  if (!textureInfo) {
    return {
      reason: `texture-transform target "${pointerSegments.slice(0, extensionIndex).join("/")}" does not exist on the referenced material`
    };
  }
  const textureTransformPath = pointerSegments[extensionIndex + 2];
  if (textureTransformPath === "texCoord") {
    return {
      reason: "animated KHR_texture_transform.texCoord is unsupported because texCoord selection is structural, not a runtime float/vector update"
    };
  }
  if (textureTransformPath !== "offset" && textureTransformPath !== "rotation" && textureTransformPath !== "scale") {
    return {
      reason: `KHR_texture_transform property "${textureTransformPath}" is not animatable; supported properties are offset, rotation, and scale`
    };
  }
  const componentSegment = pointerSegments[extensionIndex + 3];
  if (pointerSegments.length > extensionIndex + 4) {
    return {
      reason: `KHR_texture_transform.${textureTransformPath} does not support nested property paths`
    };
  }
  let component;
  if (componentSegment !== void 0) {
    component = Number(componentSegment);
    if (textureTransformPath === "rotation") {
      return {
        reason: "KHR_texture_transform.rotation does not support component indices"
      };
    }
    if (!Number.isInteger(component) || component < 0 || component > 1) {
      return {
        reason: `KHR_texture_transform.${textureTransformPath} component index "${componentSegment}" is invalid; only 0 and 1 are supported`
      };
    }
  }
  return {
    type: "textureTransform",
    textureSlot: textureSlotDefinition.slot,
    path: textureTransformPath,
    component,
    baseTransform: resolveTextureTransform(textureInfo)
  };
}
function getNestedMaterialValue(material, pathSegments) {
  let value = material;
  for (const pathSegment of pathSegments) {
    value = value == null ? void 0 : value[pathSegment];
    if (!value) {
      return null;
    }
  }
  return value;
}
function splitJsonPointer(pointer) {
  return pointer.slice(1).split("/").map((segment) => segment.replace(/~1/g, "/").replace(/~0/g, "~"));
}
function getUnsupportedMaterialPointerReason(pointerSegments) {
  const extensionName = getPointerExtensionName(pointerSegments);
  if (extensionName) {
    const extensionSupport = getRegisteredGLTFExtensionSupport(extensionName);
    if ((extensionSupport == null ? void 0 : extensionSupport.supportLevel) === "none") {
      return `${extensionName} is referenced by this pointer, but ${extensionSupport.comment.charAt(0).toLowerCase()}${extensionSupport.comment.slice(1)}`;
    }
  }
  return `no runtime target exists for material property "${pointerSegments.join("/")}"`;
}
function getUnsupportedTextureTransformSlotReason(pointerSegments) {
  const extensionName = getPointerExtensionName(pointerSegments);
  if (extensionName) {
    const extensionSupport = getRegisteredGLTFExtensionSupport(extensionName);
    if ((extensionSupport == null ? void 0 : extensionSupport.supportLevel) === "none") {
      return `${extensionName} is referenced by this pointer, but ${extensionSupport.comment.charAt(0).toLowerCase()}${extensionSupport.comment.slice(1)}`;
    }
  }
  return `texture-transform target "${pointerSegments.join("/")}" has no runtime texture-slot mapping`;
}
function getPointerExtensionName(pointerSegments) {
  const extensionIndex = pointerSegments.indexOf("extensions");
  const extensionName = pointerSegments[extensionIndex + 1];
  return extensionIndex >= 0 && extensionName ? extensionName : null;
}
function warnUnsupportedAnimationPointer(pointer, reason) {
  import_core8.log.warn(`KHR_animation_pointer target ${pointer} will be skipped because ${reason}`)();
}
function accessorToJsArray1D(accessor, accessorCache) {
  if (accessorCache.has(accessor)) {
    return accessorCache.get(accessor);
  }
  const { value: array, components } = resolveAnimationAccessor(accessor);
  assert(components === 1, "accessorToJsArray1D must have exactly 1 component");
  const result = Array.from(array);
  accessorCache.set(accessor, result);
  return result;
}
function accessorToJsArray2D(accessor, accessorCache) {
  if (accessorCache.has(accessor)) {
    return accessorCache.get(accessor);
  }
  const { value: array, components } = resolveAnimationAccessor(accessor);
  assert(components >= 1, "accessorToJsArray2D must have at least 1 component");
  const result = [];
  for (let i = 0; i < array.length; i += components) {
    result.push(Array.from(array.slice(i, i + components)));
  }
  accessorCache.set(accessor, result);
  return result;
}
function resolveAnimationAccessor(accessor) {
  var _a;
  if (accessor.value) {
    return { value: accessor.value, components: accessor.components };
  }
  const bytes = (_a = accessor.bufferView) == null ? void 0 : _a.data;
  assert(bytes !== void 0);
  assert(accessor.componentType === 5126);
  const components = accessor.type === "SCALAR" ? 1 : Number(accessor.type.slice(3));
  const value = new Float32Array(bytes.buffer, bytes.byteOffset + (accessor.byteOffset || 0), accessor.count * components);
  return { value, components };
}
function assert(condition, message) {
  if (!condition) {
    throw new Error(message);
  }
}

// dist/gltf/gltf-material-variants.js
var import_engine6 = require("@luma.gl/engine");
var GLTFMaterialVariants = class {
  /** Source variants in their authored order. */
  variants;
  /** Application-visible source variant names. */
  names;
  /** Currently selected variant, or `null` when source defaults are active. */
  activeVariant = null;
  modelNodes;
  constructor(gltf, scenes) {
    var _a, _b;
    const sourceVariants = ((_b = (_a = gltf.extensions) == null ? void 0 : _a["KHR_materials_variants"]) == null ? void 0 : _b["variants"]) || [];
    this.variants = sourceVariants.map((variant, index) => ({
      name: variant.name || `Variant-${index}`,
      index
    }));
    this.names = this.variants.map((variant) => variant.name);
    const visitedModelNodes = /* @__PURE__ */ new Set();
    for (const scene of scenes) {
      scene.preorderTraversal((node) => {
        if (node instanceof import_engine6.ModelNode && node.userData["gltfMaterialVariants"]) {
          visitedModelNodes.add(node);
        }
      });
    }
    this.modelNodes = Array.from(visitedModelNodes);
  }
  /** Applies one named variant atomically and restores unmapped primitives to source defaults. */
  selectVariant(variantName) {
    const variant = this.variants.find((candidate) => candidate.name === variantName);
    if (!variant) {
      throw new Error(`Unknown glTF material variant: ${variantName}`);
    }
    for (const modelNode of this.modelNodes) {
      const sourceVariants = modelNode.userData["gltfMaterialVariants"];
      const mapping = sourceVariants.mappings.get(variant.index);
      modelNode.model.setMaterial((mapping == null ? void 0 : mapping.material) || sourceVariants.defaultMaterial);
      modelNode.model.setParameters((mapping == null ? void 0 : mapping.parameters) || sourceVariants.defaultParameters);
    }
    this.activeVariant = variantName;
  }
  /** Restores every primitive's authored default material and pipeline parameters. */
  resetVariant() {
    for (const modelNode of this.modelNodes) {
      const sourceVariants = modelNode.userData["gltfMaterialVariants"];
      modelNode.model.setMaterial(sourceVariants.defaultMaterial);
      modelNode.model.setParameters(sourceVariants.defaultParameters);
    }
    this.activeVariant = null;
  }
};

// dist/gltf/create-scenegraph-from-gltf.js
function createScenegraphsFromGLTF(device, gltf, options) {
  var _a, _b;
  if (options == null ? void 0 : options.strictExtensions) {
    assertSupportedGLTFExtensions(gltf, device);
  }
  const { scenes, materials, gltfMeshIdToNodeMap, gltfNodeIdToNodeMap, gltfNodeIndexToNodeMap, generatedTextures } = parseGLTF(device, gltf, options);
  const animations = parseGLTFAnimations(gltf);
  const sourceLights = gltf.lights || ((_b = (_a = gltf.extensions) == null ? void 0 : _a["KHR_lights_punctual"]) == null ? void 0 : _b["lights"]) || [];
  const lightDefinitions = sourceLights.map((light) => ({
    ...light,
    ...Array.isArray(light["color"]) ? { color: [...light["color"]] } : {},
    ...light["spot"] ? { spot: { ...light["spot"] } } : {}
  }));
  const cameras = (gltf.cameras || []).map((camera) => {
    const runtimeCamera = { ...camera };
    if (camera.perspective) {
      runtimeCamera.perspective = { ...camera.perspective };
    }
    if (camera.orthographic) {
      runtimeCamera.orthographic = { ...camera.orthographic };
    }
    return runtimeCamera;
  });
  const lightOptions = {
    useByteColors: (options == null ? void 0 : options.useByteColors) ?? true,
    nodeVisibility: gltfNodeIdToNodeMap,
    lightDefinitions
  };
  const lights = parseGLTFLights(gltf, lightOptions);
  const refreshLights = () => {
    lights.splice(0, lights.length, ...parseGLTFLights(gltf, lightOptions));
  };
  const animator = new GLTFAnimator({
    onVisibilityChange: refreshLights,
    cameras,
    lightDefinitions,
    onLightChange: refreshLights,
    animations,
    gltfNodeIdToNodeMap,
    materials
  });
  const variants = new GLTFMaterialVariants(gltf, scenes);
  const extensionSupport = getGLTFExtensionSupport(gltf, device);
  const sceneBounds = scenes.map((scene) => getScenegraphBounds(scene.getBounds()));
  const modelBounds = getCombinedScenegraphBounds(sceneBounds);
  const skins = new GLTFSkinController({ gltf, scenes, gltfNodeIndexToNodeMap });
  animator.setUpdateHandler(() => skins.update());
  let destroyed = false;
  const destroy = () => {
    if (destroyed) {
      return;
    }
    destroyed = true;
    const groups = /* @__PURE__ */ new Set([
      ...scenes,
      ...gltfMeshIdToNodeMap.values(),
      ...gltfNodeIdToNodeMap.values()
    ]);
    const modelNodes = /* @__PURE__ */ new Set();
    const ownedMaterials = new Set(materials);
    for (const group of groups) {
      group.preorderTraversal((node) => {
        var _a2;
        if (node instanceof import_engine7.ModelNode) {
          modelNodes.add(node);
          if ((_a2 = node.model) == null ? void 0 : _a2.material) {
            ownedMaterials.add(node.model.material);
          }
        }
      });
    }
    for (const modelNode of modelNodes) {
      modelNode.destroy();
    }
    for (const group of groups) {
      group.destroy();
    }
    for (const material of ownedMaterials) {
      material.destroy();
    }
    for (const texture of generatedTextures) {
      texture.destroy();
    }
    generatedTextures.clear();
  };
  return {
    scenes,
    materials,
    variants,
    cameras,
    animator,
    animations,
    lights,
    extensionSupport,
    sceneBounds,
    modelBounds,
    gltfMeshIdToNodeMap,
    gltfNodeIdToNodeMap,
    gltfNodeIndexToNodeMap,
    skins,
    gltf,
    destroy
  };
}
function getScenegraphBounds(bounds) {
  if (!bounds) {
    return {
      bounds: null,
      center: [0, 0, 0],
      size: [0, 0, 0],
      radius: 0.5,
      recommendedOrbitDistance: 1
    };
  }
  const normalizedBounds = [
    [bounds[0][0], bounds[0][1], bounds[0][2]],
    [bounds[1][0], bounds[1][1], bounds[1][2]]
  ];
  const size = [
    normalizedBounds[1][0] - normalizedBounds[0][0],
    normalizedBounds[1][1] - normalizedBounds[0][1],
    normalizedBounds[1][2] - normalizedBounds[0][2]
  ];
  const center = [
    normalizedBounds[0][0] + size[0] * 0.5,
    normalizedBounds[0][1] + size[1] * 0.5,
    normalizedBounds[0][2] + size[2] * 0.5
  ];
  const maxHalfExtent = Math.max(size[0], size[1], size[2]) * 0.5;
  const radius = Math.max(0.5 * Math.hypot(size[0], size[1], size[2]), 1e-3);
  return {
    bounds: normalizedBounds,
    center,
    size,
    radius,
    recommendedOrbitDistance: Math.max(Math.max(maxHalfExtent, 1e-3) / Math.tan(Math.PI / 6) * 1.15, radius * 1.1)
  };
}
function getCombinedScenegraphBounds(sceneBounds) {
  let combinedBounds = null;
  for (const sceneBoundInfo of sceneBounds) {
    if (!sceneBoundInfo.bounds) {
      continue;
    }
    if (!combinedBounds) {
      combinedBounds = [
        [...sceneBoundInfo.bounds[0]],
        [...sceneBoundInfo.bounds[1]]
      ];
      continue;
    }
    for (let axis = 0; axis < 3; axis++) {
      combinedBounds[0][axis] = Math.min(combinedBounds[0][axis], sceneBoundInfo.bounds[0][axis]);
      combinedBounds[1][axis] = Math.max(combinedBounds[1][axis], sceneBoundInfo.bounds[1][axis]);
    }
  }
  return getScenegraphBounds(combinedBounds);
}

// dist/gltf/gltf-animated-crowd.js
var import_core10 = require("@luma.gl/core");
var import_engine9 = require("@luma.gl/engine");

// dist/gltf/gltf-gpu-animation.js
function createGLTFCrowdGPUAnimationLayout(gltf, options = {}) {
  var _a;
  if (options.enabled === false || !((_a = gltf.animations) == null ? void 0 : _a.length)) {
    return null;
  }
  const sampleRate = options.sampleRate ?? 30;
  const maxFrames = options.maxFrames ?? 8192;
  if (!Number.isFinite(sampleRate) || sampleRate <= 0 || !Number.isSafeInteger(maxFrames)) {
    return null;
  }
  let frameCount = 0;
  const clips = gltf.animations.map((animation, animationIndex) => {
    var _a2;
    let duration = 0;
    for (const sampler of animation.samplers || []) {
      const values = (_a2 = gltf.accessors[sampler.input]) == null ? void 0 : _a2.value;
      for (const value of values || []) {
        duration = Math.max(duration, Number(value));
      }
    }
    const clipFrameCount = Math.max(1, Math.ceil(duration * sampleRate) + 1);
    const clip = {
      name: animation.name || `Animation-${animationIndex}`,
      duration,
      frameOffset: frameCount,
      frameCount: clipFrameCount
    };
    frameCount += clipFrameCount;
    return clip;
  });
  return frameCount <= maxFrames ? { sampleRate, frameCount, clips } : null;
}
function getGLTFCrowdGPUAnimationFrames(clip, time, sampleRate) {
  const frame = Math.min(Math.max(time, 0) * sampleRate, clip.frameCount - 1);
  const firstFrame = Math.floor(frame);
  const secondFrame = Math.min(firstFrame + 1, clip.frameCount - 1);
  return [clip.frameOffset + firstFrame, clip.frameOffset + secondFrame, frame - firstFrame];
}

// dist/gltf/gltf-lod.js
var import_core9 = require("@luma.gl/core");
var import_engine8 = require("@luma.gl/engine");
var DEFAULT_LOD_RATIOS = [0.5, 0.25];
var DEFAULT_SCREEN_COVERAGE = [0.5, 0.2, 0.01];
var LOD_EXTENSION_NAME = "MSFT_lod";
function getGLTFNodeLODs(gltf, source) {
  var _a, _b;
  const sourceNodeIndex = typeof source === "number" ? source : gltf.nodes.findIndex((node) => node === source);
  const sourceNode = gltf.nodes[sourceNodeIndex];
  if (!sourceNode) {
    return null;
  }
  const extension = (_a = sourceNode.extensions) == null ? void 0 : _a[LOD_EXTENSION_NAME];
  if (!extension) {
    return null;
  }
  (0, import_core9.assert)(Array.isArray(extension.ids) && extension.ids.length > 0);
  const nodeIndices = [sourceNodeIndex, ...extension.ids];
  const uniqueNodeIndices = /* @__PURE__ */ new Set();
  for (const nodeIndex of nodeIndices) {
    (0, import_core9.assert)(Number.isSafeInteger(nodeIndex) && nodeIndex >= 0 && nodeIndex < gltf.nodes.length && !uniqueNodeIndices.has(nodeIndex));
    uniqueNodeIndices.add(nodeIndex);
  }
  const authoredCoverage = (_b = sourceNode.extras) == null ? void 0 : _b["MSFT_screencoverage"];
  const screenCoverage = resolveScreenCoverage(nodeIndices.length, Array.isArray(authoredCoverage) ? authoredCoverage : void 0);
  return nodeIndices.map((nodeIndex, level) => ({
    level,
    nodeIndex,
    node: gltf.nodes[nodeIndex],
    screenCoverage: screenCoverage[level]
  }));
}
function generateGLTFLODLevels(gltf, options = {}) {
  var _a, _b;
  const ratios = options.ratios || DEFAULT_LOD_RATIOS;
  if (ratios.length === 0) {
    return gltf;
  }
  let previousRatio = 1;
  for (const ratio of ratios) {
    (0, import_core9.assert)(Number.isFinite(ratio) && ratio > 0 && ratio < previousRatio);
    previousRatio = ratio;
  }
  const sourceNodeIndices = new Map(gltf.nodes.map((node, index) => [node, index]));
  const reachableNodeIndices = getReachableNodeIndices(gltf, sourceNodeIndices);
  const nodes = gltf.nodes.map((node) => ({
    ...node,
    ...node.extensions ? { extensions: { ...node.extensions } } : {},
    ...node.extras ? { extras: { ...node.extras } } : {}
  }));
  const meshes = [...gltf.meshes];
  const accessors = [...gltf.accessors];
  for (const [nodeIndex, originalNode] of gltf.nodes.entries()) {
    if (originalNode.children) {
      nodes[nodeIndex].children = originalNode.children.map((child) => nodes[sourceNodeIndices.get(child)]);
    }
  }
  let hasGeneratedLevels = false;
  for (const [nodeIndex, originalNode] of gltf.nodes.entries()) {
    if (!reachableNodeIndices.has(nodeIndex) || !originalNode.mesh || ((_a = originalNode.extensions) == null ? void 0 : _a[LOD_EXTENSION_NAME])) {
      continue;
    }
    const alternativeNodeIndices = [];
    for (const [ratioIndex, targetRatio] of ratios.entries()) {
      const simplifiedMesh = simplifyGLTFMesh(originalNode.mesh, accessors, targetRatio, `${nodeIndex}-${ratioIndex + 1}`, options.preserveBoundary ?? false);
      if (!simplifiedMesh) {
        continue;
      }
      meshes.push(simplifiedMesh);
      const alternativeNodeIndex = nodes.length;
      alternativeNodeIndices.push(alternativeNodeIndex);
      nodes.push({
        ...originalNode,
        id: `${originalNode.id}-lod-${ratioIndex + 1}`,
        name: `${originalNode.name || originalNode.id} LOD ${ratioIndex + 1}`,
        mesh: simplifiedMesh,
        children: void 0,
        extensions: void 0,
        extras: { geometricError: (_b = simplifiedMesh.extras) == null ? void 0 : _b["geometricError"] }
      });
    }
    if (alternativeNodeIndices.length > 0) {
      const sourceNode = nodes[nodeIndex];
      sourceNode.extensions = {
        ...sourceNode.extensions,
        [LOD_EXTENSION_NAME]: { ids: alternativeNodeIndices }
      };
      sourceNode.extras = {
        ...sourceNode.extras,
        MSFT_screencoverage: resolveScreenCoverage(alternativeNodeIndices.length + 1, options.screenCoverage)
      };
      hasGeneratedLevels = true;
    }
  }
  if (!hasGeneratedLevels) {
    return gltf;
  }
  const scenes = gltf.scenes.map((scene) => {
    var _a2;
    return {
      ...scene,
      nodes: (_a2 = scene.nodes) == null ? void 0 : _a2.map((node) => nodes[sourceNodeIndices.get(node)])
    };
  });
  const sceneIndex = gltf.scene ? gltf.scenes.indexOf(gltf.scene) : -1;
  return {
    ...gltf,
    nodes,
    meshes,
    accessors,
    scenes,
    ...sceneIndex >= 0 ? { scene: scenes[sceneIndex] } : {},
    extensionsUsed: [.../* @__PURE__ */ new Set([...gltf.extensionsUsed || [], LOD_EXTENSION_NAME])]
  };
}
function getReachableNodeIndices(gltf, sourceNodeIndices) {
  const reachableNodeIndices = /* @__PURE__ */ new Set();
  const pendingNodes = gltf.scenes.flatMap((scene) => scene.nodes || []);
  for (const node of pendingNodes) {
    const nodeIndex = sourceNodeIndices.get(node);
    if (nodeIndex === void 0 || reachableNodeIndices.has(nodeIndex)) {
      continue;
    }
    reachableNodeIndices.add(nodeIndex);
    pendingNodes.push(...node.children || []);
  }
  return reachableNodeIndices;
}
function simplifyGLTFMesh(sourceMesh, accessors, targetRatio, suffix, preserveBoundary) {
  let hasReducedPrimitive = false;
  let geometricError = 0;
  const primitives = sourceMesh.primitives.map((primitive, primitiveIndex) => {
    var _a;
    const positions = primitive.attributes["POSITION"];
    if (!positions || primitive.mode !== void 0 && primitive.mode !== 4) {
      return primitive;
    }
    const sourceIndices = getPrimitiveIndices(primitive.indices, positions.count);
    if (!sourceIndices || sourceIndices.length < 6) {
      return primitive;
    }
    const attributes = [];
    for (const semantic of ["NORMAL", "TEXCOORD_0", "TEXCOORD_1", "JOINTS_0", "WEIGHTS_0"]) {
      const attribute = primitive.attributes[semantic];
      if (attribute) {
        attributes.push({ values: attribute.value, size: attribute.components });
      }
    }
    const simplified = (0, import_engine8.simplifyMesh)({
      positions: positions.value,
      indices: sourceIndices,
      targetRatio,
      attributes,
      preserveBoundary
    });
    if (simplified.indices.length >= sourceIndices.length || simplified.indices.length < 3) {
      return primitive;
    }
    hasReducedPrimitive = true;
    geometricError = Math.max(geometricError, simplified.geometricError);
    const indexAccessor = {
      ...primitive.indices || {
        components: 1,
        bytesPerComponent: simplified.indices.BYTES_PER_ELEMENT,
        bytesPerElement: simplified.indices.BYTES_PER_ELEMENT,
        type: "SCALAR"
      },
      id: `${((_a = primitive.indices) == null ? void 0 : _a.id) || sourceMesh.id}-lod-${suffix}-${primitiveIndex}`,
      componentType: getIndexComponentType(simplified.indices),
      count: simplified.indices.length,
      value: simplified.indices,
      byteOffset: 0,
      bufferView: void 0,
      min: void 0,
      max: void 0
    };
    accessors.push(indexAccessor);
    return { ...primitive, indices: indexAccessor };
  });
  if (!hasReducedPrimitive) {
    return null;
  }
  return {
    ...sourceMesh,
    id: `${sourceMesh.id}-lod-${suffix}`,
    name: `${sourceMesh.name || sourceMesh.id} LOD ${suffix}`,
    primitives,
    extras: { ...sourceMesh.extras, geometricError }
  };
}
function getPrimitiveIndices(accessor, vertexCount) {
  if (accessor) {
    const indices2 = accessor.value;
    return indices2 instanceof Uint8Array || indices2 instanceof Uint16Array || indices2 instanceof Uint32Array ? indices2 : null;
  }
  if (vertexCount < 3) {
    return null;
  }
  const indices = vertexCount <= 65535 ? new Uint16Array(vertexCount) : new Uint32Array(vertexCount);
  for (let index = 0; index < vertexCount; index++) {
    indices[index] = index;
  }
  return indices;
}
function getIndexComponentType(indices) {
  return indices instanceof Uint8Array ? 5121 : indices instanceof Uint16Array ? 5123 : 5125;
}
function resolveScreenCoverage(levelCount, authored) {
  return Array.from({ length: levelCount }, (_, level) => {
    const value = (authored == null ? void 0 : authored[level]) ?? DEFAULT_SCREEN_COVERAGE[level] ?? DEFAULT_SCREEN_COVERAGE[DEFAULT_SCREEN_COVERAGE.length - 1] / 2 ** (level - 2);
    (0, import_core9.assert)(Number.isFinite(value) && value > 0 && value <= 1);
    return value;
  });
}

// dist/gltf/gltf-animated-crowd.js
var GLTFCrowdActor = class {
  id;
  /** Actor-local CPU node hierarchy; contains no render Models or mutable vertex buffers. */
  root;
  /** Existing reusable glTF animator backed by one actor-local engine animation mixer. */
  animator;
  /** Existing glTF skin controller producing actor-local mesh-space joint palettes. */
  skins;
  crowd;
  nodesByIndex;
  nodesById;
  isPlaying;
  isDestroyed = false;
  skinMatricesNeedUpdate = true;
  /** @internal Actors are created and owned by {@link GLTFAnimatedCrowd}. */
  constructor(crowd, id, options = {}) {
    this.crowd = crowd;
    this.id = id;
    const hierarchy = createActorNodes(crowd.scenegraphs, id);
    this.root = hierarchy.root;
    this.nodesByIndex = hierarchy.nodesByIndex;
    this.nodesById = hierarchy.nodesById;
    this.isPlaying = options.playing ?? true;
    this.skins = new GLTFSkinController({
      gltf: crowd.gltf,
      scenes: hierarchy.scenes,
      gltfNodeIndexToNodeMap: this.nodesByIndex
    });
    this.animator = new GLTFAnimator({
      animations: crowd.scenegraphs.animations.map((animation) => ({
        name: animation.name,
        channels: animation.channels.filter((channel) => channel.type === "node")
      })),
      gltfNodeIdToNodeMap: this.nodesById,
      autoplay: "first",
      onUpdate: () => {
        this.skinMatricesNeedUpdate = true;
      }
    });
    if (options.transform) {
      this.root.setMatrix(options.transform);
    }
    const clip = options.clip || this.animator.activeClip;
    if (clip) {
      this.animator.selectClip(clip);
      const action = this.mixer.getAction(clip);
      if (options.loop) {
        action == null ? void 0 : action.setLoop(options.loop, options.repetitions);
      }
      if (options.time !== void 0 || options.phase !== void 0) {
        this.seek(options.time ?? (options.phase || 0) * ((action == null ? void 0 : action.clip.duration) || 0));
      } else if (!crowd.gpuAnimationEnabled) {
        this.animator.update(0);
      }
    }
    this.setSpeed(options.speed ?? 1);
    if (!this.isPlaying) {
      this.pause();
    }
  }
  get mixer() {
    return this.animator.mixer;
  }
  get activeClip() {
    return this.animator.activeClip;
  }
  /** Selected clip-local time in seconds. */
  get time() {
    var _a;
    return this.activeClip ? ((_a = this.mixer.getAction(this.activeClip)) == null ? void 0 : _a.time) || 0 : 0;
  }
  get speed() {
    return this.mixer.timeScale;
  }
  get playing() {
    return this.isPlaying;
  }
  get destroyed() {
    return this.isDestroyed;
  }
  /** Returns this actor's private authored node by glTF source index or identifier. */
  getNode(node) {
    return typeof node === "number" ? this.nodesByIndex.get(node) : this.nodesById.get(node);
  }
  /** Replaces actor placement without mutating another actor or shared source nodes. */
  setTransform(transform) {
    this.root.setMatrix(transform);
    this.crowd.refresh();
    return this;
  }
  /** Selects or crossfades an authored clip independently from every neighboring actor. */
  selectClip(name, options = {}) {
    const clip = this.animator.selectClip(name, options);
    if (options.time !== void 0 || options.phase !== void 0) {
      this.seek(options.time ?? (options.phase || 0) * clip.clip.duration);
    }
    if (!this.isPlaying) {
      this.pause();
    }
    this.crowd.refresh();
    return this;
  }
  /** Seeks all active actor-local actions to seconds and updates its joint palettes once. */
  seek(timeSeconds) {
    for (const clip of this.animator.getAnimations()) {
      if (clip.action.playing) {
        clip.action.setTime(timeSeconds);
      }
    }
    if (!this.crowd.gpuAnimationEnabled) {
      this.animator.update(0);
    }
    this.crowd.refresh();
    return this;
  }
  setPhase(phase) {
    var _a;
    const duration = this.activeClip ? ((_a = this.mixer.getAction(this.activeClip)) == null ? void 0 : _a.clip.duration) || 0 : 0;
    return this.seek(duration * phase);
  }
  setSpeed(speed) {
    this.mixer.timeScale = speed;
    return this;
  }
  setLoop(loop, repetitions) {
    var _a;
    if (this.activeClip) {
      (_a = this.mixer.getAction(this.activeClip)) == null ? void 0 : _a.setLoop(loop, repetitions);
    }
    return this;
  }
  play() {
    this.isPlaying = true;
    for (const clip of this.animator.getAnimations()) {
      if (clip.action.playing) {
        clip.action.resume();
      }
    }
    return this;
  }
  pause() {
    this.isPlaying = false;
    for (const clip of this.animator.getAnimations()) {
      if (clip.action.playing) {
        clip.action.pause();
      }
    }
    return this;
  }
  /** Advances only this actor and immediately refreshes the existing shared instance buffers. */
  update(deltaSeconds) {
    this.advance(deltaSeconds);
    this.crowd.refresh();
    return this;
  }
  /** @internal Allows the crowd to upload all actors together after one shared frame update. */
  advance(deltaSeconds) {
    if (this.isPlaying && !this.isDestroyed) {
      if (this.crowd.gpuAnimationEnabled) {
        this.mixer.advance(deltaSeconds);
      } else {
        this.animator.update(deltaSeconds);
      }
    }
  }
  /** @internal Reuses the crowd's one scene traversal for both placement and joint palettes. */
  updateSkinMatrices(worldMatrices) {
    if (!this.skinMatricesNeedUpdate) {
      return;
    }
    for (const binding of this.skins.bindings) {
      (0, import_engine9.updateSkinJointMatrices)({
        joints: binding.joints,
        meshNode: binding.node,
        worldMatrices,
        inverseBindMatrices: binding.inverseBindMatrices,
        target: binding.jointMatrices
      });
    }
    this.skinMatricesNeedUpdate = false;
  }
  /** Releases lightweight actor-local state without touching shared GPU models or source data. */
  destroy() {
    if (this.isDestroyed) {
      return;
    }
    this.isDestroyed = true;
    this.isPlaying = false;
    this.crowd.removeActor(this.id);
    this.root.destroy();
  }
};
var GLTFAnimatedCrowd = class {
  device;
  gltf;
  scenegraphs;
  capacity;
  primitiveGroups;
  models;
  /** Whether clip interpolation, rigid transforms, skin palettes, and morph weights are GPU-read. */
  gpuAnimationEnabled;
  actorsById = /* @__PURE__ */ new Map();
  actorLODLevels = /* @__PURE__ */ new Map();
  actorLODCoverage = /* @__PURE__ */ new Map();
  maximumGroupLevels = /* @__PURE__ */ new Map();
  lodOptions;
  lodSource;
  lodScreenCoverage;
  maximumLODLevel;
  lodVertexCounts;
  gpuAnimationLayout;
  gpuAnimationClips;
  currentLODView = null;
  currentVertexBudget;
  isLODEnabled;
  lodBias = 1;
  currentLODStats;
  nextActorIndex = 0;
  isDestroyed = false;
  suspendedRefreshCount = 0;
  constructor(device, gltf, options = {}) {
    var _a, _b;
    const { capacity = 16, lod, gpuAnimation: gpuAnimation2, ...parseOptions } = options;
    (0, import_core10.assert)(Number.isSafeInteger(capacity) && capacity > 0);
    this.device = device;
    this.capacity = capacity;
    this.lodOptions = lod;
    this.isLODEnabled = (lod == null ? void 0 : lod.enabled) ?? false;
    (0, import_core10.assert)((lod == null ? void 0 : lod.vertexBudget) === void 0 || Number.isSafeInteger(lod.vertexBudget) && lod.vertexBudget >= 0);
    this.currentVertexBudget = (lod == null ? void 0 : lod.vertexBudget) || void 0;
    const authoredLOD = Boolean(lod && gltf.nodes.some((node) => {
      const levels = getGLTFNodeLODs(gltf, node);
      return Boolean(levels && levels.length > 1);
    }));
    this.gltf = (lod == null ? void 0 : lod.autoGenerate) && !authoredLOD ? generateGLTFLODLevels(gltf, {
      ratios: lod.ratios,
      screenCoverage: lod.screenCoverage,
      preserveBoundary: lod.preserveBoundary
    }) : gltf;
    this.lodSource = authoredLOD ? "authored" : this.gltf !== gltf ? "generated" : "none";
    const requestedGPUAnimationLayout = gpuAnimation2 ? createGLTFCrowdGPUAnimationLayout(this.gltf, gpuAnimation2) : null;
    const jointsPerInstance = Math.max(0, ...(this.gltf.skins || []).map((skin2) => skin2.joints.length));
    const maximumMorphTargetCount = Math.max(0, ...this.gltf.nodes.flatMap((node) => {
      var _a2;
      return (((_a2 = node.mesh) == null ? void 0 : _a2.primitives) || []).map((primitive) => {
        var _a3;
        return ((_a3 = primitive.targets) == null ? void 0 : _a3.length) || 0;
      });
    }));
    const maximumAnimationFrameStride = 4 + jointsPerInstance * 4 + maximumMorphTargetCount;
    this.gpuAnimationLayout = requestedGPUAnimationLayout && (device.type === "webgpu" || maximumAnimationFrameStride <= device.limits.maxTextureDimension2D && requestedGPUAnimationLayout.frameCount <= device.limits.maxTextureDimension2D) ? requestedGPUAnimationLayout : null;
    this.gpuAnimationEnabled = Boolean(this.gpuAnimationLayout);
    this.gpuAnimationClips = new Map((((_a = this.gpuAnimationLayout) == null ? void 0 : _a.clips) || []).map((clip) => [clip.name, clip]));
    const configuration = {
      capacity,
      jointsPerInstance,
      ...this.gpuAnimationLayout ? { gpuAnimation: this.gpuAnimationLayout } : {}
    };
    this.scenegraphs = createScenegraphsFromGLTF(device, this.gltf, {
      ...parseOptions,
      modelOptions: {
        ...parseOptions.modelOptions,
        userData: { ...(_b = parseOptions.modelOptions) == null ? void 0 : _b.userData, gltfAnimatedCrowd: configuration }
      }
    });
    this.primitiveGroups = createPrimitiveGroups(this.scenegraphs, Boolean(lod));
    this.models = this.primitiveGroups.map((group) => group.model);
    this.maximumLODLevel = Math.max(0, ...this.primitiveGroups.map((group) => group.lodLevel));
    for (const group of this.primitiveGroups) {
      this.maximumGroupLevels.set(group.nodeIndex, Math.max(this.maximumGroupLevels.get(group.nodeIndex) || 0, group.lodLevel));
    }
    const lodVertexCounts = Array.from({ length: this.maximumLODLevel + 1 }, () => 0);
    for (const group of this.primitiveGroups) {
      const maximumGroupLevel = this.getMaximumGroupLevel(group.nodeIndex);
      for (let level = 0; level < lodVertexCounts.length; level++) {
        if (Math.min(level, maximumGroupLevel) === group.lodLevel) {
          lodVertexCounts[level] += group.vertexCount;
        }
      }
    }
    this.lodVertexCounts = lodVertexCounts;
    const authoredScreenCoverage = lod ? this.getAuthoredScreenCoverage() : [0];
    this.lodScreenCoverage = (lod == null ? void 0 : lod.screenCoverage) || authoredScreenCoverage;
    this.currentLODStats = {
      source: this.lodSource,
      visibleActors: 0,
      culledActors: 0,
      drawCount: 0,
      triangles: 0,
      vertices: 0,
      ...this.currentVertexBudget ? { vertexBudget: this.currentVertexBudget } : {},
      demotedActors: 0,
      budgetSatisfied: true,
      levels: []
    };
    if (this.gpuAnimationLayout) {
      this.bakeGPUAnimationFrames();
    }
  }
  get actors() {
    return [...this.actorsById.values()];
  }
  get actorCount() {
    return this.actorsById.size;
  }
  get destroyed() {
    return this.isDestroyed;
  }
  /** Whether existing authored or generated crowd levels are currently selected per actor. */
  get lodEnabled() {
    return this.isLODEnabled;
  }
  /** Current visible actor buckets and actual per-level instanced draw work. */
  get lodStats() {
    return this.currentLODStats;
  }
  /** Current baked animation ownership, clip size, and independently deformed source groups. */
  get animationStats() {
    var _a, _b;
    return {
      mode: this.gpuAnimationEnabled ? "gpu" : "cpu",
      ...this.gpuAnimationLayout ? { sampleRate: this.gpuAnimationLayout.sampleRate } : {},
      frameCount: ((_a = this.gpuAnimationLayout) == null ? void 0 : _a.frameCount) || 0,
      clipCount: ((_b = this.gpuAnimationLayout) == null ? void 0 : _b.clips.length) || 0,
      morphGroupCount: this.primitiveGroups.filter((group) => group.morphTargetCount > 0).length
    };
  }
  /** Enables or disables per-actor LOD without recreating actors, models, or GPU resources. */
  setLODEnabled(enabled) {
    this.isLODEnabled = enabled;
    this.actorLODLevels.clear();
    this.actorLODCoverage.clear();
    this.refresh();
    return this;
  }
  /** Applies a relative projected-size bias; larger values retain higher-detail actors longer. */
  setLODBias(bias) {
    (0, import_core10.assert)(Number.isFinite(bias) && bias > 0);
    this.lodBias = bias;
    this.refresh();
    return this;
  }
  /** Sets a global indexed-vertex budget; zero or undefined restores ideal screen-space detail. */
  setLODVertexBudget(vertexBudget) {
    (0, import_core10.assert)(vertexBudget === void 0 || Number.isSafeInteger(vertexBudget) && vertexBudget >= 0);
    this.currentVertexBudget = vertexBudget || void 0;
    this.refresh();
    return this;
  }
  /** Updates screen-space actor selection from camera matrices without allocating GPU resources. */
  setLODView(view) {
    this.currentLODView = view;
    if (this.isLODEnabled) {
      this.refresh();
    }
    return this;
  }
  /** Adds independent CPU clip/node state without parsing the source or allocating GPU models. */
  addActor(options = {}) {
    (0, import_core10.assert)(!this.isDestroyed && this.actorsById.size < this.capacity);
    const id = options.id || `gltf-crowd-actor-${this.nextActorIndex++}`;
    (0, import_core10.assert)(!this.actorsById.has(id));
    let actor;
    this.suspendedRefreshCount++;
    try {
      actor = new GLTFCrowdActor(this, id, options);
      this.actorsById.set(id, actor);
    } finally {
      this.suspendedRefreshCount--;
    }
    this.refresh();
    return actor;
  }
  /** Adds many independently initialized actors while uploading shared GPU buffers only once. */
  addActors(options) {
    (0, import_core10.assert)(!this.isDestroyed && this.actorsById.size + options.length <= this.capacity);
    const actors = [];
    this.suspendedRefreshCount++;
    try {
      for (const actorOptions of options) {
        actors.push(this.addActor(actorOptions));
      }
    } finally {
      this.suspendedRefreshCount--;
      this.refresh();
    }
    return actors;
  }
  getActor(id) {
    return this.actorsById.get(id);
  }
  /** Compacts actor slots and updates every shared primitive without recreating its pipeline. */
  removeActor(id) {
    const actor = this.actorsById.get(id);
    if (!actor) {
      return false;
    }
    this.actorsById.delete(id);
    this.actorLODLevels.delete(id);
    this.actorLODCoverage.delete(id);
    if (!actor.destroyed) {
      actor.destroy();
    }
    this.refresh();
    return true;
  }
  /** Removes and compacts many actors while uploading surviving instance slots only once. */
  removeActors(ids) {
    let removedActorCount = 0;
    this.suspendedRefreshCount++;
    try {
      for (const id of ids) {
        if (this.removeActor(id)) {
          removedActorCount++;
        }
      }
    } finally {
      this.suspendedRefreshCount--;
      this.refresh();
    }
    return removedActorCount;
  }
  /** Evaluates clips and optional camera detail selection with exactly one shared GPU upload. */
  update(deltaSeconds, view) {
    if (view) {
      this.currentLODView = view;
    }
    for (const actor of this.actorsById.values()) {
      actor.advance(deltaSeconds);
    }
    this.refresh();
    return this;
  }
  /** Issues exactly one instanced draw for each compatible source primitive. */
  draw(renderPass) {
    if (this.isDestroyed || this.actorCount === 0) {
      return 0;
    }
    let drawCount = 0;
    for (const group of this.primitiveGroups) {
      if (group.model.instanceCount > 0 && group.model.draw(renderPass)) {
        drawCount++;
      }
    }
    return drawCount;
  }
  /** @internal Reuses fixed GPU buffers while packing current actor node and joint transforms. */
  refresh() {
    var _a;
    if (this.isDestroyed || this.suspendedRefreshCount > 0 || !this.primitiveGroups) {
      return;
    }
    const actors = [...this.actorsById.values()];
    const actorWorldMatrices = this.gpuAnimationEnabled ? [] : actors.map((actor) => {
      const worldMatrices = collectNodeWorldMatrices(actor.root);
      actor.updateSkinMatrices(worldMatrices);
      return worldMatrices;
    });
    const idealLevels = actors.map((actor) => this.selectActorLOD(actor));
    const { levels: selectedLevels, demotedActors } = this.applyVertexBudget(actors, idealLevels);
    const visibleActors = selectedLevels.filter((level) => level !== null).length;
    const levelStats = /* @__PURE__ */ new Map();
    for (const level of selectedLevels) {
      if (level === null) {
        continue;
      }
      const statistics = levelStats.get(level) || { level, actors: 0, triangles: 0 };
      statistics.actors++;
      levelStats.set(level, statistics);
    }
    let drawCount = 0;
    let triangles = 0;
    let vertices = 0;
    for (const group of this.primitiveGroups) {
      const modelNode = findCrowdModelNode(this.scenegraphs, group.sourceNodeIndex, group.model);
      if (!modelNode) {
        continue;
      }
      const resources = modelNode.userData["gltfAnimatedCrowd"];
      const maximumGroupLevel = this.getMaximumGroupLevel(group.nodeIndex);
      let instanceCount = 0;
      for (let actorIndex = 0; actorIndex < actors.length; actorIndex++) {
        const selectedLevel = selectedLevels[actorIndex];
        if (selectedLevel === null || Math.min(selectedLevel, maximumGroupLevel) !== group.lodLevel) {
          continue;
        }
        const actor = actors[actorIndex];
        const actorNode = actor.getNode(group.nodeIndex);
        const matrix = this.gpuAnimationEnabled ? actor.root.matrix : actorNode && actorWorldMatrices[actorIndex].get(actorNode);
        for (let columnIndex = 0; columnIndex < 4; columnIndex++) {
          for (let rowIndex = 0; rowIndex < 4; rowIndex++) {
            resources.transformColumns[columnIndex][instanceCount * 4 + rowIndex] = (matrix == null ? void 0 : matrix[columnIndex * 4 + rowIndex]) || 0;
          }
        }
        if (resources.animationParameters && resources.animationBlend) {
          this.writeGPUAnimationParameters(actor, resources, instanceCount);
        }
        if (resources.morphWeights) {
          const targetCount = resources.morphTargetCount;
          const packedTargetCount = Math.ceil(targetCount / 4);
          const offset = instanceCount * packedTargetCount * 4;
          resources.morphWeights.fill(0, offset, offset + packedTargetCount * 4);
          const weights = actorNode == null ? void 0 : actorNode.userData["morphWeights"];
          if (Array.isArray(weights)) {
            resources.morphWeights.set(weights.slice(0, targetCount), offset);
          }
        }
        if (resources.jointMatrices) {
          const jointPalette = (_a = actor.skins.getBinding(group.nodeIndex)) == null ? void 0 : _a.jointMatrices;
          const offset = instanceCount * resources.jointsPerInstance * 16;
          resources.jointMatrices.fill(0, offset, offset + resources.jointsPerInstance * 16);
          if (jointPalette) {
            resources.jointMatrices.set(jointPalette, offset);
          }
        }
        instanceCount++;
      }
      if (instanceCount > 0) {
        for (let columnIndex = 0; columnIndex < resources.transformBuffers.length; columnIndex++) {
          resources.transformBuffers[columnIndex].write(resources.transformColumns[columnIndex].subarray(0, instanceCount * 4));
        }
        if (resources.animationParameterBuffer && resources.animationParameters) {
          resources.animationParameterBuffer.write(resources.animationParameters.subarray(0, instanceCount * 4));
        }
        if (resources.animationBlendBuffer && resources.animationBlend) {
          resources.animationBlendBuffer.write(resources.animationBlend.subarray(0, instanceCount * 4));
        }
        if (resources.morphWeights && resources.morphWeightData) {
          const packedTargetCount = Math.ceil(resources.morphTargetCount / 4);
          const weights = resources.morphWeights.subarray(0, instanceCount * packedTargetCount * 4);
          if (resources.morphWeightData instanceof import_core10.Buffer) {
            resources.morphWeightData.write(weights);
          } else {
            resources.morphWeightData.writeData(weights, {
              width: packedTargetCount,
              height: instanceCount
            });
          }
        }
        if (resources.jointMatrices && resources.skinJointMatrices) {
          const jointMatrices = resources.jointMatrices.subarray(0, instanceCount * resources.jointsPerInstance * 16);
          if (resources.skinJointMatrices instanceof import_core10.Buffer) {
            resources.skinJointMatrices.write(jointMatrices);
          } else {
            resources.skinJointMatrices.writeData(jointMatrices, {
              width: resources.jointsPerInstance * 4,
              height: instanceCount
            });
          }
        }
        const groupTriangles = instanceCount * group.triangleCount;
        const statistics = levelStats.get(group.lodLevel) || {
          level: group.lodLevel,
          actors: 0,
          triangles: 0
        };
        statistics.triangles += groupTriangles;
        levelStats.set(group.lodLevel, statistics);
        triangles += groupTriangles;
        vertices += instanceCount * group.vertexCount;
        drawCount++;
      }
      group.model.setInstanceCount(instanceCount);
    }
    this.currentLODStats = {
      source: this.lodSource,
      visibleActors,
      culledActors: actors.length - visibleActors,
      drawCount,
      triangles,
      vertices,
      ...this.currentVertexBudget ? { vertexBudget: this.currentVertexBudget } : {},
      demotedActors,
      budgetSatisfied: !this.isLODEnabled || !this.currentVertexBudget || vertices <= this.currentVertexBudget,
      levels: [...levelStats.values()].sort((first, second) => first.level - second.level)
    };
  }
  bakeGPUAnimationFrames() {
    var _a;
    const layout = this.gpuAnimationLayout;
    if (!layout) {
      return;
    }
    this.suspendedRefreshCount++;
    const bakingActor = new GLTFCrowdActor(this, "__gltf-gpu-animation-baker__", { playing: false });
    try {
      for (const clip of layout.clips) {
        const animation = bakingActor.animator.selectClip(clip.name);
        animation.action.setLoop("once", 1);
        for (let clipFrame = 0; clipFrame < clip.frameCount; clipFrame++) {
          const time = Math.min(clipFrame / layout.sampleRate, clip.duration);
          animation.action.setTime(time);
          bakingActor.animator.update(0);
          const worldMatrices = collectNodeWorldMatrices(bakingActor.root);
          bakingActor.updateSkinMatrices(worldMatrices);
          for (const group of this.primitiveGroups) {
            const modelNode = findCrowdModelNode(this.scenegraphs, group.sourceNodeIndex, group.model);
            if (!modelNode) {
              continue;
            }
            const resources = modelNode.userData["gltfAnimatedCrowd"];
            const values = resources.animationFrameValues;
            const frameStride = resources.animationFrameStride;
            if (!values || !frameStride) {
              continue;
            }
            const frameOffset = (clip.frameOffset + clipFrame) * frameStride * 4;
            const node = bakingActor.getNode(group.nodeIndex);
            const nodeMatrix = node && worldMatrices.get(node);
            if (nodeMatrix) {
              values.set(nodeMatrix, frameOffset);
            }
            const jointPalette = (_a = bakingActor.skins.getBinding(group.nodeIndex)) == null ? void 0 : _a.jointMatrices;
            if (jointPalette) {
              values.set(jointPalette, frameOffset + 16);
            }
            const weights = node == null ? void 0 : node.userData["morphWeights"];
            if (Array.isArray(weights)) {
              for (let targetIndex = 0; targetIndex < resources.morphTargetCount; targetIndex++) {
                const offset = frameOffset + (4 + resources.animationJointCount * 4 + targetIndex) * 4;
                values[offset] = Number(weights[targetIndex] || 0);
              }
            }
          }
        }
      }
    } finally {
      bakingActor.destroy();
      this.suspendedRefreshCount--;
    }
    for (const group of this.primitiveGroups) {
      const modelNode = findCrowdModelNode(this.scenegraphs, group.sourceNodeIndex, group.model);
      if (!modelNode) {
        continue;
      }
      const resources = modelNode.userData["gltfAnimatedCrowd"];
      if (!resources.animationFrames || !resources.animationFrameValues) {
        continue;
      }
      if (resources.animationFrames instanceof import_core10.Buffer) {
        resources.animationFrames.write(resources.animationFrameValues);
      } else {
        resources.animationFrames.writeData(resources.animationFrameValues, {
          width: resources.animationFrameStride,
          height: layout.frameCount
        });
      }
    }
  }
  writeGPUAnimationParameters(actor, resources, instanceIndex) {
    const layout = this.gpuAnimationLayout;
    if (!layout || !resources.animationParameters || !resources.animationBlend) {
      return;
    }
    const animations = actor.animator.getAnimations().filter((animation) => animation.action.shouldApply && animation.action.weight > 0);
    const primary = animations.find((animation) => animation.name === actor.activeClip) || animations[0];
    const primaryClip = primary && this.gpuAnimationClips.get(primary.name) || layout.clips[0];
    const primaryFrames = getGLTFCrowdGPUAnimationFrames(primaryClip, (primary == null ? void 0 : primary.action.time) || 0, layout.sampleRate);
    const offset = instanceIndex * 4;
    resources.animationParameters.set([...primaryFrames, (primary == null ? void 0 : primary.action.weight) || 1], offset);
    const secondary = animations.find((animation) => animation !== primary);
    const secondaryClip = secondary && this.gpuAnimationClips.get(secondary.name);
    if (secondary && secondaryClip) {
      const frames = getGLTFCrowdGPUAnimationFrames(secondaryClip, secondary.action.time, layout.sampleRate);
      const totalWeight = ((primary == null ? void 0 : primary.action.weight) || 0) + secondary.action.weight;
      resources.animationBlend.set([...frames, totalWeight > 0 ? secondary.action.weight / totalWeight : 0], offset);
    } else {
      resources.animationBlend.fill(0, offset, offset + 4);
    }
  }
  getAuthoredScreenCoverage() {
    for (let nodeIndex = 0; nodeIndex < this.gltf.nodes.length; nodeIndex++) {
      const levels = getGLTFNodeLODs(this.gltf, nodeIndex);
      if (levels && levels.length > 1) {
        return levels.map((level) => level.screenCoverage);
      }
    }
    return [0];
  }
  getMaximumGroupLevel(nodeIndex) {
    return this.maximumGroupLevels.get(nodeIndex) || 0;
  }
  applyVertexBudget(actors, idealLevels) {
    const levels = [...idealLevels];
    if (!this.isLODEnabled || !this.currentVertexBudget || this.maximumLODLevel === 0) {
      return { levels, demotedActors: 0 };
    }
    let vertices = levels.reduce((total, level) => total + (level === null ? 0 : this.lodVertexCounts[level]), 0);
    if (vertices <= this.currentVertexBudget) {
      return { levels, demotedActors: 0 };
    }
    const actorIndices = actors.map((actor, actorIndex) => ({ actor, actorIndex })).filter(({ actorIndex }) => levels[actorIndex] !== null).sort((first, second) => {
      const firstCoverage = this.actorLODCoverage.get(first.actor.id) ?? Number.POSITIVE_INFINITY;
      const secondCoverage = this.actorLODCoverage.get(second.actor.id) ?? Number.POSITIVE_INFINITY;
      return firstCoverage - secondCoverage || first.actorIndex - second.actorIndex;
    });
    let demotedActors = 0;
    for (const { actorIndex } of actorIndices) {
      if (vertices <= this.currentVertexBudget) {
        break;
      }
      let level = levels[actorIndex];
      let actorWasDemoted = false;
      while (vertices > this.currentVertexBudget && level < this.maximumLODLevel) {
        const nextLevel = level + 1;
        const savedVertices = this.lodVertexCounts[level] - this.lodVertexCounts[nextLevel];
        if (savedVertices < 0) {
          break;
        }
        levels[actorIndex] = nextLevel;
        vertices -= savedVertices;
        level = nextLevel;
        actorWasDemoted = true;
      }
      if (actorWasDemoted) {
        demotedActors++;
      }
    }
    return { levels, demotedActors };
  }
  selectActorLOD(actor) {
    var _a;
    if (!this.isLODEnabled || !this.currentLODView || this.maximumLODLevel === 0) {
      this.actorLODLevels.set(actor.id, 0);
      this.actorLODCoverage.set(actor.id, Number.POSITIVE_INFINITY);
      return 0;
    }
    const { viewMatrix, projectionMatrix, viewportWidth, viewportHeight } = this.currentLODView;
    const { center, radius } = this.scenegraphs.modelBounds;
    const actorMatrix = actor.root.matrix;
    const worldX = actorMatrix[0] * center[0] + actorMatrix[4] * center[1] + actorMatrix[8] * center[2] + actorMatrix[12];
    const worldY = actorMatrix[1] * center[0] + actorMatrix[5] * center[1] + actorMatrix[9] * center[2] + actorMatrix[13];
    const worldZ = actorMatrix[2] * center[0] + actorMatrix[6] * center[1] + actorMatrix[10] * center[2] + actorMatrix[14];
    const cameraX = viewMatrix[0] * worldX + viewMatrix[4] * worldY + viewMatrix[8] * worldZ + viewMatrix[12];
    const cameraY = viewMatrix[1] * worldX + viewMatrix[5] * worldY + viewMatrix[9] * worldZ + viewMatrix[13];
    const cameraZ = viewMatrix[2] * worldX + viewMatrix[6] * worldY + viewMatrix[10] * worldZ + viewMatrix[14];
    const actorScale = Math.max(Math.hypot(actorMatrix[0], actorMatrix[1], actorMatrix[2]), Math.hypot(actorMatrix[4], actorMatrix[5], actorMatrix[6]), Math.hypot(actorMatrix[8], actorMatrix[9], actorMatrix[10]));
    const worldRadius = radius * actorScale;
    const clipX = projectionMatrix[0] * cameraX + projectionMatrix[4] * cameraY + projectionMatrix[8] * cameraZ + projectionMatrix[12];
    const clipY = projectionMatrix[1] * cameraX + projectionMatrix[5] * cameraY + projectionMatrix[9] * cameraZ + projectionMatrix[13];
    const clipW = projectionMatrix[3] * cameraX + projectionMatrix[7] * cameraY + projectionMatrix[11] * cameraZ + projectionMatrix[15];
    if (clipW <= 0) {
      this.actorLODLevels.delete(actor.id);
      this.actorLODCoverage.delete(actor.id);
      return null;
    }
    const normalizedRadiusX = worldRadius * Math.abs(projectionMatrix[0]) / clipW;
    const normalizedRadiusY = worldRadius * Math.abs(projectionMatrix[5]) / clipW;
    if (Math.abs(clipX / clipW) > 1 + normalizedRadiusX || Math.abs(clipY / clipW) > 1 + normalizedRadiusY) {
      this.actorLODLevels.delete(actor.id);
      this.actorLODCoverage.delete(actor.id);
      return null;
    }
    const viewportScale = viewportWidth && viewportHeight ? viewportHeight / Math.min(viewportWidth, viewportHeight) : 1;
    const coverage = normalizedRadiusY * viewportScale * this.lodBias;
    this.actorLODCoverage.set(actor.id, coverage);
    const candidateLevel = this.lodScreenCoverage.findIndex((threshold) => coverage >= threshold);
    const nextLevel = candidateLevel === -1 ? null : Math.min(candidateLevel, this.maximumLODLevel);
    const previousLevel = this.actorLODLevels.get(actor.id);
    const hysteresis = Math.max(0, Math.min(((_a = this.lodOptions) == null ? void 0 : _a.hysteresis) ?? 0.1, 0.99));
    if (previousLevel !== void 0 && nextLevel !== previousLevel) {
      const threshold = this.lodScreenCoverage[nextLevel === null || nextLevel > previousLevel ? previousLevel : nextLevel];
      const movesToLowerDetail = nextLevel === null || nextLevel > previousLevel;
      const crossedBoundary = movesToLowerDetail ? coverage < threshold * (1 - hysteresis) : coverage >= threshold * (1 + hysteresis);
      if (!crossedBoundary) {
        return previousLevel;
      }
    }
    if (nextLevel === null) {
      this.actorLODLevels.delete(actor.id);
      this.actorLODCoverage.delete(actor.id);
    } else {
      this.actorLODLevels.set(actor.id, nextLevel);
    }
    return nextLevel;
  }
  /** Destroys actor CPU state and the one canonical source scenegraph exactly once. */
  destroy() {
    if (this.isDestroyed) {
      return;
    }
    this.isDestroyed = true;
    for (const actor of [...this.actorsById.values()]) {
      actor.destroy();
    }
    this.actorsById.clear();
    this.scenegraphs.destroy();
  }
};
function createGLTFAnimatedCrowd(device, gltf, options = {}) {
  return new GLTFAnimatedCrowd(device, gltf, options);
}
function createActorNodes(scenegraphs, id) {
  const { gltf, gltfNodeIndexToNodeMap } = scenegraphs;
  const nodesByIndex = /* @__PURE__ */ new Map();
  const nodesById = /* @__PURE__ */ new Map();
  for (let nodeIndex = 0; nodeIndex < gltf.nodes.length; nodeIndex++) {
    const sourceNode = gltf.nodes[nodeIndex];
    const sourceRuntimeNode = gltfNodeIndexToNodeMap.get(nodeIndex);
    if (!sourceRuntimeNode) {
      continue;
    }
    const node = new import_engine9.GroupNode({
      id: sourceRuntimeNode.id,
      position: Array.from(sourceRuntimeNode.position),
      rotation: Array.from(sourceRuntimeNode.rotation),
      scale: Array.from(sourceRuntimeNode.scale),
      matrix: Array.from(sourceRuntimeNode.matrix),
      display: sourceRuntimeNode.display
    });
    const morphWeights = sourceRuntimeNode.userData["morphWeights"];
    if (Array.isArray(morphWeights)) {
      node.userData["morphWeights"] = [...morphWeights];
    }
    nodesByIndex.set(nodeIndex, node);
    nodesById.set(sourceNode.id, node);
  }
  for (let nodeIndex = 0; nodeIndex < gltf.nodes.length; nodeIndex++) {
    const sourceNode = gltf.nodes[nodeIndex];
    const node = nodesByIndex.get(nodeIndex);
    if (!node) {
      continue;
    }
    for (const child of sourceNode.children || []) {
      const childNode = nodesById.get(child.id);
      if (childNode) {
        node.add(childNode);
      }
    }
    if (sourceNode.mesh) {
      const mesh = new import_engine9.GroupNode({ id: sourceNode.mesh.name || sourceNode.mesh.id });
      node.userData["gltfMesh"] = mesh;
      node.add(mesh);
    }
  }
  const scenes = gltf.scenes.map((scene, sceneIndex) => new import_engine9.GroupNode({
    id: `${id}-scene-${sceneIndex}`,
    children: (scene.nodes || []).flatMap((sourceNode) => {
      const node = nodesById.get(sourceNode.id);
      return node ? [node] : [];
    })
  }));
  return {
    root: new import_engine9.GroupNode({ id: `${id}-root`, children: [...scenes] }),
    scenes,
    nodesByIndex,
    nodesById
  };
}
function createPrimitiveGroups(scenegraphs, includeLODLevels) {
  var _a, _b, _c, _d;
  const groups = [];
  const reachableNodes = /* @__PURE__ */ new Set();
  for (const scene of scenegraphs.scenes) {
    scene.preorderTraversal((node) => {
      if (node instanceof import_engine9.GroupNode) {
        reachableNodes.add(node);
      }
    });
  }
  for (const [nodeIndex, sourceNode] of scenegraphs.gltf.nodes.entries()) {
    if (!sourceNode.mesh) {
      continue;
    }
    const node = scenegraphs.gltfNodeIndexToNodeMap.get(nodeIndex);
    if (!node || !reachableNodes.has(node)) {
      continue;
    }
    const skinBinding = scenegraphs.skins.getBinding(nodeIndex);
    const levels = includeLODLevels ? getGLTFNodeLODs(scenegraphs.gltf, nodeIndex) : null;
    const lodNodes = levels || [{ level: 0, nodeIndex, node: sourceNode, screenCoverage: 0 }];
    for (const level of lodNodes) {
      const lodNode = scenegraphs.gltfNodeIndexToNodeMap.get(level.nodeIndex);
      const mesh = lodNode == null ? void 0 : lodNode.userData["gltfMesh"];
      if (!(mesh instanceof import_engine9.GroupNode)) {
        continue;
      }
      for (const [primitiveIndex, child] of mesh.children.entries()) {
        if (!(child instanceof import_engine9.ModelNode)) {
          continue;
        }
        const resources = child.userData["gltfAnimatedCrowd"];
        if (!resources) {
          continue;
        }
        const primitive = (_a = level.node.mesh) == null ? void 0 : _a.primitives[primitiveIndex];
        const indexCount = ((_b = primitive == null ? void 0 : primitive.indices) == null ? void 0 : _b.count) || ((_d = (_c = primitive == null ? void 0 : primitive.indices) == null ? void 0 : _c.value) == null ? void 0 : _d.length) || child.model.vertexCount;
        groups.push({
          nodeIndex,
          sourceNodeIndex: level.nodeIndex,
          lodLevel: level.level,
          triangleCount: Math.floor(indexCount / 3),
          vertexCount: indexCount,
          model: child.model,
          transformBuffers: resources.transformBuffers,
          jointCount: (skinBinding == null ? void 0 : skinBinding.joints.length) || 0,
          morphTargetCount: resources.morphTargetCount,
          ...resources.jointMatrices ? { jointMatrices: resources.jointMatrices } : {},
          ...resources.skinJointMatrices ? { skinJointMatrices: resources.skinJointMatrices } : {},
          ...resources.morphTargetData ? { morphTargetData: resources.morphTargetData } : {},
          ...resources.morphWeights ? { morphWeights: resources.morphWeights } : {},
          ...resources.animationFrames ? { animationFrames: resources.animationFrames } : {},
          ...resources.animationParameters ? { animationParameters: resources.animationParameters } : {}
        });
      }
    }
  }
  return groups;
}
function findCrowdModelNode(scenegraphs, nodeIndex, model) {
  var _a;
  const mesh = (_a = scenegraphs.gltfNodeIndexToNodeMap.get(nodeIndex)) == null ? void 0 : _a.userData["gltfMesh"];
  if (!(mesh instanceof import_engine9.GroupNode)) {
    return void 0;
  }
  return mesh.children.find((node) => node instanceof import_engine9.ModelNode && node.model === model);
}
function collectNodeWorldMatrices(root) {
  const result = /* @__PURE__ */ new Map();
  root.preorderTraversal((node, { worldMatrix }) => {
    if (node instanceof import_engine9.GroupNode) {
      result.set(node, worldMatrix);
    }
  });
  return result;
}

// dist/export/gltf-exporter.js
var import_gltf = require("@loaders.gl/gltf");
var ARRAY_BUFFER_TARGET = 34962;
var ELEMENT_ARRAY_BUFFER_TARGET = 34963;
function exportGLTF(scene, options = {}) {
  const writer = new GLTFExportWriter(scene, options);
  return writer.write();
}
var GLTFExportWriter = class {
  scene;
  options;
  binary = new GLTFBinaryBuilder();
  bufferViews = [];
  accessors = [];
  constructor(scene, options) {
    this.scene = scene;
    this.options = options;
  }
  write() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k;
    const document = {
      asset: {
        version: "2.0",
        generator: this.scene.generator || "@luma.gl/gltf"
      },
      scene: this.scene.scene ?? 0
    };
    if ((_a = this.scene.nodes) == null ? void 0 : _a.length) {
      document["nodes"] = this.scene.nodes.map((node) => this.writeNode(node));
    }
    document["scenes"] = ((_b = this.scene.scenes) == null ? void 0 : _b.length) ? this.scene.scenes.map((scene) => ({ ...scene, nodes: [...scene.nodes] })) : [{ ...this.scene.name ? { name: this.scene.name } : {}, nodes: this.collectRootNodes() }];
    if ((_c = this.scene.meshes) == null ? void 0 : _c.length) {
      document["meshes"] = this.scene.meshes.map((mesh) => this.writeMesh(mesh));
    }
    if ((_d = this.scene.materials) == null ? void 0 : _d.length) {
      document["materials"] = this.scene.materials.map((material) => ({ ...material }));
    }
    if ((_e = this.scene.textures) == null ? void 0 : _e.length) {
      document["textures"] = this.scene.textures.map((texture) => ({ ...texture }));
    }
    if ((_f = this.scene.images) == null ? void 0 : _f.length) {
      document["images"] = this.scene.images.map((image) => this.writeImage(image));
    }
    if ((_g = this.scene.samplers) == null ? void 0 : _g.length) {
      document["samplers"] = this.scene.samplers.map((sampler) => ({ ...sampler }));
    }
    if ((_h = this.scene.cameras) == null ? void 0 : _h.length) {
      document["cameras"] = this.scene.cameras.map((camera) => ({ ...camera }));
    }
    if ((_i = this.scene.skins) == null ? void 0 : _i.length) {
      document["skins"] = this.scene.skins.map((skin2) => this.writeSkin(skin2));
    }
    if ((_j = this.scene.animations) == null ? void 0 : _j.length) {
      document["animations"] = this.scene.animations.map((animation) => this.writeAnimation(animation));
    }
    if (this.scene.extensions) {
      document["extensions"] = { ...this.scene.extensions };
    }
    if (this.scene.extras !== void 0) {
      document["extras"] = this.scene.extras;
    }
    if (this.bufferViews.length) {
      document["bufferViews"] = this.bufferViews;
    }
    if (this.accessors.length) {
      document["accessors"] = this.accessors;
    }
    const binary = this.binary.finish();
    if (binary.byteLength > 0) {
      document["buffers"] = [
        {
          byteLength: binary.byteLength,
          ...!this.options.binary ? { uri: `data:application/octet-stream;base64,${encodeGLTFBase64(binary)}` } : {}
        }
      ];
    }
    const extensionsUsed = new Set(this.scene.extensionsUsed || []);
    collectExtensionNames(document, extensionsUsed);
    if (extensionsUsed.size > 0) {
      document["extensionsUsed"] = [...extensionsUsed];
    }
    if ((_k = this.scene.extensionsRequired) == null ? void 0 : _k.length) {
      document["extensionsRequired"] = [...this.scene.extensionsRequired];
    }
    return this.options.binary ? import_gltf.GLBWriter.encodeSync({
      json: document,
      ...binary.byteLength > 0 ? { binary } : {}
    }, {}) : JSON.stringify(document, null, this.options.pretty === false ? void 0 : 2);
  }
  collectRootNodes() {
    const childNodes = /* @__PURE__ */ new Set();
    for (const node of this.scene.nodes || []) {
      for (const child of node.children || []) {
        childNodes.add(child);
      }
    }
    return (this.scene.nodes || []).flatMap((_node, index) => childNodes.has(index) ? [] : [index]);
  }
  writeNode(node) {
    const { instances, ...properties } = node;
    const result = { ...properties };
    if (node.children) {
      result["children"] = [...node.children];
    }
    if (node.weights) {
      result["weights"] = [...node.weights];
    }
    if (instances && Object.keys(instances).length > 0) {
      const attributes = {};
      for (const [name, accessor] of Object.entries(instances)) {
        attributes[name] = this.writeAccessor(accessor);
      }
      result["extensions"] = {
        ...node.extensions,
        EXT_mesh_gpu_instancing: { attributes }
      };
    }
    return result;
  }
  writeMesh(mesh) {
    return {
      ...mesh.name ? { name: mesh.name } : {},
      primitives: mesh.primitives.map((primitive) => this.writePrimitive(primitive)),
      ...mesh.weights ? { weights: [...mesh.weights] } : {},
      ...mesh.extensions ? { extensions: { ...mesh.extensions } } : {},
      ...mesh.extras !== void 0 ? { extras: mesh.extras } : {}
    };
  }
  writePrimitive(primitive) {
    var _a;
    const attributes = {};
    for (const [name, accessor] of Object.entries(primitive.attributes)) {
      attributes[name] = this.writeAccessor(accessor, {
        target: ARRAY_BUFFER_TARGET,
        includeBounds: name === "POSITION"
      });
    }
    const result = { attributes };
    if (primitive.indices) {
      result["indices"] = this.writeAccessor(primitive.indices, {
        target: ELEMENT_ARRAY_BUFFER_TARGET
      });
    }
    if (primitive.material !== void 0) {
      result["material"] = primitive.material;
    }
    if (primitive.mode !== void 0) {
      result["mode"] = primitive.mode;
    }
    if ((_a = primitive.targets) == null ? void 0 : _a.length) {
      result["targets"] = primitive.targets.map((target) => Object.fromEntries(Object.entries(target).map(([name, accessor]) => [
        name,
        this.writeAccessor(accessor, {
          target: ARRAY_BUFFER_TARGET,
          includeBounds: name === "POSITION"
        })
      ])));
    }
    if (primitive.extensions) {
      result["extensions"] = { ...primitive.extensions };
    }
    if (primitive.extras !== void 0) {
      result["extras"] = primitive.extras;
    }
    return result;
  }
  writeSkin(skin2) {
    const { inverseBindMatrices, joints, ...properties } = skin2;
    return {
      ...properties,
      joints: [...joints],
      ...inverseBindMatrices ? { inverseBindMatrices: this.writeAccessor(inverseBindMatrices) } : {}
    };
  }
  writeAnimation(animation) {
    return {
      ...animation.name ? { name: animation.name } : {},
      samplers: animation.samplers.map((sampler) => ({
        input: this.writeAccessor(sampler.input, { includeBounds: true }),
        output: this.writeAccessor(sampler.output),
        ...sampler.interpolation ? { interpolation: sampler.interpolation } : {}
      })),
      channels: animation.channels.map((channel) => ({
        sampler: channel.sampler,
        target: channel.target.path === "pointer" ? {
          path: "pointer",
          extensions: { KHR_animation_pointer: { pointer: channel.target.pointer } }
        } : { ...channel.target }
      })),
      ...animation.extensions ? { extensions: { ...animation.extensions } } : {},
      ...animation.extras !== void 0 ? { extras: animation.extras } : {}
    };
  }
  writeImage(image) {
    const { data, ...properties } = image;
    if (!data) {
      return { ...properties };
    }
    const mimeType = image.mimeType || "image/png";
    if (!this.options.binary) {
      return { ...properties, uri: `data:${mimeType};base64,${encodeGLTFBase64(data)}` };
    }
    const bufferView = this.writeBufferView(data);
    return { ...properties, bufferView, mimeType };
  }
  writeAccessor(accessor, options = {}) {
    const data = toGLTFTypedArray(accessor.data);
    const bytes = new Uint8Array(data.buffer, data.byteOffset, data.byteLength);
    const result = {
      bufferView: this.writeBufferView(bytes, options.target),
      componentType: getGLTFComponentType(data),
      count: data.length / accessor.size,
      type: getGLTFAccessorType(accessor.size),
      ...accessor.normalized ? { normalized: true } : {}
    };
    if (accessor.min) {
      result["min"] = [...accessor.min];
    } else if (options.includeBounds) {
      result["min"] = getGLTFExtrema(data, accessor.size, Math.min);
    }
    if (accessor.max) {
      result["max"] = [...accessor.max];
    } else if (options.includeBounds) {
      result["max"] = getGLTFExtrema(data, accessor.size, Math.max);
    }
    if (accessor.sparse) {
      const indices = accessor.sparse.indices;
      const values = toGLTFTypedArray(accessor.sparse.values);
      result["sparse"] = {
        count: indices.length,
        indices: {
          bufferView: this.writeBufferView(new Uint8Array(indices.buffer, indices.byteOffset, indices.byteLength)),
          componentType: getGLTFComponentType(indices)
        },
        values: {
          bufferView: this.writeBufferView(new Uint8Array(values.buffer, values.byteOffset, values.byteLength))
        }
      };
    }
    this.accessors.push(result);
    return this.accessors.length - 1;
  }
  writeBufferView(bytes, target) {
    const byteOffset = this.binary.append(bytes);
    this.bufferViews.push({
      buffer: 0,
      byteOffset,
      byteLength: bytes.byteLength,
      ...target ? { target } : {}
    });
    return this.bufferViews.length - 1;
  }
};
var GLTFBinaryBuilder = class {
  chunks = [];
  byteLength = 0;
  append(values) {
    const padding = (4 - this.byteLength % 4) % 4;
    if (padding > 0) {
      this.chunks.push(new Uint8Array(padding));
      this.byteLength += padding;
    }
    const offset = this.byteLength;
    this.chunks.push(values);
    this.byteLength += values.byteLength;
    return offset;
  }
  finish() {
    const padding = (4 - this.byteLength % 4) % 4;
    const bytes = new Uint8Array(this.byteLength + padding);
    let offset = 0;
    for (const chunk of this.chunks) {
      bytes.set(chunk, offset);
      offset += chunk.byteLength;
    }
    return bytes;
  }
};
function toGLTFTypedArray(data) {
  return Array.isArray(data) ? new Float32Array(data) : data;
}
function getGLTFComponentType(data) {
  if (data instanceof Int8Array)
    return 5120;
  if (data instanceof Uint8Array)
    return 5121;
  if (data instanceof Int16Array)
    return 5122;
  if (data instanceof Uint16Array)
    return 5123;
  if (data instanceof Uint32Array)
    return 5125;
  return 5126;
}
function getGLTFAccessorType(size) {
  if (size === 1)
    return "SCALAR";
  if (size === 9)
    return "MAT3";
  if (size === 16)
    return "MAT4";
  return `VEC${size}`;
}
function getGLTFExtrema(data, size, operation) {
  const extrema = Array.from(data.subarray(0, size));
  for (let offset = size; offset < data.length; offset += size) {
    for (let component = 0; component < size; component++) {
      extrema[component] = operation(extrema[component], data[offset + component]);
    }
  }
  return extrema;
}
function collectExtensionNames(value, result) {
  if (!value || typeof value !== "object") {
    return;
  }
  for (const [name, child] of Object.entries(value)) {
    if (name === "extensions" && child && typeof child === "object") {
      for (const extension of Object.keys(child)) {
        result.add(extension);
      }
    }
    collectExtensionNames(child, result);
  }
}
function encodeGLTFBase64(bytes) {
  let binary = "";
  const chunkSize = 32768;
  for (let offset = 0; offset < bytes.length; offset += chunkSize) {
    binary += String.fromCharCode(...bytes.subarray(offset, offset + chunkSize));
  }
  return btoa(binary);
}

// dist/pbr/pbr-environment.js
var import_engine10 = require("@luma.gl/engine");
var import_textures = require("@loaders.gl/textures");
function loadPBREnvironment(device, props) {
  const specularMipLevels = props.specularMipLevels ?? 1;
  const brdfLutTexture = new import_engine10.DynamicTexture(device, {
    id: "brdfLUT",
    sampler: {
      addressModeU: "clamp-to-edge",
      addressModeV: "clamp-to-edge",
      minFilter: "linear",
      magFilter: "linear"
    },
    // Texture accepts a promise that returns an image as data (Async Textures)
    data: (0, import_textures.loadImageTexture)(resolveTextureUrl(props.brdfLutUrl))
  });
  const diffuseEnvSampler = makeCube(device, {
    id: "DiffuseEnvSampler",
    getTextureForFace: (face) => (0, import_textures.loadImageTexture)(resolveTextureUrl(props.getTexUrl("diffuse", FACES.indexOf(face), 0))),
    sampler: {
      addressModeU: "clamp-to-edge",
      addressModeV: "clamp-to-edge",
      minFilter: "linear",
      magFilter: "linear"
    }
  });
  const specularEnvSampler = makeCube(device, {
    id: "SpecularEnvSampler",
    getTextureForFace: (face) => {
      const imageArray = [];
      const direction = FACES.indexOf(face);
      for (let lod = 0; lod < specularMipLevels; lod++) {
        imageArray.push((0, import_textures.loadImageTexture)(resolveTextureUrl(props.getTexUrl("specular", direction, lod))));
      }
      return Promise.all(imageArray);
    },
    sampler: {
      addressModeU: "clamp-to-edge",
      addressModeV: "clamp-to-edge",
      minFilter: "linear",
      // [GL.TEXTURE_MIN_FILTER]: GL.LINEAR_MIPMAP_LINEAR,
      magFilter: "linear"
    }
  });
  return {
    brdfLutTexture,
    diffuseEnvSampler,
    specularEnvSampler
  };
}
var FACES = ["+X", "-X", "+Y", "-Y", "+Z", "-Z"];
function resolveTextureUrl(url) {
  var _a, _b;
  const baseUrl = ((_a = globalThis.document) == null ? void 0 : _a.baseURI) ?? ((_b = globalThis.location) == null ? void 0 : _b.href);
  return baseUrl ? new URL(url, baseUrl).toString() : url;
}
function makeCube(device, { id, getTextureForFace, sampler }) {
  const data = Promise.all(FACES.map((face) => getTextureForFace(face))).then((faceDataArray) => {
    const cubeData = {};
    FACES.forEach((face, index) => {
      cubeData[face] = faceDataArray[index];
    });
    return cubeData;
  });
  return new import_engine10.DynamicTexture(device, {
    id,
    dimension: "cube",
    mipmaps: false,
    sampler,
    data
  });
}
//# sourceMappingURL=index.cjs.map
