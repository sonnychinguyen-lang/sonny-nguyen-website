import { PrimitiveTopology } from '@luma.gl/core';
import { GLEnum } from "./gltf-webgl-constants.js";
type GLTFIndexArray = Uint8Array | Uint16Array | Uint32Array;
type GLTFDrawMode = GLEnum.POINTS | GLEnum.LINES | GLEnum.LINE_STRIP | GLEnum.LINE_LOOP | GLEnum.TRIANGLES | GLEnum.TRIANGLE_STRIP | GLEnum.TRIANGLE_FAN;
export type NormalizedGLTFTopology = {
    topology: PrimitiveTopology;
    indices?: Uint16Array | Uint32Array;
};
/** Converts a WebGL draw mode into a luma.gl primitive topology string. */
export declare function convertGLDrawModeToTopology(drawMode: GLTFDrawMode): PrimitiveTopology;
/**
 * Converts WebGL-only glTF primitive modes to portable indexed list topologies.
 *
 * The returned indices are newly allocated so the post-processed source document remains
 * unchanged. Uint8 source indices are widened because WebGPU does not support uint8 index buffers.
 */
export declare function normalizeGLTFTopology(drawMode: GLTFDrawMode, sourceIndices: GLTFIndexArray | undefined, vertexCount: number): NormalizedGLTFTopology;
export {};
//# sourceMappingURL=convert-webgl-topology.d.ts.map