import { Buffer, type VertexFormat } from '@luma.gl/core';
import { type GPUVectorFormat } from '@luma.gl/gpgpu/gpu-data';
import { GPUCommandGraph, GraphVectorView, type GraphDataView } from "./gpu-command-graph.js";
/** Aligned buffer range that can be bound for a logical graph data view. */
export type GraphDataViewBinding = {
    buffer: Buffer;
    offset: number;
    size: number;
};
/** Aligned byte range occupied by a logical graph data view's storage binding. @internal */
export type GraphDataViewBindingRange = {
    offset: number;
    size: number;
};
/** Packed 32-bit scalar formats supported by graph-native analysis primitives. @internal */
export type GPUScalarFormat = 'uint32' | 'sint32' | 'float32';
/**
 * Validates that a view has one of the requested formats and a packed, uint32-aligned layout.
 *
 * Packed primitive shaders index storage buffers in 32-bit components, so they cannot consume
 * interleaved rows or arbitrary byte offsets.
 *
 * @internal
 */
export declare function validatePackedView<T extends GPUVectorFormat>(view: GraphDataView, formats: readonly T[], name: string): asserts view is GraphDataView<T>;
/** Validates a packed `uint32` view used for flags, counts, or indices. @internal */
export declare function validatePackedUint32View(view: GraphDataView, name: string): void;
/**
 * Returns the aligned storage-buffer binding that contains a logical data view.
 *
 * WebGPU storage bindings begin at 256-byte-aligned offsets. Generated shaders add the component
 * offset from {@link getViewElementOffset} to reach the view's actual first row.
 */
export declare function getViewBinding(view: GraphDataView, getBuffer: (view: GraphDataView) => Buffer): GraphDataViewBinding;
/** Returns the aligned byte range made available to a logical data view binding. @internal */
export declare function getViewBindingRange(view: GraphDataView): GraphDataViewBindingRange;
/** Returns the view offset in 32-bit components from its aligned storage binding. */
export declare function getViewElementOffset(view: GraphDataView): number;
/** Returns whether two logical views touch any of the same bytes. @internal */
export declare function doGraphDataViewsOverlap(first: GraphDataView, second: GraphDataView): boolean;
/** Creates a packed, fixed-width graph-owned transient buffer and a typed view spanning it. */
export declare function createTransientView<T extends VertexFormat, Parameters>(graph: GPUCommandGraph<Parameters>, id: string, format: T, length: number, usage?: number): GraphDataView<T>;
/** Creates graph-owned scratch storage with the same chunk topology as a vector. @internal */
export declare function createTransientVectorView<T extends VertexFormat, Parameters>(graph: GPUCommandGraph<Parameters>, id: string, template: GraphVectorView<T>): GraphVectorView<T>;
/** Validates that two vectors have identical ordered chunk lengths. @internal */
export declare function validateMatchingVectorTopology(first: GraphVectorView, second: GraphVectorView, name: string): void;
//# sourceMappingURL=graph-data-view-utils.d.ts.map