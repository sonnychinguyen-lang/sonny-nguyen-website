import type { Buffer, BufferLayout } from '@luma.gl/core';
import type { DynamicBuffer } from '@luma.gl/engine';
import type { GPUData } from "./gpu-data.js";
import type { GPUVector } from "./gpu-vector.js";
/** Minimal vector collection consumed by table-agnostic lookup helpers. */
export type GPUVectorCollection = {
    gpuVectors: Record<string, GPUVector>;
};
/** Returns the single GPUData chunk for APIs that require contiguous GPU vector storage. */
export declare function getGPUVectorData(vector: GPUVector): GPUData;
/** Returns the single backing buffer for APIs that bind contiguous GPU vector storage directly. */
export declare function getGPUVectorBuffer(vector: GPUVector): Buffer | DynamicBuffer;
/** Derives model-ready attribute buffers from layout metadata and GPU vector storage. */
export declare function getGPUVectorBuffersForLayout(bufferLayout: BufferLayout[], gpuVectors: Record<string, GPUVector>): Record<string, Buffer | DynamicBuffer>;
/** Derives model-ready attribute buffers from layout metadata and batch-local GPU data. */
export declare function getGPUDataBuffersForLayout(bufferLayout: BufferLayout[], gpuData: Record<string, GPUData>): Record<string, Buffer | DynamicBuffer>;
/** Returns a required GPU vector from a table by column name. */
export declare function getRequiredGPUVector(table: GPUVectorCollection, columnName: string, ownerName?: string): GPUVector;
//# sourceMappingURL=gpu-vector-utils.d.ts.map