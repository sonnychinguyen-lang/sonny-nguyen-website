import { GPUDataEvaluator } from "../operation/gpu-data-evaluator.js";
import { ArithmeticArgument } from "./arithmetic-operation.js";
/**
 * Adds corresponding row elements from two or more tables or literals.
 *
 * The returned table is lazy; no CPU or GPU work is performed until
 * {@link GPUDataEvaluator.evaluate} is called on the result.
 */
export declare function add(...args: ArithmeticArgument[]): GPUDataEvaluator;
/**
 * Subtracts corresponding row elements from two or more tables or literals.
 *
 * The returned table is lazy; no CPU or GPU work is performed until
 * {@link GPUDataEvaluator.evaluate} is called on the result.
 */
export declare function subtract(...args: ArithmeticArgument[]): GPUDataEvaluator;
/**
 * Multiplies corresponding row elements from two or more tables or literals.
 *
 * The returned table is lazy; no CPU or GPU work is performed until
 * {@link GPUDataEvaluator.evaluate} is called on the result.
 */
export declare function multiply(...args: ArithmeticArgument[]): GPUDataEvaluator;
/**
 * Divides corresponding row elements from two or more tables or literals.
 *
 * The returned table is lazy; no CPU or GPU work is performed until
 * {@link GPUDataEvaluator.evaluate} is called on the result.
 */
export declare function divide(...args: ArithmeticArgument[]): GPUDataEvaluator;
/**
 * Raises each row element in `base` to the corresponding power in `exponent`.
 */
export declare function pow(base: ArithmeticArgument, exponent: ArithmeticArgument): GPUDataEvaluator;
/**
 * Computes the square root of each row element.
 */
export declare function sqrt(arg: ArithmeticArgument): GPUDataEvaluator;
/**
 * Computes the absolute value of each row element.
 */
export declare function abs(arg: ArithmeticArgument): GPUDataEvaluator;
/**
 * Applies sine to each row element.
 */
export declare function sin(arg: ArithmeticArgument): GPUDataEvaluator;
/**
 * Applies cosine to each row element.
 */
export declare function cos(arg: ArithmeticArgument): GPUDataEvaluator;
/**
 * Applies tangent to each row element.
 */
export declare function tan(arg: ArithmeticArgument): GPUDataEvaluator;
/**
 * Applies the natural exponential to each row element.
 */
export declare function exp(arg: ArithmeticArgument): GPUDataEvaluator;
/**
 * Applies the natural logarithm to each row element.
 */
export declare function log(arg: ArithmeticArgument): GPUDataEvaluator;
//# sourceMappingURL=arithmetic.d.ts.map