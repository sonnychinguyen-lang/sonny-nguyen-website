import { GPUDataEvaluator, type GPUDataEvaluatorInput } from "../operation/gpu-data-evaluator.js";
/**
 * Selects between `whenTrue` and `whenFalse` using per-row or per-lane non-zero condition values.
 *
 * `condition`, `whenTrue`, and `whenFalse` may each be scalar rows (size 1) or match the resolved
 * output row size. Scalar rows broadcast across lanes.
 */
export declare function select(condition: GPUDataEvaluatorInput, whenTrue: GPUDataEvaluatorInput, whenFalse: GPUDataEvaluatorInput): GPUDataEvaluator;
//# sourceMappingURL=select.d.ts.map