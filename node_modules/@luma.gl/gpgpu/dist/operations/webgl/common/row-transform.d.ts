import { SignedDataType, Buffer, BufferLayout } from '@luma.gl/core';
import { type ShaderModule } from '@luma.gl/shadertools';
import { GPUDataEvaluator } from "../../../operation/gpu-data-evaluator.js";
export declare function runRowTransform({ module, elementWise, expression, inputs, output, operationType, outputBuffer }: {
    module: ShaderModule;
    elementWise?: boolean;
    expression?: (laneIndex: number) => string;
    inputs: {
        [name: string]: GPUDataEvaluator;
    } | GPUDataEvaluator[];
    output: GPUDataEvaluator;
    /** If specified, coerce all parameters to operation to this type.
     * Default to output's data type.
     */
    operationType?: SignedDataType;
    outputBuffer: Buffer;
}): void;
export declare function getInputModule(name: string, inType: SignedDataType, inSize: number, normalized?: boolean, outType?: SignedDataType): ShaderModule;
export declare function getInputBufferLayout(name: string, source: GPUDataEvaluator): BufferLayout;
export declare function getOutputModule(name: string, outType: SignedDataType, outSize: number, normalized?: boolean): ShaderModule & {
    varyings: string[];
};
//# sourceMappingURL=row-transform.d.ts.map