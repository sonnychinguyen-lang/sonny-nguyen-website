// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
export function getLiteralValue(type, value) {
    switch (type) {
        case 'u32':
            return `${value}u`;
        case 'f32':
            return Number.isInteger(value) ? `${value}.0` : `${value}`;
        default:
            return `${value}`;
    }
}
export function formatLiteralValue(type, value) {
    switch (type) {
        case 'uint32':
            return getLiteralValue('u32', Math.trunc(value));
        case 'sint32':
            return `${Math.trunc(value)}`;
        case 'float32':
            return getLiteralValue('f32', value);
        default:
            throw new Error(`WebGPU operations only support 32-bit output types, got ${type}`);
    }
}
export function getZeroValue(type) {
    switch (type) {
        case 'uint32':
            return '0u';
        case 'sint32':
            return '0';
        case 'float32':
            return '0.0';
        default:
            throw new Error(`WebGPU operations only support 32-bit output types, got ${type}`);
    }
}
export function getWGSLType(type) {
    switch (type) {
        case 'uint32':
            return 'u32';
        case 'sint32':
            return 'i32';
        case 'float32':
            return 'f32';
        default:
            throw new Error(`WebGPU operations only support 32-bit storage types, got ${type}`);
    }
}
//# sourceMappingURL=helper.js.map