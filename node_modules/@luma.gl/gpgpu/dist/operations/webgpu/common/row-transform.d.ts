import { Buffer, SignedDataType } from '@luma.gl/core';
import { type ShaderModule } from '@luma.gl/shadertools';
import { GPUDataEvaluator } from "../../../operation/gpu-data-evaluator.js";
export declare function runRowComputation({ module, elementWise, expression, inputs, output, operationType, outputBuffer }: {
    module: ShaderModule;
    elementWise?: boolean;
    expression?: (laneIndex: number) => string;
    inputs: {
        [name: string]: GPUDataEvaluator;
    } | GPUDataEvaluator[];
    output: GPUDataEvaluator;
    operationType?: SignedDataType;
    outputBuffer: Buffer;
}): void;
//# sourceMappingURL=row-transform.d.ts.map