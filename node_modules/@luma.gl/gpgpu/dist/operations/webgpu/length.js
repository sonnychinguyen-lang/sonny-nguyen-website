// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
import { runRowComputation } from "./common/row-transform.js";
const source = `\
fn row_length(x: array<{TYPE}, {X_LEN}>) -> array<f32, 1> {
  var sum = 0.0;
  for (var i = 0u; i < {X_LEN}u; i = i + 1u) {
    sum += f32(x[i]) * f32(x[i]);
  }
  return array<f32, 1>(sqrt(sum));
}
`;
export const length = ({ inputs, output, target }) => {
    runRowComputation({
        module: { name: 'row_length', source },
        inputs,
        output,
        operationType: 'float32',
        outputBuffer: target
    });
    return { success: true };
};
//# sourceMappingURL=length.js.map