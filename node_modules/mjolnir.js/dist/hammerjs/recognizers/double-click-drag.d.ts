import { Recognizer } from "../recognizer/recognizer.js";
import { RecognizerState } from "../recognizer/recognizer-state.js";
import type { HammerInput } from "../input/types.js";
export type DoubleClickDragRecognizerOptions = {
    /** Name of the event.
     * @default 'doubleclickdrag'
     */
    event?: string;
    /** Enable this event.
     * @default true
     */
    enable?: boolean;
    /** Required number of pointers.
     * @default 1
     */
    pointers?: number;
    /** Maximum time in ms between the two taps.
     * @default 500
     */
    interval?: number;
    /** Maximum press time in ms for each tap.
     * @default 350
     */
    time?: number;
    /** Maximum movement in px allowed while tapping.
     * @default 28
     */
    threshold?: number;
    /** Minimum vertical movement in px before drag zoom begins.
     * @default 1
     */
    dragThreshold?: number;
    /** Vertical pixels required to double the scale.
     * @default 120
     */
    pixelsPerScale?: number;
};
/**
 * DoubleClickDrag
 * Recognized when a second tap/click turns into a vertical drag gesture.
 */
export declare class DoubleClickDragRecognizer extends Recognizer<Required<DoubleClickDragRecognizerOptions>> {
    private _tapStart;
    private _lastTap;
    private _drag;
    private _emittedStart;
    constructor(options?: DoubleClickDragRecognizerOptions);
    getTouchAction(): string[];
    getEventNames(): string[];
    process(input: HammerInput): RecognizerState;
    reset(): void;
    emit(input?: HammerInput): void;
    private _handleStart;
    private _handleMove;
    private _handleEnd;
    private _isTapMatch;
    private _isValidTap;
    private _getPointerId;
    private _isSamePointer;
}
//# sourceMappingURL=double-click-drag.d.ts.map